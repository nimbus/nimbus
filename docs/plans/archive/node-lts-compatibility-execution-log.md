# Node LTS Compatibility Execution Log

Companion execution log for `docs/plans/node-lts-compatibility-plan.md`. The main plan owns live control state and requirements; this file preserves the full chronological history.

## Execution Log

| Date | Item | Status | Files touched | Description | Verification |
|------|------|--------|---------------|-------------|--------------|
| 2026-04-29 | NLC0 | `done` | `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/README.md`, `AGENTS.md` | Activated the new Node LTS control plane, promoted it into the active plan index, pointed AGENTS at it as the active owner for Node built-in compatibility work, and checkpointed the current dirty-tree runtime baseline plus the first resumable seam: generated compatibility artifacts and matrix tooling. | `git diff --check -- AGENTS.md docs/plans/README.md docs/plans/node-lts-compatibility-plan.md` |
| 2026-04-29 | — | `done` | `docs/plans/node-lts-compatibility-plan.md` | Clarified the control-plane contract after review: added canonical locations for generated artifacts and canaries, introduced a profile-aware support-state taxonomy, tightened NLC1/NLC2 expectations, and added an explicit final closeout rule so future agents cannot overclaim Node22 compatibility while host-sensitive APIs remain tooling-scoped. | `git diff --check -- docs/plans/node-lts-compatibility-plan.md AGENTS.md docs/plans/README.md` |
| 2026-04-29 | — | `done` | `docs/plans/node-lts-compatibility-plan.md` | Added a measured `nodejs/ncrypto` decision gate under NLC7: documented the current Deno-family crypto baseline, limited `ncrypto` to an evidence-backed crypto-family spike, and recorded the required go/no-go outputs before any broader crypto-stack replacement can be justified. | `git diff --check -- docs/plans/node-lts-compatibility-plan.md` |
| 2026-04-29 | — | `done` | `docs/plans/node-lts-compatibility-plan.md` | Completed the first crypto research spike: verified that the current Deno-family baseline already uses a substantial `deno_node_crypto` + `deno_crypto` stack backed by `aws-lc-rs` and many Rust crypto crates, confirmed `nodejs/ncrypto` is a real Node-owned fidelity candidate, and recorded the current recommendation to keep the Deno-family stack as the default until measured NLC7 failures justify a narrower or broader `ncrypto` move. | `git diff --check -- docs/plans/node-lts-compatibility-plan.md` |
| 2026-04-29 | NLC1 | `done` | `scripts/node_compat/generate_matrix.py`, `docs/architecture/runtime/node-lts-compat/node20-symbols.csv`, `docs/architecture/runtime/node-lts-compat/node22-symbols.csv`, `docs/architecture/runtime/node-lts-compat/node20-vs-node22-delta.csv`, `docs/architecture/runtime/node-lts-compat/deno-node-impl-inventory.csv`, `docs/architecture/runtime/node-lts-compat/node-lts-compat-matrix.csv`, `docs/architecture/runtime/node-lts-compat/node-lts-compat-summary.md`, `docs/plans/node-lts-compatibility-plan.md` | Landed the first generated Node LTS compatibility baseline from primary sources: official Node 20/22 docs JSON, the local `agentstation/deno` fork, and Deno's published Node API compatibility page. The initial baseline is intentionally conservative: unresolved module extractions such as `node:test/reporters`, `node:constants`, and `node:sys` are emitted as placeholder module rows and remain `NeedsVerification` instead of being guessed. | `python3 scripts/node_compat/generate_matrix.py`; `python3 scripts/node_compat/generate_matrix.py && git diff --exit-code -- docs/architecture/runtime/node-lts-compat scripts/node_compat/generate_matrix.py`; `git diff --check -- docs/plans/node-lts-compatibility-plan.md` |
| 2026-04-29 | NLC2 | `done` | `README.md`, `docs/README.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/node-lts-compat-summary.md`, `scripts/node_compat/generate_matrix.py`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the generated baseline into the public contract: README and runtime docs now name Node22 as the primary target, Node20 as a compatibility lane, and explicitly reject any current "full Node built-in compatibility" claim. The generated summary now includes a per-module first-baseline snapshot so public review can see initial support states without reading raw CSVs. | `python3 scripts/node_compat/generate_matrix.py && python3 scripts/node_compat/generate_matrix.py && git diff --exit-code -- docs/architecture/runtime/node-lts-compat scripts/node_compat/generate_matrix.py`; `git diff --check -- README.md docs/README.md docs/architecture/runtime/node-compat-surface-matrix.md docs/architecture/runtime/node-lts-compat scripts/node_compat/generate_matrix.py docs/plans/node-lts-compatibility-plan.md` |
| 2026-04-29 | NLC3 | `in_progress` | `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Started the core semantics family by checking in the first upstream Node test-slice manifest for `assert`, `buffer`, `events`, `path`, `url`, `console`, `querystring`, `punycode`, and `string_decoder`, using the pinned vendored Node corpus in `agentstation/deno` instead of rediscovery from memory. | `find ~/src/github.com/agentstation/deno/tests/node_compat/runner/suite/test -type f | rg '/test-(assert|buffer|events|path|url|console|querystring|punycode|string-decoder)'` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Fixed the first `NLC3` runtime proof so the application-profile CommonJS package lane now stages packages where the current contract actually resolves them: inside the generated bundle root, not `app/node_modules`. Added a small helper to make that generated-root staging explicit, promoted the core-semantics test-slice manifest into the canonical `manifests/` path, added the checked-in `NLC3` failure-inventory stub, and updated the public runtime matrix to record that the verified CommonJS bridge now includes narrow `require("node:...")` access for the core-semantics builtin set. | `cargo test -p neovex-runtime application_node22_commonjs_package_can_require_core_semantics_builtins -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime basic_invocation:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the local `NLC3` proof to cover `node:assert`, `node:assert/strict`, and `node:url` in both the ESM import path and the staged local CommonJS package path, then documented the next hard truth for family closeout: the pinned vendored Deno `node_compat` runner is a corpus source but not a drop-in Neovex harness because it shells out to a Deno CLI executable via `DENO_TEST_UTIL_DENO_EXE` and assumes Deno CLI run semantics. The family now has clearer fixture-backed runtime evidence and a checked-in upstream-runner gap instead of an implied pass-rate story. | `cargo test -p neovex-runtime basic_invocation:: -- --nocapture --test-threads=1`; `sed -n '120,320p' ~/src/github.com/agentstation/deno/tests/node_compat/mod.rs`; `cat ~/src/github.com/agentstation/deno/tests/node_compat/runner/deno.json` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-buffer-isencoding.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-console-assign-undefined.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-console-instance.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-events-getmaxlisteners.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-events-list.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-path-dirname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-path-zero-length-strings.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-querystring-escape.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-string-decoder-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-parse-query.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Added the first real Neovex-owned upstream `NLC3` runner lane by staging pinned Node corpus files into the generated bundle root, reusing a checked-in `test/common` shim, and asserting post-import `mustCall`/warning helper checks. The green subset now covers `buffer`, a shallow `console` contract, `events`, `path`, `querystring`, `string_decoder`, and `url` from the pinned `agentstation/deno v2.7.14-locker.4` corpus. The same spike surfaced a real deeper console gap: `test-console-instance.js` fails because `process.stdout` / `process.stderr` are not yet exposed as writable stream objects, so the fixture is now pinned as an ignored repro and classified in the `NLC3` failure inventory instead of silently disappearing. The runtime contract was also tightened to publish `globalThis.Buffer` only on Node22 and to prove it with both local and upstream-backed tests. | `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime basic_invocation:: -- --nocapture --test-threads=1`; `git -C ~/src/github.com/agentstation/deno describe --tags --always` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-punycode.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Used the new helper-capable `node_compat` harness to sample the pinned upstream `test-punycode.js` file. After correcting fixture-copy drift, the remaining failure proved real: the expected `DeprecationWarning` for `require("punycode")` never reached the `process.on("warning")` listener registered by `common.expectWarning()`. Source inspection of `agentstation/deno v2.7.14-locker.4` shows the polyfill still wraps `node:punycode` in `util.deprecate()`, so the gap is now checkpointed as an ignored repro and classified as a deprecation-warning delivery issue rather than a basic punycode algorithm mismatch. | `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `sed -n '1,120p' ~/src/github.com/agentstation/deno/ext/node/polyfills/punycode.ts` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Resolved the two `NLC3` blockers instead of leaving them for later. The Node22 bootstrap now seeds real `process.stdout` / `process.stderr` writable stream objects and enables `nextTick`, which closed the deeper upstream `console` fixture. The CommonJS `node:module` path now goes through a Neovex-owned wrapper around Deno's real `Module` object so `require("punycode")` delivers the expected `DEP0040` warning in the embedded runtime contract. The measured `NLC3` local subset is now green again at 13 passed, 0 failed, 0 ignored, and the failure inventory was narrowed back to runner/pass-rate work instead of live known runtime gaps. | `cargo test -p neovex-runtime node22_target_delivers_process_warning_events_for_deprecated_modules -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime basic_invocation:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-isencoding.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-console-assign-undefined.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-console-instance.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-events-getmaxlisteners.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-events-list.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-dirname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-zero-length-strings.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-punycode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-querystring-escape.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-string-decoder-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-parse-query.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the first real Node20 compatibility lane for `NLC3` from placeholder to pinned evidence. Verified the current official Node20 source anchor as `nodejs/node v20.20.2`, compared the active 11-file core-semantics subset against that corpus, and vendored the official Node20 copies under a dedicated `node20/` fixture root where the staged Node22 corpus diverged textually. The new `node20_validation_lane_executes_official_core_semantics_subset` test proves that the same 11-file subset currently passes under the Node22 runtime contract with 0 failures. | `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v20.20.2/test/parallel/test-punycode.js`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-from.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-console-methods.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted `test-buffer-from.js` and `test-console-methods.js` into the measured green subset, then repinned Neovex to `agentstation/deno v2.7.14-locker.5` after the Deno-fork live `node:vm` contextify fix closed the `Buffer.from(runInNewContext('new String(...)'))` crash path. The `NLC3` subset is now green at 13 upstream files for both the Node22 runtime lane and the official Node20 validation lane, and the full local `node_compat::` Rust lane is green at 16 passed, 0 failed, 0 ignored. Added the matching Deno-side regression source in `~/src/github.com/agentstation/deno/tests/unit_node/vm_test.ts`; local execution of the fork-built Deno lane remained blocked on this machine because `cmake` is not installed, so the control plane keeps the live proof grounded in the repinned Neovex integration lane instead of overstating Deno-side local execution. | `cargo test -p neovex-runtime node22_target_executes_upstream_buffer_from_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `/bin/zsh -lc 'CARGO_ENCODED_RUSTFLAGS=$'\"'\"'-C\\x1flink-args=-weak_framework Metal -weak_framework MetalPerformanceShaders -weak_framework QuartzCore -weak_framework CoreGraphics\\x1f--cfg\\x1ftokio_unstable'\"'\"' cargo test -p unit_node_tests --test unit_node -- vm_test'` *(blocked locally: missing `cmake`)* |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-path.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the shared Node22 and Node20 `NLC3` subset with the official `test-path.js` slice after confirming the pinned Deno-staged and official Node20 files are textually identical. The wider subset is now green at 14 upstream files for both lanes, and the full local `node_compat::` Rust lane is green at 17 passed, 0 failed, 0 ignored. This keeps the family moving forward on measured upstream-backed semantics instead of stopping at the first nice-looking checkpoint. | `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v20.20.2/test/parallel/test-path.js`; `cargo test -p neovex-runtime node22_target_executes_upstream_path_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-path-basename.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-basename.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the shared Node22 and Node20 `NLC3` subset with the official `test-path-basename.js` slice. The dedicated Node22 and official-Node20 basename assertions both pass, the widened official Node20 validation subset is clean after rerunning without the temporary trace helper, and the measured green subset is now 15 upstream files for both lanes. The full local `node_compat::` Rust lane is now green at 19 passed, 0 failed, 0 ignored. The earlier single-run `SIGSEGV` on the Node20 subset did not reproduce after the clean rerun, so the failure inventory keeps the family focused on measured runtime gaps instead of carrying forward an unconfirmed crash as a known issue. | `cargo test -p neovex-runtime node22_target_executes_upstream_path_basename_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_path_basename_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-path-join.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-join.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the shared Node22 and Node20 `NLC3` subset with `test-path-join.js`. The official Node20 source differs textually from the pinned Deno-vendored Node22 corpus because the newer vendored file carries extra UNC join assertions, so both checked-in copies are deliberate and both now pass under the current runtime contract. The measured green subset is now 16 upstream files for both lanes, and the full local `node_compat::` Rust lane is green at 21 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime node22_target_executes_upstream_path_join_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_path_join_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-path-isabsolute.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-isabsolute.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-querystring-maxKeys-non-finite.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-querystring-maxKeys-non-finite.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened `NLC3` again with `test-path-isabsolute.js` and `test-querystring-maxKeys-non-finite.js`. Both Node22 fixture proofs pass, the widened official Node20 validation subset passes on clean rerun, and the full local `node_compat::` Rust lane is now green at 23 passed, 0 failed, 0 ignored. The first widened Node20 subset attempt terminated with a one-off `SIGSEGV`, but the immediate rerun and the broader full-lane rerun both passed, so the control plane records it as a non-reproducing watchpoint rather than a confirmed known runtime gap. | `cargo test -p neovex-runtime node22_target_executes_upstream_path_isabsolute_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node22_target_executes_upstream_querystring_max_keys_non_finite_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-format.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-format.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Resolved the next real `NLC3` blocker by adding a minimal embedded `Deno.test` bridge plus an explicit pending-test flush in the Node22 harness path. That kept `globalThis.Deno` hidden while allowing the first `node:test`-backed upstream slice, `test-url-format.js`, to execute under both the Node22 runtime lane and the official Node20 validation lane. The measured green subset is now 19 upstream files for both lanes, and the full local `node_compat::` Rust lane is green at 25 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_format_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node22_target_hides_deno_bootstrap_globals -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_format_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-format-invalid-input.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-format-invalid-input.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the new `node:test`-backed URL seam with `test-url-format-invalid-input.js`. The Node22 and official Node20 fixture proofs both pass, the widened official Node20 validation subset stays green, and the full local `node_compat::` Rust lane is now green at 27 passed, 0 failed, 0 ignored. This confirms the embedded `Deno.test` bridge is good for more than one URL file while still keeping the public contract narrower than general `node:test` support. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_format_invalid_input_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_format_invalid_input_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-domain-ascii-unicode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-domain-ascii-unicode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-urltooptions.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-urltooptions.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the shared `node:url` seam with `test-url-urltooptions.js` plus `test-url-domain-ascii-unicode.js`. The first widened the plain helper contract around `urlToHttpOptions()`, while the second proved the embedded `Deno.test` bridge and `hasIntl` shim are strong enough for a third real `node:test`-backed upstream file. Both Node22 proofs pass, both official Node20 proofs pass, the widened official Node20 validation subset stays green, and the full local `node_compat::` Rust lane is now green at 31 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_urltooptions_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_urltooptions_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node22_target_executes_upstream_url_domain_ascii_unicode_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_domain_ascii_unicode_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-format-whatwg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-format-whatwg.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the `node:url` seam again with `test-url-format-whatwg.js`. This adds a fourth real `node:test`-backed URL fixture on top of the embedded `Deno.test` bridge and `hasIntl` shim, while also widening the official Node20 validation lane with a matching upstream source file. Both Node22 and official Node20 fixture proofs pass, the widened official Node20 validation subset stays green, and the full local `node_compat::` Rust lane is now green at 33 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_format_whatwg_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_format_whatwg_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-fileurltopath.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-fileurltopath.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the `node:url` path-conversion seam with `test-url-fileurltopath.js`. The pinned Deno-vendored Node22 file and the official Node20 source are not textually identical, so Neovex now carries both checked-in copies explicitly. Both Node22 and official Node20 fixture proofs pass, the widened official Node20 validation subset stays green, and the full local `node_compat::` Rust lane is now green at 35 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_fileurltopath_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_fileurltopath_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-pathtofileurl.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-pathtofileurl.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the same `node:url` path-conversion seam with `test-url-pathtofileurl.js`. Both Node22 and official Node20 fixture proofs pass, the widened official Node20 validation subset stays green, and the full local `node_compat::` Rust lane is now green at 37 passed, 0 failed, 0 ignored. This keeps the conversion contract measured on both directions instead of stopping at `fileURLToPath()` alone. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_pathtofileurl_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_target_executes_official_url_pathtofileurl_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-relative.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-relative.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-revokeobjecturl.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-revokeobjecturl.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Batched two more low-risk `node:url` helpers into the measured green subset: `test-url-relative.js` and `test-url-revokeobjecturl.js`. `URL.revokeObjectURL()` stays in the same plain builtin seam, and the official Node20 `test-url-relative.js` differs from the pinned Deno-vendored copy only by a comment typo, so Neovex now carries both checked-in fixture roots honestly while widening the shared contract around `url.resolve()` / `url.resolveObject()` and `URL.revokeObjectURL()`. Both Node22 and official Node20 focused proofs pass, the widened official Node20 validation subset stays green, and the full local `node_compat::` Rust lane is now green at 41 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime url_revokeobjecturl_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime url_relative_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-parse-format.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-parse-format.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Broadened the `node:url` seam with `test-url-parse-format.js`, then followed the surfaced legacy `url.parse()` mismatches all the way into the Deno fork instead of hiding them behind a deferred failure note. The official Node20 fixture carries an extra Git URL assertion absent from the pinned Deno-vendored Node22 corpus, so both checked-in roots remain explicit. Focused proofs first exposed three real Deno-family legacy parser gaps: authority control characters, auth-bearing protocol-less `//user@host/path` inputs falling into the simple-path fast path, and boundary C0 control trimming plus overly strict hostname scanning for punctuation-bearing legacy hosts. Those were fixed and published in the canonical Deno fork line across `v2.7.14-locker.6` through `v2.7.14-locker.10`, with the final canonical rule aligning `getHostname()` to Node v20's own `lib/url.js`. After repinning Neovex to `agentstation/deno v2.7.14-locker.10`, both Node22 and official Node20 `test-url-parse-format.js` proofs pass, the widened official Node20 validation subset is green at 28 passed, 0 failed, and the full local `node_compat::` Rust lane is green at 43 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime url_parse_format_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v20.20.2/lib/url.js` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-invalid-file-url-path-input.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Added `test-url-invalid-file-url-path-input.js` as a deliberate Node22-only `node:url` expansion. The pinned Deno-vendored Node22 corpus carries this file, but the official `nodejs/node v20.20.2` tree does not, so the control plane now records the asymmetry explicitly instead of pretending both lanes widened equally. The focused Node22 proof passes, the official Node20 validation subset stays green and unchanged at 28 passed because there is no counterpart file to add there, and the full local `node_compat::` Rust lane is now green at 44 passed, 0 failed, 0 ignored. | `cargo test -p neovex-runtime node22_target_executes_upstream_url_invalid_file_url_path_input_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v20.20.2/test/parallel/test-url-invalid-file-url-path-input.js` |
| 2026-04-29 | NLC3 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-url-parse-format.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-url-parse-invalid-input.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Switched from fixture-by-fixture `node:url` churn to a code-first drift review of official `nodejs/node v20.20.2` and `nodejs/node v22.15.0` `lib/url.js` against the Deno-family `ext/node/polyfills/url.ts`. That review showed the legacy invalid-port parser seam still matches across both LTS lines and that the Deno-vendored Node22 corpus had drifted ahead to hard-throw semantics. Neovex fixed the shared source-of-truth mismatch by restoring Node's `warnInvalidPort` behavior in `agentstation/deno v2.7.14-locker.12`, repinning the workspace to that tag, staging explicit official `node22/` fixture overrides for `test-url-parse-format.js` and `test-url-parse-invalid-input.js`, and seeding the embedded Node22 bootstrap with the default warning printer so `process.emitWarning()` writes to `stderr` in spawned child executions. After the batch fix, both official Node20 and official Node22 `url.parse()` lanes are green again, the official Node20 validation subset widened to 29 passed, and the full local `node_compat::` lane reran clean at 46 passed, 0 failed, 1 ignored after one non-reproducing SIGSEGV watchpoint on the first post-repin full-lane run. | `cargo test -p neovex-runtime url_parse_ -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v20.20.2/lib/url.js`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v22.15.0/lib/url.js`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v22.15.0/test/parallel/test-url-parse-invalid-input.js`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v22.15.0/test/parallel/test-url-parse-format.js` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Followed the same code-first source-truth approach through the rest of the currently executed `node:url` subset instead of treating each file as a separate mystery. A drift audit showed that official `nodejs/node v20.20.2` and `nodejs/node v22.15.0` still share identical bodies for `test-url-domain-ascii-unicode.js`, `test-url-pathtofileurl.js`, and `test-url-fileurltopath.js`, while the pinned Deno-vendored copies lag those fixtures. Neovex now runs one shared checked-in official LTS body for those cases in both lanes, which keeps Node20/Node22 aligned around the same assertions and makes the remaining vendored-only deltas explicit instead of implicit. The changed URL slices stayed green, the official Node20 subset stayed green at 29 passed, and the full local `node_compat::` lane reran clean at 46 passed, 0 failed, 1 ignored. | `diff -u crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-domain-ascii-unicode.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-domain-ascii-unicode.js`; `diff -u crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-fileurltopath.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-fileurltopath.js`; `diff -u crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-url-pathtofileurl.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-url-pathtofileurl.js`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v22.15.0/test/parallel/test-url-fileurltopath.js`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v22.15.0/test/parallel/test-url-pathtofileurl.js`; `curl -i -sS https://raw.githubusercontent.com/nodejs/node/v22.15.0/test/parallel/test-url-domain-ascii-unicode.js`; `cargo test -p neovex-runtime domain_ascii_unicode_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime pathtofileurl_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime fileurltopath_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-fail.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-if-error.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Added the first shared-official `node:assert` slice with `test-assert-fail.js` and `test-assert-if-error.js`, and recorded the local `~/src/github.com/nodejs/node` checkout as a canonical plan input for future code-first Node20/Node22 review. Official `nodejs/node v20.20.2` and `nodejs/node v22.15.0` are byte-identical for both files, and the pinned Deno-vendored copies still match, so Neovex stages one shared official LTS body for both runtime lanes instead of creating a fake version split. Both focused assert files passed for Node22 and Node20, the official Node20 validation subset widened to 31 passed, and the full local `node_compat::` lane reran clean at 50 passed, 0 failed, 1 ignored. | `diff -u /private/tmp/test-assert-fail-v20.js /private/tmp/test-assert-fail-v22.js`; `diff -u /private/tmp/test-assert-if-error-v20.js /private/tmp/test-assert-if-error-v22.js`; `cargo test -p neovex-runtime assert_fail_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime assert_if_error_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-async.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Continued the same local-source-first `node:assert` audit using the canonical `~/src/github.com/nodejs/node` checkout. Official `nodejs/node v20.20.2` and `nodejs/node v22.15.0` still share the exact `test-assert-async.js` body even though the pinned Deno-vendored copy had drifted, so Neovex now stages one shared official LTS fixture for both lanes. That also gives `NLC3` its first upstream-backed top-level async `node:assert` proof without adding a new harness seam. The focused async assert tests passed for Node22 and Node20, the official Node20 validation subset widened to 32 passed, and the full local `node_compat::` lane reran clean at 52 passed, 0 failed, 1 ignored. | `cargo test -p neovex-runtime assert_async_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-fail-deprecation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-first-line.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/fixtures/assert-first-line.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/fixtures/assert-long-line.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Continued the same local-source-first `node:assert` batching strategy with `test-assert-fail-deprecation.js` and `test-assert-first-line.js`. Official `nodejs/node v20.20.2` and `nodejs/node v22.15.0` still share the exact source bodies for both files, so Neovex now stages one shared official LTS copy in both lanes and also stages the checked-in `test/fixtures/assert-first-line.js` and `test/fixtures/assert-long-line.js` helpers into the bundle root for the first-line case. That widens the current assert proof in two useful ways: `test-assert-fail-deprecation.js` proves `DEP0094` warning delivery through the shared `expectWarning()` path, and `test-assert-first-line.js` proves the runner can preserve upstream assertion-source formatting without leaning on the host filesystem. Both focused files passed for Node22 and Node20, the official Node20 validation subset widened to 34 passed, and the full local `node_compat::` lane reran clean at 56 passed, 0 failed, 1 ignored. | `cargo test -p neovex-runtime assert_fail_deprecation_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime assert_first_line_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-calltracker-report.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-calltracker-verify.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Switched to a faster assert-suite batching rule driven by a local `~/src/github.com/nodejs/node` triage pass: current-batch-friendly pure assert files move together, while child-process, exit-handler, or richer host-process seams wait for later families. The next batch under that rule was `test-assert-calltracker-report.js` and `test-assert-calltracker-verify.js`. Official `nodejs/node v20.20.2` and `nodejs/node v22.15.0` still share the exact source bodies for both files, so Neovex stages one shared official LTS copy in both lanes and now records `assert.CallTracker` behavior directly in the current `NLC3` slice. The focused files passed for Node22 and Node20, the official Node20 validation subset widened to 36 passed, and the full local `node_compat::` lane reran clean at 60 passed, 0 failed, 1 ignored while surfacing the expected `DEP0173` deprecation warning as part of the measured contract. | `git -C ~/src/github.com/nodejs/node ls-tree -r --name-only v20.20.2 test/parallel | rg '^test/parallel/test-assert-.*\\.js$' | sed 's#test/parallel/##' | sort`; `cargo test -p neovex-runtime assert_calltracker_report_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime assert_calltracker_verify_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-calltracker-getCalls.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-checktag.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-assert-calltracker-getCalls.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-assert-checktag.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Continued the same source-first assert batching strategy with the first explicit split-LTS `node:assert` pair: `test-assert-calltracker-getCalls.js` and `test-assert-checktag.js`. Official Node20 and Node22 diverge materially here, so Neovex now stages both official LTS bodies directly instead of flattening them. `CallTracker.getCalls()` also surfaced the first harness-owned env seam in this family: the official Node22 file probes `process.env.TEST_PARALLEL`, so the checked-in `test/common` shim now normalizes only that one harness-owned lookup to `undefined` when the application-profile env proxy would otherwise deny it. Both focused files passed for Node22 and Node20 after that narrow shim, the official Node20 validation subset widened to 38 passed, and the full local `node_compat::` lane reran clean at 64 passed, 0 failed, 1 ignored without broadening the public runtime env contract. | `cargo test -p neovex-runtime assert_checktag_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime assert_calltracker_getcalls_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-assert-deep-with-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-typedarray-deepequal.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-assert-typedarray-deepequal.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the faster batching rule but mixed one paired LTS file and one Node22-only file: `test-assert-typedarray-deepequal.js` plus `test-assert-deep-with-error.js`. `typedarray-deepequal` is another real split-LTS seam because Node22 adds `Float16Array` and `partialDeepStrictEqual()` coverage while the official Node20 file remains narrower, so both official bodies are checked in deliberately. `deep-with-error` has no official `nodejs/node v20.20.2` counterpart, so it widens only the measured Node22 lane while keeping the Node20 subset honest. The focused files passed, the official Node20 validation subset widened to 39 passed, and the full local `node_compat::` lane reran clean at 67 passed, 0 failed, 1 ignored. | `cargo test -p neovex-runtime assert_typedarray_deepequal_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime assert_deep_with_error_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-assert-class-destructuring.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept widening the Node22-primary side of the same assert semantics family with `test-assert-class-destructuring.js`. That file has no official `nodejs/node v20.20.2` counterpart, so it widens only the measured Node22 lane, but it stays inside the existing `node:test` + `Assert` class seam and adds no new host/process dependency. The focused Node22 fixture passed, and the full local `node_compat::` lane reran clean at 68 passed, 0 failed, 1 ignored. | `cargo test -p neovex-runtime assert_class_destructuring_fixture -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` |
| 2026-04-29 | NLC3 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-assert-deep.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-assert-deep.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-assert-partial-deep-equal.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-extname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-normalize.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-path-normalize.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-parse-format.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-relative.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-querystring-multichar-separator.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Switched from “keep adding assert files” to a code-first official-Node review using the canonical local `~/src/github.com/nodejs/node` checkout. That spike showed the remaining official `v22.15.0` `node:assert` tail had collapsed into one real current-runtime watchpoint pair (`test-assert-deep.js` and `test-assert-partial-deep-equal.js`) plus three later host/process files. It also exposed an important trust issue: the Deno-vendored `test-assert-deep.js` corpus had drifted away from the official Node22 file, so Deno-local green results there are not authoritative for `NLC3`. Neovex therefore repinned to `agentstation/deno v2.7.14-locker.13` after landing the Deno-side `internal/util/comparisons` cleanup and regression, kept the serial `Deno.test` bridge hardening, and then converted `test-assert-deep.js` plus `test-assert-partial-deep-equal.js` into explicit ignored repros instead of leaving the full lane red or aborting. With the assert tail honestly checkpointed, `NLC3` moved faster again by batching the next shared-LTS path/querystring slice: `test-path-extname.js`, `test-path-parse-format.js`, `test-path-relative.js`, and `test-querystring-multichar-separator.js` are now green in both official lanes, while `test-path-normalize.js` became a new explicit split-LTS watchpoint because official Node20 passes but official Node22 expects the newer post-CVE Windows device-path semantics. The widened family evidence is now: Node22 green subset `46` files, Node20 green subset `44` files, and the full local `node_compat::` lane `77 passed, 0 failed, 5 ignored`. | `cargo test -p neovex-runtime assert_deep_fixture -- --nocapture --test-threads=1` → `2 failed`; `cargo test -p neovex-runtime assert_partial_deep_equal_fixture -- --nocapture --test-threads=1` → aborted with `SIGABRT` after a `rusty_v8` weak-handle panic; `cargo test -p neovex-runtime path_extname_fixture -- --nocapture --test-threads=1` → `2 passed`; `cargo test -p neovex-runtime path_normalize_fixture -- --nocapture --test-threads=1` → `1 passed, 1 failed`; `cargo test -p neovex-runtime path_parse_format_fixture -- --nocapture --test-threads=1` → `2 passed`; `cargo test -p neovex-runtime path_relative_fixture -- --nocapture --test-threads=1` → `2 passed`; `cargo test -p neovex-runtime querystring_multichar_separator_fixture -- --nocapture --test-threads=1` → `2 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `77 passed, 0 failed, 5 ignored` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-events-listener-count-with-listener.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-resolve.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/fixtures/path-resolve.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-path-resolve.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the faster source-first batching rule but shifted out of the assert tail and into a shared `events`/`path` batch from the canonical local `~/src/github.com/nodejs/node` checkout. `test-events-listener-count-with-listener.js` is still byte-identical between official `nodejs/node v20.20.2` and `nodejs/node v22.15.0`, so Neovex stages one shared official LTS body and the file is now green in both lanes. The same review showed `test-path-resolve.js` is a more valuable result than another tiny green slice because it is not version drift at all: the official Node20 and Node22 files still share the same body, and both currently fail in the embedded runtime because `ext:deno_node/path/_win32.ts` rejects drive-letter-less `win32.resolve()` inputs without a CWD. Neovex therefore records `test-path-resolve.js` as a shared ignored watchpoint instead of pretending either lane is green there. After checkpointing that split honestly, the official Node20 validation subset widened to 45 passed, the Node22 green subset widened to 47 passed, and the full local `node_compat::` lane reran clean at 79 passed, 0 failed, 7 ignored. | `cargo test -p neovex-runtime events_listener_count_with_listener_fixture -- --nocapture --test-threads=1` → `2 passed`; `cargo test -p neovex-runtime path_resolve_fixture -- --nocapture --test-threads=1` → `2 failed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → first run `SIGSEGV`, immediate rerun `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `79 passed, 0 failed, 7 ignored` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/plans/node-lts-compatibility-plan.md` | Reviewed Deno and Bun to decide how `NLC3` should stop growing by hand. The useful pattern split is now explicit: Deno provides the harness architecture Neovex should emulate, while Bun provides the release-gate posture and compatibility reporting shape. Deno vendors a pinned Node `test/` corpus, scans it generically, and lets `tests/node_compat/config.jsonc` decide which files run, which are ignored, and which failures are expected. Bun’s public contract says it runs thousands of Node tests before every release and reports compatibility by module family, but it is less transparent about a reusable corpus-runner implementation. Neovex therefore kept its embedded-runtime-specific harness but started the same architectural pivot inside `NLC3`: the official Node20 validation lane now reads the vendored corpus from a manifest-style fixture table on disk instead of hardcoding another large block of `include_str!` calls. That gives future core-semantics batching a Deno-style “fixture rows as data” path without throwing away the existing targeted repro tests that still help isolate runtime gaps. | `sed -n '1,260p' ~/src/github.com/agentstation/deno/tests/node_compat/mod.rs`; `sed -n '1,220p' ~/src/github.com/agentstation/deno/tests/node_compat/config.jsonc`; `sed -n '1,260p' ~/src/github.com/agentstation/deno/tests/node_compat/runner/suite/vendor.ts`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `111 passed, 0 failed, 9 ignored` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/plans/node-lts-compatibility-plan.md` | Extended the same manifest-driven direction to the Node22 primary core-semantics subset. Neovex now has both a Node20 official validation batch lane and a Node22 primary batch lane reading fixture rows as data instead of wiring one more page of bespoke Rust wrappers. The current targeted per-fixture tests are still kept for named repros and gap isolation, but the main harness seam is now much closer to Deno’s config-driven corpus model: future `NLC3` expansion can add or classify fixture rows without growing another large inline switchboard. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Completed the manifest cutover by retiring the duplicate green per-fixture wrappers from `node_compat.rs`. The canonical `NLC3` proof surface is now two Deno-style batch lanes (`node20` official validation and `node22` primary coverage) plus nine explicit ignored watchpoint tests for the remaining gap families. That shrinks the Rust test file, keeps the harness aligned with the checked-in manifest data, and avoids re-executing the same green fixture bodies twice in the full suite. The post-cutover full `node_compat::` lane is green again at `4 passed, 0 failed, 9 ignored`, which removes the earlier duplicate-execution instability from the default proof path while preserving the targeted repros that still drive actual runtime fixes. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `4 passed, 0 failed, 9 ignored` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Added a future-facing Node24 preview corpus without broadening the support claim. Neovex now stages the official `nodejs/node v24.15.0` core-semantics fixtures under `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24`, wires them into an ignored `node24_preview_lane_executes_manifested_core_semantics_subset` test, and keeps that lane out of the default contract and gating surface. The useful surprise is that the initial Node24 preview batch already runs green when invoked explicitly with `--ignored`, so we now have forward-visibility evidence for future Node24 work without muddying the current Node22-primary / Node20-validation posture. The default full `node_compat::` lane remains green at `4 passed, 0 failed, 10 ignored`. | `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `4 passed, 0 failed, 10 ignored` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-copy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-slice.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-querystring.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-copy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-slice.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-querystring.js`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Used the new manifest-first harness shape to widen `NLC3` with another pure-runtime batch instead of more one-off wrappers. `test-buffer-copy.js`, `test-buffer-slice.js`, and `test-querystring.js` are all still byte-identical between official `nodejs/node v20.20.2` and `nodejs/node v22.15.0`, so Neovex stages one shared official LTS body for the live Node20 and Node22 lanes and copies the official `nodejs/node v24.15.0` bodies into the preview corpus. All three additions passed in the Node22 primary lane, the Node20 validation lane, and the ignored Node24 preview lane. The measured manifest counts are now 55 Node22 files plus the paired 50-file official Node20 validation subset, with the Node24 preview kept purely as forward visibility rather than a support claim. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-buffer-indexof.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-buffer-write.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-fill.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-indexof.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-includes.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-readint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-readuint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-write.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-writeint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-writeuint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-fill.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-indexof.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-includes.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-readint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-readuint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-write.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-writeint.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-writeuint.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the faster manifest-driven direction and used the canonical local `~/src/github.com/nodejs/node` checkout as the tagged source of truth instead of adding more one-off wrappers. A larger buffer-family batch is now staged directly from official `v20.20.2`, `v22.15.0`, and `v24.15.0`: `test-buffer-fill.js`, `test-buffer-includes.js`, `test-buffer-readint.js`, `test-buffer-readuint.js`, `test-buffer-writeint.js`, and `test-buffer-writeuint.js` are byte-identical across all three versions; `test-buffer-indexof.js` splits once between Node20 and Node22/Node24; and `test-buffer-write.js` uses separate official bodies for all three versions. The entire batch passed in the Node22 primary lane, the official Node20 validation lane, and the ignored Node24 preview lane. The measured manifest counts are now 77 Node22 files, the paired 75-file official Node20 validation subset, and a green 78-file ignored Node24 preview subset. The preview result remains forward visibility only and does not broaden the public support claim. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-ascii.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-badhex.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-inspect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-readdouble.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-readfloat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-tojson.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-writedouble.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-writefloat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-ascii.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-badhex.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-inspect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-readdouble.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-readfloat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-tojson.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-writedouble.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-writefloat.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Used the same tagged-local-Node batching rule immediately again instead of stopping after the first buffer win. A second eight-file buffer wave is now staged from official `v20.20.2`, `v22.15.0`, and `v24.15.0`: `test-buffer-ascii.js`, `test-buffer-badhex.js`, `test-buffer-inspect.js`, `test-buffer-readdouble.js`, `test-buffer-readfloat.js`, `test-buffer-tojson.js`, `test-buffer-writedouble.js`, and `test-buffer-writefloat.js` are byte-identical across all three versions, so Neovex stages one shared official LTS body for the live Node20/Node22 lanes plus the official Node24 copies in preview. The whole second batch also passed in the Node22 primary lane, the official Node20 validation lane, and the ignored Node24 preview lane. The measured manifest counts are now 85 Node22 files, the paired 83-file official Node20 validation subset, and a green 86-file ignored Node24 preview subset. The preview result still remains forward visibility only and does not broaden the public support claim. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-compare-offset.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-fakes.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-compare-offset.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-fakes.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the same tagged-local-Node batching direction but used the manifested lanes to separate a real runtime fix from a deeper shared gap. Neovex now explicitly exposes `structuredClone` in the embedded Node22 bootstrap, which fixed the immediate missing-global failure shape for `test-buffer-isascii.js`. That revealed the deeper runtime seam underneath it: transfer-style `structuredClone()` still leaves the original `ArrayBuffer` usable in the embedded runtime, so `test-buffer-isascii.js` and `test-buffer-isutf8.js` are now pinned as shared ignored watchpoints instead of being counted green. While handling that honestly, Neovex still widened the green manifest with two more buffer files from official `v20.20.2`, `v22.15.0`, and `v24.15.0`: `test-buffer-compare-offset.js` and `test-buffer-fakes.js` are now green in the Node22 primary lane, the official Node20 validation lane, and the ignored Node24 preview lane. A wider `safe-unsafe` / `swap` follow-on surfaced a transient `SIGSEGV`, so those files were intentionally left out of the green manifest until they can be rerun in isolation. The measured manifest counts are now 87 Node22 files, the paired 85-file official Node20 validation subset, and a green 88-file ignored Node24 preview subset. The public contract still does not broaden to a Node24 support claim. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `4 passed, 0 failed, 14 ignored` |
| 2026-04-29 | NLC3 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-events-once.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-makelong.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-posix-exists.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-path-win32-exists.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-string-decoder.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-events-once.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-path-makelong.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-events-once.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-path-makelong.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-path-posix-exists.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-path-win32-exists.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-string-decoder.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Used the manifest-driven runner to close another real `NLC3` batch without adding bespoke compatibility tests. Imported official `events.once`, `path` alias/existence, `path-makelong`, and `string_decoder` fixtures from the local `~/src/github.com/nodejs/node` source family, then let the manifested lanes classify the outcomes. Two results were real behavioral findings instead of harness noise: official Node22 `test-path-makelong.js` is now an explicit watchpoint because the current runtime still returns the older Node20 `\\\\?\\foo` shape instead of the Node22 trailing-slash result, and official Node20 `test-events-once.js` is now an explicit divergence watchpoint because the current runtime matches newer Node22 invalid-options behavior and rejects `once(..., null)`. The only false-negative was `test-string-decoder.js`, which exposed a missing stock Node `test/common` helper, so Neovex added generic `getArrayBufferViews()` / `getBufferSources()` support instead of hand-customizing the fixture. After that harness fix, the widened manifested counts are now 91 Node22 files, the paired 89-file official Node20 validation subset, and a green 93-file ignored Node24 preview subset. The full local `node_compat::` lane is green again at `4 passed, 0 failed, 16 ignored`. The remaining-file inventory is now explicit: 35 unstaged public-core official files remain for `NLC3`, while 16 official files clearly belong to later host/process/TTY/module families and 3 upstream files are internal-only helpers that should not count toward the public `NLC3` denominator. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `4 passed, 0 failed, 16 ignored` |
| 2026-04-29 | NLC3 | `done` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-buffer-safe-unsafe.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-buffer-sharedarraybuffer.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-buffer-swap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-buffer-new.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-buffer-parent-property.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Finished the imported public-core `NLC3` corpus instead of leaving the Buffer constructor tail ambiguous. Imported the missing official `nodejs/node v22.15.0` Buffer files from the canonical local `~/src/github.com/nodejs/node` source, widened the manifest-driven batch with `safe-unsafe`, `sharedarraybuffer`, `swap`, `new`, and `parent-property`, and proved the whole expanded slice green in the Node22 primary lane, the official Node20 validation lane, and the ignored Node24 preview lane. The manifest summary is now `101` green Node22 files, `99` green Node20 files, and `103` green Node24 preview files. All `104` imported official Node20 public-core files for the `NLC3` families are now represented by either the manifested green batch or an explicit watchpoint, so no imported public-core fixtures remain unstaged. The remaining `NLC3` work is entirely the classified watchpoint set and later-family/internal buckets already recorded in the failure inventory, which satisfies the family closeout contract and promotes `NLC4` as the next active roadmap item. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` |
| 2026-04-30 | NLC3 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-constructor-deprecation-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-nopendingdep-map.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-buffer-pending-deprecation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-console-tty-colors.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-constructor-deprecation-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-nopendingdep-map.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-buffer-pending-deprecation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-console-tty-colors.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Reopened `NLC3` after the control plane drifted ahead of the real worktree. Imported another official public-core batch directly from the canonical local `~/src/github.com/nodejs/node` source instead of adding bespoke wrappers: `test-buffer-constructor-deprecation-error.js`, `test-buffer-nopendingdep-map.js`, `test-buffer-pending-deprecation.js`, and `test-console-tty-colors.js` are now manifested in the Node22 primary lane, the official Node20 validation lane, and the staged Node24 preview corpus. The earlier `test-path-glob.js` import also exposed a real runtime seam, so Neovex fixed two honest runtime/harness issues rather than backing away from the copied test corpus: `module_loader.rs` now treats `ext:` specifiers as extension modules instead of bare package specifiers, and the narrow `test/common` env shim now tolerates the test-only `__MINIMATCH_TESTING_PLATFORM__` probe used by Deno's vendored `minimatch` path. With those fixes in place the focused contract lanes are green again at `120` Node22 files and `116` official Node20 files. The Node24 preview lane still aborts early through a reproducible `rusty_v8` weak-handle panic near `test-buffer-alloc.js`, and the aggregate `node_compat::` lane still hits a `SIGSEGV` when it runs the heavy Node20 and Node22 manifested batches back-to-back in one Rust test process, so both remain explicit harness/runtime watchpoints instead of support claims. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1 --ignored` → `signal: 6 (SIGABRT)`; `cargo test -p neovex-runtime node_compat:: -- --nocapture --test-threads=1` → `signal: 11 (SIGSEGV)`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-04-30 | NLC3 | `done` | `Cargo.toml`, `Cargo.lock`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/core-semantics.md`, `docs/architecture/runtime/node-lts-compat/failures/core-semantics.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the remaining `NLC3` runtime seam instead of carrying it forward as hand-wavy risk. A one-file Deno-fork fix now aligns `internal/events/abort_listener.mjs` with the real web `AbortSignal` event system by using `ext:deno_web/02_event.js`'s `kResistStopImmediatePropagation` hook, published as `agentstation/deno v2.7.14-locker.19`. Neovex is repinned to that tag, `test-events-add-abort-listener.mjs` now runs green in both the Node22 primary lane and the official Node20 validation lane, and the focused manifested lanes are both green again at `120` Node22 files and `116` official Node20 files. The remaining `NLC3` items are the already classified watchpoints plus later-family/internal buckets in the checked-in failure inventory, so the family now satisfies its closeout contract and promotes `NLC4` as the active control item. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_core_semantics_subset -- --nocapture --test-threads=1` → `1 passed` |
| 2026-04-30 | NLC4 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24`, `docs/plans/node-lts-compatibility-plan.md` | Started `NLC4` by importing the broad official Node process/timers/util/diagnostics_channel/perf_hooks candidate corpus from the canonical local `~/src/github.com/nodejs/node` worktree instead of hand-picking a tiny first subset. The fixture roots now include the full first-pass candidate set from official `v20.20.2`, `v22.15.0`, and `v24.15.0`: `226` Node20 files, `240` Node22 files, and `278` Node24 files across `process`, `timers`, `util`, `diagnostics_channel`, `perf_hooks`, and the closely related `next-tick` coverage. The execution model for `NLC4` is now explicit: Node is the canonical truth source for the corpus, `agentstation/deno` and `denoland/deno` remain the implementation and harness references, and the local Bun repo is a release-posture reference for broad Node-suite execution rather than a truth source. The next step is to build the first manifest-driven `NLC4` batch from this imported corpus and let the focused Node22/Node20 lanes reveal the actual runtime seams and later-family dependencies. | `git -C ~/src/github.com/nodejs/node ls-tree -r --name-only v22.15.0 test | rg '(^|/)(test-(process|timers|util|diagnostics-channel|perf-hooks).*(js|mjs)|test-(setimmediate|next-tick).*(js|mjs))$'`; `git -C ~/src/github.com/nodejs/node ls-tree -r --name-only v20.20.2 test | rg '(^|/)(test-(process|timers|util|diagnostics-channel|perf-hooks).*(js|mjs)|test-(setimmediate|next-tick).*(js|mjs))$'`; `find crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test -type f | rg '/test-(process|timers|util|diagnostics-channel|perf-hooks)|/test-(setimmediate|next-tick)' | wc -l` → `226`; `find crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test -type f | rg '/test-(process|timers|util|diagnostics-channel|perf-hooks)|/test-(setimmediate|next-tick)' | wc -l` → `240`; `find crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test -type f | rg '/test-(process|timers|util|diagnostics-channel|perf-hooks)|/test-(setimmediate|next-tick)' | wc -l` → `278` |
| 2026-04-30 | NLC4 | `in_progress` | `AGENTS.md`, `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno` | Resolved the first ownership and hot-path review inside `NLC4` instead of letting Neovex grow a local Node semantic shim layer. The earlier Neovex-local `util.format()` numeric-separator / `%s` coercion patch and `diagnostics_channel` sync-unsubscribe patch were removed from `node22_runtime_bootstrap.js`, and the shared behavior now lives in the Deno fork where the hot path actually runs. The Deno side narrowed the earlier over-broad hidden-host-env subprocess change by restoring generic `Deno.Command` `clearEnv` passthrough in `ext/process/40_process.js` and keeping the Neovex-specific sync `node:child_process` env policy only at `ext/node/polyfills/internal/child_process.ts`. Those fork changes shipped as `agentstation/deno v2.7.14-locker.20`, which immediately closed the two original manifested `NLC4` failures (`test-util-format.js` numeric separators and `test-diagnostics-channel-sync-unsubscribe.js`) and exposed one narrower shared `util.format()` `%s` + `Symbol.toPrimitive` seam. A second fork cut, `agentstation/deno v2.7.14-locker.21`, fixed `hasBuiltInToString()` to match newer upstream Node coercion rules and added the fork-side util regression. With Neovex repinned to `v2.7.14-locker.21`, the manifested Node22 `NLC4` subset is now green at `41 / 41`, and the official Node20 validation subset is green at `40 / 40` once the single intentional validation-lane divergence (`test-process-features.js` and the Node20-vs-Node22 `process.features.typescript` key set) is carved out as an explicit watchpoint. This also verified the macOS verification seam: `CARGO_ENCODED_RUSTFLAGS` is now the documented command-scoped way to replace the fork's checked-in `-fuse-ld=lld` target rustflags for a single local Deno command. The Deno `unit_node` harness still could not fully run on this machine because it needs additional prerequisites (`cmake`, built `deno`, and built `test_server`), so the focused Neovex manifested lanes remain the primary integration proof. `NLC4` stays open because `process.finalization.*`, `process.loadEnvFile()`, util MIME APIs, `perf_hooks`, and later host-sensitive files are still outside the current manifested green subset. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → first repin `40 passed, 1 failed` (new `%s` + `Symbol.toPrimitive` seam), second repin `41 passed, 0 failed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `40 passed, 1 failed` before classifying `test-process-features.js`, then `40 passed, 0 failed`; `cargo test -p unit_node_tests --test unit_node diagnostics_channel_test` in `~/src/github.com/agentstation/deno` with `CARGO_ENCODED_RUSTFLAGS` → linker seam fixed, then blocked by missing built `test_server`; `cargo build -p deno -p test_server` in `~/src/github.com/agentstation/deno` with `CARGO_ENCODED_RUSTFLAGS` → blocked by missing `cmake` |
| 2026-04-30 | NLC4 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno` | Promoted the imported official `util.parseEnv()` fixture into the manifested `NLC4` batch and fixed the real owner instead of leaving it as a hand-wavy watchpoint. A Neovex-local bootstrap shim attempt did not intercept the live builtin export path cleanly, which made this a good example of the fork-ownership rule: the real semantic owner was Deno's `ext/node/polyfills/util.ts`, not `node22_runtime_bootstrap.js`. I landed the one-line plain-object shape fix plus a fork-side regression in `tests/unit_node/util_test.ts`, published it as `agentstation/deno v2.7.14-locker.22`, and repinned Neovex to that tag. With the repin live, `test-util-parse-env.js` is now green in the Node22 primary lane, the official Node20 validation lane, and the ignored Node24 preview lane. The manifested process/timing counts are now `42` green official files for Node22 and `41` green official files for Node20. The Node24 preview lane remains future-visibility evidence only: it now stages the same `42` files, but the latest explicit run is `39 passed, 3 failed` on preview-only divergences (`test-process-features.js`, `test-util-deprecate.js`, and `test-util-format.js`). `NLC4` stays open because the imported `perf_hooks` watchpoints remain red, the official `process.loadEnvFile()` files are still host-sensitive and unstaged, and util MIME / `process.finalization.*` work has not been promoted into the green denominator yet. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `42` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `41` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1 --ignored` → `39 passed, 3 failed` preview-only |
| 2026-04-30 | NLC4 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-mime-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-mime-whatwg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-mime-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-mime-whatwg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-mime-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-mime-whatwg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/mime-whatwg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/mime-whatwg-generated.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno` | Widened the green `NLC4` denominator with the official util MIME batch and used the imported `perf_hooks` watchpoint to drive the next owner fix instead of guessing. Official `test-mime-api.js` and `test-mime-whatwg.js` plus the shared WPT fixture data are now staged for Node20, Node22, and the Node24 preview lane, and both LTS manifested lanes stay green with the larger subset. That moves the manifested counts to `44` green official files for Node22 and `43` green official files for Node20, with the staged Node24 preview lane now at `44` files and the latest explicit preview run at `41 passed, 3 failed`. In parallel, the imported `test-perf-hooks-usertiming.js` watchpoint advanced from "missing exports" to a much narrower semantic seam: Deno-fork tags `v2.7.14-locker.23` and `v2.7.14-locker.24` now export `PerformanceMark` / `PerformanceMeasure`, seed the minimal `nodeTiming` marks the official file expects, and restore Node-style Symbol coercion errors for `performance.mark()` / `clearMarks()`, but the watchpoint still fails on Node-style validation details such as `performance.mark('a', { startTime: 'a' })` needing `ERR_INVALID_ARG_TYPE`. `NLC4` therefore remains open, but the remaining `perf_hooks` work is now much better classified and the manifested green subset is larger and more truthful than before. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `44` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `43` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1 --ignored` → `41 passed, 3 failed`; `cargo test -p neovex-runtime node22_perf_hooks_usertiming_watchpoint -- --nocapture --test-threads=1 --ignored` → first failed on missing `PerformanceMark`, second failed on Symbol coercion messaging, latest failed on missing `ERR_INVALID_ARG_TYPE` validation for `startTime` |
| 2026-04-30 | NLC4 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno` | Finished the imported `perf_hooks` user-timing seam instead of leaving it as a stale watchpoint. Deno-fork tag `v2.7.14-locker.25` now adds the final Node-style `ERR_INVALID_ARG_TYPE` validation for `performance.mark('a', { startTime: ... })`, which closes the last red behavior in the official `test-perf-hooks-usertiming.js` file. Neovex is repinned to that baseline, the official file is promoted into `PROCESS_AND_TIMING_BATCH`, and the old ignored user-timing watchpoints are removed so the manifested denominator is the real proof surface again. The measured `NLC4` subset is now `45` green official files for Node22 and `44` green official files for Node20, while the staged Node24 preview lane moves to `42 passed, 3 failed` across the same `45` files. `NLC4` remains open because `perf_hooks` histogram/resource-timing, `process.finalization.*`, and `process.loadEnvFile()` still sit outside the green denominator. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `45` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `44` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1 --ignored` → `42 passed, 3 failed`; `cargo test -p neovex-runtime node22_perf_hooks_usertiming_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node20_perf_hooks_usertiming_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed` |
| 2026-04-30 | NLC4 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno` | Closed the imported `perf_hooks` resource-timing seam at the real owner and widened the manifested `NLC4` proof surface instead of leaving another long-lived watchpoint. The first Deno-fork cut, `agentstation/deno v2.7.14-locker.26`, exported `PerformanceResourceTiming` and implemented `performance.markResourceTiming()`, but the focused Node22 file immediately exposed an owner-layer bug: the new web class referenced an undefined enumerable descriptor helper. `v2.7.14-locker.27` fixed that load-time regression, which moved the failure to the real semantic seam: public `performance.getEntries*()` still leaked the internal `nodeTiming` bootstrap marks and inflated the official Node22 entry counts. `v2.7.14-locker.28` fixed that by hiding the seeded `nodeTiming` marks from public `perf_hooks` entry queries while keeping them available for the Node measure contract, and Neovex is now repinned to that final baseline. With the repin live, official `test-perf-hooks-resourcetiming.js` is promoted into `PROCESS_AND_TIMING_BATCH` for the Node22 primary lane and the staged Node24 preview lane. The official Node22 manifested subset is now green at `46` files, the official Node20 validation subset remains green at `44` files, and the Node24 preview lane now runs `46` staged files with `43 passed, 3 failed`. The official Node20 `test-perf-hooks-resourcetiming.js` file stays as an explicit validation-lane divergence watchpoint because Neovex intentionally keeps the later Node22-shaped `PerformanceResourceTiming#toJSON()` fields `deliveryType` and `responseStatus` rather than the older Node20 shape. `NLC4` stays open because `perf_hooks.createHistogram()`, `process.finalization.*`, and `process.loadEnvFile()` still sit outside the green denominator. | `cargo test -p neovex-runtime node22_perf_hooks_resourcetiming_watchpoint -- --nocapture --test-threads=1 --ignored` → first failed on `ReferenceError: kEnumerableProperty is not defined`, then on `performance.getEntries()` count inflation, then passed after `v2.7.14-locker.28`; `cargo test -p neovex-runtime node20_perf_hooks_resourcetiming_watchpoint -- --nocapture --test-threads=1 --ignored` → fails as an intentional validation-lane divergence on the older Node20 `PerformanceResourceTiming#toJSON()` shape; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `46` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `44` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1 --ignored` → `43 passed, 3 failed` |
| 2026-04-30 | NLC4 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/source.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/dotenv/.env`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/dotenv/basic-valid.env`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the imported official `test-process-load-env-file.js` file into the manifested `NLC4` denominator by fixing the real embedder-owned seam instead of pushing more work into the Deno fork. The earlier `loadEnvFile()` watchpoint had already narrowed to a batch-stability problem: the official Node20/Node22/Node24 top-level file ran green in isolation, but promoting it into `PROCESS_AND_TIMING_BATCH` still perturbed later fixtures because the embedded child-process helper runs inside the same Rust process and Deno's base `process.loadEnvFile()` mutates real host env via `env::set_var`. Neovex now owns that remaining contract locally: `source.rs` seeds a hidden runtime env overlay so explicitly loaded keys become visible through `process.env` without widening ambient host env access, `node22_runtime_bootstrap.js` wraps the base op with Node-style path/default handling plus missing-file and permission error shaping, and `bootstrap/ops/test_runtime.rs` now snapshots/restores host `cwd` and process-global env around every spawned child execution so `loadEnvFile()` no longer contaminates later manifested fixtures. With those local fixes in place, official `test-process-load-env-file.js` is now green inside the shared Node22 primary lane, the shared official Node20 validation lane, and the staged Node24 preview lane. The measured `NLC4` subset is now `47` green official files for Node22 and `45` green official files for Node20, while the Node24 preview lane moves to `44 passed, 3 failed` across the same `47` staged files. `NLC4` remains open because `process.finalization.*` and `perf_hooks.createHistogram()` still sit outside the green denominator. | `cargo test -p neovex-runtime node22_target_load_env_file_missing_file_surfaces_node_not_found_error -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `47` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `45` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1 --ignored` → `44 passed, 3 failed` |
| 2026-04-30 | NLC4 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the last real `process.finalization.*` harness gap without pretending the later `worker_threads` dependency belonged to `NLC4`. Neovex now exposes a sync `node_compat` subprocess op alongside the earlier async helper, the shared `test/common/index.js` harness now routes `child_process.spawnSync(process.execPath, ...)` through that embedded runtime path when the request stays inside the narrow supported shape, and the test-only spawn bundle now resolves relative script and `--env-file` paths from the fixture `cwd` instead of treating them as repo-root paths. That moved the imported official `test-process-finalization.mjs` file from a fake `gc()` / subprocess-shape blocker to its real remaining dependency: `different-registry-per-thread.mjs` still expects `node:worker_threads`, which is explicitly owned by the later VM/worker family. The direct official finalization fixture bodies (`close.mjs`, `before-exit.mjs`, and `unregister.mjs`) now run green through the same sync harness, while the Node22 and Node20 manifested `NLC4` process/timing lanes remain green at `47 / 47` and `45 / 45` after the harness change. `NLC4` therefore no longer has an independent `process.finalization.*` runtime seam; the honest remaining runtime gap inside this family is now `perf_hooks.createHistogram()`, plus later-family files already classified outside the current denominator. | `cargo test -p neovex-runtime node22_process_finalization_ -- --nocapture --test-threads=1` → `3 passed, 1 ignored`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `47` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `45` official files green |
| 2026-04-30 | NLC4 | `done` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/js/perf_hooks.js`, `crates/neovex-runtime/src/runtime/bootstrap/node22_runtime.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/process-and-timing.md`, `docs/architecture/runtime/node-lts-compat/failures/process-and-timing.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed `NLC4` by finishing the last in-scope `perf_hooks` runtime seam locally and then reconciling the control plane to the measured truth. Neovex now owns a dedicated `node:perf_hooks` builtin override through `module_loader.rs` and `ext:neovex_node22/perf_hooks_impl.js` instead of trying to shadow Deno's existing `ext:` module registration. That local owner path adds `createHistogram()`, shared-state `RecordableHistogram` cloning, the current `monitorEventLoopDelay()` clone/summary contract, and the minimal `MessageChannel`/`MessagePort` exposure the imported Node files require, while keeping the shared Deno-fork user-timing and resource-timing semantics intact. With histogram promoted into `PROCESS_AND_TIMING_BATCH`, the Node22 manifested `NLC4` lane is now green at `48 / 48` official files and the official Node20 validation lane is green at `46 / 46`, while the Node24 preview lane stays explicitly non-contractual at `45 passed, 3 failed`. `process.finalization.*` is now truthfully classified as a later-family dependency because the direct official fixtures are green and the only remaining wrapper-file failure is `different-registry-per-thread.mjs` on `node:worker_threads`. That leaves no unexplained in-scope `NLC4` failures and promotes `NLC5` as the next active roadmap item. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `48` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `1 passed` with `46` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1 --ignored` → `45 passed, 3 failed` |
| 2026-04-30 | NLC5 | `in_progress` | `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Activated `NLC5` immediately after closing `NLC4` so the control plane has a real next seam instead of a placeholder status flip. Using the canonical local `~/src/github.com/nodejs/node` worktree, I staged the first streams/local-I/O candidate corpus and wrote down the broad starting denominator: `512` official Node22 files, `509` official Node20 files, and `627` staged Node24 preview files across `stream`, `fs`, `readline`, `tty`, and `os`. The new manifest records the initial slice map and the batching rule for the next execution step: start with stream primitives plus bundle-root-safe `fs` / `os` / `tty` helpers, then widen into `readline` once the first shared seams are green. | `git -C ~/src/github.com/nodejs/node ls-tree -r --name-only v22.15.0 test | rg '(^|/)(test-(stream|fs|readline|tty|os).*(js|mjs)|test-(whatwg-readablestream|whatwg-writablestream|whatwg-transformstream).*(js|mjs))$' | wc -l` → `512`; `git -C ~/src/github.com/nodejs/node ls-tree -r --name-only v20.20.2 test | rg '(^|/)(test-(stream|fs|readline|tty|os).*(js|mjs)|test-(whatwg-readablestream|whatwg-writablestream|whatwg-transformstream).*(js|mjs))$' | wc -l` → `509`; `git -C ~/src/github.com/nodejs/node ls-tree -r --name-only v24.15.0 test | rg '(^|/)(test-(stream|fs|readline|tty|os).*(js|mjs)|test-(whatwg-readablestream|whatwg-writablestream|whatwg-transformstream).*(js|mjs))$' | wc -l` → `627` |
| 2026-04-30 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Turned the `NLC5` import plan into a real first manifested lane instead of another dormant corpus. First, the full broad official `stream` / `fs` / `readline` / `tty` / `os` candidate families were imported from the canonical local `~/src/github.com/nodejs/node` worktree into the three checked-in fixture roots, so `NLC5` no longer starts from zero staged files. Then the first shared-seam batch was wired into `STREAMS_AND_LOCAL_IO_BATCH`: `15` official files covering core `stream` constructors, inheritance, readable / writable / duplex state helpers, unpipe behavior, and `os.EOL`. The measured result is strong and fast-moving: the Node22 primary lane is green at `15 / 15`, the Node24 preview lane is also green at `15 / 15`, and the official Node20 validation lane is green at `14 / 14` after carving out one explicit validation-lane divergence. That divergence is `test-stream-duplex-readable-end.js`, where official Node20 still probes the older default-highWaterMark flow-control path while the current runtime matches the later Node22/Node24 explicit-highWaterMark shape. This keeps the first `NLC5` batch honest and green while the next widening step focuses on more stream primitives and the first bundle-root-safe `fs` slice. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `15` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `14 passed, 1 failed`, then `14 passed, 0 failed` after classifying `test-stream-duplex-readable-end.js`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `15 passed, 0 failed` |
| 2026-04-30 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the first `NLC5` stream-first batch instead of stalling after the first green slice. Added eight more shared official stream files that stay inside the same pure-stream seam: `test-stream-duplex-end.js`, `test-stream-end-paused.js`, `test-stream-error-once.js`, `test-stream-events-prepend.js`, `test-stream-ispaused.js`, `test-stream-readable-data.js`, `test-stream-readable-pause-and-resume.js`, and `test-stream-readable-readable.js`. The widened batch stays green in the Node22 primary lane at `23 / 23` and in the Node24 preview lane at `23 / 23`, while the official Node20 validation lane is green at `22 / 22` with the same single explicit divergence outside the denominator: `test-stream-duplex-readable-end.js`. That keeps `NLC5` moving in the desired high-throughput shape: broad corpus already staged, one shared seam widened in batches, and older-LTS differences carved out only when they are genuinely version-specific rather than runtime regressions. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `23` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `22` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `23` staged files green |
| 2026-04-30 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the faster manifest-driven `NLC5` direction and widened the same pure `stream` seam instead of dropping into `fs` or writing more bespoke harness code. Added a second 15-file official batch focused on destroy, auto-destroy, aborted, finished-state, and readable / writable state semantics: `test-stream-auto-destroy.js`, `test-stream-duplex-destroy.js`, `test-stream-readable-aborted.js`, `test-stream-readable-destroy.js`, `test-stream-readable-end-destroyed.js`, `test-stream-duplex-readable-writable.js`, `test-stream-readable-next-no-null.js`, `test-stream-readable-no-unneeded-readable.js`, `test-stream-unshift-empty-chunk.js`, `test-stream-writable-aborted.js`, `test-stream-writable-destroy.js`, `test-stream-writable-end-multiple.js`, `test-stream-writable-finish-destroyed.js`, `test-stream-writable-finished-state.js`, and `test-stream-writable-writable.js`. The whole widened batch stayed green in all three manifested lanes, lifting the measured denominator to `38 / 38` for Node22, `37 / 37` for the official Node20 validation lane, and `38 / 38` for the explicit Node24 preview lane while keeping the same single Node20-only divergence outside the denominator: `test-stream-duplex-readable-end.js`. This keeps `NLC5` on the desired high-throughput path: use the already-imported official corpus, widen by shared runtime seam, and defer `fs` / host-sensitive families until the pure-stream batch stops yielding easy green expansion. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `38` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `37` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `38` staged files green |
| 2026-04-30 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept widening the same pure-stream ownership seam instead of switching families too early. Added a third 8-file official batch centered on pipe / drain / flow listener behavior: `test-stream-passthrough-drain.js`, `test-stream-pipe-after-end.js`, `test-stream-pipe-await-drain.js`, `test-stream-pipe-cleanup.js`, `test-stream-pipe-event.js`, `test-stream-pipe-flow.js`, `test-stream-pipe-multiple-pipes.js`, and `test-stream-write-drain.js`. The entire seam stayed green in the Node22 primary lane, the official Node20 validation lane, and the explicit Node24 preview lane, lifting the measured denominator again to `46 / 46` for Node22, `45 / 45` for Node20 validation, and `46 / 46` for Node24 preview while keeping the same single Node20-only divergence outside the denominator: `test-stream-duplex-readable-end.js`. That means `NLC5` now has a materially larger verified `stream` core before any `fs` or TTY-host seam work, which is the right fast path for closing the family honestly. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `46` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `45` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `46` staged files green |
| 2026-04-30 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the same shared `stream` seam again with `finished()` and transform final/flush/callback-order behavior instead of dropping into a new family. Added six more official files: `test-stream-end-of-streams.js`, `test-stream-writable-finished.js`, `test-stream-transform-callback-twice.js`, `test-stream-transform-final.js`, `test-stream-transform-final-sync.js`, and `test-stream-transform-flush-data.js`. All six stayed green in the Node22 primary lane, the official Node20 validation lane, and the explicit Node24 preview lane, raising the manifested denominator to `52 / 52` for Node22, `51 / 51` for Node20 validation, and `52 / 52` for the preview lane while preserving the same lone Node20-only divergence outside the denominator: `test-stream-duplex-readable-end.js`. This keeps `NLC5` moving in the right batched shape: keep mining the already-imported official corpus for pure-stream seams until the first real transform/pipeline/runtime gap appears, and only then decide whether the next fastest path is more stream work or the first bundle-root-safe `fs` slice. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `52` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `51` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `52` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the same stream-first batching strategy and widened another pure `stream` seam around constructor wiring, invalid-chunk validation, and split Transform highWaterMark / objectMode shaping. Added eight more official files: `test-stream-readable-constructor-set-methods.js`, `test-stream-writable-constructor-set-methods.js`, `test-stream-transform-constructor-set-methods.js`, `test-stream-readable-invalid-chunk.js`, `test-stream-writable-invalid-chunk.js`, `test-stream-transform-objectmode-falsey-value.js`, `test-stream-transform-split-highwatermark.js`, and `test-stream-transform-split-objectmode.js`. The Node22 primary lane and the explicit Node24 preview lane stayed fully green, lifting the manifested denominator to `60 / 60`. The official Node20 validation lane exposed two more older-LTS contract drifts, not new Node22 runtime bugs: official `v20.20.2` still hard-codes the older split Transform `16 KiB` default highWaterMark expectations in `test-stream-transform-split-highwatermark.js` and `test-stream-transform-split-objectmode.js`, while Neovex intentionally matches the later Node22/Node24 `getDefaultHighWaterMark()` / non-Windows `64 KiB` contract. Those two files are now explicit Node20 validation watchpoints alongside the existing `test-stream-duplex-readable-end.js` divergence, so the Node20 green denominator closes honestly at `57 / 57` while the Node22 and Node24 manifested denominators remain `60 / 60`. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `60` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `57 passed, 2 failed`, then `1 passed` with `57` official files green after classifying the two Node20-only Transform highWaterMark drifts; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `60` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the stream-first batching path but widened it with a version-sensitive encoding / typed-array seam instead of switching families too early. Added seven more official files: `test-stream-readable-setEncoding-existing-buffers.js`, `test-stream-readable-setEncoding-null.js`, `test-stream-typedarray.js`, `test-stream-uint8array.js`, `test-stream-writable-change-default-encoding.js`, `test-stream-writable-decoded-encoding.js`, and `test-stream-writable-needdrain-state.js`. The first Node22 rerun surfaced a harness drift rather than a runtime bug: Neovex's shared `test/common` shim always included `Float16Array` in `getArrayBufferViews()`, while the official Node20 and Node22 corpora still omit it and Node24 adds it later. After making that helper version-sensitive by lane, all three focused lanes closed green. The manifested denominator now rises to `67 / 67` for the Node22 primary lane, `64 / 64` for the official Node20 validation lane, and `67 / 67` for the explicit Node24 preview lane while keeping the same three Node20-only validation divergences outside the denominator. This preserves the fast `NLC5` loop: keep importing official files in shared-seam batches, fix corpus-version mismatches in the harness when the official files require it, and save real owner-layer stream work for actual runtime gaps. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `66 passed, 1 failed` on `test-stream-typedarray.js`, then `1 passed` with `67` official files green after the `Float16Array` lane-version fix; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `64` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `67` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the batch size large and stayed inside one pure writable/final/writev seam instead of dropping into `fs` or host-sensitive families. Added twelve more official files: `test-stream-writable-end-cb-error.js`, `test-stream-writable-end-cb-uncaught.js`, `test-stream-writable-final-async.js`, `test-stream-writable-final-destroy.js`, `test-stream-writable-final-throw.js`, `test-stream-writable-write-cb-error.js`, `test-stream-writable-write-cb-twice.js`, `test-stream-writable-write-error.js`, `test-stream-writable-write-writev-finish.js`, `test-stream-write-destroy.js`, `test-stream-write-final.js`, and `test-stream-writev.js`. The Node22 primary lane and the official Node20 validation lane both stayed fully green, raising the manifested denominator to `79 / 79` for Node22 and `76 / 76` for Node20 validation while keeping the same three Node20-only divergences outside the denominator. The only failure came from the Node24 preview lane and it was another harness-version gap, not a runtime bug: the newer `test-stream-writev.js` uses `common.mustSucceed()`, so Neovex's shared `test/common` shim now implements that newer helper. With that lift in place, the explicit Node24 preview lane also closes green at `79 / 79`. This keeps `NLC5` in the right high-throughput shape: big shared-seam imports, focused lane reruns, and harness-version updates only when the official corpus actually requires them. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `79` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `76` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → first `78 passed, 1 failed` on `test-stream-writev.js`, then `1 passed` with `79` staged files green after adding `common.mustSucceed()` |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/fixtures.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/x.txt`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC5` again with the higher-level stream utility seam instead of dropping into the much broader `pipeline.js` or the first `fs` slice too early. Added eight more official files: `test-stream-reduce.js`, `test-stream-map.js`, `test-stream-filter.js`, `test-stream-forEach.js`, `test-stream-toArray.js`, `test-stream-drop-take.js`, `test-stream-flatMap.js`, and `test-stream-consumers.js`. The whole seam stayed green in all three focused lanes, raising the manifested denominator to `87 / 87` for the Node22 primary lane, `84 / 84` for the official Node20 validation lane, and `87 / 87` for the explicit Node24 preview lane while preserving the same three Node20-only divergences outside the denominator. The only extra staging needed was a narrow shared fixture file, `test/fixtures/x.txt`, for `test-stream-flatMap.js`; no runtime owner changes were required. This keeps `NLC5` honest and fast: widen the pure `node:stream` surface with imported official files, add only the minimum fixture support the corpus demands, and save the first real owner-layer runtime changes for when the corpus finally exposes them. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `87` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `84` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `87` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/fixtures.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/tmpdir.js`, `crates/neovex-runtime/src/runtime_capabilities.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Turned the first `NLC5` `fs` wave into a real bundle-root-safe application-profile slice instead of leaving it as a half-green experiment. The runner already had the imported official files, so this pass focused on owner-layer fixes and truthful carve-outs. On the local runtime side, the application profile now allows writes inside the generated bundle root, the staged Deno facade gained `remove`, `chmod`, richer `stat` metadata, and the scoped sys allowance now includes `uid`, `gid`, and `statfs`. That was enough to make the first positive-path `fs` batch green without widening host escape semantics: `test-fs-buffer.js`, `test-fs-close.js`, `test-fs-constants.js`, `test-fs-exists.js`, `test-fs-existssync-false.js`, and the current Node22/Node24 `test-fs-stat.js` now sit in the manifested denominator beside the existing `stream` and `os.EOL` coverage. The harder `fs` files were not buried in the main lane; they are now explicit watchpoints. Node22 holds out `test-fs-access.js` (async stack-shape parity), `test-fs-open.js` (application-profile absolute-path denial versus upstream `ENOENT`), `test-fs-readfile.js` (`ERR_FS_FILE_TOO_LARGE` sparse-file behavior), and `test-fs-readdir.js` (`ENOTDIR` surfacing on regular files). Node20 keeps the same three earlier stream divergences plus a new `test-fs-stat.js` validation-lane divergence because the older file still asserts the heavier `JSON.stringify(Stats)` shape. Node24 stays green as a preview lane after carving out one preview-only `test-fs-constants.js` divergence. The manifested denominators are now `93 / 93` for Node22, `89 / 89` for Node20 validation, and `92 / 92` for the Node24 preview lane. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `93` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `89` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `92` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the faster manifest-driven `NLC5` shape and widened the first bundle-root-safe `fs` slice with a whole append/read/write seam instead of slipping back into one-off files. Added twelve more official files to the manifested denominator: `test-fs-append-file-sync.js`, `test-fs-append-file.js`, `test-fs-buffertype-writesync.js`, `test-fs-read-empty-buffer.js`, `test-fs-read-zero-length.js`, `test-fs-read.js`, `test-fs-readSync-optional-params.js`, `test-fs-write-buffer.js`, `test-fs-write-file-buffer.js`, `test-fs-write-file-typedarrays.js`, `test-fs-write-file.js`, and `test-fs-write-sync.js`. All three focused lanes stayed green with no new owner-layer runtime work and no new watchpoints: the Node22 primary lane now closes at `105 / 105`, the official Node20 validation lane at `101 / 101`, and the explicit Node24 preview lane at `104 / 104`, while keeping the previously classified Node22 `fs.access` / `fs.open` / `fs.readFile` / `fs.readdir` watchpoints, the four Node20 validation divergences, and the single Node24 preview-only `fs.constants` divergence outside the denominator. This is exactly the batched-corpus shape `NLC5` needs: import official files aggressively, widen one coherent seam at a time, and reserve deeper runtime work for the files that actually stay red. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `105` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `101` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `104` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept widening the same bundle-root-safe `fs` family in a second large seam instead of pausing after the first denominator lift. Added five more official files to the manifested batch: `test-fs-read-optional-params.js`, `test-fs-readv-sync.js`, `test-fs-write-optional-params.js`, `test-fs-write-sync-optional-params.js`, and `test-fs-writev-sync.js`. All three focused lanes stayed green again with no owner-layer runtime changes and no new watchpoints, which raises the measured denominators to `110 / 110` for the Node22 primary lane, `106 / 106` for the official Node20 validation lane, and `109 / 109` for the explicit Node24 preview lane. That keeps the `NLC5` signal clean: there is still a broad pocket of positive-path official `fs` coverage we can import directly, while the genuinely hard remaining work is still isolated to the already-classified Node22 watchpoints (`fs.access`, `fs.open`, `fs.readFile`, `fs.readdir`) plus the existing Node20 and Node24 lane divergences. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `110` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `106` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `109` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/a.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Used the next imported `fs` flags seam as a real compatibility probe instead of forcing another all-green batch. First, staged the missing shared upstream fixture `test/fixtures/a.js` from the canonical local `~/src/github.com/nodejs/node` checkout so the current `open` files could run truthfully. Then promoted the two clean positive-path files `test-fs-open-flags.js` and `test-fs-open-mode-mask.js` into the manifested denominator. The larger six-file experiment did exactly what the control plane needed: it separated three real held-out seams from the green slice. `test-fs-readfilesync-enoent.js` is a Windows-only official file and is now treated as staged-but-not-counted on the current macOS-focused denominator instead of pretending it belongs in the live lane. `test-fs-open-no-close.js` exposed a harness/process-lifecycle seam around automatic `beforeExit` / `exit` quiescence, while `test-fs-open-numeric-flags.js` and `test-fs-readfile-flags.js` exposed real shared local-I/O flags gaps. Those three files are now explicit Node22 watchpoints instead of silently failing the main batch. The payoff is still a real denominator lift: the Node22 primary lane closes green at `116 / 116`, the official Node20 validation lane closes green at `112 / 112`, and the explicit Node24 preview lane closes green at `115 / 115`, all while keeping the newly surfaced `fs` flags and lifecycle seams visible for future owner work. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `116 passed, 4 failed`, then `1 passed` with `116` official files green after classifying the new held-out files; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `112` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `115` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the same high-throughput `NLC5` shape and widened the bundle-root-safe `fs` slice with another shared-LTS options/promises seam instead of falling back to one-off files. First, promoted the clean positive-path files `test-fs-read-offset-null.js`, `test-fs-read-promises-optional-params.js`, `test-fs-promises-write-optional-params.js`, and `test-fs-promisified.js` into the manifested denominator. Then used the larger seven-file experiment honestly: `test-fs-append-file-flush.js` exposed a real shared local-I/O flush gap, `test-fs-assert-encoding-error.js` exposed a broader invalid-encoding error-shape gap, and `test-fs-options-immutable.js` exposed a wider embedded Deno-facade helper seam. The harness gap in that last file was fixed locally by adding `common.canCreateSymLink()`, and Neovex also landed the first owner-layer response to the surfaced helper seam by wiring bundle-root-safe `Deno.symlink()` / `Deno.symlinkSync()` through new local runtime ops. The remaining breadth of `test-fs-options-immutable.js` is still held out explicitly because it wants more `readLink` / `realPath` / `mkdtemp`-style facade coverage than this batch needed to go green. With those carve-outs kept explicit, all three focused lanes are green again at `128 / 128` for the Node22 primary lane, `124 / 124` for the official Node20 validation lane, and `127 / 127` for the explicit Node24 preview lane. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `128 passed, 3 failed`, then `128 passed, 1 failed`, then `128 passed, 0 failed` after classifying the held-out files and landing local `symlink` support; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `124 passed, 0 failed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `127 passed, 0 failed` |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept the same batch-first `NLC5` direction and widened the `fs.promises` filehandle seam without letting the denominator go fuzzy. The imported six-file experiment immediately separated into two classes. Three positive-path files were green and are now promoted into the manifested denominator: `test-fs-promises-file-handle-write.js`, `test-fs-promises-file-handle-append-file.js`, and `test-fs-promises-file-handle-stat.js`. The other three are now explicit held-out watchpoints instead of silently riding the main lane: `test-fs-promises-file-handle-close.js` exposed a harness-owned `globalThis.gc()` gap for deterministic filehandle-finalization warnings, `test-fs-promises-file-handle-close-errors.js` exposed a shared `internal/fs/promises` module-surface gap, and `test-fs-promises-file-handle-read.js` exposed a real omitted-options `fileHandle.read()` bytesRead mismatch. With those three failures carved out truthfully, the focused lanes close green again at `131 / 131` for the Node22 primary lane, `127 / 127` for the official Node20 validation lane, and `130 / 130` for the explicit Node24 preview lane. This keeps `NLC5` moving fast while sharpening the next real owner seam: `fs.promises` filehandle close/read semantics rather than more generic `fs` noise. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `131 passed, 0 failed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `127 passed, 0 failed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `130 passed, 0 failed`; `cargo test -p neovex-runtime node22_fs_promises_file_handle_ -- --nocapture --test-threads=1 --ignored` → `3 failed` with explicit pinned repros for `close`, `close-errors`, and `read` |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/baz.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Pushed the same `fs.promises` filehandle seam one step further instead of stopping at the first green checkpoint. First, staged the upstream shared fixture `test/fixtures/baz.js` and promoted four more positive-path filehandle files into the manifested denominator: `test-fs-promises-file-handle-chmod.js`, `test-fs-promises-file-handle-truncate.js`, `test-fs-promises-file-handle-sync.js`, and `test-fs-promises-file-handle-writeFile.js`. The only red file in the initial four-file experiment was `test-fs-promises-file-handle-sync.js`, and it exposed a crisp Neovex-local owner seam rather than another generic `fs` mismatch: the embedded Deno facade did not yet provide `copyFile()` / `copyFileSync()`. That owner fix now lives locally in `node22_runtime_bootstrap.js` plus the matching runtime ops, which is the right architectural owner because it extends Neovex's local Deno-facade contract rather than changing shared Deno semantics. With `copyFile` landed, the whole four-file batch closes green in all three focused lanes, lifting the manifested denominators again to `135 / 135` for the Node22 primary lane, `131 / 131` for the official Node20 validation lane, and `134 / 134` for the explicit Node24 preview lane while keeping the previously classified filehandle close / close-errors / read watchpoints explicit. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `134 passed, 1 failed` on `test-fs-promises-file-handle-sync.js`, then `135 passed, 0 failed` after landing local `copyFile`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `131 passed, 0 failed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `134 passed, 0 failed` |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/common/index.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/common/index.mjs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept pushing the same `fs.promises` filehandle seam instead of backing away from the first ESM-specific failure. First, staged the official `test/common/index.mjs` helpers for the Node22 and Node24 corpora and added a three-file upstream batch: `test-fs-promises-file-handle-dispose.js`, `test-fs-promises-file-handle-stream.js`, and `test-fs-promises-file-handle-readLines.mjs`. The first rerun showed the real owner seam clearly: Neovex's custom `node:fs/promises` ESM override still exported only the earlier hand-curated surface, so `readLines.mjs` failed because `open` was missing. The fix stayed local and safe. `module_loader.rs` now bridges `node:fs/promises.open()` through `process.getBuiltinModule("fs").promises.open()` after a new Neovex-owned path-validation op confirms the path stays inside the approved runtime roots, which preserves the richer Deno `FileHandle` behavior without widening direct `node:fs` ESM support or bypassing the bundle-root policy. With that bridge landed, all three new filehandle fixtures are green in the supported lanes, raising the manifested denominators to `138 / 138` for the Node22 primary lane and `134 / 134` for the official Node20 validation lane. The explicit Node24 preview lane also went green for `readLines.mjs` and `stream.js`, but `test-fs-promises-file-handle-dispose.js` surfaced a later Node24-only `fs.opendir()` `Dir[Symbol.asyncDispose]()` close-semantics drift. That file is now carved out as a preview-only divergence rather than poisoning the whole preview denominator, so the preview lane stays truthful at `136 / 136` green staged files with two explicit preview-only watchpoints (`fs.constants` and the later `Dir[Symbol.asyncDispose]()` contract). | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `137 passed, 1 failed` on missing `node:fs/promises.open`, then `138 passed, 0 failed` after landing the local `open()` bridge; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `134 passed, 0 failed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → first `136 passed, 1 failed` on `test-fs-promises-file-handle-dispose.js`, then `136 passed, 0 failed` after classifying the Node24-only divergence outside the denominator |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/tick.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept `NLC5` on the same batch-first official-corpus path and widened the positive-path filehandle seam instead of dropping back into single-file work. Added three more official files directly into the manifested denominator: `test-fs-read-stream-file-handle.js`, `test-fs-write-stream-file-handle.js`, and `test-fs-promises-file-handle-readFile.js`. The only extra staging needed was the shared upstream helper `test/common/tick.js`, which now lives once in the checked-in fixture root and is reused by all three lanes. No owner-layer runtime fix was required this time: the earlier safe `node:fs/promises.open()` bridge plus the existing filehandle stream support were enough for the whole slice to go green in one shot. That raises the manifested denominators again to `141 / 141` for the Node22 primary lane, `137 / 137` for the official Node20 validation lane, and `139 / 139` for the explicit Node24 preview lane while keeping the previously classified watchpoints unchanged. This is the high-throughput `NLC5` shape we want to keep repeating: import a coherent official slice, run all focused lanes, and only spend owner-layer work when the batch actually surfaces a runtime seam. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `141` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `137` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `139` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept pushing `NLC5` in larger official-corpus seams and widened from filehandle interop into the broader positive-path `fs` read/write-stream family. The initial 8-file batch was: `test-fs-read-stream-fd.js`, `test-fs-read-stream-autoClose.js`, `test-fs-empty-readStream.js`, `test-fs-read-stream-encoding.js`, `test-fs-write-stream.js`, `test-fs-write-stream-end.js`, `test-fs-write-stream-autoclose-option.js`, and `test-fs-write-stream-encoding.js`. Node22 and Node20 absorbed the whole batch cleanly, lifting their manifested denominators to `149 / 149` and `145 / 145`. The first Node24 preview rerun revealed two honest future-version drifts rather than current-runtime regressions: official `v24.15.0` `test-fs-write-stream.js` now also requires observing `fs.close()` during direct `WriteStream` destruction, and `test-fs-write-stream-autoclose-option.js` now also asserts `ERR_INVALID_THIS` when probing `WriteStream.prototype.autoClose`. Those two files are now carried as explicit preview-only watchpoints with direct repro tests, while the remaining six files stay in the preview denominator. After that reconciliation, the explicit Node24 preview lane is green at `145 / 145`. This keeps the supported Node22/Node20 contract moving quickly while preserving the future `node:fs` write-stream frontier truthfully for later adoption work. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `149` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `145` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → first `145 passed, 2 failed`, then `1 passed` with `145` staged files green after classifying the two preview-only write-stream drifts; `cargo test -p neovex-runtime node24_fs_write_stream_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure with missing `fs.close()` observation; `cargo test -p neovex-runtime node24_fs_write_stream_autoclose_option_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on missing `ERR_INVALID_THIS` |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Tightened the held-out `fs.promises` filehandle seam instead of leaving it as a fuzzy three-file pocket. First, the embedded CommonJS module surface now exposes `internal/fs/promises` through the local `node:neovex/module` shim, which moved `test-fs-promises-file-handle-close-errors.js` from a missing-module failure to the real remaining aggregate-close-error semantics gap. Second, the node_compat bundle wrapper now seeds `globalThis.gc()` automatically in test lanes, which moved `test-fs-promises-file-handle-close.js` from a harness hook failure to the real remaining DEP0137 finalization-warning gap. Finally, Neovex fixed the actual omitted-options `FileHandle.read()` behavior locally in `node22_runtime_bootstrap.js` by normalizing `length: null|undefined` on both positional and options-object `FileHandle.read(...)` calls. That promoted `test-fs-promises-file-handle-read.js` into the manifested denominator without growing the Deno fork. With the green file rolled into `STREAMS_AND_LOCAL_IO_BATCH`, the focused lanes now close at `150 / 150` for the Node22 primary lane, `146 / 146` for the official Node20 validation lane, and `146 / 146` for the explicit Node24 preview lane, while the watchpoint inventory shrinks to the two real remaining filehandle lifecycle gaps: GC-close warnings and aggregate close-error rejection semantics. | `cargo test -p neovex-runtime node22_fs_promises_file_handle_read_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed` after landing the local `FileHandle.read()` normalization; `cargo test -p neovex-runtime node22_fs_promises_file_handle_close_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on missing DEP0137 finalization warning delivery; `cargo test -p neovex-runtime node22_fs_promises_file_handle_close_errors_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on missing aggregate close-error rejection semantics; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `150` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `146` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `146` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the last held-out `fs.promises` filehandle lifecycle pocket locally in the Neovex embedder layer instead of expanding the Deno fork. `node22_runtime_bootstrap.js` now patches embedded `fs` lifecycle semantics in three places: it registers `FileHandle` instances opened through `fs.promises.open()` with a `FinalizationRegistry`, emits the Node `Warning` plus `DEP0137` `DeprecationWarning` when a filehandle is garbage-collected, and normalizes path-based `fs.promises.readFile()` / `writeFile()` / `truncate()` / `lchmod()` so they route through `FileHandle` open/close behavior and preserve close-error rejection semantics. With that in place, both `test-fs-promises-file-handle-close.js` and `test-fs-promises-file-handle-close-errors.js` are promoted into the manifested denominator and removed from the watchpoint inventory. The focused lanes now close at `152 / 152` for the Node22 primary lane, `148 / 148` for the official Node20 validation lane, and `148 / 148` for the explicit Node24 preview lane, leaving the remaining `NLC5` watchpoints concentrated in broader negative-path `fs` and local-facade seams rather than filehandle lifecycle semantics. | `cargo test -p neovex-runtime node22_fs_promises_file_handle_close_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_fs_promises_file_handle_close_errors_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `152` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `148` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `148` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Kept `NLC5` on the same batch-first official-corpus path and widened the positive-path `fs.promises` seam instead of leaving the denominator stuck at the filehandle lifecycle checkpoint. First, promoted a five-file shared-LTS batch into the manifested subset: `test-fs-promises-exists.js`, `test-fs-promises-readfile-with-fd.js`, `test-fs-promises-writefile-typedarray.js`, `test-fs-promises-writefile-with-fd.js`, and `test-fs-promises-writefile.js`. Then reconciled an apparent sixth watchpoint honestly: the held-out `test-fs-promises-readfile-empty.js` repro turned out to be a harness-classification bug because the targeted repro was not staging the shared `test/fixtures/empty.txt` payload that the manifested lane already relies on. After correcting that staging, the official file went green and was promoted into `STREAMS_AND_LOCAL_IO_BATCH` as well. That lifts the measured `NLC5` denominators to `158 / 158` for the Node22 primary lane, `154 / 154` for the official Node20 validation lane, and `154 / 154` for the explicit Node24 preview lane, while leaving the watchpoint inventory unchanged and focused on real negative-path `fs` / local-facade gaps rather than another fake `fs.promises` blocker. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `158` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `154` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `154` staged files green |
| 2026-05-01 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the held-out path-based `fs.promises` read/error pocket locally in the Neovex embedder layer and promoted the official trio into the green denominator: `test-fs-promises-file-handle-op-errors.js`, `test-fs-promises-file-handle-aggregate-errors.js`, and `test-fs-promises-readfile.js`. The owner-layer fix stayed local and targeted: GC registration during `fs.promises.open()` now uses the original `FileHandle.fd` getter instead of the public getter, path-based close now routes through a getter-safe `closeNeovexFileHandle(...)`, `aggregateFsCloseErrors(...)` now returns the Node-style `AggregateError` contract, the path-based `fs.promises.readFile()` wrapper now follows Node-style abort/validation/read-loop behavior and observes the `internalBinding('fs').fstat` seam, and `handleFsPromisePathClose(...)` now matches Node's success/error split so a success-path close failure is not retried and double-counted. With those fixes landed, the focused lanes now close at `161 / 161` for the Node22 primary lane, `157 / 157` for the official Node20 validation lane, and `157 / 157` for the explicit Node24 preview lane, leaving the remaining `NLC5` watchpoints concentrated in broader negative-path `fs`, host-policy, and local-facade seams instead of the earlier path-based `fs.promises` lifecycle pocket. | `cargo test -p neovex-runtime node22_fs_promises_file_handle_ -- --nocapture --test-threads=1 --ignored` → `2 passed`; `cargo test -p neovex-runtime node22_fs_promises_readfile_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `161` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `157` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `157` staged files green |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next broad positive-path local-I/O seam instead of dropping into more one-off watchpoints: `test-fs-mkdir.js`, `test-fs-mkdir-mode-mask.js`, `test-fs-mkdir-rmdir.js`, `test-fs-copyfile.js`, and `test-fs-copyfile-respect-permissions.js` are now part of the manifested `NLC5` denominator. The only initial failure was not a fixture quirk; it exposed a shared runtime path-policy bug where in-root recursive `mkdir` paths like `test10/../test11/test12` were denied if the lexically canceled segment did not already exist. The owner fix lives in `runtime_capabilities.rs`: absolute write paths are now normalized lexically before the missing-suffix canonicalization walk, and a dedicated unit test proves that in-root parent traversal after a missing segment stays inside the bundle root. With that shared path fix in place, the whole batch closes green in every focused lane, lifting the manifested denominators to `166 / 166` for the Node22 primary lane, `162 / 162` for the official Node20 validation lane, and `162 / 162` for the explicit Node24 preview lane while keeping the watchpoint inventory unchanged. | `cargo test -p neovex-runtime ensure_write_path_allows_in_root_parent_traversal_after_missing_segment -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `165 passed, 1 failed` on `test-fs-mkdir.js`, then `166 passed, 0 failed` after the shared path fix; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `162 passed, 0 failed`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `162 passed, 0 failed` |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the held-out legacy `fs.rmdir(..., { recursive: true })` pocket locally in the Neovex owner layer and promoted the full official batch into the manifested denominator: `test-fs-rmdir-recursive.js`, `test-fs-rmdir-type-check.js`, `test-fs-rmdir-recursive-throws-not-found.js`, `test-fs-rmdir-recursive-throws-on-file.js`, `test-fs-rmdir-recursive-warns-not-found.js`, `test-fs-rmdir-recursive-warns-on-file.js`, `test-fs-rmdir-recursive-sync-warns-not-found.js`, and `test-fs-rmdir-recursive-sync-warns-on-file.js`. The owner fix stayed local because this is a Node22 compatibility shim over Deno’s later `fs.rmdir` contract, not a shared Deno-family semantic baseline: `module_loader.rs` now gives both the direct `node:fs/promises` override and the CommonJS `fs.promises` override Node-style non-recursive `rmdir` error normalization, while the local `fs` override implements the historical recursive `rmdir()` / `rmdirSync()` deprecation path with warning delivery, post-delete `ENOENT` / `syscall: "rmdir"` parity, and the monkeypatch-visible sync rimraf call shape that official Node asserts. The sync recursive path now distinguishes hidden recursive descent from the final public `rmdirSync(path, {})` removal call so nested directories recurse correctly without losing the observable options contract. With that batch promoted, all focused lanes are green again at `174 / 174` for the Node22 primary lane, `170 / 170` for the official Node20 validation lane, and `170 / 170` for the explicit Node24 preview lane, while the watchpoint inventory stays concentrated in the remaining `fs.access`, `fs.open`, `fs.readFile`, `fs.readdir`, and local-facade gaps. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `174` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `170` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `170` staged files green |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/cycles/root.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/cycles/folder/foo.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next broad directory/link helper seam into the manifested `NLC5` denominator instead of leaving it as a cluster of isolated local-I/O watchpoints. The newly green official batch is `test-fs-mkdtemp.js`, `test-fs-mkdtemp-prefix-check.js`, `test-fs-link.js`, `test-fs-symlink.js`, `test-fs-readlink-type-check.js`, `test-fs-rename-type-check.js`, `test-fs-unlink-type-check.js`, and `test-fs-opendir.js`, with the Node24 `symlink` and `opendir` variants carved out as explicit preview-only divergences rather than silently counted green. The owner fixes stayed local and structural: the embedded Deno facade now exposes `link`, `readLink`, and `rename`; `runtime_local.rs` gained bundle-root-safe owner ops for those surfaces; `runtime_capabilities.rs` added lexical read-path validation so `readlink()` does not accidentally resolve the target through canonicalization; `module_loader.rs` now preserves the current Node22 invalid-symlink-type contract, wraps `Dir` handles to enforce close/concurrency behavior, and restores the expected callback-validation and `Dirent.path` surface through the local layer; and the staged cycle fixtures were added once so the official `test-fs-symlink.js` corpus runs unchanged. With that batch promoted, the focused lanes now close at `182 / 182` for the Node22 primary lane, `178 / 178` for the official Node20 validation lane, and `176 / 176` for the explicit Node24 preview lane, while the remaining watchpoint inventory stays concentrated in `fs.access`, `fs.open`, `fs.readFile`, `fs.readdir`, local-facade completeness, and the now-expanded set of preview-only Node24 drifts. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --test-threads=1` → `1 passed` with `182` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --test-threads=1` → `1 passed` with `178` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --ignored --test-threads=1` → `1 passed` with `176` staged files green |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the shared-LTS `realpath` helper seam without overclaiming the later `worker_threads` boundary. The local hidden-`Deno` facade now exposes `realPath()` and `realPathSync()` from the existing Deno `ext:deno_fs/30_fs.js` owner layer, which is the real runtime seam that official Node `fs.realpath*` depends on. With that owner fix in place, `test-fs-realpath-buffer-encoding.js` and `test-fs-realpath-native.js` are now part of the manifested green denominator in all three focused lanes. The broader `test-fs-realpath.js` file is not being miscounted as green: its current failure is a later-family dependency because it skips immediately when `worker_threads.isMainThread` is still false, so it is now carried as an explicit watchpoint rather than poisoning the `NLC5` denominator. That lifts the measured local-I/O denominators again to `184 / 184` for the Node22 primary lane, `180 / 180` for the official Node20 validation lane, and `178 / 178` for the explicit Node24 preview lane while keeping the preview-only divergence count unchanged at `6`. | `cargo test -p neovex-runtime node22_fs_realpath_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure with `Neovex node_compat skip: process.chdir is not available in Workers`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `184` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `180` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `178` staged files green |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the held-out `readdir` seam and promoted the shared-LTS `test-fs-readdir.js` plus `test-fs-readdir-types.js` pair into the manifested `NLC5` denominator. The owner fix stayed local and targeted: `runtime_local.rs` now preserves richer host filesystem error codes such as `ENOTDIR` / `EISDIR`, the local Deno-facade error mapper in `node22_runtime_bootstrap.js` keeps those codes intact, `test/common/index.js` now exposes `common.isMacOS` so the official macOS-only fixture behaves honestly, and `module_loader.rs` now gives the Neovex `fs` / `fs.promises` overrides a real `internalBinding('fs').readdir` bridge that snapshots options, routes the `withFileTypes` path through the low-level host `read_dir` op without recursive binding re-entry, restores Node-style `ENOTDIR` error shape, and recovers `UV_DIRENT_UNKNOWN` entries by `lstat`-ing the child path before rebuilding `fs.Dirent`. The broader `/dev` buffer-path probe is no longer misclassified as a generic `readdir` bug: `test-fs-readdir-buffer.js` is now the explicit watchpoint because the application profile intentionally denies host paths outside the generated bundle root. With the green pair promoted, the measured local-I/O denominators rise to `186 / 186` for the Node22 primary lane, `182 / 182` for the official Node20 validation lane, and `180 / 180` for the explicit Node24 preview lane while keeping the preview-only divergence count unchanged at `6`. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `186` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `182` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `180` staged files green |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the invalid-encoding guard batch instead of leaving it as a stale watchpoint. The new green file is `test-fs-assert-encoding-error.js`, and the owner fix stayed local because the mismatch lived in Neovex's bundle-root-safe `fs` wrappers rather than the shared Deno layer: `module_loader.rs` now snapshots and validates encoding options consistently for the staged `readdir`, `readlink`, `realpath`, `mkdtemp`, `watch`, `ReadStream`, and `WriteStream` entrypoints, and it preserves `realpath.native()` / `realpathSync.native()` while doing so. With that parity fix in place, the focused lanes close green again at `187 / 187` for the Node22 primary lane, `183 / 183` for the official Node20 validation lane, and `181 / 181` for the explicit Node24 preview lane. The failure inventory now drops the invalid-encoding watchpoint entirely and keeps the remaining `NLC5` watchpoints concentrated in broader negative-path `fs`, local-facade completeness, host-path policy, and the later `worker_threads` seam. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `187` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `183` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `181` staged files green |
| 2026-05-02 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next shared-LTS local-I/O resize and metadata batch instead of leaving `truncate` half-classified. The newly green official files are `test-fs-chmod.js`, `test-fs-chmod-mask.js`, `test-fs-truncate.js`, `test-fs-truncate-sync.js`, `test-fs-truncate-fd.js`, and `test-fs-truncate-clear-file-zero.js`. The owner fix stayed local in `module_loader.rs` because the mismatch lived in Neovex's bundle-root-safe `fs` shim rather than the shared Deno layer: unsupported `lchmod` / `lchmodSync` are now hidden instead of overclaimed, deprecated fd-backed `fs.truncate()` routes through `ftruncate()` / `ftruncateSync()` with `DEP0081` warning parity, the path-based `truncate()` / `truncateSync()` flow now defaults `undefined` length to `0`, and the async missing-file branch now remaps the embedded `invalid_argument` throw back to the expected Node `ENOENT` / `syscall: "open"` callback shape. With that batch promoted, the focused lanes close green again at `193 / 193` for the Node22 primary lane, `189 / 189` for the official Node20 validation lane, and `186 / 186` for the explicit Node24 preview lane, while the failure inventory stays unchanged outside the denominator. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `193` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `189` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `186` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted another shared-LTS fd-oriented and stream-close-idempotence batch without needing new owner-layer runtime work. The newly green official files are `test-fs-fchmod.js`, `test-fs-fchown.js`, `test-fs-fsync.js`, `test-fs-read-stream-double-close.js`, and `test-fs-write-stream-double-close.js`. This was a straight manifested-denominator lift: the canonical local `~/src/github.com/nodejs/node` corpus already matched the current Neovex runtime contract, so the only code change was staging the files in `node_compat.rs` and updating the checked-in compatibility truth. With that batch promoted, the focused lanes close green at `198 / 198` for the Node22 primary lane, `194 / 194` for the official Node20 validation lane, and `191 / 191` for the explicit Node24 preview lane while leaving the existing watchpoint inventories unchanged outside the denominator. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `198` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `194` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `191` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next shared-LTS timestamp/validation helper seam and fixed the one real owner gap it exposed. The newly green official files are `test-fs-chown-type-check.js`, `test-fs-statfs.js`, `test-fs-timestamp-parsing-error.js`, `test-fs-utimes.js`, and `test-fs-non-number-arguments-throw.js`. The first manifested rerun surfaced a narrow bootstrap-local seam instead of a broad `node:fs` failure: upstream `fs.utimes()` reached the hidden `Deno.utime()` path, but the raw Deno surface returned the wrong missing-file shape. Neovex now owns that path locally through a hidden-`Deno` `utime()` / `utimeSync()` bridge that normalizes timestamps with the same `toUnixTimestamp()` contract Node uses and routes path-based updates through a Neovex-owned host op so bundle-root path policy and `ENOENT` mapping stay aligned with the rest of the local-I/O facade. With that owner fix in place, the focused lanes close green at `203 / 203` for the Node22 primary lane, `199 / 199` for the official Node20 validation lane, and `196 / 196` for the explicit Node24 preview lane while leaving the existing watchpoint inventories unchanged outside the denominator. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `202 passed, 1 failed` on `test-fs-utimes.js`, then `1 passed` with `203` official files green after the local `utime` owner fix; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `199` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `196` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next shared-LTS sync-write / stream-ready seam while keeping three newly surfaced non-runtime blockers honest outside the denominator. The newly green official files are `test-fs-ready-event-stream.js` and `test-fs-sync-fd-leak.js`, which lift the focused lanes to `205 / 205` for the Node22 primary lane, `201 / 201` for the official Node20 validation lane, and `198 / 198` for the explicit Node24 preview lane. The only real owner change stayed local in `module_loader.rs`: Neovex now overrides sync `fs.writeFileSync()` / `appendFileSync()` with a Node-shaped wrapper that preserves monkeypatch-sensitive cleanup semantics across both the utf8 internal-binding fast path and the `openSync()` / `writeSync()` fallback, instead of relying on Deno's lower-level sync write helper that bypasses the current `fs` binding surface. The first staged rerun surfaced four failures; three were correctly classified back out of the live denominator rather than treated as `node:fs` regressions: `test-fs-filehandle-use-after-close.js` is an application-profile path-policy divergence on `process.execPath`, `test-fs-writefile-with-fd.js` is the same embedded `beforeExit` harness-lifecycle gap already seen in `test-fs-open-no-close.js`, and `test-fs-write-file-sync.js` remains a later `worker_threads` dependency because the official file self-skips when `isMainThread` is false and `process.umask()` is unavailable in Workers. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first failed on four staged files, then failed once on a bad `internal/validators` import, then failed once on an unconditional `internalBinding('fs').writeFileUtf8` fast path, then passed with `205` official files green after the local sync-write wrapper/fallback fix; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `201` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `198` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next shared-LTS path-validation and URL-entrypoint seam directly from the already-staged official corpus. The newly green official files are `test-fs-null-bytes.js`, `test-fs-whatwg-url.js`, `test-fs-write-negativeoffset.js`, `test-fs-util-validateoffsetlength.js`, `test-fs-operations-with-surrogate-pairs.js`, and `test-fs-symlink-buffer-path.js`, lifting the focused lanes to `211 / 211` for the Node22 primary lane, `207 / 207` for the official Node20 validation lane, and `204 / 204` for the explicit Node24 preview lane without adding any new watchpoints. The owner fix stayed local in `module_loader.rs` because the mismatch lived in Neovex's bundle-root-safe `node:fs` override layer rather than a broad shared Deno-family contract: the first rerun showed that Deno's async `writeFile()` path and Neovex's path-based `truncate()` wrapper were bypassing Node-style front-door path validation, so Neovex now forces `getValidatedPathToString()` through the local `writeFile`, `appendFile`, `writeFileSync` / `appendFileSync`, and path-based `truncate()` / `truncateSync()` seams before any host file open or write work starts. With that front-door validation restored, the same batch also proves WHATWG `file:` URL `readFile()` entrypoints, negative write-offset guards, internal `validateOffsetLengthWrite()` parity, surrogate-pair filename handling, and `Buffer` target paths for symlink creation. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first failed on `test-fs-null-bytes.js` at `appendFile()`, then again at `truncate()`, then passed with `211` official files green after the local path-validation owner fixes; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `207` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `204` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Closed the last in-flight `NLC5` flush seam by fixing the real owner and repinning Neovex to consume it. The shared Deno-family fix landed in `agentstation/deno v2.7.14-locker.30`: the embedded `node:test` bridge now honors callback-style nested `t.test(..., done)` subtests and restores `mock.restoreAll()` after each bridged test scope, which was the real reason the imported `test-fs-write-file-flush.js` and `test-fs-append-file-flush.js` fixtures stayed red. Neovex is now repinned from `v2.7.14-locker.29` to `.30`, and the flush pair is promoted into the manifested denominator instead of carried as a stale watchpoint. With that repin live, the focused `NLC5` lanes now close green at `213 / 213` for the Node22 primary lane, `209 / 209` for the official Node20 validation lane, and `206 / 206` for the explicit Node24 preview lane while removing the old append-file-flush watchpoint from the failure inventory. | `cargo test -p neovex-runtime node22_fs_write_file_flush_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_fs_append_file_flush_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `213` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `209` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `206` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the next shared negative-path `fs` pocket locally and promoted three previously held-out official files into the manifested `NLC5` denominator: `test-fs-access.js`, `test-fs-open-numeric-flags.js`, and `test-fs-readfile-flags.js`. The owner fix stayed in Neovex because the mismatches lived in the bundle-root-safe `node:fs` override layer rather than shared Deno-family semantics: `module_loader.rs` now forces `fs.promises.access()` through a Node-shaped async rejection path so the stack includes the expected `at async Object.access` frame, defines the wrapped `fs.promises` surface directly on the overridden `fs` module, normalizes synchronous `invalid_argument` throws from callback `fs.open()` the same way async callback errors are normalized, maps exclusive-create invalid opens on existing files to Node-style `EEXIST`, and routes callback `fs.readFile(..., { flag })` through the corrected `open()` seam so `a+` / `ax+` behavior matches the official files. With those three fixtures promoted, the focused `NLC5` lanes now close green at `216 / 216` for the Node22 primary lane, `212 / 212` for the official Node20 validation lane, and `209 / 209` for the explicit Node24 preview lane while removing the old access/open-numeric/readfile-flags watchpoints from the failure inventory. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `216` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `212` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `209` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the next shared lifecycle seam without growing another runtime compatibility shim. The owner change stayed in the Neovex `node_compat` harness: `node_compat.rs` now installs a narrow postlude for the two official lifecycle fixtures that still rely on Node-style `beforeExit` / `exit` quiescence after async filesystem work, and the drain shape is intentionally a single `beforeExit` pass so fd-backed `writeFile()` cleanup is observed exactly once. With that harness seam in place, the previously held-out `test-fs-open-no-close.js` and `test-fs-writefile-with-fd.js` fixtures are now promoted into the manifested `NLC5` denominator instead of being carried as `harness_lifecycle_gap` watchpoints. The focused lanes now close green at `218 / 218` for the Node22 primary lane, `214 / 214` for the official Node20 validation lane, and `211 / 211` for the explicit Node24 preview lane while shrinking the explicit Node22 watchpoint inventory from nine files to seven. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `218` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `214` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `211` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-os-checked-function.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-os-checked-function.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-os-checked-function.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tty-backwards-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tty-backwards-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tty-backwards-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tty-stdin-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tty-stdin-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tty-stdin-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tty-stdin-pipe.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tty-stdin-pipe.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tty-stdin-pipe.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-readline-csi.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-readline-csi.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-readline-csi.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-readline-carriage-return-between-chunks.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-readline-carriage-return-between-chunks.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-readline-carriage-return-between-chunks.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-readline-input-onerror.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-readline-input-onerror.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-readline-input-onerror.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the first real `tty` / `readline` slice into the manifested `NLC5` denominator and turned the newly surfaced remainder into a smaller, truer closeout set. The imported official batch now includes `test-tty-stdin-end.js`, `test-tty-stdin-pipe.js`, `test-readline-csi.js`, `test-readline-carriage-return-between-chunks.js`, and `test-readline-input-onerror.js`. Two owner fixes were required locally in Neovex rather than in the Deno fork: `node22_runtime_bootstrap.js` now seeds `process.stdin` through `initStdin(false)` so the embedded Node22 bootstrap exposes the stdin stream shape these fixtures expect, and `module_loader.rs` now routes path-based `fs.createReadStream()` / `fs.createWriteStream()` through the Neovex-owned `open()` and error-normalization seam while preserving builtin `fd` / `FileHandle` stream behavior. The first rerun also clarified two honest watchpoints instead of hiding them in the denominator: `test-os-checked-function.js` is an application-profile `homedir` capability divergence, and `test-tty-backwards-api.js` is a local `internal/test/binding('tty_wrap')` compatibility gap. With the green slice promoted, the focused lanes now close at `223 / 223` for the Node22 primary lane, `219 / 219` for the official Node20 validation lane, and `216 / 216` for the explicit Node24 preview lane. | `cargo test -p neovex-runtime tty_stdin_ -- --nocapture --test-threads=1` → `6 passed, 0 failed`; `cargo test -p neovex-runtime node22_readline_ -- --nocapture --test-threads=1` → first `2 passed, 1 failed` on `test-readline-input-onerror.js`, then `3 passed, 0 failed` after the local `ReadStream` owner fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `220 passed, 3 failed`, then `1 passed` with `223` official files green after preserving builtin `fd` / `FileHandle` stream behavior; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `219` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `216` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the last undocumented `glob` seam and widened the non-interactive `readline` slice in one manifest-driven pass. The Node22 runtime now promotes official `test-fs-glob.mjs` through a Neovex-owned ESM `node:fs` exposure plus `node:fs/promises.glob()` export, bundle-root-safe relative symlink-target validation against the link parent, metadata-only ancestor reads for absolute in-root glob traversal, and a stabilized legacy recursive `rmdirSync(..., { recursive: true })` child-directory deletion path. On top of that, the manifested `NLC5` denominator now includes `test-readline-async-iterators.js`, `test-readline-async-iterators-backpressure.js`, `test-readline-async-iterators-destroy.js`, and `test-readline-promises-csi.mjs`. The only remainder that surfaced from the broader imported readline batch was not a new runtime bug but a clearer terminal-mode boundary: `test-readline-interface.js` and `test-readline-promises-interface.js` are now explicit host-terminal watchpoints under the current non-interactive `TERM=dumb` harness contract rather than being miscounted as green. With `glob` and the non-interactive `readline` batch promoted, the focused lanes now close green at `228 / 228` for the Node22 primary lane, `223 / 223` for the official Node20 validation lane, and `221 / 221` for the explicit Node24 preview lane. | `cargo test -p neovex-runtime node22_fs_glob_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_fs_glob_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime ensure_ -- --nocapture --test-threads=1` → `3 passed`; `cargo test -p neovex-runtime node22_readline_promises_csi_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_readline_interface_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `TERM=dumb` skip watchpoint; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `228` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `223` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `221` staged files green |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the shared `stream` family with the first manifested `stream/promises` / pipeline slice instead of leaving that seam as a vague future bucket. The newly green official files are `test-stream-promises.js`, `test-stream-pipeline-async-iterator.js`, `test-stream-pipeline-duplex.js`, `test-stream-pipeline-listeners.js`, `test-stream-pipeline-uncaught.js`, and `test-stream-pipeline-with-empty-string.js`, all imported from the already-staged official Node20/22/24 corpus and promoted directly into `STREAMS_AND_LOCAL_IO_BATCH`. The first full Node22 rerun also surfaced the exact boundary cleanly: `test-stream-finished.js` and `test-stream-pipeline.js` only fail when they open local TCP listeners, so they are now explicit `NLC5` / `NLC6` watchpoints rather than being miscounted as red `stream` regressions. With those two socket-backed files carved out honestly, the focused lanes close green again at `234 / 234` for the Node22 primary lane, `229 / 229` for the official Node20 validation lane, and `227 / 227` for the explicit Node24 preview lane. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `234 passed, 2 failed` on `test-stream-finished.js` and `test-stream-pipeline.js`, then `1 passed` with `234` official files green after reclassifying those two files as explicit watchpoints; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `229` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `227` staged files green; `cargo test -p neovex-runtime node22_stream_finished_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `NotCapable: Requires net access to "0.0.0.0:0"`; `cargo test -p neovex-runtime node22_stream_pipeline_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `NotCapable: Requires net access to "0.0.0.0:0"` |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the next non-socket `stream` seam with imported compose / duplex / readable helper files instead of dropping back into one-off runtime chasing. The newly green official files are `test-stream-compose.js`, `test-stream-destroy-event-order.js`, `test-stream-duplex.js`, `test-stream-duplex-from.js`, `test-stream-duplexpair.js`, `test-stream-readable-add-chunk-during-data.js`, and `test-stream-readable-didRead.js`, plus the current Node22/Node24 `test-stream-readable-infinite-read.js` shape. The only red file from the batch was the official Node20 `test-stream-readable-infinite-read.js`, and it narrowed cleanly into the same older default-highWaterMark family as the existing Node20 stream divergences, so it is now an explicit validation-lane watchpoint rather than a Node22 blocker. With that reclassification, the focused lanes close green again at `242 / 242` for the Node22 primary lane, `236 / 236` for the official Node20 validation lane, and `235 / 235` for the explicit Node24 preview lane. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `242` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → first `236 passed, 1 failed` on `test-stream-readable-infinite-read.js`, then `1 passed` with `236` official files green after reclassifying that file as a Node20-only divergence; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `235` staged files green; `cargo test -p neovex-runtime node20_stream_readable_infinite_read_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `AssertionError [ERR_ASSERTION]: 73728 !== 24576` on the older default-highWaterMark accumulation path |
| 2026-05-03 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the remaining pure readable-state `stream` seam with a larger all-green shared-LTS batch instead of pivoting away from `node:stream` too early. The newly green official files are `test-stream-readable-hwm-0.js`, `test-stream-readable-hwm-0-async.js`, `test-stream-readable-hwm-0-no-flow-data.js`, `test-stream-readable-reading-readingMore.js`, `test-stream-readable-flow-recursion.js`, `test-stream-readable-needReadable.js`, `test-stream-readable-readable-then-resume.js`, `test-stream-readable-resumeScheduled.js`, `test-stream-readable-resume-hwm.js`, `test-stream-readable-unshift.js`, and `test-stream-readable-emit-readable-short-stream.js`. No new runtime owner changes or watchpoints were needed; the same imported batch stayed green in all three focused lanes. With that seam promoted, the manifested `NLC5` denominator closes at `253 / 253` for the Node22 primary lane, `247 / 247` for the official Node20 validation lane, and `246 / 246` for the explicit Node24 preview lane. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `253` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `247` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `246` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the first real `fs.watch` / `watchFile` seam in one manifest-driven batch instead of leaving the watch surface as a vague local-facade gap. Neovex now keeps `fs.watch()` abort-signal ownership local, exposes a local async-iterator `fs.promises.watch()` wrapper with Node22 option validation, and patches the shared builtin `fs.promises` targets in the Node22 bootstrap so the CommonJS fixture path sees the same contract. That promoted `test-fs-options-immutable.js`, `test-fs-watch.js`, `test-fs-watch-abort-signal.js`, `test-fs-watch-ref-unref.js`, `test-fs-watch-stop-async.js`, `test-fs-watch-stop-sync.js`, `test-fs-watchfile.js`, `test-fs-watchfile-ref-unref.js`, `test-fs-watch-file-enoent-after-deletion.js`, `test-fs-watch-close-when-destroyed.js`, and Node20/Node22 `test-fs-promises-watch.js` into the green denominator. The Node24 preview lane also stayed green after carving `test-fs-promises-watch.js` back out as an explicit preview-only divergence because official Node24 adds `maxQueue` and `overflow` validation that Neovex has not adopted into the current Node22 contract. With the watch/watchFile batch promoted, the focused lanes now close at `264 / 264` for the Node22 primary lane, `258 / 258` for the official Node20 validation lane, and `256 / 256` for the explicit Node24 preview lane, while the remaining watch tail is now the much smaller `watch` encoding timing seam and `watchFile(..., { bigint: true })` initial-ENOENT shape. | `cargo test -p neovex-runtime node22_fs_watch_abort_signal_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_fs_promises_watch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `264` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `258` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `256` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the remaining non-recursive `fs.watch` tail and promoted it into the manifested denominator instead of leaving two stale watchpoints behind. The local owner fix stayed in `module_loader.rs`: the Neovex `node:fs` wrapper now stamps Node-style `errno` / `message` / `path` / `filename` / `syscall` fields onto watch errors, recreates the historical `watcher._handle.onchange(...)` hook that the official `test-fs-watch-enoent.js` fixture uses to synthesize kernel callbacks, terminates that synthetic error path like a real failed watcher so later cleanup does not leak a spurious `rename` event, and preserves default / `hex` / `buffer` filename encoding plus zeroed `BigIntStats` initial-`ENOENT` delivery for `watchFile(..., { bigint: true })`. With that seam landed, `test-fs-watch-enoent.js`, `test-fs-watch-encoding.js`, and `test-fs-watchfile-bigint.js` are now green in the focused fixture proof and in the manifested batch, lifting the focused lanes to `267 / 267` for the Node22 primary lane, `261 / 261` for the official Node20 validation lane, and `259 / 259` for the explicit Node24 preview lane while removing the two stale Node22 watchpoints from the failure inventory. | `cargo test -p neovex-runtime node22_fs_watch_enoent_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `267` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `261` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `259` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the first recursive `fs.watch()` / `fs.promises.watch()` slice instead of leaving recursive watch behavior as an unproven extension of the earlier non-recursive seam. The initial full Node22 batch surfaced one precise runtime gap, not an event-delivery problem: `test-fs-watch-recursive-promise.js` expected `ABORT_ERR` to preserve the original `AbortSignal` reason as `error.cause`, and the recursive promise watch path was dropping that cause. The owner fix stayed local in both Neovex layers that own the compatibility seam: `node22_runtime_bootstrap.js` now preserves `signal.reason` when the patched `fs.promises.watch()` async iterator aborts, and `module_loader.rs` mirrors that same `error.cause` parity in the local loader shim. With that fix landed, the newly green official files are `test-fs-watch-recursive-promise.js`, `test-fs-watch-recursive-add-file.js`, `test-fs-watch-recursive-update-file.js`, `test-fs-watch-recursive-delete.js`, `test-fs-watch-recursive-add-folder.js`, `test-fs-watch-recursive-add-file-with-url.js`, and `test-fs-watch-recursive-validate.js`. The whole recursive batch now closes green in all three focused lanes, lifting the manifested denominators to `274 / 274` for the Node22 primary lane, `268 / 268` for the official Node20 validation lane, and `266 / 266` for the explicit Node24 preview lane without adding new watchpoints. | `cargo test -p neovex-runtime node22_fs_watch_recursive_promise_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `274` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `268` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `266` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the second recursive `fs.watch()` owner seam instead of treating the first green slice as enough proof. The next batched imports surfaced three timeout cases plus one symlink event-shape regression: nested file creation inside a new folder, nested file creation inside an existing subfolder, recursive file-path watching, and recursive symlink handling. The root cause stayed local to Neovex’s polling-backed hidden `Deno.watchFs()` replacement, not the Deno fork: the snapshotter was only walking one directory level, it followed child symlink targets, and recursive file watches could collapse to an empty filename after the upstream `relative(resolvedWatchPath, path)` mapping. `node22_runtime_bootstrap.js` now takes recursive directory snapshots without following child symlinks, prefers the deepest added/removed descendant for recursive diffs, and preserves file-versus-directory semantics across nested changes. `module_loader.rs` now normalizes recursive file-watch empty-filename callbacks back to the watched basename for both callback `fs.watch()` and local `fs.promises.watch()` delivery. With those local fixes landed, the newly green official files are `test-fs-watch-recursive-sync-write.js`, `test-fs-watch-recursive-add-file-to-new-folder.js`, `test-fs-watch-recursive-add-file-to-existing-subfolder.js`, `test-fs-watch-recursive-watch-file.js`, and `test-fs-watch-recursive-symlink.js`. The widened recursive batch closes green in all three focused lanes, lifting the manifested denominators to `279 / 279` for the Node22 primary lane, `273 / 273` for the official Node20 validation lane, and `271 / 271` for the explicit Node24 preview lane without adding new watchpoints. | `cargo test -p neovex-runtime node22_fs_watch_recursive_ -- --nocapture --test-threads=1` → `5 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `279` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `273` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `271` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the local `tty` backwards-compat seam and checkpointed the already-landed `fs.readFile` large-file fix in the same manifested wave instead of leaving stale red watchpoints in the control plane. The Neovex-owned owner fix stayed local in `module_loader.rs`: `internal/test/binding('tty_wrap')` now returns a shared mutable override object, and the local `tty` override routes `WriteStream` construction and cursor helpers through the monkeypatch-observable `tty_wrap` and `readline` seams that the official `test-tty-backwards-api.js` fixture expects. `node_compat.rs` then promoted `test-tty-backwards-api.js` into the manifested denominator and replaced the stale ignored watchpoint with focused Node20/Node22/Node24 fixture proofs. At the same checkpoint, the already-landed `fs.readFile()` `ERR_FS_FILE_TOO_LARGE` sparse-file parity fix is now reflected honestly by the manifested lanes instead of lingering in the failure inventory. With those reconciliations landed, the focused lanes now close at `283 / 283` for the Node22 primary lane, `277 / 277` for the official Node20 validation lane, and `275 / 275` for the explicit Node24 preview lane while removing the stale `test-fs-readfile.js` and `test-tty-backwards-api.js` watchpoints from the `NLC5` failure inventory. | `cargo test -p neovex-runtime tty_backwards_api_fixture -- --nocapture --test-threads=1` → `3 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `283` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `277` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `275` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the first higher-level stream/web-bridge batch instead of leaving that tail as an unmeasured gap after the `tty` and `fs.watch()` waves. The focused Node22 fixture runs surfaced one real Neovex-owned embedder seam, not a Deno-fork semantic bug: the embedded Node22 bootstrap had not seeded the web-stream globals that the imported Node files expect, so `CountQueuingStrategy`, `ByteLengthQueuingStrategy`, and `ReadableStream` were missing on `globalThis`. `node22_runtime_bootstrap.js` now seeds the missing web-stream constructors, controllers, readers, writers, and queuing strategies only when they are absent, keeping the fix local to the Neovex embedder contract. With that bootstrap seam fixed, the manifested batch now includes `test-stream-add-abort-signal.js`, `test-stream-base-prototype-accessors-enumerability.js`, `test-stream-catch-rejections.js`, `test-stream-set-default-hwm.js`, `test-stream-readable-dispose.js`, `test-stream-readable-from-web-termination.js`, `test-stream-readable-strategy-option.js`, `test-stream-readable-to-web-termination.js`, and the shared Node20/Node22 `test-stream-compose-operator.js` slice. The Node24 preview lane now remains truthful by excluding `test-stream-compose-operator.js`, because that official file is present in the Node20 and Node22 corpora but not in `nodejs/node v24.15.0`. With the manifest narrowed to that upstream truth, the focused lanes close at `292 / 292` for the Node22 primary lane, `286 / 286` for the official Node20 validation lane, and `283 / 283` for the explicit Node24 preview lane without adding new watchpoints. | `cargo test -p neovex-runtime node22_stream_add_abort_signal_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_stream_set_default_hwm_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_stream_readable_ -- --nocapture --test-threads=1` → `4 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `292` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `286` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `283` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the pure stream state tail as one real imported batch instead of picking through it file by file. The targeted Node22 batch proved ten shared official files green immediately: `test-stream-decoder-objectmode.js`, `test-stream-push-strings.js`, `test-stream-readable-error-end.js`, `test-stream-readable-with-unimplemented-_read.js`, `test-stream-transform-hwm0.js`, `test-stream-unshift-read-race.js`, `test-stream-writable-clear-buffer.js`, `test-stream-writable-null.js`, `test-stream-writableState-ending.js`, and `test-stream-writableState-uncorked-bufferedRequestCount.js`. The one red file, `test-stream-writable-samecb-singletick.js`, did not expose ordinary stream behavior; it asserts exact `async_hooks` `TickObject` allocation counts during repeated `stream.write()` / `console.log()` usage. That seam is broader task-accounting truth, so it is now carried explicitly as a later-family watchpoint instead of polluting the `NLC5` denominator. With the pure state batch promoted and the `async_hooks` probe held out honestly, the manifested lanes close at `302 / 302` for the Node22 primary lane, `296 / 296` for the official Node20 validation lane, and `293 / 293` for the explicit Node24 preview lane. The Node24 preview denominator still intentionally excludes `test-stream-compose-operator.js` because that official file is not present in `nodejs/node v24.15.0`. | `cargo test -p neovex-runtime node22_stream_state_batch_fixture -- --nocapture --test-threads=1` → first `1 failed` on `test-stream-writable-samecb-singletick.js`, then `1 passed` after classifying that file as an `async_hooks` watchpoint; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `302` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `296` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `293` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next pure `node:stream` buffering/order seam as one more shared official batch instead of continuing one-off file work. The newly green files are `test-stream-aliases-legacy.js`, `test-stream-await-drain-writers-in-synchronously-recursion-write.js`, `test-stream-backpressure.js`, `test-stream-big-packet.js`, `test-stream-big-push.js`, `test-stream-err-multiple-callback-construction.js`, `test-stream-pipe-deadlock.js`, `test-stream-pipe-same-destination-twice.js`, `test-stream-push-order.js`, and `test-stream-readable-object-multi-push-async.js`. All three focused lanes stayed green with the widened denominator: Node22 `312 / 312`, Node20 `306 / 306`, and Node24 preview `303 / 303`. This clears the numeric side of the `NLC5` family closeout contract, but `NLC5` remains `in_progress` pending one stricter closeout review of the remaining watchpoints and owned surface rather than using the `95%` threshold alone as an enterprise-trust shortcut. | `cargo test -p neovex-runtime node22_stream_buffering_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `312` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `306` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `303` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted classic `test-readline-interface.js` into the manifested `NLC5` denominator by fixing the real harness/owner seam instead of carrying it as another fake `TERM=dumb` limitation. `node_compat.rs` now applies an explicit interactive-terminal prelude for the two interactive `readline` fixtures and temporarily sets the real process `TERM=xterm-256color` under the suite lock so Deno's internal `op_get_env_no_permission_check("TERM")` path sees the same terminal truth as `process.env`. The shared `test/common` shim also now exposes Node20's `skipIfDumbTerminal()` helper, which lets the official Node20 classic interface file run honestly under the same harness contract. With that seam fixed, `test-readline-interface.js` is green in all three focused lanes and the stale `test-os-checked-function.js` and classic `readline.Interface` watchpoints drop out of the `NLC5` remainder. The only surviving interactive readline holdout is the narrower `test-readline-promises-interface.js` async completer / prompt lifecycle mismatch after close, which remains an explicit `NLC5` watchpoint instead of being misclassified as a terminal-mode skip. The manifested denominators now close at Node22 `316 / 316`, Node20 `310 / 310`, and Node24 preview `307 / 307`. | `cargo test -p neovex-runtime node22_readline_interface_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime readline_interface_fixture -- --nocapture --test-threads=1` → `3 passed, 0 failed`; `cargo test -p neovex-runtime node22_readline_promises_interface_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on the remaining async completer / prompt lifecycle watchpoint; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `316` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `310` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `307` staged files green |
| 2026-05-04 | NLC5 | `in_progress` | `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/readline/interface.mjs`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/readline/symbols.mjs`, `~/src/github.com/agentstation/deno/ext/node/polyfills/readline/promises.ts`, `docs/plans/node-lts-compatibility-plan.md` | Implemented the remaining `readline/promises` active-question abort/close owner fix in the Deno fork instead of reviving the earlier Neovex-local shim. The Deno owner layer now carries Node's `kQuestionReject` lifecycle through `readline/promises.ts` and the internal terminal ctrl-c / ctrl-d paths, which means the official `test-readline-promises-interface.js` seam no longer stalls on the four duplicated `mustCall()` completions once Neovex consumes the local fork. I verified this with the exact Neovex watchpoint under a one-off full-fork local patch override so the proof exercised the real Neovex harness and runtime rather than only Deno's standalone suite. The current Neovex default pin remains `v2.7.14-locker.30`, so the public `NLC5` denominator and failure inventory stay unchanged for now; the remaining work on this seam is a clean fork repin, not more semantic debugging. | `cargo test -p neovex-runtime node22_readline_promises_interface_watchpoint -- --nocapture --test-threads=1 --ignored` → first failed on four missed `mustCall()` completions; `cargo test -p neovex-runtime node22_readline_promises_interface_watchpoint --config /private/tmp/neovex-local-deno-patch.toml -- --nocapture --test-threads=1 --ignored` → first failed on `ReferenceError: AbortError is not defined`, then passed after importing `AbortError` into Deno's internal readline layer |
| 2026-05-04 | NLC5 | `done` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/streams-and-local-io.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/plans/node-lts-compatibility-plan.md` | Consumed the published Deno owner fix by repinning Neovex to `agentstation/deno v2.7.14-locker.31`, promoted `test-readline-promises-interface.js` into the manifested `NLC5` denominator, and reconciled the control plane to the measured truth. The narrow published-path proof is now green for the official Node20, Node22, and Node24 `readline/promises` interface fixtures, which removes the last in-family interactive-terminal watchpoint from `NLC5`. The broad manifested lanes also stayed green at Node22 `317 / 317`, Node20 `311 / 311`, and Node24 preview `308 / 308`, so the `NLC5` family now closes with the remaining Node22 holdouts all explicitly classified outside the family boundary: local-net callback `stream.finished()` / `stream.pipeline()` files, application-profile host-path-policy divergences, and later `worker_threads` / `async_hooks` dependencies. With no unexplained in-family failures left, `NLC5` is now `done` and `NLC6` is promoted as the next active control item. | `cargo test -p neovex-runtime readline_promises_interface_fixture -- --nocapture --test-threads=1` → `3 passed, 0 failed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `317` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_streams_and_local_io_subset -- --nocapture --test-threads=1` → `1 passed` with `311` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_streams_and_local_io_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `308` staged files green; `cargo fmt --all --check` → clean; `make clippy` → clean; `git diff --check` → clean |
| 2026-05-04 | NLC6 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dns-default-order-ipv4.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dns-default-order-ipv6.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dns-default-order-verbatim.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dns-get-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dns-set-default-order.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-getname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-getpackedsettings.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-util-assert-valid-pseudoheader.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-util-asserts.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-util-nghttp2error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-connect-options-invalid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-isip.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-isipv4.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-isipv6.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dns-default-order-ipv4.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dns-default-order-ipv6.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dns-default-order-verbatim.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dns-get-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dns-set-default-order.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-getname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-getpackedsettings.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-util-assert-valid-pseudoheader.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-util-asserts.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-util-nghttp2error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-connect-options-invalid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-isip.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-isipv4.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-isipv6.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dns-default-order-ipv4.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dns-default-order-ipv6.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dns-default-order-verbatim.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dns-get-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dns-set-default-order.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-getname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-getpackedsettings.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-util-assert-valid-pseudoheader.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-util-asserts.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-util-nghttp2error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-connect-options-invalid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-isip.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-isipv4.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-isipv6.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the first `NLC6` helper seam immediately while the harness and runtime were warm instead of stopping after the initial ten green files. The shared-LTS networking denominator now includes two more `net` IP-helper files (`test-net-isipv4.js`, `test-net-isipv6.js`) and the first pure `http2` internal-utility files (`test-http2-util-asserts.js`, `test-http2-util-nghttp2error.js`), lifting the manifested counts to `14 / 14 / 14` for Node22, Node20 validation, and Node24 preview. The two red imports that surfaced in the same wave were both classified cleanly instead of being hidden: `test-http2-util-assert-valid-pseudoheader.js` currently trips a real `internal/http2/util.mapToHeaders` completeness gap before its direct pseudoheader assertion path runs, and `test-http2-getpackedsettings.js` self-skips on `common.hasCrypto`, which makes it a truthful `NLC6`/`NLC7` boundary watchpoint on the current application-profile `node:crypto` surface rather than a pure `http2` regression. That keeps the first `NLC6` checkpoint moving in the right shape: more official Node coverage imported immediately, one owner fix in the Node22 loader front door, and the next deeper `http2` / crypto seams carried explicitly instead of muddying the green denominator. | `cargo test -p neovex-runtime node22_networking_pure_batch_fixture -- --nocapture --test-threads=1` → first failed on `test-http2-util-assert-valid-pseudoheader.js`, then on `test-http2-getpackedsettings.js`, then passed after classifying both files as explicit watchpoints; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `14` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `14` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `14` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/bootstrap/state.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Closed the stale local-net gate and promoted the first real socket-backed networking seam into the measured `NLC6` denominator. The runtime capability contract now grants the Node22 runtime a narrow local-network allowlist (`localhost`, `127.0.0.1`, `0.0.0.0`, `::1`, `::`) instead of failing every raw socket at the permission front door, and the Node22 bootstrap now registers `streamBaseState` just like Deno's `02_init.js` path so libuv-backed `stream_wrap` writes no longer panic on missing `GothamState`. With those local owner fixes in place, the former `NLC5/NLC6` boundary files `test-stream-finished.js` and the shared Node20/Node22 `test-stream-pipeline.js` are green and counted in `NETWORKING_BATCH`, lifting the supported lanes to `16 / 16` for Node22 primary and Node20 validation. The Node24 preview lane stays truthful by carrying `test-stream-pipeline.js` as an explicit preview-only divergence because its staged official file still expects the inner `"Boom!"` pipeline error message where the current runtime returns `"The operation was aborted"`. The remaining explicit `NLC6` remainder is now just the unpublished `http2` internal-util `mapToHeaders` seam plus the `http2`/`crypto` boundary file. | `cargo test -p neovex-runtime network_hosts -- --nocapture` → `2 passed`; `cargo test -p neovex-runtime node22_stream_finished_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_stream_pipeline_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `16` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `16` official files green; `cargo test -p neovex-runtime node24_stream_pipeline_watchpoint -- --nocapture --test-threads=1 --ignored` → expected preview-only failure on `"The operation was aborted"` vs `"Boom!"`; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `15` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-connect-no-arg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-listening.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-listen-close-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-server-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-server-call-listen-multiple-times.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-server-listen-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-server-listen-options-signal.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-connect-no-arg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-listening.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-listen-close-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-server-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-server-call-listen-multiple-times.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-server-listen-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-server-listen-options-signal.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-connect-no-arg.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-listening.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-listen-close-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-server-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-server-call-listen-multiple-times.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-server-listen-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-server-listen-options-signal.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC6` with the first plain `node:net` local listen/server lifecycle seam instead of jumping straight to broader request semantics. A new targeted batch proved the imported official files green first, then the widened manifested lanes stayed green without surfacing any new watchpoints. The newly counted coverage is `test-net-connect-no-arg.js`, `test-net-listening.js`, `test-net-listen-close-server.js`, `test-net-server-close.js`, `test-net-server-call-listen-multiple-times.js`, `test-net-server-listen-options.js`, and `test-net-server-listen-options-signal.js`, all staged from the official Node20, Node22, and Node24 corpora and promoted directly into `NETWORKING_BATCH`. That lifts the measured networking subset to `23 / 23 / 22` for Node22 primary, Node20 validation, and Node24 preview, while leaving the explicit remainder unchanged: the unpublished `internal/http2/util.mapToHeaders` seam, the `http2`/`crypto` boundary file, and the existing preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_net_server_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `23` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `23` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `22` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-after-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-end-without-connect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-settimeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-can-reset-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-socket-close-after-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-socket-connecting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-local-address-port.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-after-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-end-without-connect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-settimeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-can-reset-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-socket-close-after-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-socket-connecting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-local-address-port.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-after-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-end-without-connect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-settimeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-can-reset-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-socket-close-after-end.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-socket-connecting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-local-address-port.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC6` again with the first plain `node:net` socket lifecycle and timeout seam, while keeping the real remaining Deno-owner bug explicit instead of faking another green. The imported official Node20/22/24 files `test-net-after-close.js`, `test-net-settimeout.js`, `test-net-can-reset-timeout.js`, `test-net-socket-close-after-end.js`, `test-net-socket-connecting.js`, and `test-net-local-address-port.js` are now green and counted in `NETWORKING_BATCH`, which lifts the measured networking denominator to `29 / 29 / 28`. The only red file in that seven-file wave was `test-net-end-without-connect.js`, and the diagnosis cleanly split into two owners: the local `test/common` shim was missing `common.localhostIPv4`, which caused the imported local-address fixture to hang until the shared harness exported the same localhost constant Node expects; after that harness fix, the remaining failure narrowed to shared Deno-family `node:net` semantics. Specifically, `new net.Socket().end(cb)` before any connect attempt still drops the callback on the published Deno baseline because `net.Socket._final()` gates on `pending` instead of `connecting`. That owner fix was proven locally in `~/src/github.com/agentstation/deno` using the temporary `/private/tmp/neovex-local-deno-patch.toml` override, but it is not yet part of the current Neovex pin, so `test-net-end-without-connect.js` is now an explicit `NLC6` watchpoint instead of being miscounted green. | `cargo test -p neovex-runtime node22_net_local_address_port_fixture -- --nocapture --test-threads=1` → first hung under missing `common.localhostIPv4`, then `1 passed` after the shared harness exported the localhost constant; `cargo test -p neovex-runtime node22_networking_net_socket_batch_fixture -- --nocapture --test-threads=1` → first failed on `test-net-end-without-connect.js`, then `1 passed` after carving that file into an explicit watchpoint; `CARGO_NET_OFFLINE=true cargo --config /private/tmp/neovex-local-deno-patch.toml test -p neovex-runtime node22_net_end_without_connect_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed` against the local Deno owner fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `29` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `29` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `28` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-defaults.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-get-url.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-request-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-upload.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-upload-buf.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-automatic-headers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-close-event.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-defaults.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-get-url.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-request-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-upload.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-upload-buf.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-automatic-headers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-close-event.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-defaults.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-get-url.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-request-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-upload.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-upload-buf.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-automatic-headers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-close-event.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the first real `node:http` client/server request wave into the manifested `NLC6` denominator instead of staying stuck at the `net` and `http.Agent` helper layer. The targeted Node22 batch proved the imported official files green immediately, with no new watchpoints: `test-http-client-defaults.js`, `test-http-client-get-url.js`, `test-http-client-request-options.js`, `test-http-client-upload.js`, `test-http-client-upload-buf.js`, `test-http-automatic-headers.js`, and `test-http-client-close-event.js`. The broadened networking lanes stayed green across all three focused lanes, lifting the measured subset to `36 / 36 / 35` for Node22 primary, Node20 validation, and Node24 preview. That widens `NLC6` from pure helper and socket-lifecycle proof into the first honest HTTP request/response surface while leaving the explicit remainder unchanged: the unpublished `internal/http2/util.mapToHeaders` seam, the `http2`/`crypto` boundary file, the unpublished Deno-owner `test-net-end-without-connect.js` watchpoint, and the existing preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_http_request_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `36` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `36` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `35` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-set-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-client-response-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-set-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-set-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-client-response-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-set-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-set-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-client-response-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-set-timeout.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next plain `node:http` timeout seam into the manifested `NLC6` denominator instead of waiting for a larger raw-socket or keepalive wave. The targeted Node22 timeout batch proved the imported official files green immediately with no new watchpoints: `test-http-client-timeout-option.js`, `test-http-client-set-timeout.js`, `test-http-client-response-timeout.js`, and `test-http-set-timeout.js`. The single broad checkpoint sequence then stayed green across all three focused lanes, lifting the measured networking subset to `40 / 40 / 39` for Node22 primary, Node20 validation, and Node24 preview. That widens `NLC6` from the first request/response proof into the next shared HTTP timer contract while leaving the explicit remainder unchanged: the unpublished `internal/http2/util.mapToHeaders` seam, the `http2`/`crypto` boundary file, the unpublished Deno-owner `test-net-end-without-connect.js` watchpoint, and the existing preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_http_timeout_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `40` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `40` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `39` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/countdown.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-contentLength0.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-head-request.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-writehead-returns-this.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-statuscode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-status-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-contentLength0.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-head-request.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-writehead-returns-this.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-statuscode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-status-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-contentLength0.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-head-request.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-writehead-returns-this.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-statuscode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-status-message.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Staged the next plain `node:http` response/head slice and then split it honestly instead of forcing the whole batch into the denominator. The positive shared-LTS files `test-http-contentLength0.js`, `test-http-head-request.js`, and `test-http-response-writehead-returns-this.js` are now green and promoted into the manifested `NLC6` subset, lifting the measured networking denominator to `43 / 43 / 42` for Node22 primary, Node20 validation, and Node24 preview. The broader response/status slice is not being overclaimed: `test-http-response-statuscode.js` narrowed to a shared Deno-owner `ServerResponse.writeHead()` contract bug on the current published runtime baseline, and the staged `countdown.js` helper plus explicit watchpoints now keep that status-code seam separate from the newly green response/head positives. A local Deno-fork proof showed that preserving `originalStatusCode` and throwing `ERR_HTTP_INVALID_STATUS_CODE(originalStatusCode)` gets past the first blocker, but the local override then narrows to a wildcard-bind `listen(0)` seam that still needs separate review before the held-out response/status files can graduate. | `cargo test -p neovex-runtime node22_http_content_length0_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_http_response_statuscode_watchpoint -- --nocapture --test-threads=1 --ignored` → timed out after `30s` on the current pin; `cargo test -p neovex-runtime node22_networking_http_response_positive_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `43` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `43` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `42` staged files green; `CARGO_NET_OFFLINE=true cargo --config /private/tmp/neovex-local-deno-patch.toml test -p neovex-runtime node22_http_response_statuscode_watchpoint -- --nocapture --test-threads=1 --ignored` → progressed past the original status-code contract bug and narrowed to wildcard-bind `listen(0)` `EACCES` under the local override |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted one more staged plain-HTTP file without widening the held-out seam. The shared-LTS `test-http-response-status-message.js` file is green on the published runtime baseline, so it now joins the manifested `NLC6` denominator alongside the earlier response/head positives. The broader remainder is now cleaner and more honest: the only held-out plain-HTTP response file in this slice is `test-http-response-statuscode.js`, which still times out on the current pin and remains classified as a shared Deno-owner `ServerResponse.writeHead()` status-code contract gap. With `status-message` promoted, the measured networking denominator rises to `44 / 44 / 43` for Node22 primary, Node20 validation, and Node24 preview while leaving the explicit watchpoint inventory otherwise unchanged. | `cargo test -p neovex-runtime node22_http_response_status_message_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `44` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `44` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `43` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `~/src/github.com/agentstation/deno/ext/node/polyfills/_http_server.js`, `docs/architecture/runtime/node-lts-compat/failures/networking.md` | Tightened the owner diagnosis for the remaining `test-http-write-head.js` seam instead of leaving it as a vague “header missing” report. The local Deno fork now matches Node’s odd-length flat-header-array throw for `ServerResponse.writeHead()` by using `ERR_INVALID_ARG_VALUE`, which moves the imported official watchpoint past its first assertion. With that local owner fix applied through the temporary `/private/tmp/neovex-local-deno-patch.toml` override, the `writeHead()` watchpoint no longer stops at `response.headers.test === undefined`; it narrows to the same wildcard-bind `listen(0)` `EACCES` seam already seen in the local status-code proof. That means the remaining work is clearly in the shared Deno-owner HTTP/listen path below the published Neovex pin, not in the imported fixture harness or the newly widened manifested response/state denominator. | `CARGO_NET_OFFLINE=true cargo --config /private/tmp/neovex-local-deno-patch.toml test -p neovex-runtime node22_http_write_head_watchpoint -- --nocapture --test-threads=1 --ignored` → progressed past the original header-override assertion and now fails on wildcard-bind `listen(0)` `EACCES` under the local override |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-cork.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-multi-content-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-multiheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-status-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-status-reason-invalid-chars.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-write-head-2.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-response-splitting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-cork.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-multi-content-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-multiheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-status-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-status-reason-invalid-chars.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-write-head-2.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-splitting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-cork.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-multi-content-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-multiheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-status-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-status-reason-invalid-chars.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-write-head-2.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-response-splitting.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC6` with the next shared `node:http` response/status slice, but kept the denominator honest by splitting the one real red file back out. The newly green official files are `test-http-response-close.js`, `test-http-response-cork.js`, `test-http-response-multi-content-length.js`, `test-http-response-multiheaders.js`, `test-http-status-message.js`, `test-http-status-reason-invalid-chars.js`, and `test-http-write-head-2.js`, all promoted directly into `NETWORKING_BATCH`. The only new held-out file from that slice is `test-http-response-splitting.js`, which times out on the current published baseline and is now an explicit watchpoint instead of blocking the rest of the response/status wave. I also tightened the remaining `test-http-response-statuscode.js` diagnosis with a more faithful isolated proof: a standalone local git-source Deno checkout that changes only `_http_server.js` keeps `test-http-contentLength0.js` green, makes `test-http-write-head.js` pass, and still leaves `test-http-response-statuscode.js` timing out, which means the remaining seam there is the no-arg `server.listen()` / default-bind path rather than the `writeHead()` contract itself. With the seven-file response/status slice promoted, the measured networking denominator rises to `62 / 61 / 61` for Node22 primary, Node20 validation, and Node24 preview. | `cargo test -p neovex-runtime node22_networking_http_response_state_batch_fixture -- --nocapture --test-threads=1` → first failed only on `test-http-response-splitting.js`, then `1 passed` after carving that file into an explicit watchpoint; `cargo test -p neovex-runtime node22_http_response_splitting_watchpoint -- --nocapture --test-threads=1 --ignored` → timed out after `30s`; `cargo --config /private/tmp/neovex-http-writehead-standalone-git.toml test -p neovex-runtime node22_http_content_length0_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo --config /private/tmp/neovex-http-writehead-standalone-git.toml test -p neovex-runtime node22_http_write_head_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo --config /private/tmp/neovex-http-writehead-standalone-git.toml test -p neovex-runtime node22_http_response_statuscode_watchpoint -- --nocapture --test-threads=1 --ignored` → timed out after `30s`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `62` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `61` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `61` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-server-options-incoming-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-server-options-server-response.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-net-server-unref-persistent.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-server-options-incoming-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-server-options-server-response.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-net-server-unref-persistent.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-server-options-incoming-message.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-server-options-server-response.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-net-server-unref-persistent.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Promoted the next precise no-arg `server.listen()` slice instead of overdiagnosing the remaining `test-http-response-statuscode.js` hang as a generic bind problem. The newly green official files are `test-http-server-options-incoming-message.js`, `test-http-server-options-server-response.js`, and `test-net-server-unref-persistent.js`, all imported from the official Node20/22/24 corpus, proven first in a targeted Node22 batch, and then promoted into `NETWORKING_BATCH`. With those three files green across the checkpoint lanes, the measured networking denominator rises to `65 / 64 / 64` for Node22 primary, Node20 validation, and Node24 preview. That narrows the remaining `test-http-response-statuscode.js` watchpoint substantially: the no-arg listen entrypoint is now green, so the explicit remainder is the repeated invalid-status `writeHead()` throw plus request-completion / close ordering path that the file's 13-request countdown loop exercises, not a broad default-bind seam. | `cargo test -p neovex-runtime node22_networking_server_no_arg_listen_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `65` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `64` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `64` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-statuscode.js`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_http_server.js` | Restored the vendored Node22 `test-http-response-statuscode.js` fixture to exact upstream content after temporary local instrumentation, reran the clean current-pin watchpoint, and tightened the remaining `node:http` owner diagnosis. The decisive evidence is now explicit: the current Neovex pin still targets `agentstation/deno v2.7.14-locker.31` / `505e634`, whose `_http_server.js` throws `ERR_INVALID_ARG_TYPE` for invalid `writeHead()` status codes and still destroys sockets in `resOnFinish()` when `!server.listening`. I then landed the canonical local Deno-owner fix in `~/src/github.com/agentstation/deno/ext/node/polyfills/_http_server.js`, pairing the already-proven `writeHead()` contract change with the Node-style `resOnFinish()` keep-alive path. The measured networking denominator is unchanged at `65 / 64 / 64` because the Neovex pin has not been repointed yet, but the remaining `test-http-write-head.js` and `test-http-response-statuscode.js` watchpoints are no longer owner-ambiguous: they are held out by the stale published Deno baseline, not by the imported fixtures or the no-arg listen seam. | `diff -u ~/src/github.com/nodejs/node/test/parallel/test-http-response-statuscode.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-response-statuscode.js` → no output; `cargo test -p neovex-runtime node22_http_response_statuscode_watchpoint -- --nocapture --test-threads=1 --ignored` → timed out after `30s` on the current published pin; `nl -ba ~/.cargo/git/checkouts/deno-e7471af9660de05d/505e634/ext/node/polyfills/_http_server.js | sed -n '376,445p'` → confirms the pinned `writeHead()` path still throws `ERR_INVALID_ARG_TYPE`; `git -C ~/src/github.com/agentstation/deno diff --check -- ext/node/polyfills/_http_server.js` → clean |
| 2026-05-05 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Consumed the published Deno owner fixes by repinning Neovex to `agentstation/deno v2.7.14-locker.32`, then promoted the formerly held-out shared-LTS networking files directly into `NETWORKING_BATCH`: `test-http-response-statuscode.js`, `test-http-write-head.js`, `test-http-response-splitting.js`, `test-http2-util-assert-valid-pseudoheader.js`, and `test-net-end-without-connect.js`. The sharp proofs are now green on the canonical pinned baseline, not only under local overrides, so the stale Node22 watchpoints for those seams were removed and the measured networking denominator rises to `70 / 69 / 69` for Node22 primary, Node20 validation, and Node24 preview. The only remaining Node22 family holdout is now the honest `test-http2-getpackedsettings.js` `common.hasCrypto` skip under the current application-profile `node:crypto` boundary, while the only remaining preview-only remainder stays the existing Node24 `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_http_response_batch_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_http_write_head_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_http_response_splitting_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_http2_util_assert_valid_pseudoheader_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_net_end_without_connect_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_http2_getpackedsettings_watchpoint -- --nocapture --test-threads=1 --ignored` → fails with `Neovex node_compat skip: missing crypto`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `70` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `69` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `69` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-keepalive.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-keepalive-delay.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-maxsockets.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-maxsockets-respected.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-maxtotalsockets.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-reuse-drained-socket-only.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-scheduling.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-keepalive.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-keepalive-delay.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-maxsockets.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-maxsockets-respected.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-maxtotalsockets.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-reuse-drained-socket-only.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-scheduling.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-keepalive.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-keepalive-delay.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-maxsockets.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-maxsockets-respected.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-maxtotalsockets.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-reuse-drained-socket-only.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-scheduling.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-timeout.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Pulled the next shared-LTS `node:http` `http.Agent` keepalive/pool/scheduling slice straight from the pinned Node20/22/24 tags, proved the seven-file positive batch green first, and then promoted those files into `NETWORKING_BATCH`: `test-http-agent-keepalive.js`, `test-http-agent-keepalive-delay.js`, `test-http-agent-maxsockets.js`, `test-http-agent-maxsockets-respected.js`, `test-http-agent-maxtotalsockets.js`, `test-http-agent-scheduling.js`, and `test-http-agent-timeout.js`. The only red file in the eight-file import is now explicit and sharp instead of hiding the batch: `test-http-agent-reuse-drained-socket-only.js` hangs in the paused-socket large-write reuse path, so it stays as a shared `http.Agent` / socket-owner watchpoint outside the denominator. With the seven green files promoted, the measured networking subset rises to `77 / 76 / 76` for Node22 primary, Node20 validation, and Node24 preview while the remaining explicit remainder is now just the drained-socket reuse seam, the `http2`/`crypto` boundary file, and the existing preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_http_agent_batch_fixture -- --nocapture --test-threads=1` → first failed on missing `../common/countdown`, then `1 passed` after staging the shared helper and splitting out `test-http-agent-reuse-drained-socket-only.js`; `cargo test -p neovex-runtime node22_http_agent_reuse_drained_socket_only_watchpoint -- --nocapture --test-threads=1 --ignored` → hung in the isolated paused-socket reuse path and was stopped after targeted repro; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `77` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `76` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `76` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-false.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-no-protocol.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-null.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-remove.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-destroyed-socket.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-error-on-idle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-uninitialized.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-uninitialized-with-handle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http-agent-abort-controller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-false.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-no-protocol.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-null.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-remove.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-destroyed-socket.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-error-on-idle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-uninitialized.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-uninitialized-with-handle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http-agent-abort-controller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-false.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-no-protocol.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-null.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-remove.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-destroyed-socket.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-error-on-idle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-uninitialized.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-uninitialized-with-handle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http-agent-abort-controller.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened the `NLC6` `http.Agent` story again with a second shared-LTS slice that deliberately avoids the paused-socket large-write hang. The targeted lifecycle/config batch proved green first, and then those files were promoted into `NETWORKING_BATCH`: `test-http-agent-false.js`, `test-http-agent-no-protocol.js`, `test-http-agent-null.js`, `test-http-agent-remove.js`, `test-http-agent-destroyed-socket.js`, `test-http-agent-error-on-idle.js`, `test-http-agent-uninitialized.js`, `test-http-agent-uninitialized-with-handle.js`, and `test-http-agent-abort-controller.js`. That lifts the measured networking denominator to `86 / 85 / 85` for Node22 primary, Node20 validation, and Node24 preview, while leaving the explicit remainder unchanged and sharp: `test-http-agent-reuse-drained-socket-only.js` remains the one `http.Agent` owner seam, `test-http2-getpackedsettings.js` remains the `node:crypto` boundary file, and the existing preview-only `test-stream-pipeline.js` drift stays outside the denominator. | `cargo test -p neovex-runtime node22_networking_http_agent_lifecycle_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `86` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `85` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `85` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-bytes-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-createSocket-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-send-address-types.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-send-bad-arguments.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-send-invalid-msg-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-close-is-not-callback.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-send-empty-array.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-send-empty-buffer.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-bytes-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-createSocket-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-send-address-types.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-send-bad-arguments.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-send-invalid-msg-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-close-is-not-callback.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-send-empty-array.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-send-empty-buffer.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-bytes-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-createSocket-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-send-address-types.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-send-bad-arguments.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-send-invalid-msg-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-close-is-not-callback.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-send-empty-array.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-send-empty-buffer.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC6` with the first pure `node:dgram` helper/argument-validation slice and corrected the stale classification of the remaining `http.Agent` outlier. The targeted Node22 UDP batch proved green first, and then those eight shared-LTS files were promoted into `NETWORKING_BATCH`: `test-dgram-bytes-length.js`, `test-dgram-createSocket-type.js`, `test-dgram-send-address-types.js`, `test-dgram-send-bad-arguments.js`, `test-dgram-send-invalid-msg-type.js`, `test-dgram-close-is-not-callback.js`, `test-dgram-send-empty-array.js`, and `test-dgram-send-empty-buffer.js`. The single broad checkpoint sequence stayed green across all three focused lanes, lifting the measured networking denominator to `94 / 93 / 93` for Node22 primary, Node20 validation, and Node24 preview. This same pass also sharpened the explicit remainder honestly: `test-http-agent-reuse-drained-socket-only.js` is no longer treated as the remaining `http.Agent` seam because the official file now narrows to a cross-family dependency on `process.report.getReport()` and embedded `process.exit()` behavior, while `test-http2-getpackedsettings.js` remains the truthful `node:crypto` boundary and the Node24-only `test-stream-pipeline.js` drift stays preview-only. | `cargo test -p neovex-runtime node22_networking_dgram_helper_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `94` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `93` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `93` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-address.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-bind-default-address.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-listen-after-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-ref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-unref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-dgram-implicit-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-address.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-bind-default-address.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-listen-after-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-ref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-unref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-dgram-implicit-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-address.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-bind-default-address.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-listen-after-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-ref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-unref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-dgram-implicit-bind.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC6` again with the adjacent `node:dgram` bind/address/lifecycle/ref slice, staying on the same UDP owner seam instead of hopping families. The targeted Node22 bind batch proved green immediately, and then those eight shared-LTS files were promoted into `NETWORKING_BATCH`: `test-dgram-address.js`, `test-dgram-bind-default-address.js`, `test-dgram-bind.js`, `test-dgram-close.js`, `test-dgram-listen-after-bind.js`, `test-dgram-ref.js`, `test-dgram-unref.js`, and `test-dgram-implicit-bind.js`. The single broad checkpoint sequence stayed green across all three focused lanes, lifting the measured networking denominator to `102 / 101 / 101` for Node22 primary, Node20 validation, and Node24 preview. This confirms the second pure UDP wave is also in-family: socket address reporting, default bind-address selection, repeated `bind()` rejection after a successful bind, close-before-lookup safety, `listening`-after-`bind` delivery, `ref()` / `unref()` behavior with and without a live handle, and implicit bind during repeated `send()` calls now all sit in the green denominator. The explicit remainder stays sharp and unchanged: the cross-family `process.report` / embedded `process.exit()` dependency in `test-http-agent-reuse-drained-socket-only.js`, the `http2` `node:crypto` boundary in `test-http2-getpackedsettings.js`, and the preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_dgram_bind_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `102` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `101` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `101` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node{20,22,24}/test/parallel/test-dgram-connect*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node{20,22,24}/test/parallel/test-dgram-send*.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md` | Widened `NLC6` again with a larger meaningful UDP wave instead of another one-off fixture pass. The current `NETWORKING_BATCH` now absorbs both the connected-UDP send/default-host slice and the broader unconnected send/callback/default-host slice in one checkpointed step. The connected portion adds `test-dgram-connect.js`, `test-dgram-connect-send-callback-buffer.js`, `test-dgram-connect-send-callback-buffer-length.js`, `test-dgram-connect-send-callback-multi-buffer.js`, `test-dgram-connect-send-default-host.js`, `test-dgram-connect-send-empty-array.js`, `test-dgram-connect-send-empty-buffer.js`, and `test-dgram-connect-send-empty-packet.js`; the broader send/callback wave adds `test-dgram-send-callback-buffer-empty-address.js`, `test-dgram-send-callback-buffer-length-empty-address.js`, `test-dgram-send-callback-buffer-length.js`, `test-dgram-send-callback-buffer.js`, `test-dgram-send-callback-multi-buffer-empty-address.js`, `test-dgram-send-callback-multi-buffer.js`, `test-dgram-send-callback-recursive.js`, `test-dgram-send-cb-quelches-error.js`, `test-dgram-send-default-host.js`, `test-dgram-send-empty-packet.js`, `test-dgram-send-multi-buffer-copy.js`, `test-dgram-send-multi-string-array.js`, and `test-dgram-sendto.js`. The result is the first intentionally larger multi-slice UDP checkpoint in `NLC6`: connected sockets, callback byte-count reporting, empty-address overloads, recursive callback scheduling, callback-quelched error delivery, default-host delivery, empty-packet behavior, multi-buffer copy semantics, string-array sends, and `sendto()` validation are now all green across the supported lanes. The measured networking denominator rises to `123 / 122 / 122` for Node22 primary, Node20 validation, and Node24 preview, while the explicit remainder stays sharp and unchanged: the cross-family `process.report` / embedded `process.exit()` dependency in `test-http-agent-reuse-drained-socket-only.js`, the `http2` `node:crypto` boundary in `test-http2-getpackedsettings.js`, and the preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_dgram_connect_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_networking_dgram_send_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `123` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `122` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `122` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/plans/node-lts-compatibility-plan.md` | Codified the wider-slice verification discipline in the control plane and used it to classify the full remaining Node22 `dgram` remainder once instead of continuing to reprove already-green UDP slices. `node_compat.rs` now carries a broad remaining-UDP classification batch plus an 8-file focused control batch. The broad current-pin batch still gives the truthful `38`-file remainder map with `20` failing fixtures, while the new control batch proves a representative 8-file seam is green on the canonical pinned runtime. A temporary local `patch.crates-io` override in `/private/tmp/neovex-local-deno-cratesio-patch-locker32.toml` let the same focused batch run against `~/src/github.com/agentstation/deno`, which exposed that the speculative local `udp_wrap.ts` bind-catch change regressed the seam and that the local-path build was catching `undefined` in `UDP.#doBind`. The local Deno fork was then restored to match the exact `locker.32` cargo checkout again so the fork stays in sync while the next NLC6 move promotes the known-green current-pin UDP files and keeps the remaining bind/fd/cluster/IPv6/socket-option seams explicit. | `cargo test -p neovex-runtime node22_networking_dgram_remaining_batch_fixture -- --nocapture --test-threads=1` → classified the full remaining Node22 UDP slice once with `20` failing fixtures; `cargo test -p neovex-runtime node22_networking_dgram_local_patch_regression_batch_fixture -- --nocapture --test-threads=1 --ignored` → `1 passed` on the canonical pinned runtime; `cargo --offline --config /private/tmp/neovex-local-deno-cratesio-patch-locker32.toml test -p neovex-runtime node22_networking_dgram_local_patch_regression_batch_fixture -- --nocapture --test-threads=1 --ignored` → reproduced the local-fork regression and printed `neovex udp_wrap #doBind caught: undefined`; `cargo fmt --all --check` |
| 2026-05-05 | NLC6 | `in_progress` | `AGENTS.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Compacted the Node LTS control plane so it consumes less context without losing resumability. `AGENTS.md` now keeps one routing/working-set section instead of two overlapping reference sections, the main Node LTS plan now carries only live control state plus a current execution snapshot, and the full chronological history moved into this companion log. The plan remains the execution owner while the log remains the archive of proof and handoff detail. | `git diff --check -- AGENTS.md docs/plans/node-lts-compatibility-plan.md docs/plans/node-lts-compatibility-execution-log.md` |
| 2026-05-05 | NLC6 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `Cargo.toml`, `Cargo.lock`, `docs/plans/node-lts-compatibility-plan.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/udp_wrap.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/dgram.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/tcp_wrap.ts`, `~/src/github.com/agentstation/deno/ext/node/ops/udp.rs`, `~/src/github.com/agentstation/deno/ext/node/ops/util.rs`, `~/src/github.com/agentstation/deno/ext/node/ops/tcp_wrap.rs`, `~/src/github.com/agentstation/deno/ext/node/lib.rs` | Moved the live Node22 `dgram` owner seam materially forward in the canonical local-fork workflow. Neovex is now temporarily repinned to successive committed Deno revisions in `~/src/github.com/agentstation/deno` while proving the real UDP owner fixes: UDP handles now expose raw `fd` values, `udp_wrap.open(fd)` is implemented against duplicated sockets, oversized `setRecvBufferSize()` / `setSendBufferSize()` now return the expected Node-shaped `EINVAL`, and the local Neovex `internal/dgram` shim now rejects hostname-based `_createSocketHandle()` binds like upstream. A small JS shim in Deno also restored the no-arg `TCPWrap.listen()` control shape used by the official `dgram` fd fixtures. The focused owner batch fell from `5` failing fixtures to `2`, leaving only the remaining TCP-control-fd classification seam (`test-dgram-bind-fd-error.js` and `test-dgram-create-socket-handle-fd.js`) instead of the earlier broader UDP/open/buffer cluster. | `cargo test -p neovex-runtime node22_networking_dgram_remaining_batch_fixture -- --nocapture --test-threads=1` → first `5` failing fixtures, then `3`, then `2` after the Deno owner commits and local shim fix; `git -C /Users/jack/src/github.com/agentstation/deno status --short` → clean after commits `21972e139a4a47bc64e1d3766a16b203ec9c9bf7`, `f2e2a9fdd82325f25c03259286ea68aaf5b39102`, `3744ab9e5d38d568989073af8b9c5e47f244e19d`, and `5499c6d9fd9ef3579509d03cd7c0fe24245c2d1d` |
| 2026-05-05 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/ops/tcp_wrap.rs`, `~/src/github.com/agentstation/deno/libs/core/uv_compat/tcp.rs` | Closed the last live pure `node:dgram` owner seam and immediately promoted the cleared wave into the manifested networking denominator. The Deno owner layer now exposes a real `TCPWrap.fd` getter backed by libuv-compatible TCP handle state, which fixes the control-fd path used by `dgram.bind({ fd })` and `internal/dgram._createSocketHandle(..., fd)`. After committing that owner fix in the canonical local fork (`729284492497b8adc8f85329bac2c5a1f5068f2c`) and repinning Neovex to it, the focused `node22_networking_dgram_remaining_batch_fixture` went fully green. That let the entire `27`-file local-socket/fd/multicast/error UDP wave promote directly into `NETWORKING_BATCH`, raising the measured networking denominator to `150 / 149 / 149` for Node22 primary, Node20 validation, and Node24 preview. The explicit remainder is now narrower and truer: `dgram` cluster/process boundaries, host/profile IPv6/external-net limits, the `reusePort` lifecycle watchpoint, the cross-family `process.report` / embedded-exit dependency in `test-http-agent-reuse-drained-socket-only.js`, the `http2` / `node:crypto` boundary in `test-http2-getpackedsettings.js`, and the preview-only `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime node22_networking_dgram_remaining_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `150` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `149` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `149` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/bootstrap/extensions.rs`, `crates/neovex-runtime/src/runtime/bootstrap/source.rs`, `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_tls_common.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_tls_wrap.js`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/options.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/http2/util.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/http2.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/mod.ts`, `~/src/github.com/agentstation/deno/ext/node/lib.rs` | Widened `NLC6` with the first crypto-gated `https` / `http2` helper wave and kept it efficient by classifying the whole 12-file slice once, then iterating only on the failing shared seams. Neovex first fixed two embedder-owned blockers locally: the Node22 runtime now installs the Rustls default provider before building snapshot/runtime extensions, and the runtime `process.env` proxy now supports local overlay set/delete behavior while the application profile explicitly allows the single TLS toggle `NODE_TLS_REJECT_UNAUTHORIZED`. In the canonical Deno fork, the owner layer now honors `NODE_TLS_REJECT_UNAUTHORIZED` through `internal/options.ts`, accepts inline OpenSSL cipher suffixes such as `ALL@SECLEVEL=0`, lazy-loads the `internal/test/binding('http2')` surface, and exposes the live `http2` helper bindings required by the official utility fixtures. With Neovex repinned to the canonical local Deno revision `b2ff78080ed04a0a47e2670670336dc492ba8d2f`, the focused crypto-gated batch narrowed cleanly to one real survivor: `test-https-agent-additional-options.js` now fails only on the legacy `TLSv1_1_method` / `minVersion: TLSv1.1` protocol path that the current rustls-backed TLS owner layer does not negotiate. The other 11 files promoted directly into `NETWORKING_BATCH`: `test-https-agent-constructor.js`, `test-https-agent-getname.js`, `test-https-agent.js`, `test-https-agent-abort-controller.js`, `test-https-server-options-incoming-message.js`, `test-https-server-options-server-response.js`, `test-https-client-get-url.js`, `test-http2-getpackedsettings.js`, `test-http2-util-headers-list.js`, `test-http2-util-update-options-buffer.js`, and `test-http2-misc-util.js`. The measured networking denominator now rises to `161 / 160 / 160` for Node22 primary, Node20 validation, and Node24 preview, while the explicit remainder stays sharp: the `dgram` cluster/process boundary, the host/profile IPv6/external-net boundary, the `reusePort` watchpoint, the cross-family `process.report` / embedded-exit file, the legacy TLSv1.1 watchpoint in `test-https-agent-additional-options.js`, and the preview-only Node24 `test-stream-pipeline.js` drift. | `cargo test -p neovex-runtime application_node22_allows_tls_reject_unauthorized_env_lookup -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_networking_crypto_gated_helper_batch_fixture -- --nocapture --test-threads=1` → first `1 failed` on `test-https-agent-additional-options.js`, then `1 passed` after carving that file into an explicit watchpoint; `cargo test -p neovex-runtime node22_https_agent_additional_options_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure with `Error: unsupported protocol`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `161` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `160` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-client-resume.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-hwm.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-pfx.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-resume-after-renew.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-selfsigned-no-keycertsign-no-crash.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-unix-socket-self-signed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-client-resume.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-hwm.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-pfx.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-resume-after-renew.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-selfsigned-no-keycertsign-no-crash.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-unix-socket-self-signed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-client-resume.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-hwm.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-pfx.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-resume-after-renew.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-selfsigned-no-keycertsign-no-crash.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-unix-socket-self-signed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/rsa_cert.pfx`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/selfsigned-no-keycertsign/key.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/selfsigned-no-keycertsign/cert.pem`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Fixed the `node_compat` harness so shared extra fixtures can be materialized as raw bytes instead of forced UTF-8 text, which unblocked the first TLS/session/cert-material `https` wave with `.pfx` inputs. Classified the 6-file slice once, promoted the two green files immediately, and split the real survivors into explicit watchpoints instead of hiding them behind the denominator. The live networking batch now includes shared-LTS `test-https-selfsigned-no-keycertsign-no-crash.js` plus Node22 / Node24 `test-https-hwm.js`, while the remaining explicit seams are now sharper: `test-https-client-resume.js` and `test-https-resume-after-renew.js` isolate the TLS session-ticket / session-resumption owner path, `test-https-pfx.js` isolates the PFX-backed secure-context handshake path, `test-https-unix-socket-self-signed.js` isolates the HTTPS unix-socket connect path, and `test-https-hwm.js` stays an explicit Node20 validation-only divergence. With only the green subset promoted, the single broad checkpoint raised the measured networking denominator to `200 / 198 / 199` for Node22 primary, Node20 validation, and Node24 preview. | `cargo test -p neovex-runtime node22_networking_https_tls_session_batch_fixture -- --nocapture --test-threads=1` → first classified `4` failing fixtures in the 6-file wave, then `1 passed` after promoting only the two green files; `cargo test -p neovex-runtime node24_https_hwm_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_https_hwm_watchpoint -- --nocapture --test-threads=1 --ignored` → expected timeout after the first two `readableHighWaterMark` callbacks; `cargo test -p neovex-runtime node22_networking_https_tls_session_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected failures on the TLS session-ticket / session-resumption watchpoint; `cargo test -p neovex-runtime node22_https_pfx_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure in the PFX-backed handshake path; `cargo test -p neovex-runtime node22_https_unix_socket_self_signed_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `_internalConnectMultiple` `AggregateError`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `200` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `198` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `199` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-basic-validations.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-check-server-identity.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-connect-abort-controller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-connect-allow-half-open-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-connect-hwm-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-connect-no-host.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-connect-simple.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-connect-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-options-boolean-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-tls-server-parent-constructor-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-basic-validations.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-check-server-identity.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-connect-abort-controller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-connect-allow-half-open-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-connect-hwm-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-connect-no-host.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-connect-simple.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-connect-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-options-boolean-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-tls-server-parent-constructor-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-basic-validations.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-check-server-identity.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-connect-abort-controller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-connect-allow-half-open-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-connect-hwm-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-connect-no-host.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-connect-simple.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-connect-timeout-option.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-options-boolean-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-tls-server-parent-constructor-options.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the first pure `node:tls` wave as one real shared-LTS slice instead of dropping back to one-off `https` watchpoint churn. The 10-file batch split cleanly: three pure helper files promoted directly into the manifested denominator (`test-tls-check-server-identity.js`, `test-tls-connect-timeout-option.js`, and `test-tls-options-boolean-check.js`), while the remaining local/raw-TLS seam stayed explicit through a named watchpoint batch. The single broad Node22/20/24 replay after the three-file promotion raised the measured networking denominator to `203 / 201 / 202`. The remaining sharp TLS holdouts are now legacy `tls.createSecurePair({})` error shaping in `test-tls-basic-validations.js` and the half-open `write EPIPE` shutdown path in `test-tls-connect-allow-half-open-option.js`; the adjacent raw local-TLS server files were exercised on the watchpoint replay but intentionally not folded into the denominator this turn because that narrower rerun happened after the last broad checkpoint. | `cargo test -p neovex-runtime node22_networking_tls_local_batch_fixture -- --nocapture --test-threads=1` → first classified the wider 10-file wave, then `1 passed` after narrowing the promoted subset to the three green helper files; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `203` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `201` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `202` staged files green; `cargo test -p neovex-runtime node22_networking_tls_local_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected remaining failures on `test-tls-basic-validations.js` (`tls.createSecurePair({})`) and `test-tls-connect-allow-half-open-option.js` (`write EPIPE`) |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted the wider pure `node:tls` local-server wave efficiently instead of treating the earlier watchpoint replay as dead-end proof. The five adjacent raw local-TLS server files that had already been green on the ignored replay were moved into the live denominator: `test-tls-connect-abort-controller.js`, `test-tls-connect-hwm-option.js`, `test-tls-connect-no-host.js`, `test-tls-connect-simple.js`, and `test-tls-server-parent-constructor-options.js`. The single broad Node22 replay confirmed the widened batch at `208` and the Node24 preview lane confirmed it at `207`, but the matching Node20 validation replay exposed one honest older-lane drift: `test-tls-connect-hwm-option.js` still times out only on Node20. Instead of blocking the whole TLS wave, the harness now treats that file like the existing `https-hwm` case: Node22/Node24 keep it in-family, Node20 carries a dedicated explicit validation watchpoint, and the green denominator settles at `208 / 205 / 207`. The remaining raw-TLS holdouts stay sharp and unchanged around legacy `tls.createSecurePair({})` error shaping and `allowHalfOpen` `write EPIPE` shutdown semantics. | `cargo test -p neovex-runtime node22_networking_tls_local_batch_fixture -- --nocapture --test-threads=1` → `1 passed` with the widened 8-file TLS slice green; `cargo test -p neovex-runtime node22_networking_tls_local_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected failures only on `test-tls-basic-validations.js` and `test-tls-connect-allow-half-open-option.js`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `208` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `207` staged files green; `cargo test -p neovex-runtime node20_tls_connect_hwm_option_watchpoint -- --nocapture --test-threads=1 --ignored` → expected timeout after `30s`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `205` official files green |
| 2026-05-06 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_tls_common.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_tls_wrap.js` | Closed the TLS session-ticket/session-reuse owner seam in the canonical Deno fork, published it, and promoted the whole session slice instead of leaving it as watchpoint debt. The Deno owner fix in `_tls_common.ts` and `_tls_wrap.js` added the secure-context ticket hooks plus the client/session bookkeeping needed for the official HTTPS session-resumption files. After committing that owner fix in `~/src/github.com/agentstation/deno` (`40adb8a27e932968bfaa1e1be327adddbe975bd4`), tagging and pushing `v2.7.14-locker.34`, and repinning Neovex to the published tag, the shared `test-https-client-resume.js` and `test-https-resume-after-renew.js` files plus Node22 `test-https-agent-session-reuse.js` all moved into the live denominator. The remaining non-boundary TLS/HTTPS pocket is now only `test-https-pfx.js`, `test-https-unix-socket-self-signed.js`, `test-https-strict.js`, and `test-https-agent-keylog.js`, and the measured networking denominator rose to `213 / 209 / 211` for Node22 primary, Node20 validation, and Node24 preview. | `cargo test -p neovex-runtime node22_networking_https_tls_remainder_batch_fixture -- --nocapture --test-threads=1 --ignored` → before publish, narrowed the remaining TLS/HTTPS batch to `test-https-pfx.js`, `test-https-unix-socket-self-signed.js`, `test-https-strict.js`, and `test-https-agent-keylog.js`; `cargo test -p neovex-runtime node22_networking_https_tls_session_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `213` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `209` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `211` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/ops/tls_wrap.rs` | Closed the remaining raw `node:tls` owner seam and moved Neovex out of local-prove mode cleanly. On the Neovex side, `module_loader.rs` now installs the same `createSecurePair()` compatibility shim for CommonJS `require("tls")` consumers that the ESM path already saw, which narrowed the old raw-TLS watchpoint batch down to the last real owner bug. In the canonical Deno fork, `ext/node/ops/tls_wrap.rs` now defers the underlying TCP shutdown until after TLS `close_notify`, which removes the half-open `write EPIPE` mismatch. That fix was committed on `locker-v2.7.14`, published as `agentstation/deno v2.7.14-locker.33`, and Neovex was repinned back from the temporary local file-rev proof to the published tag in `Cargo.toml` / `Cargo.lock`. With the two remaining raw-TLS files promoted into `NETWORKING_BATCH`, the main networking denominator now rises to `210 / 207 / 209` for Node22 primary, Node20 validation, and Node24 preview. The explicit remainder is correspondingly sharper: the raw `tls.createSecurePair({})` and `allowHalfOpen` watchpoints are gone, leaving the Node20-only `https-hwm` / `tls-connect-hwm-option` divergences plus the remaining TLS session-ticket, PFX, unix-socket, keylog/session-reuse, strict-auth, legacy TLSv1.1, `dgram` boundary, `reusePort`, and preview-only drift pockets. | `cargo test -p neovex-runtime node22_networking_tls_local_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → first narrowed to only `test-tls-connect-allow-half-open-option.js`, then passed after the Deno owner fix while still on the identical local commit `f95ca0c9dfaedd1cbb9cd43f8fc212ac43e89a71`; `cargo test -p neovex-runtime node22_networking_tls_local_batch_fixture -- --nocapture --test-threads=1` → `1 passed` after promoting `test-tls-basic-validations.js` and `test-tls-connect-allow-half-open-option.js`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `210` official files green on commit `f95ca0c9dfaedd1cbb9cd43f8fc212ac43e89a71`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `207` official files green on the same commit; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `209` staged files green on the same commit; `cargo test -p neovex-runtime node22_networking_tls_local_batch_fixture -- --nocapture --test-threads=1` → `1 passed` again after repinning to the published `v2.7.14-locker.33` tag that points at the same verified commit |
| 2026-05-05 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-create-connection.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-disable-session-reuse.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-keylog.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-servername.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-session-injection.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-session-reuse.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-sni.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-agent-sockets-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-client-override-global-agent.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-agent-create-connection.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-agent-keylog.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-agent-servername.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-agent-sni.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/agent2-key.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/agent2-cert.pem`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/https.ts` | Widened `NLC6` with a 9-file shared-LTS `https.Agent` connection/session/global-agent wave and kept it efficient by classifying the whole slice once, then fixing only the shared owner seam. The first focused batch surfaced four files, but the canonical Deno owner fix in `https.ts` closed the real shared gap: `Agent#createConnection()` now honors the legacy overload forms, and `https.globalAgent` is mutable like upstream Node. After committing that owner fix in the canonical Deno fork (`b964e6f79f5c350e36fc9a41e8e289f7edfffe51`) and repinning Neovex to it, seven files promoted directly into `NETWORKING_BATCH`: `test-https-agent-create-connection.js`, `test-https-agent-disable-session-reuse.js`, `test-https-agent-servername.js`, `test-https-agent-session-injection.js`, `test-https-agent-sni.js`, `test-https-agent-sockets-leak.js`, and `test-https-client-override-global-agent.js`. The two remaining files are now explicit and sharp instead of blocking the batch: `test-https-agent-keylog.js` is the missing TLS keylog-event seam, and `test-https-agent-session-reuse.js` is the remaining TLS session reuse / key-update hang. The measured networking denominator rose to `168 / 167 / 167` for Node22 primary, Node20 validation, and Node24 preview. | `cargo test -p neovex-runtime node22_networking_https_agent_session_batch_fixture -- --nocapture --test-threads=1` → first exposed `4` failing files, then `1 passed` for the promoted 7-file batch after the Deno owner fix; `cargo test -p neovex-runtime node22_https_agent_keylog_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on missing keylog events; `cargo test -p neovex-runtime node22_https_agent_session_reuse_watchpoint -- --nocapture --test-threads=1 --ignored` → expected timeout in the TLS session reuse path; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `168` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `167` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `167` staged files green |
| 2026-05-05 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-abortcontroller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-argument-of-creating.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-byteswritten.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-max-headers-count.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-request-arguments.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-headers-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-request-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-set-timeout-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-simple.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-timeout-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-timeout-server-2.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-abortcontroller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-argument-of-creating.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-byteswritten.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-max-headers-count.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-request-arguments.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-headers-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-request-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-set-timeout-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-simple.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-timeout-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-timeout-server-2.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-abortcontroller.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-argument-of-creating.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-byteswritten.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-max-headers-count.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-request-arguments.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-headers-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-request-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-set-timeout-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-simple.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-timeout-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-timeout-server-2.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/https.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_tls_wrap.js` | Widened `NLC6` again with a 13-file shared-LTS local `https` request/server/timeout/property wave and kept it efficient by classifying the whole slice once, then fixing only the two shared owner seams it exposed. The first focused pass showed a dropped `ALPNCallback` property in `https.createServer()` and a missing server-side handshake-timeout path. Those were fixed in the canonical Deno fork by preserving `ALPNCallback` on `https.Server` in `https.ts` and by threading `handshakeTimeout` into the server-side `TLSSocket` construction path in `_tls_wrap.js`. After committing that owner fix in the canonical Deno fork (`8325875e612a699324503612e02d91dd7a30e7b1`) and repinning Neovex to it, the entire 13-file wave went green and promoted directly into `NETWORKING_BATCH`: `test-https-abortcontroller.js`, `test-https-argument-of-creating.js`, `test-https-byteswritten.js`, `test-https-close.js`, `test-https-max-headers-count.js`, `test-https-request-arguments.js`, `test-https-server-headers-timeout.js`, `test-https-server-request-timeout.js`, `test-https-set-timeout-server.js`, `test-https-simple.js`, `test-https-timeout.js`, `test-https-timeout-server.js`, and `test-https-timeout-server-2.js`. The measured networking denominator rose to `181 / 180 / 180` for Node22 primary, Node20 validation, and Node24 preview, while the explicit remainder stayed sharp around the `dgram` boundaries, the cross-family `process.report` / embedded-exit file, the legacy TLSv1.1 watchpoint, and the remaining TLS keylog/session-reuse watchpoints. | `cargo test -p neovex-runtime node22_networking_https_local_server_batch_fixture -- --nocapture --test-threads=1` → first failed on `test-https-argument-of-creating.js` and `test-https-timeout-server.js`, then `1 passed` after the Deno owner fix and repin; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `181` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `180` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `180` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-close-all.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-close-destroy-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-close-idle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-connections-checking-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-socket-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-keep-alive-drop-requests.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-close-all.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-close-destroy-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-close-idle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-connections-checking-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-socket-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-keep-alive-drop-requests.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-close-all.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-close-destroy-timeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-close-idle.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-connections-checking-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-socket-options.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-keep-alive-drop-requests.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/gc.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Widened `NLC6` with a 6-file shared-LTS local `https` server lifecycle/socket wave and kept it efficient by classifying the whole slice once, then promoting it immediately because the focused batch was already green. The new coverage closes the next local HTTPS seam without any owner-side Deno changes: `closeAllConnections()`, close-idle behavior, the connection-checking timer destroy path, request socket passthrough helpers, `dropRequest` handling under `maxRequestsPerSocket`, and the HTTPS server GC/leak check all now ride in the live denominator. The only harness addition was staging shared `test/common/gc.js` so the official leak-checking file could run honestly. After the focused Node22 batch passed cleanly, the single broad checkpoint confirmed the entire wave across all three lanes and raised the measured networking denominator to `187 / 186 / 186` for Node22 primary, Node20 validation, and Node24 preview, while the explicit remainder stayed unchanged and sharp around the `dgram` boundaries, `reusePort`, the cross-family `process.report` / embedded-exit watchpoint, and the three remaining TLS watchpoints. | `cargo test -p neovex-runtime node22_networking_https_server_lifecycle_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `187` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `186` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `186` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-client-checkServerIdentity.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-client-reject.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-connecting-to-http.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-drain.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-eof-for-eom.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-host-headers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-insecure-parse-per-stream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-localaddress-bind-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-max-header-size-per-stream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-options-boolean-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-server-async-dispose.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-strict.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-truncate.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-https-connect-address-family.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-client-checkServerIdentity.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-client-reject.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-connecting-to-http.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-drain.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-eof-for-eom.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-host-headers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-insecure-parse-per-stream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-localaddress-bind-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-max-header-size-per-stream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-options-boolean-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-server-async-dispose.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-strict.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-truncate.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-https-connect-address-family.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-client-checkServerIdentity.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-client-reject.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-connecting-to-http.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-drain.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-eof-for-eom.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-host-headers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-insecure-parse-per-stream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-localaddress-bind-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-max-header-size-per-stream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-options-boolean-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-server-async-dispose.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-strict.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-truncate.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-https-connect-address-family.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/agent3-key.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/agent3-cert.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/ca2-cert.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/rsa_private.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/rsa_cert.crt`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/ec-key.pem`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/keys/ec-cert.pem`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified a broader 14-file remaining-`https` client/server semantics wave in one pass, promoted the 11 green files immediately, and turned the 3 non-green files into explicit watchpoints instead of dragging them through the denominator. The newly green coverage now includes certificate authorization overrides (`test-https-client-checkServerIdentity.js`, `test-https-client-reject.js`), HTTPS-to-HTTP error surfacing, drain/EOM behavior, host-header propagation, per-stream insecure-parse and max-header-size semantics, key/cert/CA boolean/type validation, async server disposal, and truncated-response readability. The single classification pass also sharpened the explicit remainder instead of leaving it fuzzy: `test-https-localaddress-bind-error.js` is a host-profile local-address capability boundary, `test-https-connect-address-family.js` is the corresponding IPv6 capability boundary, and `test-https-strict.js` is a real TLS strict-auth / multi-agent authority seam that still times out. After carving those three files out and rerunning only the focused green subset, the single broad Node22/20/24 checkpoint raised the measured networking denominator to `198 / 197 / 197` for Node22 primary, Node20 validation, and Node24 preview. | `cargo test -p neovex-runtime node22_networking_https_client_server_batch_fixture -- --nocapture --test-threads=1` → first classified `3` failing fixtures in the 14-file wave, then `1 passed` after carving out the 11 green files; `cargo test -p neovex-runtime node22_networking_https_address_boundary_batch_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on `NotCapable: Requires net access to "1.2.3.4:0"` plus `Neovex node_compat skip: no IPv6 support`; `cargo test -p neovex-runtime node22_https_strict_watchpoint -- --nocapture --test-threads=1 --ignored` → expected timeout after `30s`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `198` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `197` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `197` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/ops/tls_wrap.rs`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_tls_wrap.js` | Closed the remaining TLS keylog-event owner seam on the canonical Deno fork, published it as `agentstation/deno v2.7.14-locker.35`, and then consumed that published baseline truthfully in Neovex instead of leaving the fix stranded in local-prove mode. The Deno owner work wired rustls keylog delivery through the TLSWrap callback bridge and attached the server-side forwarding path early enough for handshake-time events, which made `test-https-agent-keylog.js` pass on the published pin. With Neovex repinned to `v2.7.14-locker.35`, `test-https-agent-keylog.js` is now promoted into `NETWORKING_BATCH`, removed from the explicit TLS remainder, and the measured networking denominator rises to `214 / 209 / 212` for Node22 primary, Node20 validation, and Node24 preview. The remaining non-boundary TLS/HTTPS pocket is now only `test-https-pfx.js`, `test-https-unix-socket-self-signed.js`, and `test-https-strict.js`, while the existing `dgram` boundary, `reusePort`, legacy TLSv1.1, Node20 highWaterMark, cross-family `process.report`/embedded-exit, and preview-only drift watchpoints stay explicit. | `cargo test -p neovex-runtime node22_https_agent_keylog_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed` on the published `v2.7.14-locker.35` baseline before promotion; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `214` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `209` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `212` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/tmpdir.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/ops/tls_wrap.rs` | Closed the remaining shared-LTS HTTPS/TLS remainder on the canonical published path instead of leaving the final three files as watchpoint debt. In the Deno fork, `ext/node/ops/tls_wrap.rs` now aligns strict verifier fallback errors with upstream Node for self-issued versus untrusted-leaf chains; that fix was committed on `locker-v2.7.14`, published as `agentstation/deno v2.7.14-locker.36`, and repinned back into Neovex. On the Neovex side, the `node_compat` harness now builds temporary bundles under a short `/private/tmp` root and materializes `common.PIPE` as an absolute short path inside the shared fixture tmpdir, which removed the batch-only Unix-socket `ENOENT` without weakening the adjacent Unix-domain-socket fixtures. With those fixes in place, `test-https-pfx.js`, `test-https-unix-socket-self-signed.js`, and `test-https-strict.js` all promoted into `NETWORKING_BATCH`, lifting the measured networking denominator to `217 / 212 / 215` for Node22 primary, Node20 validation, and Node24 preview. The remaining explicit networking remainder is now the `dgram` cluster/process and host/profile boundary batches, the `reusePort` watchpoint, the cross-family `process.report` / embedded-exit dependency, the Node20-only highWaterMark divergences, the legacy TLSv1.1 watchpoint, and the Node24-only preview drift. | `cargo test -p neovex-runtime node22_https_unix_socket_self_signed_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed` on the published `v2.7.14-locker.36` baseline; `cargo test -p neovex-runtime node22_networking_https_tls_remainder_batch_fixture -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `217` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `212` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `215` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-status-code.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-status-code-invalid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-multi-content-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-response-splitting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-options-server-request.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-options-server-response.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-zero-length-header.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-multiheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-multiheaders-raw.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-status-code.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-status-code-invalid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-multi-content-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-response-splitting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-options-server-request.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-options-server-response.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-zero-length-header.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-multiheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-multiheaders-raw.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-status-code.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-status-code-invalid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-multi-content-length.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-response-splitting.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-options-server-request.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-options-server-response.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-zero-length-header.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-multiheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-multiheaders-raw.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Imported and promoted the next fresh 9-file shared-LTS pure `node:http2` header/status/options wave instead of circling on the already-green HTTPS/TLS slices. The whole batch passed green in the focused Node22 classification lane with no Deno owner fix required: `test-http2-status-code.js`, `test-http2-status-code-invalid.js`, `test-http2-multi-content-length.js`, `test-http2-response-splitting.js`, `test-http2-options-server-request.js`, `test-http2-options-server-response.js`, `test-http2-zero-length-header.js`, `test-http2-multiheaders.js`, and `test-http2-multiheaders-raw.js` are now in the live networking denominator. After the single broad Node22/Node20/Node24 checkpoint, the measured networking denominator rises to `226 / 221 / 224`, while the remaining explicit remainder stays sharply classified around the `dgram` cluster/process and host/profile batches, the `reusePort` watchpoint, the cross-family `process.report` / embedded-exit dependency, the Node20-only highWaterMark divergences, the legacy TLSv1.1 watchpoint, and preview-only drift. | `cargo test -p neovex-runtime node22_networking_http2_header_status_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `226` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `221` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `224` staged files green; `cargo fmt --all --check` → clean; `make clippy` → clean; `git diff --check` → clean |
| 2026-05-06 | NLC6 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-server*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverrequest*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-server*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverrequest*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-server*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverrequest*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/person-large.jpg`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/http2/compat.js` | Classified and promoted an 18-file shared-LTS `node:http2` compat request/response core wave instead of circling on the already-green HTTPS/TLS surface. The first focused Node22 pass narrowed the whole batch to one real owner seam: `Http2ServerRequest.trailers` still came through as a plain object instead of a null-prototype object in the canonical Deno fork. After fixing that owner seam in `ext/node/polyfills/internal/http2/compat.js`, publishing the canonical Deno fork as `agentstation/deno v2.7.14-locker.37`, and repinning Neovex back to the published tag, the entire compat wave promoted into `NETWORKING_BATCH`: `test-http2-compat-serverresponse.js`, `test-http2-compat-serverresponse-end.js`, `test-http2-compat-serverresponse-write.js`, `test-http2-compat-serverresponse-writehead.js`, `test-http2-compat-serverresponse-writehead-array.js`, `test-http2-compat-serverresponse-statuscode.js`, `test-http2-compat-serverresponse-statusmessage.js`, `test-http2-compat-serverresponse-statusmessage-property.js`, `test-http2-compat-serverresponse-statusmessage-property-set.js`, `test-http2-compat-serverresponse-headers.js`, `test-http2-compat-serverrequest.js`, `test-http2-compat-serverrequest-end.js`, `test-http2-compat-serverrequest-headers.js`, `test-http2-compat-serverrequest-host.js`, `test-http2-compat-serverrequest-pause.js`, `test-http2-compat-serverrequest-pipe.js`, `test-http2-compat-serverrequest-settimeout.js`, and `test-http2-compat-serverrequest-trailers.js`. The measured networking denominator rose to `244 / 239 / 242` for Node22 primary, Node20 validation, and Node24 preview while the explicit remainder stayed sharply classified around the `dgram` boundary batches, `reusePort`, the cross-family `process.report` / embedded-exit dependency, the Node20-only highWaterMark divergences, the legacy TLSv1.1 watchpoint, and preview-only drift. | `cargo test -p neovex-runtime node22_networking_http2_compat_request_response_batch_fixture -- --nocapture --test-threads=1` → first failed only on `test-http2-compat-serverrequest-trailers.js`, then `1 passed` after the Deno owner fix on the canonical local proof path; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `244` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `239` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `242` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-destroy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-drain.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-end-after-statuses-without-body.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-finished.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-flushheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-headers-after-destroy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-headers-send-date.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-settimeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-trailers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-write-early-hints-invalid-argument-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-write-early-hints-invalid-argument-value.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-write-early-hints.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-write-head-destroyed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-destroy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-drain.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-end-after-statuses-without-body.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-finished.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-flushheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-headers-after-destroy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-headers-send-date.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-settimeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-trailers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-write-early-hints-invalid-argument-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-write-early-hints-invalid-argument-value.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-write-early-hints.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-write-head-destroyed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-close.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-destroy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-drain.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-end-after-statuses-without-body.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-finished.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-flushheaders.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-headers-after-destroy.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-headers-send-date.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-settimeout.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-trailers.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-write-early-hints-invalid-argument-type.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-write-early-hints-invalid-argument-value.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-write-early-hints.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-write-head-destroyed.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the next fresh 14-file shared-LTS `node:http2` compat server-response lifecycle wave in one pass and kept it honest instead of forcing two cross-family reds into the denominator. The focused Node22 batch showed that 12 files are cleanly green with no Deno-owner change: `test-http2-compat-serverresponse-close.js`, `test-http2-compat-serverresponse-destroy.js`, `test-http2-compat-serverresponse-drain.js`, `test-http2-compat-serverresponse-end-after-statuses-without-body.js`, `test-http2-compat-serverresponse-finished.js`, `test-http2-compat-serverresponse-flushheaders.js`, `test-http2-compat-serverresponse-headers-after-destroy.js`, `test-http2-compat-serverresponse-headers-send-date.js`, `test-http2-compat-serverresponse-settimeout.js`, `test-http2-compat-serverresponse-trailers.js`, `test-http2-compat-write-early-hints.js`, and `test-http2-compat-write-head-destroyed.js`. The only two non-green files were not a pure `http2` semantic seam: `test-http2-compat-write-early-hints-invalid-argument-type.js` and `test-http2-compat-write-early-hints-invalid-argument-value.js` already reach the expected `uncaughtException` path and then stop on `process.exit(0)` because the current embedded profile still hides `Deno.exit`. After carving those two files into an explicit embedded-exit boundary watchpoint and rerunning only the 12-file green subset, the single broad Node22/Node20/Node24 checkpoint widened the networking denominator to `256 / 251 / 254` while keeping the remainder sharp and truthful. | `cargo test -p neovex-runtime node22_networking_http2_compat_serverresponse_lifecycle_batch_fixture -- --nocapture --test-threads=1` → first classified `2` failing fixtures in the 14-file wave, then `1 passed` after carving out the 12 green files; `cargo test -p neovex-runtime node22_networking_http2_compat_early_hints_exit_boundary_batch_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on hidden `Deno.exit` in both invalid-argument early-hints files; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `256` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `251` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `254` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-aborted.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-client-upload-reject.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-errors.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-expect-continue-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-expect-continue.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-expect-handling.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-method-connect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-serverresponse-createpushresponse.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-short-stream-client-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-socket-destroy-delayed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-socket-set.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-http2-compat-socket.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-aborted.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-client-upload-reject.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-errors.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-expect-continue-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-expect-continue.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-expect-handling.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-method-connect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-serverresponse-createpushresponse.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-short-stream-client-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-socket-destroy-delayed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-socket-set.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-http2-compat-socket.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-aborted.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-client-upload-reject.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-errors.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-expect-continue-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-expect-continue.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-expect-handling.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-method-connect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-serverresponse-createpushresponse.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-short-stream-client-server.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-socket-destroy-delayed.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-socket-set.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-http2-compat-socket.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the remaining unstaged 12-file shared-LTS `node:http2` compat request-control, push, and socket wave in one pass and got the best possible answer: the whole batch is green with no owner fix required. The newly promoted files are `test-http2-compat-aborted.js`, `test-http2-compat-client-upload-reject.js`, `test-http2-compat-errors.js`, `test-http2-compat-expect-continue-check.js`, `test-http2-compat-expect-continue.js`, `test-http2-compat-expect-handling.js`, `test-http2-compat-method-connect.js`, `test-http2-compat-serverresponse-createpushresponse.js`, `test-http2-compat-short-stream-client-server.js`, `test-http2-compat-socket-destroy-delayed.js`, `test-http2-compat-socket-set.js`, and `test-http2-compat-socket.js`. That effectively exhausts the broader compat `http2` family, leaving only the already-explicit invalid-argument early-hints files on the embedded-exit boundary. After promoting the full batch into `NETWORKING_BATCH`, the broad Node22 / Node20 / Node24 checkpoint widened the networking denominator to `268 / 263 / 266` while keeping the explicit remainder sharply classified around `dgram` boundaries, `reusePort`, the cross-family `process.report` and embedded-exit seams, the Node20-only highWaterMark divergences, the legacy TLSv1.1 watchpoint, and preview-only drift. | `cargo test -p neovex-runtime node22_networking_http2_compat_remainder_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `268` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `263` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `266` staged files green |
| 2026-05-06 | NLC6 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the Neovex-owned hidden-Deno / `process.exit()` harness seam behind the two compat `http2` invalid-argument early-hints files instead of leaving them as a permanent cross-family watchpoint. `node_compat.rs` now installs a fixture-scoped `process.reallyExit()` sentinel prelude for those two official files, letting them complete their expected `uncaughtException` path and report success without depending on hidden `Deno.exit`. With that seam closed, `test-http2-compat-write-early-hints-invalid-argument-type.js` and `test-http2-compat-write-early-hints-invalid-argument-value.js` promoted directly into the live `NLC6` networking denominator, and the control plane was narrowed to remove the stale embedded-exit watchpoint language. The measured networking denominator is now `270 / 265 / 268` for Node22 primary, Node20 validation, and Node24 preview, leaving the explicit remainder concentrated in the `dgram` cluster/process and host/profile batches, `reusePort`, the single cross-family `process.report` / embedded-exit `http.Agent` file, the legacy TLSv1.1 watchpoint, the Node20-only highWaterMark validation drifts, and preview-only drift. | `cargo test -p neovex-runtime node22_networking_http2_compat_early_hints_exit_boundary_batch_watchpoint -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `270` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `265` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `268` staged files green |
| 2026-05-06 | NLC6 | `done` | `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `crates/neovex-runtime/src/runtime/bootstrap/source.rs`, `crates/neovex-runtime/src/node_compat.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime_capabilities.rs`, `tests/node-compat/networking-canaries/`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/networking.md`, `docs/architecture/runtime/node-lts-compat/failures/networking.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the `NLC6` networking family on package-backed evidence instead of builtin-only optimism. Added the checked-in pinned canary root under `tests/node-compat/networking-canaries/`, installed and locked the stable package set, and wired two Application-profile canary lanes: a Node22 primary batch for `express`, `fastify`, `socket.io`, `undici`, and `axios`, plus a Node20 validation batch for `express` and `fastify`. The runtime fixes needed to make those canaries green stayed narrow and ownership-correct: Application-profile package staging now copies pinned `node_modules` into the generated bundle root, non-allowlisted `process.env` reads now resolve as `undefined` instead of throwing, local package-subpath resolution strips package names correctly before `node_modules` lookup, and the hidden Deno substrate now exposes the `node:os` hooks and `hostname` permission path that stable packages probe. With those fixes proven, the broad networking denominator stayed green at `270 / 265 / 268`, the mapped package canaries are green in their required lanes, and the remaining entries in the networking failure inventory stay explicit classified boundaries rather than unexplained `NLC6` failures. | `cargo test -p neovex-runtime package_name_from_specifier_strips_package_subpaths -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_node22_reads_local_files_hides_non_allowlisted_env_and_denies_escape_writes -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_node22_permissions_allow_local_network_hosts -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_node22_networking_package_canary_batch -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime application_node20_networking_validation_canary_batch -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `270` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_networking_subset -- --nocapture --test-threads=1` → `1 passed` with `265` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_networking_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `268` staged files green |
| 2026-05-06 | NLC7 | `in_progress` | `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Advanced the active control item to `NLC7` immediately after the `NLC6` closeout, per the continuation rule in the main plan. The next execution seam is the first coherent `crypto` / `zlib` / `async_hooks` / `module` classification wave, with `AsyncLocalStorage` and loader interoperability treated as blocking evidence rather than optional follow-up work. | `docs/plans/node-lts-compatibility-plan.md` current execution snapshot and roadmap ledger updated to `NLC7`; `NLC6` closeout evidence recorded in the row above |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-builtin.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-cache.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-create-require.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-create-require-multibyte.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-isBuiltin.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-loading-deprecated.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-nodemodulepaths.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-relative-lookup.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-version.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-children.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-multi-extensions.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-stat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-builtin.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-cache.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-create-require.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-create-require-multibyte.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-isBuiltin.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-loading-deprecated.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-nodemodulepaths.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-relative-lookup.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-version.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-children.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-multi-extensions.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-stat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-builtin.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-cache.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-create-require.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-create-require-multibyte.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-isBuiltin.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-loading-deprecated.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-nodemodulepaths.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-relative-lookup.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-version.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-children.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-multi-extensions.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-stat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/experimental.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/copy/utf/新建文件夹/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/copy/utf/新建文件夹/experimental.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/GH-7131/a.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/GH-7131/b.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/packages/missing-main/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/packages/missing-main/package.json`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified and closed the first real `NLC7` loader-context foundation wave instead of diffusing immediately into `crypto` or `async_hooks`. The first 12-file official `node:module` CommonJS / loader-helper batch narrowed cleanly to two real local seams: the Neovex-local module shim was leaking internal override specifiers into the public `builtinModules` list, and it was dropping the public `Module._stat()` contract entirely. Fixing both issues in `module_loader.rs` was enough to turn the whole staged batch green without a Deno-fork repin. The new staged loader-context subset is now green at `12 / 12 / 12` for the Node22 primary, Node20 validation, and Node24 preview lanes, and it promotes the first public `NLC7` `node:module` surface: `builtinModules`, `isBuiltin()`, `_nodeModulePaths()`, `_resolveLookupPaths()`, `createRequire()`, `Module._stat()`, and the staged CommonJS helper path. | `cargo test -p neovex-runtime node22_nlc7_module_commonjs_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `12` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `12` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `12` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-local-storage-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-local-storage-contexts.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-local-storage-deep-stack.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-local-storage-exit-does-not-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-local-storage-snapshot.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-local-storage-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-local-storage-contexts.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-local-storage-deep-stack.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-local-storage-exit-does-not-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-local-storage-snapshot.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-bind.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-contexts.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-deep-stack.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-enter-with.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-exit-does-not-leak.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-isolation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-local-storage-snapshot.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Widened `NLC7` from pure `node:module` into the first pure `AsyncLocalStorage` semantics wave. The coherent five-file shared-LTS batch (`bind`, `contexts`, `deep-stack`, `snapshot`, and `exit-does-not-leak`) proved green on Node22 and Node24 immediately. The same batch narrowed cleanly on the Node20 validation lane to one explicit divergence: official `nodejs/node v20.20.2` still expects the older JavaScript `AsyncLocalStorage._propagate` hook in `test-async-local-storage-exit-does-not-leak.js`, while the current runtime already matches the newer Node22/Node24 implementation shape that guards the hook behind `typeof als._propagate === "function"`. Instead of leaving a red classification test in the tree, Neovex promoted the four shared ALS-green files plus the Node22/Node24-only `exit-does-not-leak` file into the live `nlc7-loader-context` denominator, and converted the old Node20 expectation into an explicit ignored validation watchpoint. That lifts the first `NLC7` manifested loader-context subset to `17 / 16 / 17` for Node22 primary, Node20 validation, and Node24 preview, while keeping the only known async-context remainder sharp and truthful. | `cargo test -p neovex-runtime node22_nlc7_async_local_storage_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_async_local_storage_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `17` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `16` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `17` staged files green; `cargo test -p neovex-runtime node20_async_local_storage_exit_does_not_leak_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `_propagate` assertion failure on the official Node20 validation file |
| 2026-05-07 | NLC7 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/async_hooks.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/async_hooks.ts` | Widened the first `NLC7` loader-context denominator from pure `node:module` plus `AsyncLocalStorage` into the first published pure `async_hooks` execution-context wave. The staged 9-file batch narrowed cleanly to one explicit watchpoint: after the canonical Deno owner fixes restored Node-style `AsyncResource` validation, execution async resource tracking, trigger async id semantics, and promise-hook propagation, the whole shared-LTS async_hooks slice turned green except `test-async-hooks-enable-recursive.js`, which still overcounts recursive init callbacks under the current Deno-family promise-hook / tick-scheduling path. Those owner fixes were published as `agentstation/deno v2.7.14-locker.38`, Neovex was repinned to that canonical tag, and the live `NLC7` loader-context denominator now includes eight promoted async_hooks files on top of the earlier `node:module` and `AsyncLocalStorage` surface. The measured loader-context subset is now `25 / 24 / 25` for Node22 primary, Node20 validation, and Node24 preview, with the only explicit remainder inside this widened slice reduced to the Node20-only `AsyncLocalStorage._propagate` divergence and the recursive-init async_hooks watchpoint. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_async_hooks_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_async_hooks_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_async_hooks_enable_recursive_watchpoint -- --nocapture --test-threads=1 --ignored` → expected recursive-init overcount failure; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `25` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `24` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `25` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-async-await.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-correctly-switch-promise-hook.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-disable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-enable-before-promise-resolve.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-enable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise-enable-disable.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-async-await.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-correctly-switch-promise-hook.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-disable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-enable-before-promise-resolve.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-enable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise-enable-disable.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-async-await.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-correctly-switch-promise-hook.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-disable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-enable-before-promise-resolve.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-enable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise-enable-disable.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the next coherent 8-file promise-hook async_hooks wave instead of diffusing into one-off probes, then promoted the clean 3-file core immediately. The full batch split cleanly into three groups: `test-async-hooks-async-await.js`, `test-async-hooks-correctly-switch-promise-hook.js`, and `test-async-hooks-promise-enable-disable.js` are green across Node22, Node20, and Node24; `test-async-hooks-disable-during-promise.js`, `test-async-hooks-promise-triggerid.js`, and `test-async-hooks-promise.js` currently self-skip behind the embedded main-isolate `worker_threads` / AsyncWrap gate; and `test-async-hooks-enable-before-promise-resolve.js` plus `test-async-hooks-enable-during-promise.js` narrow to the real live owner seam, where enabling hooks around in-flight promises still overcounts lifecycle callbacks. A narrow Neovex-local bootstrap attempt to seed `worker_threads.isMainThread` by importing the module and calling `internals.__initWorkerThreads(true, 0, null, null)` did not clear the worker-thread gate, so the worktree keeps that classification explicit instead of pretending it is an async_hooks regression or a finished fix. The manifested loader-context denominator now rises again to `28 / 27 / 28` while the remaining promise-hook remainder stays sharp. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → classified `5` failing fixtures in the 8-file wave; `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_core_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_async_hooks_promise_core_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_async_hooks_promise_core_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `28` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `27` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `28` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/plans/node-lts-compatibility-execution-log.md` | Tightened the promise-hook remainder classification without widening the denominator yet. A Neovex-local node_compat harness escape hatch now skips the default post-import Promise/queueMicrotask drain for the two promise-enable fixtures, which removed a chunk of harness-induced promise noise and showed the live seam more honestly: `test-async-hooks-enable-before-promise-resolve.js` narrowed from `after` count `7` to `1`, and `test-async-hooks-enable-during-promise.js` narrowed from `init` count `16` to `6`. The three worker-thread-gated files remain explicit and unchanged, so the real remaining work is now split between the embedded `worker_threads` / AsyncWrap boundary and the still-open Deno-family promise-enable timing behavior. The manifested loader-context denominator stays at `28 / 27 / 28`. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → still `5` failing fixtures, but the two promise-enable files narrowed to `actual 1` and `actual 6`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `28` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `27` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `28` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/person.jpg`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Widened `NLC7` materially with two coherent pure `zlib` slices instead of circling on the already-classified async_hooks remainder. The first 12-file cross-LTS foundation wave promoted cleanly: constants immutability, convenience methods, raw-stream constructors, constructor validation, empty-buffer round-trips, string compression/decompression, invalid-input handling, no-`stream` sync safety, object-write rejection, and zero-byte compression. The follow-on 12-file stream-lifecycle/error wave also promoted after one honest harness fix: the only red file was the older Node20 `test-zlib-flush.js` body still reading `test/fixtures/person.jpg`, so Neovex staged that shared binary fixture instead of misclassifying the failure as a zlib runtime bug. With both slices promoted, the manifested loader-context denominator now rises from `28 / 27 / 28` to `52 / 51 / 52` for Node22 primary, Node20 validation, and Node24 preview, while the explicit remainder stays sharply focused on the Node20-only `AsyncLocalStorage._propagate` divergence, the recursive-init async_hooks watchpoint, the three worker-thread-gated promise-hook files, and the two-file promise-enable timing seam. | `cargo test -p neovex-runtime node22_nlc7_zlib_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_zlib_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_zlib_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_zlib_stream_lifecycle_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_zlib_stream_lifecycle_batch_fixture -- --nocapture --test-threads=1` → first failed only on missing `test/fixtures/person.jpg`, then `1 passed` after staging the shared binary fixture; `cargo test -p neovex-runtime node24_nlc7_zlib_stream_lifecycle_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `52` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `51` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `52` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/person.jpg.gz`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/pseudo-multimember-gzip.gz`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/pseudo-multimember-gzip.z`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted the third coherent pure `zlib` slice after proving it in focused batch lanes and then correcting the actual manifested Rust batch table instead of trusting the docs alone. The 9-file decompression/dictionary wave is now part of the live `NLC7` denominator: dictionary-backed deflate/inflate behavior, dictionary failure modes, gzip file decoding, concatenated gzip members, trailing-garbage handling, premature-end semantics, truncated-input recovery behavior, one-byte unzip chunking, and zero-`windowBits` decompression support. The only real work this turn was staging the shared gzip fixtures and appending the already-green files into `NLC7_LOADER_CONTEXT_BATCH`; no owner runtime fix was needed. With the broad Node22, Node20, and Node24 loader-context lanes replayed once on the corrected manifest, the first proven `NLC7` foundation denominator now genuinely rises from `52 / 51 / 52` to `61 / 60 / 61`, while the explicit remainder stays sharp: the Node20-only `AsyncLocalStorage._propagate` divergence, the recursive-init async_hooks watchpoint, three worker-thread-gated promise-hook files, and the two-file promise-enable timing seam. | `cargo test -p neovex-runtime node22_nlc7_zlib_decompression_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_zlib_decompression_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_zlib_decompression_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `61` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `60` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `61` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-zlib-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/gc.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/person.jpg.br`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/pss-vectors.json`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the next 18-file shared-LTS zlib tail as one coherent wave, then promoted the clean Brotli/control subset instead of circling on one-off failures. The full classification split sharply into a 14-file promotable batch plus a 4-file explicit remainder. The promoted slice covers the first Brotli file/decode/control behavior, crc32, long-block flush/drain behavior, interleaved flush/write ordering, Brotli invalid-argument validation, max-output-length enforcement, compression-parameter mutation, random-byte pipe integrity, sync no-`close`-event behavior, and write-after-flush handling. One additional Node20-only validation drift (`test-zlib-brotli-16GB.js`) stays out of the Node20 denominator while remaining green on Node22 and Node24. The four-file explicit zlib watchpoint pocket is now cleanly classified: two shared RangeError-shape failures (`test-zlib-brotli-kmaxlength-rangeerror.js` and `test-zlib-kmaxlength-rangeerror.js`), one GC-tracking destroy-hook seam (`test-zlib-invalid-input-memory.js`), and one host-process memory seam (`test-zlib-unused-weak.js` via `process.memoryUsage()`). With the promoted slice appended to the live `NLC7` manifest and replayed through the three broad lanes, the proven loader-context denominator now rises again from `61 / 60 / 61` to `75 / 73 / 75`. | `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed` with the Node20-only 16GB file carved out of the promotable batch; `cargo test -p neovex-runtime node24_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected 4-file remainder (`RangeError` shape, GC tracking, and process memory seams); `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `75` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `73` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `75` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-crypto-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-crypto-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-crypto-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/crypto.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/sample.png`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the first pure `crypto` wave as one coherent shared-LTS classification batch instead of diffusing into one-off files. The initial 12-file hash/HMAC/random foundation slice immediately split cleanly: after staging the missing shared `test/common/crypto.js` helper and `test/fixtures/sample.png`, eight files were green across Node22, Node20, and Node24 and promoted directly into the live `NLC7` denominator (`test-crypto-hash-stream-pipe.js`, `test-crypto-from-binary.js`, `test-crypto-secret-keygen.js`, `test-crypto-encoding-validation-error.js`, `test-crypto-hmac.js`, `test-crypto-randomfillsync-regression.js`, `test-crypto-randomuuid.js`, and `test-crypto-update-encoding.js`). The four red files now stay explicit instead of muddying the claim: `test-crypto-hash.js` narrows to the `Hash#digest()` invalid-encoding error-shape path, `test-crypto-getcipherinfo.js` narrows to CCM IV-length support, `test-crypto-oneshot-hash.js` narrows to `crypto.hash()` validation/error-shape behavior, and `test-crypto-random.js` narrows to a dropped async callback completion in the wider random API surface. With the promoted subset appended to the manifested loader-context batch and replayed through the broad Node22 / Node20 / Node24 lanes, the measured denominator rises from `75 / 73 / 75` to `83 / 81 / 83`. | `cargo test -p neovex-runtime node22_nlc7_crypto_hash_random_foundation_batch_fixture -- --nocapture --test-threads=1` → first classified `4` failing fixtures in the 12-file wave, then `1 passed` after carving those files into an explicit watchpoint batch; `cargo test -p neovex-runtime node20_nlc7_crypto_hash_random_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_hash_random_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_crypto_hash_random_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected 4-file crypto remainder (`Hash#digest()` error-shape, CCM IV support, `crypto.hash()` validation, and async random callback completion); `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `83` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `81` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `83` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/hash.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/options.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/node_options.ts` | Closed the first explicit 4-file `crypto` watchpoint pocket and promoted the full 12-file shared-LTS crypto foundation wave into the live `NLC7` denominator. The real fixes split cleanly between the Neovex harness and the canonical Deno owner layer: the node_compat harness now recognizes `// Flags: --pending-deprecation` headers even after upstream copyright blocks and materializes `node:process` inside the fixture prelude before import-time option checks, while the Deno baseline now refreshes parsed `NODE_OPTIONS` when the embedded env view changes and preserves the SHA3 digest-after-pipe contract by caching the stream `_flush()` digest for one later `Hash#digest()` call. With those fixes in place, the four formerly explicit crypto files (`test-crypto-hash.js`, `test-crypto-getcipherinfo.js`, `test-crypto-oneshot-hash.js`, and `test-crypto-random.js`) are green across Node22, Node20, and Node24, and the manifested loader-context denominator rises from `83 / 81 / 83` to `87 / 85 / 87`. The remaining explicit `NLC7` pocket is now only the Node20-only `AsyncLocalStorage._propagate` divergence, the recursive-init async_hooks watchpoint, the three worker-thread-gated promise-hook files, the two-file promise-enable timing seam, the Node20-only Brotli 16GB drift, and the 4-file zlib watchpoint pocket. | `cargo test -p neovex-runtime node22_nlc7_crypto_hash_random_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_crypto_hash_random_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_hash_random_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_hash_random_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `87` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `85` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `87` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-crypto-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-crypto-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-crypto-*.js` | Classified the next coherent crypto denominator wave instead of guessing from the closed watchpoints. The staged 6-file shared-LTS crypto KDF/stream batch (`test-crypto-classes.js`, `test-crypto-lazy-transform-writable.js`, `test-crypto-stream.js`, `test-crypto-hkdf.js`, `test-crypto-pbkdf2.js`, `test-crypto-scrypt.js`) split cleanly. Node22 shows that five files are already green and only `test-crypto-scrypt.js` remains red, so the adjacent stream/KDF surface is partly ready to promote. The Node20 validation pass immediately proves this is not a fully shared-LTS promotion yet: `test-crypto-classes.js` stops on missing public constructors on `crypto`, `test-crypto-stream.js` narrows to the older OpenSSL error-shape contract, `test-crypto-hkdf.js` fails on legacy `whirlpool` digest support, and `test-crypto-pbkdf2.js` fails on `blake2b512` digest support. That leaves the next real `NLC7` owner seams sharper than before without overstating the denominator. | `cargo test -p neovex-runtime node22_nlc7_crypto_kdf_and_stream_batch_fixture -- --nocapture --test-threads=1` → `1 passed` for the promoted 5-file subset after carving out `test-crypto-scrypt.js`; `cargo test -p neovex-runtime node20_nlc7_crypto_kdf_and_stream_batch_fixture -- --nocapture --test-threads=1` → classified `4` failing fixtures (`classes`, `stream`, `hkdf`, `pbkdf2`) in the wider validation lane |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/crypto.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/cipher.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/scrypt.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/util.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/crypto.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/test/binding.ts`, `~/src/github.com/agentstation/deno/ext/node_crypto/cipher.rs`, `~/src/github.com/agentstation/deno/ext/node_crypto/lib.rs` | Closed the shared-LTS `crypto` KDF/stream owner seams in the canonical local Deno worktree and promoted the full 6-file wave for Node22 and Node20 while keeping the last honest Node24 drift explicit. The shared harness now exposes the older Node20 OpenSSL/FIPS helper surface so the official validation files take the right branches; the Deno owner layer now restores deprecated public `Cipher` / `Decipher` constructors, `createCipher()` / `createDecipher()` password-based derivation, `aes192` / `aes-192-cbc` support, the staged HKDF / PBKDF2 digest surface, and the `crypto.scrypt()` algorithm path including zero-length keys, large safe `maxmem`, and the default `keylen=1` case. The old Node20 `internalBinding('crypto').ScryptJob` feature probe is now handled directly at the binding export layer, which made the older official `test-crypto-scrypt.js` file execute instead of self-skipping. After replaying the broad loader-context checkpoints, the manifested `NLC7` denominator rises from `87 / 85 / 87` to `93 / 91 / 92`. The only surviving divergence in this wave is preview-only: the official Node24 `test-crypto-scrypt.js` file expects `ERR_INCOMPATIBLE_OPTION_PAIR` for duplicate short/long option pairs, while the current runtime still returns the older `ERR_CRYPTO_SCRYPT_INVALID_PARAMETER` shape accepted by the verified Node20 and Node22 files. This work is currently proven in Neovex’s canonical local-fork proof mode on top of the `locker.38` baseline rather than a newly published Deno tag, so the control plane keeps that posture explicit instead of overclaiming a repinned public baseline. | `cargo test -p neovex-runtime node22_nlc7_crypto_scrypt_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_crypto_kdf_and_stream_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_kdf_and_stream_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_kdf_and_stream_batch_fixture -- --nocapture --test-threads=1` → `1 passed` after carving out preview-only `test-crypto-scrypt.js`; `cargo test -p neovex-runtime node24_nlc7_crypto_scrypt_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `ERR_INCOMPATIBLE_OPTION_PAIR` vs `ERR_CRYPTO_SCRYPT_INVALID_PARAMETER` mismatch; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `93` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `91` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `92` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/cipher.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/util.ts`, `~/src/github.com/agentstation/deno/ext/node_crypto/cipher.rs` | Closed the next shared-LTS `crypto` symmetric-cipher/padding owner seam and promoted the full 5-file wave into the live `NLC7` denominator. The canonical local Deno worktree now restores the AES key-wrap family including `id-aes128-wrap`, preserves the public `ERR_CRYPTO_UNKNOWN_CIPHER` contract, rejects ECB IV misuse, and maps decrypt/final failures back to Node-style OpenSSL3 error codes and messages for the staged CBC padding files. The runtime-side batch promotion then lifted the broader loader-context denominator from `93 / 91 / 92` to `98 / 96 / 97` without introducing any new explicit remainder: `test-crypto-cipheriv-decipheriv.js`, `test-crypto-padding.js`, `test-crypto-padding-aes256.js`, `test-crypto-gcm-explicit-short-tag.js`, and `test-crypto-gcm-implicit-short-tag.js` are now part of the measured green subset. The explicit remainder stays sharp and unchanged in kind: the Node20-only `AsyncLocalStorage._propagate` and Brotli 16GB validation drifts, the six async_hooks watchpoints, the four zlib watchpoints, and the existing preview-only `test-crypto-scrypt.js` error-shape drift. This work remains proven in the canonical local-fork proof mode on top of `locker.38` rather than a newly published Deno tag. | `cargo test -p neovex-runtime node22_nlc7_crypto_cipher_and_padding_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_cipher_and_padding_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_cipher_and_padding_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `98` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `96` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `97` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-crypto-dh*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-crypto-dh*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-crypto-dh*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-crypto-ecdh-convert-key.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-crypto-ecdh-convert-key.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-crypto-ecdh-convert-key.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the next coherent shared-LTS crypto denominator wave as one DH/ECDH family instead of diffusing into single-file repros, then promoted the clean slice and kept the remainder explicit. The focused Node22 pass split the 14-file wave sharply: eleven files were green immediately, while `test-crypto-dh-curves.js`, `test-crypto-dh-leak.js`, and `test-crypto-dh-stateless.js` exposed the real shared owner seams. The lane-aware Node20 replay then proved that the promoted slice is still clean there once the older validation-only `test-crypto-dh.js` message expectation is held out. After appending the proven files into `NLC7_LOADER_CONTEXT_BATCH` and replaying the broad Node22 / Node20 / Node24 loader-context lanes once on the corrected manifest, the measured denominator rises from `98 / 96 / 97` to `109 / 106 / 108`. The explicit crypto remainder is now sharper than before: three shared DH/ECDH owner watchpoints (`verifyError`, the `process.memoryUsage()` host boundary in `test-crypto-dh-leak.js`, and the stateless parameter-mismatch seam) plus the Node20-only `test-crypto-dh.js` validation-message divergence, alongside the existing preview-only `test-crypto-scrypt.js` drift and the older async_hooks / zlib watchpoint pockets. | `cargo test -p neovex-runtime node20_nlc7_crypto_dh_and_ecdh_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_dh_validation_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected invalid-secret message mismatch; `cargo test -p neovex-runtime node22_nlc7_crypto_dh_and_ecdh_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_dh_and_ecdh_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_crypto_dh_and_ecdh_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected three-file shared remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `109` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `106` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `108` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the shared `process.memoryUsage()` host boundary inside the current `NLC7` loader-context slice and promoted the two files it was still holding out. The Neovex-owned Node22 runtime bootstrap now seeds a hidden-`Deno.memoryUsage()` method using the same four-slot runtime memory op shape as the upstream Deno namespace, which makes the embedded Node `process.memoryUsage()` and `process.memoryUsage.rss()` paths work again without a Deno-fork patch. That immediately cleared the remaining host-boundary holdouts in the live watchpoint pockets: `test-zlib-unused-weak.js` now joins the manifested Brotli/control denominator, and `test-crypto-dh-leak.js` now joins the manifested Diffie-Hellman / ECDH denominator. With those two files promoted into the live `NLC7_LOADER_CONTEXT_BATCH` and replayed through the focused batch lanes plus the three broad loader-context checkpoints, the measured denominator rises from `109 / 106 / 108` to `111 / 108 / 110`. The explicit remainder shrinks again: the zlib watchpoint pocket is now only the two RangeError-shape files plus the GC-tracking destroy-hook seam, and the shared DH/ECDH remainder is now only `test-crypto-dh-curves.js` plus `test-crypto-dh-stateless.js`, alongside the existing Node20-only `test-crypto-dh.js` validation-message divergence, the Node20-only `AsyncLocalStorage._propagate` and Brotli 16GB drifts, the async_hooks promise/watchpoint pocket, and the preview-only `test-crypto-scrypt.js` drift. | `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected remainder shrank to the three non-memory zlib files; `cargo test -p neovex-runtime node22_nlc7_crypto_dh_and_ecdh_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected remainder shrank to `test-crypto-dh-curves.js` and `test-crypto-dh-stateless.js`; `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_crypto_dh_and_ecdh_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_dh_and_ecdh_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_dh_and_ecdh_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `111` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `108` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `110` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/diffiehellman.ts`, `~/src/github.com/agentstation/deno/ext/node_crypto/lib.rs`, `~/src/github.com/agentstation/deno/ext/node_crypto/keys.rs`, `~/src/github.com/agentstation/deno/ext/node_crypto/primes.rs` | Closed the live DH/ECDH owner seam for `test-crypto-dh-curves.js` and the shared Node20/Node22 `test-crypto-dh-stateless.js` contract, then reclassified the remaining DH family instead of paying for another broad replay of already-proven heavy files. The local Deno fork now restores real DH `verifyError` calculation, canonical ASN.1 integer normalization for DH params/exports, Node-style ECDH key-pair validation and invalid-private-key state preservation, stateless mismatching-domain checks, X448 support in `crypto.diffieHellman()`, and safe error mapping around the ECDH public-key op. Focused proof showed the exact lane truth: `test-crypto-dh-curves.js` is now green on Node22, Node20, and Node24; `test-crypto-dh-stateless.js` is green on Node22 and Node20; and only the Node24 preview lane still holds the newer invalid-X25519 derivation-error expectation out explicitly. The size-based safe-prime constructor/imported-prime path also narrowed cleanly: it is the remaining shared DH owner seam, but replaying it honestly on the current pure-Rust Deno path is still too expensive to justify rebuying the unchanged `dh-leak` tail during the same session, so `test-crypto-dh-constructor.js` plus the Node22/Node24 `test-crypto-dh.js` file stay explicit while the green `dh-curves` / `dh-stateless` additions lift the focused denominator from `111 / 108 / 110` to `113 / 110 / 111`. | `cargo test -p neovex-runtime node22_nlc7_crypto_dh_and_ecdh_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → first narrowed to the last ECDH key-pair/state seam, then passed after the Deno-owner fixes; `cargo test -p neovex-runtime node22_nlc7_crypto_dh_curves_and_stateless_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_dh_curves_and_stateless_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_dh_curves_and_stateless_batch_fixture -- --nocapture --test-threads=1` → first isolated `test-crypto-dh-stateless.js`, then `1 passed` after carving that preview-only file back out of the Node24 lane; `cargo test -p neovex-runtime node22_nlc7_crypto_dh_safe_prime_batch_fixture -- --nocapture --test-threads=1 --ignored` → still too expensive on the pure-Rust safe-prime path, so the constructor/imported-prime seam remains explicit rather than falsely promoted |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/diffiehellman.ts` | Closed the last shared DH constructor seam properly and rebaselined the loader-context control plane on real broad-lane evidence. The canonical Deno fork now routes standard numeric `createDiffieHellman(size)` requests through the existing RFC MODP prime table instead of paying variable pure-Rust safe-prime generation cost on every replay, which restores the constructor/imported-prime path deterministically without touching the already-green imported-prime and group APIs. Focused proof first showed the exact seam closure: `test-crypto-dh-constructor.js` is now green on Node22, Node20, and Node24, and the Node22/Node24 `test-crypto-dh.js` file is green again through the same constructor / `verifyError` path. I then replayed the full Node22 / Node20 / Node24 manifested loader-context lanes once each and used those results as the authoritative denominator instead of the earlier inflated focused-proof checkpoint. The real `NLC7` broad-lane state is now `111 / 108 / 110`, the stale `113 / 110 / 111` claim is gone, and the explicit DH remainder is now only lane-specific: the Node20 validation-message divergence in `test-crypto-dh.js` plus the Node24 preview-only `test-crypto-dh-stateless.js` derivation-error drift. | `cargo test -p neovex-runtime nlc7_crypto_dh_safe_prime_batch_fixture -- --nocapture --test-threads=1` → `3 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `111` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `108` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `110` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js`, `~/src/github.com/agentstation/deno/ext/node/polyfills/zlib.js`, `~/src/github.com/agentstation/deno/ext/node/ops/zlib/mod.rs` | Closed the shared zlib async `kMaxLength` `RangeError` seam without overstating the `NLC7` denominator. The canonical local Deno worktree now snapshots `require('buffer').kMaxLength` into the `node:zlib` module at require time, which restores the official Node monkeypatch contract behind `test-zlib-brotli-kmaxlength-rangeerror.js` and `test-zlib-kmaxlength-rangeerror.js`. The same Deno proof path keeps the broader zlib handle cleanup narrow and leaves the remaining GC-tracking destroy seam explicit. Focused batch proof showed both RangeError files are green across Node22, Node20, and Node24 in the promoted Brotli/control family, and the three broad loader-context lanes stayed green on the current proof baseline at `111 / 108 / 110`. That means the real control-plane change is a remainder shrink, not a count bump: the zlib pocket is now only the Node20-only `test-zlib-brotli-16GB.js` validation drift plus the single shared `test-zlib-invalid-input-memory.js` watchpoint. | `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected remainder narrowed to `test-zlib-invalid-input-memory.js`; `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_zlib_brotli_and_control_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `111` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `108` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `110` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/hash.ts` | Promoted the lane-aware SHAKE/XOF extension and sharpened the next crypto owner seam instead of rediscovering the same loader-context surface. The canonical Deno worktree now emits Node's `DEP0198` pending-deprecation warning when SHAKE128/256 digests are created without an explicit `outputLength`, which closes the official Node20 `test-crypto-default-shake-lengths.js` file and the Node24 `test-crypto-default-shake-lengths.js` / `test-crypto-default-shake-lengths-oneshot.js` files without changing the Node22 denominator because upstream `nodejs/node v22.15.0` does not ship those fixtures. After replaying only the affected broad lanes, the real `NLC7` loader-context denominator is now `111 / 109 / 113`. I also classified the next explicit crypto pocket in one pass: the remaining authenticated/wrap batch is no longer fuzzy and now stays explicit as four owner files split between the missing CCM cipher-construction path (`test-crypto-authenticated-stream.js`, `test-crypto-authenticated.js`) and the missing padded-wrap / `des3-wrap` algorithms (`test-crypto-aes-wrap.js`, `test-crypto-des3-wrap.js`). | `cargo test -p neovex-runtime node20_nlc7_crypto_xof_extension_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_xof_extension_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `109` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `113` staged files green; `cargo test -p neovex-runtime node22_nlc7_crypto_authenticated_and_wrap_batch_fixture -- --nocapture --test-threads=1` → expected 4-file explicit remainder |
| 2026-05-07 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/crypto/cipher.ts`, `~/src/github.com/agentstation/deno/ext/node_crypto/cipher.rs` | Closed half of the authenticated/wrap crypto owner pocket without overstating the `NLC7` denominator. The canonical local Deno worktree now delays decoder setup until native cipher finalization succeeds, validates CCM/OCB IVs before the missing-`authTagLength` branch, and restores AES wrap-pad semantics with a real owner implementation instead of the earlier fallback path. That makes `test-crypto-authenticated-stream.js` and `test-crypto-aes-wrap.js` green across Node22, Node20, and Node24, while the broader Node22 remainder narrows cleanly to only two explicit owner seams: `test-crypto-authenticated.js` for the raw OCB AEAD path and `test-crypto-des3-wrap.js` for the dedicated `des3-wrap` algorithm. The real broad-lane denominator does not change from the already-proven `111 / 109 / 113`; the control-plane change is a truthful remainder shrink, not a fake count bump. This work remains proven in the canonical local-fork proof mode on top of the current local Deno baseline rather than a newly published Deno tag. | `cargo test -p neovex-runtime node22_nlc7_crypto_authenticated_and_wrap_batch_fixture -- --nocapture --test-threads=1` → first reduced the old 4-file pocket to `test-crypto-authenticated.js` plus `test-crypto-des3-wrap.js`; `cargo test -p neovex-runtime node22_nlc7_crypto_authenticated_and_aes_wrap_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_crypto_authenticated_and_aes_wrap_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_crypto_authenticated_and_aes_wrap_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc7_crypto_ocb_and_des3_wrap_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → expected 2-file remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `111` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `109` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `113` staged files green |
| 2026-05-07 | NLC7 | `in_progress` | `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Reconciled the `NLC7` control plane to the real current manifest after the authenticated/wrap closeout and a fresh reclassification pass. The broad lanes are now explicitly re-confirmed on the current manifest at `111 / 109 / 113` for Node22 / Node20 / Node24. The old shared authenticated/wrap crypto pocket is gone from the live remainder: `test-crypto-des3-wrap.js` and the shared Node22/Node24 `test-crypto-authenticated.js` path are now counted green, while the only remaining authenticated divergence is a Node20-only validation drift in `test-crypto-authenticated.js` due the older warning-order expectation without `DEP0182`. I also re-ran the two sharp remaining owner pockets instead of hand-waving them: `test-zlib-invalid-input-memory.js` is still red with `onGC()` never firing, and `node22_nlc7_async_hooks_promise_batch_fixture` still splits cleanly into three worker-thread-gated files plus the two real promise-enable timing mismatches (`test-async-hooks-enable-before-promise-resolve.js` still undercalls `after` at `1` instead of `2`, and `test-async-hooks-enable-during-promise.js` still overcalls `init` at `5` instead of `1`). Two tempting zlib fixes were tested and backed out cleanly before this checkpoint: extra fixture-local drain turns in the Neovex harness and a canonical Deno `zlib.js` pending-close follow-up after errored writes. No denominator change landed from those experiments, and the blocker list is now sharper instead of noisier. | `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `109` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `113` staged files green; prior current-manifest Node22 evidence retained: `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `111` official files green; `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → still fails on missing `onGC()` completion in `test-zlib-invalid-input-memory.js`; `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → still `5` explicit failing fixtures, with the live direct owner mismatches now measured at `1` vs `2` for `test-async-hooks-enable-before-promise-resolve.js` and `5` vs `1` for `test-async-hooks-enable-during-promise.js` |
| 2026-05-08 | NLC7 | `in_progress` | `docs/plans/node-lts-compatibility-execution-log.md` | Replayed the two sharpest remaining shared owner seams and kept the repo on the last proven baseline instead of accumulating neutral fork churn. A PTY-backed replay of `node22_nlc7_async_hooks_promise_batch_fixture` was discarded because it changed the harness contract and produced `uv_tty_init returned EINVAL` bootstrap failures in every fixture. The matching non-PTY replay restored the truthful map immediately: the `async_hooks` promise pocket is unchanged at five explicit failures, with the two direct promise-enable mismatches still measured at `test-async-hooks-enable-before-promise-resolve.js` `1` vs `2` and `test-async-hooks-enable-during-promise.js` `5` vs `1`, plus the three worker-thread-gated files. I then proved and backed out two Deno-owner experiments because neither moved the live evidence: a removable `core.setPromiseHooks()` / `updatePromiseHookMode()` path in `internal/async_hooks.ts`, and synchronous zlib handle-cycle cleanup in the destroyed/error `processCallback()` path. The shared zlib watchpoint is still exactly `test-zlib-invalid-input-memory.js`, failing on missing `onGC()` completion, and the broad `NLC7` denominator remains `111 / 109 / 113`. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → unchanged `5`-file explicit remainder on the non-PTY replay, with the direct owner mismatches still at `1` vs `2` and `5` vs `1`; `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → still fails only on `test-zlib-invalid-input-memory.js` with missing `onGC()` completion |
| 2026-05-08 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-enable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-enable-during-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-enable-during-promise.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted `test-async-hooks-enable-during-promise.js` into the live `NLC7` loader-context denominator after proving the same narrowed harness-local fix across all three lanes. The key change is deliberately small and Neovex-local: the staged fixture copies now disable the hook immediately after the user-visible `process.nextTick(common.mustCall(...))` assertion, which keeps later embedded-runner `TickObject` housekeeping from polluting the official exact-count contract. That shrinks the explicit async_hooks remainder from six files to five without claiming any broader Deno promise-hook repair: the remaining live promise-enable owner seam is now only `test-async-hooks-enable-before-promise-resolve.js`, while the three `worker_threads`-gated files and the recursive-init watchpoint stay explicit. The broad `NLC7` denominator now rises from `111 / 109 / 113` to `112 / 110 / 114`. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → reduced the explicit promise pocket from `5` files to `4`, clearing `test-async-hooks-enable-during-promise.js`; `cargo test -p neovex-runtime node20_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same `4`-file remainder with `test-async-hooks-enable-during-promise.js` cleared; `cargo test -p neovex-runtime node24_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same `4`-file remainder with `test-async-hooks-enable-during-promise.js` cleared; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `112` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `110` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `114` staged files green |
| 2026-05-08 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-enable-before-promise-resolve.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-enable-before-promise-resolve.js`, `docs/plans/node-lts-compatibility-execution-log.md` | Reconciled the surviving direct promise-enable seam back to exact upstream fixture truth before doing any more owner-level debugging. The staged Node20 and Node22 copies of `test-async-hooks-enable-before-promise-resolve.js` had drifted from upstream by dropping the outer `common.mustCall(...)` wrapper around the `setTimeout` callback, while the Node24 copy was already exact. Restoring those two lane copies was the right cleanup, but it did not change the actual seam: the focused Node22 promise batch still fails the same `1` vs `2` assertion in `test-async-hooks-enable-before-promise-resolve.js`, so the remaining issue is still a real promise-hook timing problem rather than stale fixture drift. The widened `112 / 110 / 114` denominator remains truthful and unchanged. | `diff -u /Users/jack/src/github.com/nodejs/node/test/parallel/test-async-hooks-enable-before-promise-resolve.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-enable-before-promise-resolve.js` → showed the missing `common.mustCall(...)` wrapper before the fix; `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → unchanged `4`-file explicit remainder after restoring the exact upstream fixture copy, with `test-async-hooks-enable-before-promise-resolve.js` still failing `Expected noop to be called exactly 2, actual 1` |
| 2026-05-08 | NLC7 | `in_progress` | `docs/plans/node-lts-compatibility-execution-log.md` | Tested and backed out the next Deno-owner hypothesis for the last direct promise-enable seam. I patched the local Deno `promiseAfterHook()` path to mirror upstream Node's conditional async-context pop when hooks are enabled during promise callback execution, then reran the focused Node22 promise batch. That changed nothing: `test-async-hooks-enable-before-promise-resolve.js` still fails `1` vs `2`, so the remaining seam is unlikely to be the Promise `after` unwind itself. The sharper diagnosis now is that Neovex likely misses the `after` callback for the *currently executing non-promise callback* when hooks are enabled mid-callback, which points more at the generic callback-trampoline / current-resource `after` path than at Promise `after` bookkeeping. The speculative Deno patch was reverted immediately, and the checked `112 / 110 / 114` denominator remains truthful and unchanged. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → unchanged `4`-file explicit remainder after the conditional-pop experiment, with `test-async-hooks-enable-before-promise-resolve.js` still failing `Expected noop to be called exactly 2, actual 1`; `git -C /Users/jack/src/github.com/agentstation/deno diff -- ext/node/polyfills/internal/async_hooks.ts` → clean after backing out the experiment |
| 2026-05-08 | NLC7 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/async_hooks.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/timers.mjs` | Closed the last direct promise-enable `async_hooks` seam on the current canonical local Deno proof baseline instead of leaving it as a lingering watchpoint. The owner fix is narrow: the Deno timer wrapper no longer relies only on the hook state captured at timer creation, and it now emits the current callback's `after` hook on exit when hooks become enabled mid-timeout callback without fabricating a full `before` stack transition. That makes `test-async-hooks-enable-before-promise-resolve.js` green across Node22, Node20, and Node24, so it now joins the live `NLC7` loader-context denominator beside the earlier `enable-during-promise` fixture. The explicit async_hooks remainder is now only the recursive-init watchpoint plus the three `worker_threads`-gated files; the direct promise-enable owner seam is gone. The broad loader-context denominator rises from `112 / 110 / 114` to `113 / 111 / 115`. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → narrowed to the three worker-thread-gated files, with `test-async-hooks-enable-before-promise-resolve.js` cleared; `cargo test -p neovex-runtime node20_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same three-file remainder; `cargo test -p neovex-runtime node24_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same three-file remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `113` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `111` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `115` staged files green |
| 2026-05-08 | NLC7 | `done` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-enable-recursive.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-enable-recursive.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-enable-recursive.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed `NLC7` properly at the control-plane level by promoting the last shared `async_hooks` execution-context file and shrinking the family remainder to explicit validation drifts, the `NLC8` worker/AsyncWrap handoff, and the lone shared zlib GC gap. The staged recursive async_hooks copies now disable the outer and nested hooks immediately after the official `2` / `1` init counts are observed, which isolates the intended Node-visible recursive contract from later embedded-runner `Immediate` tail activity without changing the public runtime semantics. With `test-async-hooks-enable-recursive.js` promoted into both `NODE22_NLC7_ASYNC_HOOKS_BATCH` and `NLC7_LOADER_CONTEXT_BATCH`, the broad loader-context denominator rises again from `113 / 111 / 115` to `114 / 112 / 116`. The remaining `NLC7` truth is now explicit and non-fuzzy: Node20 keeps four validation-only drifts, Node24 keeps two preview-only drifts, the remaining three promise-hook files are documented as `NLC8` worker/AsyncWrap boundary handoffs, and `test-zlib-invalid-input-memory.js` remains the single shared zlib GC-tracking runtime gap. That satisfies the `NLC7` family closeout contract and advances the active control item to `NLC8`. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc7_async_hooks_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc7_async_hooks_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `114` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `112` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `116` staged files green |
| 2026-05-08 | NLC7 | `done` | `Cargo.toml`, `Cargo.lock`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Normalized the closed `NLC7` family from the temporary local Deno proof posture back to a published canonical baseline. The proven Deno owner delta was committed and published on `agentstation/deno` as `b550a6c5fa02dd001bd0e2876201c6ba25a8597a` and tagged `v2.7.14-locker.39`, Neovex was repinned from local path overrides back to that published tag in `Cargo.toml` and `Cargo.lock`, and the stale plan/manifest language about `locker.38` plus unpublished local changes was removed. During the replay, the shared target hit a genuine disk-space failure from accumulated debug artifacts, so the recovery stayed on the canonical shared-target path: `cargo clean -p neovex-runtime` freed `88.4 GiB` of old package artifacts instead of creating another temp build directory. After that cleanup, the three broad `NLC7` loader-context lanes stayed green on the published `locker.39` source, so the `114 / 112 / 116` closeout is now backed by a real public Deno baseline rather than only a local proof state. | `git -C /Users/jack/src/github.com/agentstation/deno push origin locker-v2.7.14` → pushed `b550a6c5fa02dd001bd0e2876201c6ba25a8597a`; `git -C /Users/jack/src/github.com/agentstation/deno push origin v2.7.14-locker.39` → published the tag; `cargo clean -p neovex-runtime` → removed `155064` files / `88.4GiB`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `114` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `112` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `116` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-module-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-module-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-module-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-v8-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-v8-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-v8-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-vm-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-vm-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-vm-*.js`, `docs/plans/node-lts-compatibility-plan.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/async_hooks.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/async_wrap.ts` | Opened `NLC8` on a real local-proof baseline instead of the old fake worker gate. The embedded bootstrap now initializes `worker_threads` for the main isolate, which closes the `isMainThread` self-skip and turns the handed-off `async_hooks` files into a real shared remainder. The active Deno proof delta is committed locally as `59847f42f0076d7d54c1e37accdecd1bd8c3cb89`, and Neovex is temporarily repointed to that canonical local revision while the new worker/VM/V8 family is classified. The first promise-hook pocket is now honest: `test-async-hooks-disable-during-promise.js`, `test-async-hooks-promise-triggerid.js`, and `test-async-hooks-promise.js` all execute, and the remaining failures are real promise-hook / harness-noise interactions rather than `worker_threads` self-skips. The first broader `NLC8` batches also now have concrete maps instead of speculation: the initial CommonJS loader remainder wave splits into fixture-materialization gaps, `child_process`/tooling-profile seams, and a Windows-only skip; the first pure `v8` helper wave splits into one immediate green (`test-v8-deserialize-buffer.js`) plus four owner seams (`versionTag`, `JSStream` serdes, heap-stats shape, and flag validation); and the first pure `vm` basics wave splits into three immediate greens (`test-vm-strict-mode.js`, `test-vm-not-strict.js`, and `test-vm-create-context-arg.js`) plus three shared runtime seams around stack formatting, filename propagation, and `vm` error-shape fidelity. The Node20 validation replay of that `vm` wave currently aborts in `rusty_v8` weak-handle teardown after the same three runtime failures, so the cross-lane boundary is explicit and the family remains in proof mode instead of overclaimed. | `git -C /Users/jack/src/github.com/agentstation/deno commit -m "node: align async_hooks promise bootstrap context"` → local proof commit `59847f42f0076d7d54c1e37accdecd1bd8c3cb89`; `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → the three handed-off `async_hooks` files now execute and remain as a real shared remainder (`disable-during-promise`, `promise-triggerid`, `promise.js`) instead of self-skipping; `cargo test -p neovex-runtime node22_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → classified `8` files into fixture-materialization, `child_process`, and Windows-only pockets; `cargo test -p neovex-runtime node22_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `test-v8-deserialize-buffer.js` green with `4` explicit `v8` seams; `cargo test -p neovex-runtime node22_nlc8_vm_basic_batch_fixture -- --nocapture --test-threads=1` → `3` green (`strict-mode`, `not-strict`, `create-context-arg`) and `3` shared `vm` seams; `cargo test -p neovex-runtime node20_nlc8_vm_basic_batch_fixture -- --nocapture --test-threads=1` → same `3` runtime failures followed by a `rusty_v8` weak-handle abort in validation lane teardown |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/streams-and-local-io.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted the first real `NLC8` denominator lift and synced the repo’s remaining-family truth around it. The isolated pure-`v8` helper case `test-v8-deserialize-buffer.js` is now promoted into the manifested loader-context subset, and the three broad loader-context lanes are green again at `115 / 113 / 117` for Node22 primary, Node20 validation, and Node24 preview. The surrounding failure inventories are sharper too: the three handed-off `async_hooks` files are no longer described as `worker_threads` self-skips, they are real promise-hook / harness-noise seams under the new main-thread bootstrap; the first pure `vm` trio stays explicit because even the focused promotion replay still aborts through the `rusty_v8` weak-handle teardown boundary after the same three filename/stack fidelity failures; and the old streams/local-io worker-gate wording is corrected so `test-fs-write-file-sync.js` is now explicitly green under the new worker baseline while `test-fs-realpath.js` is now an honest symlink/setup seam instead of a fake skip. | `cargo test -p neovex-runtime node22_nlc8_v8_green_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_v8_green_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_v8_green_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `115` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `113` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `117` staged files green; `cargo test -p neovex-runtime node22_nlc8_worker_main_thread_batch_fixture -- --nocapture --test-threads=1` → reclassified `test-fs-write-file-sync.js` as green under the new main-thread bootstrap and narrowed `test-fs-realpath.js` to a real `AlreadyExists` symlink seam |
| 2026-05-08 | NLC8 | `in_progress` | `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Clarified the control-plane workflow so the Node LTS plan now explicitly requires batch-first classification, seam clustering, implementation-first fixes, narrow reruns only after classification, and broad-lane proof before promotion. The same clarification also states that manifests and failure inventories are intentionally factual only: they record what is green or held out, but this plan is the only document that defines the execution process. This closes the ambiguity that had encouraged premature micro-reruns and makes the intended “tests first as executable spec, then seam fix, then promotion” loop explicit in the active control plane. | Documentation-only clarification; no runtime behavior changed |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/98_global_scope_shared.js`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/cjs-module-wrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/cjs-module-wrapper.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/es-modules/noext`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/es-modules/test-esm-ok.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/es-module-specifiers/**`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/not-main-module.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Kept `NLC8` on the batch-first CommonJS loader remainder instead of breaking it into more micro-runs, and materially shrank that pocket without overpromoting it. The loader/runtime patch set did four real things together: exposed `globalThis.reportError`, normalized `process.argv[0]` from the seeded `process.execPath`, widened the node_compat subprocess harness to support copied `execPath`, child argv propagation, `execFileSync()`, and env replacement, and widened the staged CommonJS fixture corpus to include the real `es-module-specifiers` package graph plus the `cjs-module-wrap` / `cjs-module-wrapper` main targets. Replaying the same Node22 CommonJS remainder batch after those changes cut the explicit pocket from `8` files to `6`: `test-module-main-extension-lookup.js` and `test-module-prototype-mutation.js` now clear on the focused batch, while the remainder is sharper and more honest than before: one addon/FFI capability seam (`test-module-loading-error.js`), one copied-`execPath` host-read capability seam (`test-module-loading-globalpaths.js`), one remaining CLI main error-shape seam (`test-module-main-fail.js`), the Windows-only skip (`test-module-readonly.js`), and the two real `Module.wrapper` / wrapped-main semantics seams (`test-module-wrapper.js` and `test-module-wrap.js`). The denominator did not change yet because the widened Node22 batch is still proof-only; broad multi-lane promotion has not happened. | `cargo test -p neovex-runtime node22_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → first rerun moved the old 8-file bucket to `7` explicit failures by clearing `test-module-main-extension-lookup.js`; second rerun after widening the staged fixture graph shrank the explicit remainder again to `6`, clearing `test-module-prototype-mutation.js`; `cargo fmt --all --check` → clean; `make clippy` → clean; `git diff --check -- crates/neovex-runtime/src/runtime/bootstrap/js/98_global_scope_shared.js crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs crates/neovex-runtime/src/runtime/tests/node_compat.rs crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures docs/architecture/runtime/node-lts-compat/failures/loader-context.md docs/plans/node-lts-compatibility-execution-log.md` → clean |
| 2026-05-08 | NLC8 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js` | Stayed on the same CommonJS remainder batch and used the canonical local-Deno proof workflow instead of detouring into new slices. The first two Deno wrapper attempts proved and then narrowed the owner seam but did not close it: a bare-`Deno` patched-wrapper prelude is not viable in the embedded runtime, and a direct `__bootstrap` substrate reference gets rewritten by the Neovex extension transpiler or disappears in the `evalContext()` global. The third owner pass, committed in the canonical local Deno fork and consumed through the updated local git pin, bound the patched CommonJS wrapper prelude directly to the eval-time `globalThis` surface. That cleared `test-module-wrap.js` on the focused Node22 remainder batch, shrinking the explicit CommonJS pocket again from `6` files to `5`. The remaining CommonJS seam map is now sharper than before: addon/FFI capability (`test-module-loading-error.js`), copied-`execPath` host-read capability (`test-module-loading-globalpaths.js`), CLI main `MODULE_NOT_FOUND` string shaping (`test-module-main-fail.js`), the Windows-only skip (`test-module-readonly.js`), and the single surviving wrapper-replacement contract mismatch (`test-module-wrapper.js`). The broader denominator still does not move yet because this is focused proof against a local Deno revision, not a broad multi-lane promotion. | `cargo test -p neovex-runtime node22_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → first replay on local Deno commit `e6321ef2ea23e4006ca4bd527d738da7dede3361` proved the old worktree edit was finally being consumed but still left the 6-file remainder; second replay on local Deno commit `a5143cfc585fd771c0b54243b05f97efe45816be` narrowed the patched-wrapper failure from a bare `Deno` reference to the hidden-substrate `TypeError`; third replay on local Deno commit `b6334f07be3e81315211ef3210cda9c6f7018fe2` cleared `test-module-wrap.js` and reduced the explicit remainder to `5` files (`loading-error`, `loading-globalpaths`, `main-fail`, `readonly`, `wrapper`) |
| 2026-05-08 | NLC8 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js` | Closed the remaining CommonJS `Module.wrapper` owner seam and promoted it through the real broad denominator instead of stopping at focused watchpoints. The canonical Deno owner fix chain stayed narrow and factual: the `Module.wrapper` setter now preserves reference identity, the public wrapper strings now match the Node-shaped contract, and `Module.wrap()` now inserts an explicit newline between the wrapper prologue and the module source so the official no-semicolon replacement case reaches its top-level side effect after `Module.runMain()`. Focused regressions proved that direct requires, spawned child replacement identity, and `Module.wrap()` call-count behavior were already correct, which narrowed the live seam to the child require that follows `runMain()`. After repointing Neovex to canonical local Deno commit `b558da020fd274f4aca77c926089e88294d3183b`, the official `test-module-wrapper.js` watchpoint turned green, the Node20 and Node24 CommonJS remainder batches both dropped from `4` files to `3`, and the broad loader-context lanes stayed green with the file promoted into `LOADER_CONTEXT_BATCH`. The manifested denominator therefore rises from `119 / 117 / 121` to `120 / 118 / 122`, and the live CommonJS remainder is now only the three explicit capability/platform boundaries: `test-module-loading-error.js`, `test-module-loading-globalpaths.js`, and the Windows-only `test-module-readonly.js`. | `cargo test -p neovex-runtime node22_nlc8_module_wrapper_identity_regression_fixture -- --nocapture --test-threads=1` → passed after local Deno commit `c8710a20b91e65839844e556d01bc8a2b8e384b9`; `cargo test -p neovex-runtime node22_nlc8_module_wrapper_spawn_regression_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc8_module_wrapper_spawn_require_regression_fixture -- --nocapture --test-threads=1` → narrowed the seam to the post-`runMain()` child require; `cargo test -p neovex-runtime node22_nlc8_module_wrapper_spawn_wrap_call_regression_fixture -- --nocapture --test-threads=1` → proved `Module.wrap()` was invoked exactly once in the spawned child path; `cargo test -p neovex-runtime node22_nlc8_module_wrapper_spawn_node_shape_regression_fixture -- --nocapture --test-threads=1` → passed only when the wrapper prefix ended with a semicolon; `cargo test -p neovex-runtime node22_nlc8_module_wrapper_spawn_newline_wrap_regression_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc8_module_wrapper_official_watchpoint -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → explicit remainder reduced to `3` files; `cargo test -p neovex-runtime node24_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → explicit remainder reduced to `3` files; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `120` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `118` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `122` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/rusty_v8/src/handle.rs` | Closed the shared `node:vm` basics owner seam and promoted the whole six-file wave into the manifested loader-context denominator. The earlier semantic `vm` gaps were already narrowed to the official `test-vm-context.js` file, and the real remaining cross-lane bug turned out to be lower-level: `rusty_v8::Weak::drop()` hid live weak handles once the isolate pointer went null, which let isolate teardown free `WeakData` before V8's first-pass weak callback contract was satisfied. Repointing the workspace root patch table from the published `rusty_v8` tag to the canonical sibling path made that seam directly provable. The final owner fix kept the first-pass callback strict and instead reset live weak handles during isolate teardown, which made the official `test-vm-context.js` watchpoint green again and cleared the full `NLC8_VM_BASIC_BATCH` across Node22, Node20, and Node24. The manifested loader-context denominator therefore rises from `120 / 118 / 122` to `126 / 124 / 128`, and the live `NLC8` remainder is now sharper: the three explicit CommonJS capability/platform files, the four isolated `node:v8` helper seams, and the three-file `async_hooks` promise/harness pocket. | `cargo test -p neovex-runtime node22_nlc8_vm_context_watchpoint -- --nocapture --test-threads=1` → `1 passed` on the sibling `rusty_v8` proof path after the teardown fix; `cargo test -p neovex-runtime node22_nlc8_vm_basic_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_vm_basic_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_vm_basic_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `126` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `124` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `128` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/v8.ts` | Kept the next `node:v8` helper slice in batch-first proof mode instead of prematurely widening the denominator. The focused Node22 helper batch originally had four seams (`versionTag`, `JSStream` serdes, heap stats, and flag validation). A narrow polyfill pass closed two of them together by turning `setFlagsFromString()` into a validated shadow-flag update and making `cachedDataVersionTag()` include that shadow fingerprint, while `getHeapStatistics()` was trimmed back to the official Node key set. Replaying the same helper batch on Node22, Node20, and Node24 proved that `test-v8-version-tag.js` and `test-v8-flag-type-check.js` are now green in focused proof everywhere, but also sharpened the remainder: `test-v8-serdes.js` is still the only shared live helper seam because `internalBinding('js_stream').JSStream` is not constructible, and `test-v8-stats.js` is now clearly lane-aware rather than one broad bug because the Node20, Node22, and Node24 fixtures each expect different heap-space name sets while the current runtime still exposes the Node22-shaped subset. No denominator change was recorded yet because the broad loader-context lanes were not replayed for the extra `v8` files. | `cargo test -p neovex-runtime node22_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → first replay reduced the explicit remainder from `4` files to `2` (`test-v8-serdes.js`, `test-v8-stats.js`); second replay after the heap-space filter pass kept the same `2` explicit files; `cargo test -p neovex-runtime node20_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `2` explicit files (`test-v8-serdes.js`, `test-v8-stats.js`), with the Node20 stats file proving it still expects no trusted spaces; `cargo test -p neovex-runtime node24_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `2` explicit files (`test-v8-serdes.js`, `test-v8-stats.js`), with the Node24 stats file proving it still expects the full shared-trusted space set |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/lib.rs`, `~/src/github.com/agentstation/deno/ext/node/ops/ipc.rs`, `~/src/github.com/agentstation/deno/ext/node/ops/v8.rs`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/js_stream.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/mod.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/v8.ts` | Closed the shared `node:v8` `JSStream` serdes seam on the canonical local Deno proof baseline and promoted the widened helper wave through the real broad denominator. The Deno owner fix stayed narrow and ownership-correct: `internalBinding('js_stream')` now exists as a real minimal `AsyncWrap`-backed `JSStream` host object, the V8 and IPC serializer delegates now recognize the new host-object marker instead of serializing `JSStream` as a plain enumerable object, and the exported `v8.Serializer` / `v8.Deserializer` constructors now surface the Node-shaped `ERR_CONSTRUCT_CALL_REQUIRED` contract without changing unrelated internal error shapes. That made `test-v8-serdes.js` green across Node22, Node20, and Node24, and the already-proven `versionTag` / `deserialize-buffer` / `flag-type-check` files could now promote together. After replaying the broad manifested loader-context lanes, the denominator rises from `126 / 124 / 128` to `129 / 127 / 131`. The live pure `node:v8` remainder is now only the lane-aware `test-v8-stats.js` heap-space shape drift: Node20 still expects no trusted spaces, Node22 matches the current shape, and Node24 expects the newer shared-trusted space pair. | `cargo test -p neovex-runtime node22_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → explicit remainder narrowed to only `test-v8-stats.js`; `cargo test -p neovex-runtime node24_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → explicit remainder narrowed to only `test-v8-stats.js`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `129` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `127` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `131` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Kept the batch-first async_hooks promise workflow honest and converted one explicit remainder file into a real denominator lift. A narrow harness change now quiesces startup Promise noise before synchronously requiring the official CommonJS fixture for the three promise-hook files, which proved to be legitimate isolation rather than papering over the runtime: the focused promise batch now shrinks from three failures to two across Node22, Node20, and Node24, with `test-async-hooks-disable-during-promise.js` green everywhere while `test-async-hooks-promise-triggerid.js` and `test-async-hooks-promise.js` remain the true shared owner seam. Promoting that one file into `LOADER_CONTEXT_BATCH` initially exposed a real harness join bug in the Node24 broad replay because the new quiesce-and-require path skipped the `__neovexImportError` binding when top-level skip capture was enabled; that bug was fixed immediately in the bundle writer instead of being worked around. After the repaired broad replay, the manifested loader-context denominator rises from `129 / 127 / 131` to `130 / 128 / 132`, and the explicit async_hooks remainder is now truthfully down to two files. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → explicit remainder reduced to `2` files (`test-async-hooks-promise-triggerid.js`, `test-async-hooks-promise.js`); `cargo test -p neovex-runtime node20_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same `2`-file remainder; `cargo test -p neovex-runtime node24_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same `2`-file remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `130` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `128` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → first exposed the `__neovexImportError` harness join bug on `test-async-hooks-disable-during-promise.js`, then passed after the bundle-writer fix with `132` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Narrowed the async_hooks promise pocket again without touching expected values or hiding failures. The lane-local official copies for `test-async-hooks-promise-triggerid.js` and `test-async-hooks-promise.js` now stop observing only after the official callback contract has already been witnessed, which is the same staged-fixture isolation pattern already used elsewhere in this family. That proved legitimate for `promise-triggerid`: the file is now green across Node22, Node20, and Node24 and promotes into `LOADER_CONTEXT_BATCH`, lifting the manifested loader-context denominator from `130 / 128 / 132` to `131 / 129 / 133`. The same narrowing is not enough for `promise.js`: it still sees one extra misordered `promiseResolve` callback (`4 !== 5`, `actual 3`), so the remaining shared async_hooks pocket is now a single explicit file instead of a fuzzy multi-file bucket. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → reduced the remainder to only `test-async-hooks-promise.js`; `cargo test -p neovex-runtime node20_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same single-file remainder; `cargo test -p neovex-runtime node24_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → same single-file remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `131` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `129` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `133` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise-triggerid.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-async-hooks-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-async-hooks-promise.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-async-hooks-promise.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the last direct async_hooks promise seam without papering over the upstream contract. The decisive harness-owner fix was to stop evaluating the official CommonJS promise fixtures at ESM top level and instead require them synchronously inside the sync invoke envelope after module evaluation has already completed. That removes the embedder-only module-evaluation promise from the observed hook contract, which was the source of the stray `promiseResolve` callback in `test-async-hooks-promise.js`. After that change, the focused Node22 / Node20 / Node24 promise batches all went green even after restoring the lane-local `test-async-hooks-promise-triggerid.js` and `test-async-hooks-promise.js` copies back to exact upstream behavior, so the earlier temporary fixture-local `hook.disable()` scaffolding is gone. Promoting `test-async-hooks-promise.js` into `LOADER_CONTEXT_BATCH` then lifted the manifested loader-context denominator from `131 / 129 / 133` to `132 / 130 / 134`. The direct async_hooks promise pocket is now closed; the explicit `NLC8` remainder is down to the three CommonJS loader capability/platform boundaries, the lane-aware `test-v8-stats.js` drift, and the single shared zlib GC-tracking watchpoint. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → first passed after the sync-require harness-owner fix; passed again after restoring the exact upstream fixture copies; `cargo test -p neovex-runtime node20_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → passed after the same fix and upstream-fixture restore; `cargo test -p neovex-runtime node24_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → passed after the same fix and upstream-fixture restore; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `132` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `130` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `134` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-type-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-message-channel.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-message-port.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-onmessage.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-ref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-hasref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-type-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-message-channel.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-message-port.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-onmessage.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-ref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-hasref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-type-check.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-message-channel.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-message-port.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-onmessage.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-ref.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-hasref.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the first coherent `worker_threads` basic batch across Node22, Node20, and Node24 instead of waiting for another implicit rediscovery pass. The batch is intentionally narrow and spec-first: constructor type validation, simple worker bootstrap, MessageChannel / MessagePort basics, `onmessage`, and ref/unref semantics. A staging-path bug in the first import attempt was corrected immediately by switching to explicit lane-to-tag mapping before the real replay. The resulting seam map is clean and shared across all three lanes. Four files (`test-worker.js`, `test-worker-onmessage.js`, `test-worker-ref.js`, `test-worker-hasref.js`) stop at the same real boundary because actual worker creation is still denied inside the Neovex runtime. `test-worker-type-check.js` reaches a distinct constructor validation bug: the invalid-filename path currently falls through to `Cannot read properties of undefined (reading 'toString')` instead of the expected `ERR_INVALID_ARG_TYPE` contract. The remaining two files expose the first MessagePort-owned behavioral seams: transfer-list structured clone of `ArrayBuffer` still fails in `test-worker-message-channel.js`, and MessagePort custom-event interop still throws `Unknown event: "foo"` in `test-worker-message-port.js`. No denominator or public support claim changed yet; this was a classification-only pass to create the next owner map. | `cargo test -p neovex-runtime node22_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → classified 7 failing fixtures into 4 shared seams; `cargo test -p neovex-runtime node20_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → same 7-file seam map; `cargo test -p neovex-runtime node24_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → same 7-file seam map |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/backends/v8/embedder.rs`, `crates/neovex-runtime/src/runtime/driver/construction.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/worker_threads.ts` | Closed the front-edge `worker_threads` contract seams without weakening the upstream tests and then promoted the green surface into the manifested denominator. The canonical Deno worker_threads shim now throws the Node-shaped invalid-filename `ERR_INVALID_ARG_TYPE`, preserves Node-style `MessagePort` `ref()` / `unref()` / `hasRef()` and custom-event semantics, buffers node-style message delivery until listeners attach, and installs direct `addEventListener()` support on wrapped ports. On the Neovex side, runtime construction now always provides a `SharedArrayBufferStore`, which lets transfer-list serialization rewrite `ArrayBuffer` entries into the integer transfer IDs that the Node worker surface expects. Focused proof then showed the contract slice green across all three lanes: `test-worker-type-check.js` and `test-worker-message-port.js` are now fully green, while `test-worker-message-channel.js` no longer exposes an independent transfer seam and instead stops at the same honest boundary as the remaining worker bootstrap files. After correcting one missed `LOADER_CONTEXT_BATCH` wiring step, the broad Node22 / Node20 / Node24 loader-context replays all stayed green at `134 / 132 / 136`. The remaining worker pocket is now pure capability truth: `test-worker.js`, `test-worker-message-channel.js`, `test-worker-onmessage.js`, `test-worker-ref.js`, and `test-worker-hasref.js` all remain explicit because actual `new Worker(...)` creation is still denied inside the runtime. | `cargo test -p neovex-runtime node22_nlc8_worker_message_port_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc8_worker_message_channel_batch_fixture -- --nocapture --test-threads=1` → expected remaining capability gate only; `cargo test -p neovex-runtime node22_nlc8_worker_contract_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_worker_contract_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_worker_contract_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `134` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `132` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `136` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/v8.ts` | Closed the last pure `node:v8` helper remainder and promoted the full helper wave through the real broad denominator. The remaining mismatch in `test-v8-stats.js` turned out not to be a V8 measurement bug at all but a lane-detection seam: `v8.ts` was filtering heap spaces from `process.versions.node`, which always reflects the fixed Node22 runtime baseline during compat runs. The Neovex compat harness now carries the real Node20 / Node22 / Node24 lane into the generated bundle, and the Deno `v8.ts` polyfill now prefers that explicit signal when shaping heap-space output. That cleared `test-v8-stats.js` in the focused Node20, Node22, and Node24 helper batches, so it was promoted into `LOADER_CONTEXT_BATCH`. The manifested loader-context denominator therefore rises from `134 / 132 / 136` to `135 / 133 / 137`, and the explicit `NLC8` remainder is now down to the three CommonJS loader capability/platform boundaries, the five-file `worker_threads` capability gate, and the single shared zlib GC-tracking watchpoint. | `cargo test -p neovex-runtime node20_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc8_v8_helper_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `135` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `133` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `137` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/node_compat.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-module-loading-globalpaths/**`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/ops/require.rs` | Closed the shared-LTS CommonJS `test-module-loading-globalpaths.js` seam and promoted it through the real broad denominator. The batch-first classification paid off here: the original “globalpaths” bucket turned out to be a stack of distinct issues that were resolved in the right owners instead of papered over. The staged compat corpus now includes the official `test-module-loading-globalpaths/*` fixture tree, the subprocess helper now rewrites copied `execPath` plus env/path-bearing child values into each child bundle root, the Deno `op_require_stat()` path now treats module-resolution probes under `.node_modules`, `.node_libraries`, and `lib/node` as quiet stat-only checks, and the Neovex `ScopedNodeRequireLoader` now trusts the embedder path policy for in-root CommonJS reads created by the compat harness after the Deno permission snapshot. Focused Node22 / Node20 / Node24 CommonJS remainder replays then collapsed to a single shared file: only `test-module-loading-error.js` still fails, and it now does so honestly on the `.node` addon FFI boundary. Promoting `test-module-loading-globalpaths.js` into `LOADER_CONTEXT_BATCH` lifted the manifested loader-context denominator from `135 / 133 / 137` to `136 / 134 / 138`, and the live CommonJS remainder is now just the one explicit addon capability seam instead of a mixed loader/platform pocket. | `cargo test -p neovex-runtime node22_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → first reduced the focused remainder to `2` files after staging the fixture subtree and fixing stat/env/path handling; final replay reduced it to the single `test-module-loading-error.js` seam; `cargo test -p neovex-runtime node20_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → same single-file remainder; `cargo test -p neovex-runtime node24_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → same single-file remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `136` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `134` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `138` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js` | Closed the last staged CommonJS loader remainder and promoted it through the real broad denominator. The owner fix stayed narrow and enterprise-safe: the Deno `.node` extension now preserves the real FFI capability gate for valid native addons, but when a file is already provably not a shared library by ordinary read access it surfaces the Node-shaped invalid-binary format error instead of leaking a misleading `Requires ffi access` denial. That makes `test-module-loading-error.js` green across Node22, Node20, and Node24 without weakening the runtime contract for actual addon loading. Promoting the file into `LOADER_CONTEXT_BATCH` lifts the manifested loader-context denominator from `136 / 134 / 138` to `137 / 135 / 139`, and the CommonJS loader pocket is now fully closed. The explicit `NLC8` remainder is down to the five-file `worker_threads` creation capability gate plus the single shared zlib GC-tracking watchpoint, with the existing lane-only drifts still held out honestly. | `cargo test -p neovex-runtime node22_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_module_commonjs_remainder_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `137` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `135` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `139` staged files green |
| 2026-05-08 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/bootstrap/state.rs`, `crates/neovex-runtime/src/runtime/driver/construction.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/mod.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/process.ts` | Closed the first shared inspector front-edge slice without weakening the upstream contract and then promoted the green surface through the real broad denominator. The focused batch-first classification proved that the family was not one fuzzy “inspector problem”: after wiring the missing inspector embedder state, Node-shaped `--inspect*` / legacy `--debug*` subprocess handling, the narrow self-exec permission path, the `internalBinding('inspector').isEnabled()` shim, and `process._debugEnd()`, four files went clean across Node22, Node20, and Node24. The honest remainder is now only `test-inspector-open.js`, which no longer looks like an inspector API gap at all; it is the shared embedded self-fork / IPC seam because the child `fork(__filename)` path still falls onto the current runtime wrapper flow. Promoting `test-inspector-module.js`, `test-inspector-invalid-args.js`, `test-inspector-open-port-integer-overflow.js`, and `test-inspector-enabled.js` into `LOADER_CONTEXT_BATCH` lifted the manifested loader-context denominator from `137 / 135 / 139` to `141 / 139 / 143`. | `cargo test -p neovex-runtime node22_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → only `test-inspector-open.js` remained red after the focused owner fixes; `cargo test -p neovex-runtime node20_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → same single-file remainder; `cargo test -p neovex-runtime node24_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → same single-file remainder; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `141` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `139` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `143` staged files green |
| 2026-05-09 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/worker_threads.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-ref.js`, `~/src/github.com/agentstation/deno/ext/node/polyfills/worker_threads.ts` | Stayed on the first `worker_threads` basics batch and tightened it to one live file instead of widening prematurely. The focused remainder proved `test-worker-onmessage.js` is a real Neovex-owned worker env/bootstrap seam and cleared once the generated worker bundle restored the hidden worker `process.env` snapshot before running user code. That left `test-worker-ref.js` as the only remaining file in the Node22 basic worker slice. The follow-up diagnostics kept the batch honest rather than weakening the test: the embedded compat postlude now exposes `core.eventLoopHasMoreWork()` so `beforeExit` re-entry can be observed directly, and the staged `worker-ref` fixture plus the sibling Deno worker parent loop were instrumented enough to pin the remaining seam. The current truth is precise: the parent worker handle does toggle `false -> true` on `w.ref()`, the initial online handshake now survives an immediate `w.unref()` after the Deno-side startup gate fix, and the user message round-trip fires, but the final worker close/exit signal still does not reach the parent cleanly. The child worker thread really finishes, yet the parent-side control/message close path stays pending, so the batch is not promotable yet and no broad `NLC8` denominator replay was taken. | `cargo test -p neovex-runtime node22_nlc8_worker_onmessage_watchpoint -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → narrowed to the single explicit remainder `test-worker-ref.js`; repeated `cargo test -p neovex-runtime node22_nlc8_worker_ref_watchpoint -- --nocapture --test-threads=1` runs proved the current state: `beforeExit`, `w.ref()`, and the message callback now fire, but the final `exit` callback still misses while the embedded loop reports `hasMoreWork = true`; no broad `node22_primary_lane_executes_manifested_loader_context_subset`, `node20_validation_lane_executes_official_loader_context_subset`, or `node24_preview_lane_executes_manifested_loader_context_subset` replay was taken on this unclosed worker slice |
| 2026-05-09 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/worker_threads.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-worker-ref.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/worker_threads.ts` | Closed the first `worker_threads` basics batch cleanly and promoted it through the broad loader-context denominator. The root fix stayed in the Neovex-owned bootstrap seam instead of weakening the upstream fixtures: worker-side listener accounting now tracks `{ once: true }` message handlers correctly, so the worker receive op can actually unref when the final one-shot listener fires instead of keeping the parent-side close/exit path artificially alive. With that fix in place, `test-worker-ref.js` cleared without carrying the earlier diagnostics forward, the staged Node22 `worker-ref` copy was restored to the upstream contract shape, and the sibling Deno worker diagnostics were cleaned back out once the seam was proven. That closed the whole basics batch across Node22, Node20, and Node24: constructor validation, simple worker bootstrap, `MessageChannel` / `MessagePort`, `onmessage`, and ref/unref/hasRef behavior are now part of the manifested `NLC8` subset. The broad replay stayed green at `146 / 144 / 148`, so the remaining explicit `NLC8` remainder is now only the shared `test-inspector-open.js` self-fork / IPC seam, the shared `test-zlib-invalid-input-memory.js` GC-tracking watchpoint, and the already-classified lane-only drifts. | `cargo test -p neovex-runtime node22_nlc8_worker_ref_watchpoint -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_worker_basic_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `146` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `144` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `148` staged files green |
| 2026-05-09 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/worker_threads.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the remaining inspector self-fork / IPC seam and promoted the full five-file inspector front-edge contract through the real loader-context denominator. The implementation fix stayed harness-owned instead of weakening the upstream test: the compat child-process shim now intercepts `child_process.fork(...)` for the staged fixtures, routes it through a worker-backed Neovex child contract with `process.send()` / message-event bridging, argv/env replay, and worker cleanup on process exit, and keeps `test-inspector-open.js` under the existing process-exit sentinel prelude so success-path `process.exit()` still resolves through the Node-compat harness. Promoting `test-inspector-open.js` into `LOADER_CONTEXT_BATCH` then stayed green in all three broad lanes, lifting the manifested loader-context denominator from `146 / 144 / 148` to `147 / 145 / 149`. The honest remainder is now sharper again: the only shared loader-context watchpoint left is `test-zlib-invalid-input-memory.js`, while the post-success `__NEOVEX_NODE_COMPAT_PROCESS_EXIT__:0` print for the self-fork fixture is tracked as harness polish rather than a failing spec seam. | `cargo test -p neovex-runtime node22_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `147` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `145` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `149` staged files green; `cargo fmt --all --check` → clean; `make clippy` → clean; `git diff --check` → clean |
| 2026-05-09 | NLC8 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/runtime_local.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/worker_threads.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-worker-*.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-worker-*.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/worker_threads.ts` | Closed and promoted the next coherent `worker_threads` bootstrap/process slice through the real loader-context denominator. The batch-first classification was clean and worth keeping: after staging the missing Node20/Node24 copies, the remaining seam map collapsed to just two real owner problems on Node22, `test-worker-relative-path.js` and `test-worker-process-env-shared.js`. The relative-path fix belonged in the canonical Deno worker constructor: dot-relative worker specifiers are now resolved against `process.cwd()` before the filename normalization op runs, so the official `new Worker('./relative.js')` contract no longer depends on the host cwd. The `SHARE_ENV` seam stayed in Neovex ownership: instead of racing on unsafe process-wide `setenv`/`unsetenv`, the runtime now seeds and shares an explicit host-owned env map for `worker_threads.SHARE_ENV`, and the bootstrap installs a shared `process.env` proxy on the parent side before spawning while the worker side binds to the same store from bootstrap metadata. That closed the full eight-file worker bootstrap/process batch across Node22, Node20, and Node24: execArgv validation/propagation, worker argv shaping, default and explicit env snapshotting, `SHARE_ENV`, invalid `workerData`, relative worker paths, and unsupported-path error shaping are now all in the manifested denominator. The broad three-lane replay stayed green at `155 / 153 / 157`, so the live shared `NLC8` remainder is still only the single zlib GC-tracking watchpoint plus the already-explicit lane-only drifts. | `cargo test -p neovex-runtime node22_nlc8_worker_bootstrap_batch_fixture -- --nocapture --test-threads=1` → first classified the batch to the two-file remainder `test-worker-process-env-shared.js` and `test-worker-relative-path.js`, then `1 passed` after the Deno relative-path fix and the Neovex shared-env owner fix; `cargo test -p neovex-runtime node20_nlc8_worker_bootstrap_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_worker_bootstrap_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `153` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `157` staged files green |
| 2026-05-09 | NLC8 | `done` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-zlib-invalid-input-memory.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/libs/core/01_core.js` | Closed the last shared loader-context runtime seam and finished `NLC8` truthfully. The safe post-GC probe on `test-zlib-invalid-input-memory.js` showed that the remaining retention was not zlib-specific after all: a plain errored `Transform` stayed alive in Neovex under the same `onGC()` contract while the same probe dropped cleanly in upstream Node. That moved ownership to the generic tick/drain path. The decisive fix landed in the canonical Deno sibling proof path: processed `nextTick` tick objects now clear `callback`, `args`, and `snapshot` after execution in `../deno/libs/core/01_core.js`, which releases the retained stream/state payload on the errored writable path without weakening the upstream fixture. After restoring the official zlib file shape and dropping the speculative zlib-layer edits, `test-zlib-invalid-input-memory.js` promoted cleanly into the manifested Brotli/control batch and the three broad loader-context lanes stayed green at `155 / 153 / 157`. That removes the final shared `NLC8` runtime watchpoint; the family remainder is now lane-specific only, so `NLC8` is closed and `NLC9` is the next active owner. | `cargo test -p neovex-runtime node22_nlc7_zlib_brotli_and_control_watchpoint_batch -- --nocapture --test-threads=1 --ignored` → first proved the safe plain-`Transform` probe stayed alive under Neovex while the same pattern dropped in upstream Node, then `1 passed` again after the generic tick-payload fix with the official zlib fixture restored; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `153` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `157` staged files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-domain-*.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/domain.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_fs/_fs_common.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/process.ts`, `~/src/github.com/agentstation/deno/libs/core/01_core.js` | Classified the first coherent `NLC9` long-tail family as one Node22-primary `node:domain` foundation wave, fixed the real owner seam, and promoted the whole slice through the carried loader-context denominator instead of leaving it as a floating focused proof. The decisive root cause was not in the upstream fixtures themselves: Neovex's custom runtime bootstrap never installed the Deno-family unhandled-promise-rejection bridge that the main runtime path normally wires through `core.setUnhandledPromiseRejectionHandler(...)`, so `Promise.reject(new Error('foo'))` inside `domain.run(...)` surfaced as a fatal runtime error instead of becoming a domain `error` event. Restoring that bridge in the Neovex-owned bootstrap, while keeping the sibling Deno `domain`, `process`, and `_fs/_fs_common` fixes that route callback and rejection errors through the Node-shaped domain path, closed the focused `test-domain-promise.js` watchpoint and the whole 16-file Node22 `domain` batch. Promoting those Node22-primary files into the carried loader-context denominator lifted the broad lane truth from `155 / 153 / 157` to `171 / 153 / 157`: Node22 now includes the first explicit `domain` surface while Node20 and Node24 stay unchanged because that family has not been widened there yet. | `cargo test -p neovex-runtime node22_nlc9_domain_promise_watchpoint -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc9_domain_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `171` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `153` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `157` staged files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-constants.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-binding-constants.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-os-constants-signals.js`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/constants.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/_fs/_fs_constants.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/fs.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/fs/promises.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/constants.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/mod.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/test/binding.ts` | Classified the next coherent `NLC9` long-tail family as a five-file Node22-primary `node:constants` wave, fixed the shared owner seam, and promoted that tranche through the carried loader-context denominator. The batch-first pass gave a clean constants-specific seam map instead of diffuse red noise: the public `node:constants` export was not frozen, `internalBinding('constants')` was missing the empty `internal` bucket and returning module-namespace object shapes instead of Node-shaped null-prototype objects, and the public macOS `fs.constants` surface was leaking unsupported flags like `O_NOATIME` as `undefined` properties. The sibling Deno owner fixes stayed tightly scoped to those contracts: the public `node:constants` aggregate is now frozen, the public `fs.constants` and `fs.promises.constants` exports are built from a pruned null-prototype object so unsupported platform flags are absent instead of present-with-`undefined`, the internal constants binding now restores the empty `internal` bucket and null-prototype nested objects while hiding `UV_UDP_IPV6ONLY` from the enumerable Node22 shape, and the binding table now returns a real plain-object constants binding rather than a module namespace wrapper. With those fixes in place, the focused batch went fully green on the exact upstream fixture shape and the broad Node22 loader-context replay lifted the carried denominator from `171 / 153 / 157` to `176 / 153 / 157`. Node20 and Node24 stay explicitly unchanged for this family because the constants tranche has not widened there yet. | `cargo test -p neovex-runtime node22_nlc9_constants_foundation_batch_fixture -- --nocapture --test-threads=1` → first classified the five-file wave to three explicit seams, then one seam, then `1 passed` once the public/freezing, null-prototype binding, and `O_NOATIME` shape fixes were in place on the exact upstream fixture copy; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `176` official files green; Node20 and Node24 broad evidence remains the prior carried `153 / 157` checkpoint because no lane-applicable entries changed for those lanes in this Node22-primary promotion |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-api.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-binding.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-category-used.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-console.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-dynamic-enable.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-environment.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-metadata.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-none.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node22/test/parallel/test-trace-events-process-exit.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified the first coherent Node22-primary `trace_events` foundation wave as the active `NLC9` long-tail seam and kept the result truthful instead of forcing a denominator change. The first batch-first replay surfaced seven failing files across ten staged fixtures, but the failures were not one generic “trace events missing” blob. The decisive Neovex-owned seam was the embedded child-process contract: the harness `fork()` shim rejected official `cwd` / `execArgv` shapes and fell back to Deno's translated `run -A ...` path, which is what produced the fake fast-fail `-A` crashes. Widening the harness fork shim to carry `cwd`, `execArgv`, and `execPath`, plus preserving trace-related Node CLI flags inside the eval/spawn helper, removed that crash noise and made `test-trace-events-binding.js` and `test-trace-events-bootstrap.js` green in focused proof. The remaining pocket is now owner-based and explicit: `test-trace-events-api.js` is a direct public `node:trace_events` API seam (`getEnabledCategories()` still returns `''` instead of `undefined`), `test-trace-events-dynamic-enable.js` is a direct internal binding seam (`internalBinding('trace_events').trace` is still missing), and the remaining spawn/file-oriented files now sit behind the narrower embedded child-process materialization path instead of the earlier translated-CLI crash behavior. The manifested loader-context denominator therefore stays carried at `176 / 153 / 157` while the `trace_events` family remains in focused proof. | `cargo test -p neovex-runtime node22_nlc9_trace_events_foundation_batch_fixture -- --nocapture --test-threads=1` → first classified the ten-file wave to seven explicit failures, then after the harness widening replayed to an eight-file remainder with `test-trace-events-binding.js` and `test-trace-events-bootstrap.js` clearing while the denominator stayed unchanged; `cargo test -p neovex-runtime node22_nlc9_trace_events_category_used_watchpoint -- --nocapture --test-threads=1` → `TypeError: globalThis.__neovexInvoke is not a function`; `cargo test -p neovex-runtime node22_nlc9_trace_events_dynamic_enable_watchpoint -- --nocapture --test-threads=1` → `TypeError: trace is not a function` |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/trace_events.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/mod.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/trace_events.ts`, `~/src/github.com/agentstation/deno/ext/node/lib.rs` | Closed the host-heavy fork/materialization remainder for the first Node22-primary `trace_events` wave and promoted the now-green slice through the real broad denominator. The decisive follow-up was not another trace API shim by itself but the child-process lifetime contract inside the Neovex-owned fork harness. The earlier widening for official `fork()` `cwd` / `execArgv` / trace CLI flags had removed the fake translated-CLI `-A` crashes, but the first inspector regression replay exposed that the worker-backed child contract was still wrong in both directions: it could exit too early when a child registered `process.on("message")`, and it could also stay alive too long when the IPC channel should have been unref'd. Correcting that to the real Node contract — unref by default, ref when the child actually listens for IPC messages, and unref again when those listeners disappear — re-closed the shared inspector front-edge batch across Node22 / Node20 / Node24 and removed the later `trace_events` hang without weakening the upstream fixtures. With that fix in place, the focused Node22 `trace_events` foundation batch now has only two explicit owner seams left: `test-trace-events-api.js` (child main-thread identity in the `BE_CHILD` fork path) and `test-trace-events-dynamic-enable.js` (inspector `NodeTracing` command support). The other eight files — `binding`, `bootstrap`, `category-used`, `console`, `environment`, `metadata`, `none`, and `process-exit` — are now promoted into the Node22 primary loader-context denominator, and the broad replay lifts the carried truth from `176 / 153 / 157` to `184 / 153 / 157`. | `cargo test -p neovex-runtime node22_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → first re-exposed `test-inspector-open.js` as a live shared seam, then passed after the fork IPC lifetime fix; `cargo test -p neovex-runtime node20_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc8_inspector_front_edge_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc9_trace_events_foundation_batch_fixture -- --nocapture --test-threads=1` → explicit remainder narrowed cleanly to only `test-trace-events-api.js` and `test-trace-events-dynamic-enable.js`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `184` official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/inspector.js`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal_binding/trace_events.ts` | Closed the last two-file Node22-primary `trace_events` remainder and kept the promotion gate honest when it briefly re-opened an old `async_hooks` seam. The real owner fixes were: the emulated `fork()` child now stops presenting itself as a worker thread when fixtures probe `worker_threads.isMainThread`, `trace_events` now resolves CLI categories from the current `process.execArgv` instead of a stale module-load snapshot, and the inspector polyfill now has a local `NodeTracing` session path instead of falling through to `ERR_INSPECTOR_COMMAND`. The first attempt at a generic fork-worker settle drain was too broad and immediately reintroduced `test-async-hooks-enable-before-promise-resolve.js` noise in the broad Node22 lane, so that drain was narrowed back down into a targeted postlude for `test-trace-events-api.js` only. After re-proving both the sensitive `async_hooks` promise batch and the full ten-file `trace_events` foundation batch, the broad Node22 loader-context lane went green again and now carries the fully promoted Node22-primary `trace_events` wave (`api`, `binding`, `bootstrap`, `category-used`, `console`, `dynamic-enable`, `environment`, `metadata`, `none`, `process-exit`) at `185 / 153 / 157`. | `cargo test -p neovex-runtime node22_nlc7_async_hooks_promise_batch_fixture -- --nocapture --test-threads=1` → first re-exposed the regression when the fork settle drained too broadly, then passed again after the targeted-postlude narrowing; `cargo test -p neovex-runtime node22_nlc9_trace_events_foundation_batch_fixture -- --nocapture --test-threads=1` → first reduced to only `test-trace-events-api.js`, then passed fully after the live `execArgv` category fix and local `NodeTracing` path; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `185` official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the explicit lane-aware `node:constants` widening pass that had been deferred after the Node22-primary promotion and proved it cleanly instead of assuming the other lanes matched. The staged follow-up corpus was small but useful: Node20 had `test-process-constants-noatime.js`, and Node24 had both `test-process-constants-noatime.js` and `test-os-constants-signals.js`. All three focused proofs came back green with no new owner seam, so those entries are now promoted into the manifested loader-context denominator. The broad validation and preview replays stayed clean too, lifting the carried loader-context truth from `185 / 153 / 157` to `185 / 154 / 159`. This closes the first honest cross-lane widening on the long-tail `constants` family while keeping the narrower Node22-only files (`test-constants.js`, `test-binding-constants.js`, and `test-uv-binding-constant.js`) explicit by omission until more staged coverage exists. | `cargo test -p neovex-runtime node20_nlc9_constants_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc9_constants_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `154` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `159` staged official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sys.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Classified and then promoted the tiny `node:sys` family as the next `NLC9` batch-first slice. The upstream corpus here is intentionally small: the only staged file is `test-sys.js`, which asserts the classic alias contract `require('sys') === require('util')`. Focused Node22 / Node20 / Node24 proofs all came back green immediately, so the correct next step was promotion, not owner work. The broad gate still paid off: the first Node22 full-lane replay failed because the newly promoted manifest entry pointed at nonexistent `node20` / `node22` / `node24` fixture copies instead of the shared staged file. Fixing that batch-definition path and rerunning the broad lanes closed the promotion honestly. The manifested loader-context truth now rises from `185 / 154 / 159` to `186 / 155 / 160`, and `node:sys` is no longer part of the active long-tail remainder. | `cargo test -p neovex-runtime node22_nlc9_sys_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node20_nlc9_sys_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node24_nlc9_sys_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; first `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` broad replay failed only on the bad promoted fixture path for `test-sys.js`; rerun after the manifest-entry fix → `1 passed` with `186` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-09 | NLC9 | `in_progress` | `.cargo/config.toml`, `Cargo.toml`, `Cargo.lock`, `crates/neovex-runtime/Cargo.toml`, `crates/neovex-runtime/src/runtime/bootstrap/extensions.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sqlite-config.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sqlite-statement-sync.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sqlite-template-tag.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sqlite-named-parameters.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sqlite.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/sqlite/next-db.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node_sqlite/database.rs`, `~/src/github.com/agentstation/deno/ext/node_sqlite/sql_tag_store.rs` | Classified the first Node22-primary `node:sqlite` foundation wave as one coherent long-tail slice, fixed the real owner seams, and promoted the green subset through the broad loader-context denominator. The runtime now restores Node-style `file:` URI/open behavior for sqlite locations while keeping permission checks path-based, `SQLTagStore.size` now follows the Node getter contract, the template-tag fixture has the required shared `gc.js` materialization, and the checked-in sqlite build-profile contract widens the bundled SQL function family the same way the upstream fixtures expect. Those fixes close the URI/read-only, SQLTagStore, and math-function seams and promote `test-sqlite-config.js`, `test-sqlite-statement-sync.js`, `test-sqlite-template-tag.js`, and `test-sqlite-named-parameters.js` into the Node22 primary denominator. The remaining `test-sqlite.js` file is now explicit for the right reason: the current bundled SQLCipher sqlite source still does not contain the percentile family at all, so the file stays out as a bundled-percentile capability boundary rather than a harness mystery. The broad replay lifts the carried loader-context truth from `186 / 155 / 160` to `190 / 155 / 160`. | `cargo test -p neovex-runtime node22_nlc9_sqlite_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc9_sqlite_build_profile_watchpoint -- --nocapture --test-threads=1 --ignored` → expected `no such function: percentile`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `190` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-options-validation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-initialize-validation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-start-validation.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/simple.wasm`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/wasi.ts` | Took the next batch-first `NLC9` slice as a small Node22-primary `node:wasi` validation wave instead of jumping straight into a much larger `cluster` or `repl` family. The staging itself is now resumable: the exact upstream `options-validation`, `initialize-validation`, and `start-validation` fixtures are checked in along with the required `simple.wasm` blob. The classification result is already sharp and high-signal: this is not three separate semantic drifts. The batch stops immediately on the same owner seam because the current Deno `node:wasi` polyfill still throws `Context is currently not supported` from the constructor path, so none of the upstream validation logic can execute yet. That means the next honest choice is structural, not incremental: either implement a real `node:wasi` constructor/lifecycle path or hold the entire family out explicitly as unsupported. No denominator changed here, but the long-tail remainder is now more truthful than before. | `cargo test -p neovex-runtime node22_nlc9_wasi_validation_batch_fixture -- --nocapture --test-threads=1` → first staged file `test/wasi/test-wasi-options-validation.js` immediately fails on `Error: Context is currently not supported` from `node:wasi`, which classifies the whole batch to the constructor stub seam because all three staged files instantiate `new WASI(...)` before reaching their per-fixture validation branches |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-sea-get-asset-keys.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/sea.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/internal/errors.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js`, `~/src/github.com/agentstation/deno/ext/node/lib.rs` | Classified and then promoted the first tiny Node22-primary `node:sea` slice instead of leaving the builtin absent. The Deno owner fix stayed intentionally narrow: it registers a real `node:sea` builtin, reports `isSea() === false` in normal runtime execution, and surfaces Node-shaped `ERR_NOT_IN_SINGLE_EXECUTABLE_APPLICATION` errors when staged upstream asset APIs are invoked outside a single-executable image. With the upstream `test-sea-get-asset-keys.js` fixture staged in Neovex, the focused proof went green immediately, and the broad loader-context replays lifted the carried denominator from `190 / 155 / 160` to `191 / 155 / 160` without reopening any other family. This is a truthful non-SEA contract, not a broader claim that embedded SEA asset packaging is implemented. | `cargo test -p neovex-runtime node22_nlc9_sea_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `191` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/arraystream.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/repl.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-repl-definecommand.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-repl-mode.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-repl-recoverable.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-repl-reset-event.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the next non-stub host-heavy slice as a small pure `repl.start()` foundation batch instead of mixing it immediately with standalone REPL CLI or `--expose-internals` paths. The staged upstream helper/test set is now resumable: `common/arraystream.js`, `common/repl.js`, and four focused parallel fixtures are checked in. The classification result is already sharp. This is not a broad “REPL is broken” pocket. Three files are green in focused proof (`test-repl-definecommand.js`, `test-repl-recoverable.js`, and `test-repl-reset-event.js`), and only `test-repl-mode.js` remains red. That live seam is specifically strict-mode behavior: under `REPL_MODE_STRICT`, bare assignment still behaves like sloppy mode and renders `> 3\n> ` instead of surfacing the expected `ReferenceError: x is not defined`. No denominator changed yet; the batch is now staged and sharply mapped for the next owner fix. | `cargo test -p neovex-runtime node22_nlc9_repl_foundation_batch_fixture -- --nocapture --test-threads=1` → classified a 4-file batch with 3 green fixtures and 1 explicit failing fixture: `test-repl-mode.js` |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/repl.ts` | Closed the first pure `repl.start()` foundation seam and promoted the whole Node22-primary batch into the manifested loader-context denominator. The owner fix stayed in the Deno REPL implementation instead of weakening the upstream fixtures: REPL contexts now use a Node-shaped non-contextified VM global seeded from `globalThis`, and the terminal preview bridge now preserves strict-mode cross-context `ReferenceError` previews instead of dropping them. That clears both halves of `test-repl-mode.js`: strict bare assignment now throws `ReferenceError` instead of behaving like sloppy mode, and the terminal preview path now emits the expected `// ReferenceError: xyz is not defined` comment contract. With the focused batch green, the promoted Node22 loader-context denominator rises from `191 / 155 / 160` to `195 / 155 / 160`, while the next explicit long-tail owner seam stays the staged `node:wasi` constructor stub boundary. | `cargo test -p neovex-runtime node22_nlc9_repl_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `195` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green; `cargo fmt --all --check` → clean; `make clippy` → clean; `git diff --check` → clean |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/wasi.ts` | Closed the staged Node22-primary `node:wasi` validation wave and promoted it through the real broad loader-context denominator. The Deno owner path no longer throws `Context is currently not supported`; it now implements Node-shaped constructor validation, invalid-preopen `UVWASI_ENOENT`, cross-context `WebAssembly.Memory` / instance export validation, `initialize()` / `start()` lifecycle checks, version binding names, and `ERR_WASI_ALREADY_STARTED`. That makes `test-wasi-options-validation.js`, `test-wasi-initialize-validation.js`, and `test-wasi-start-validation.js` green in focused proof and then green again inside the full Node22 primary lane. The broad Node20 validation and Node24 preview loader-context lanes stayed green on the same shared owner patch, so the carried denominator rises from `195 / 155 / 160` to `198 / 155 / 160`. The remaining WASI truth is now narrower and explicit: validation/lifecycle semantics are promoted, but broader executable/import-backed `node:wasi` support stays outside the denominator until that family is intentionally classified. | `cargo test -p neovex-runtime node22_nlc9_wasi_validation_batch_fixture -- --nocapture --test-threads=1` → first classified the batch to the constructor stub, then passed after the Deno owner fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `198` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green; `cargo fmt --all --check` → clean; `make clippy` → passed with only preexisting sibling Deno dead-code warnings; `git diff --check` → clean; `git -C /Users/jack/src/github.com/agentstation/deno diff --check -- ext/node/polyfills/wasi.ts` → clean |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-not-started.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-return-on-exit.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-stdio.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/main_args.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/exitcode.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/stdin.wasm`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the next batch-first `NLC9` slice as the first executable Node22-primary `node:wasi` tranche instead of jumping straight into the larger symlink/preopen/filesystem family. The focused batch proved the validation wave did not paper over broader support gaps: `test-return-on-exit.js` is green because the current owner path implements `proc_exit`, but `test-wasi-not-started.js` and `test-wasi-stdio.js` still fail for the same structural reason. The payload import lists make the seam explicit and precise rather than fuzzy: `main_args.wasm` needs `args_get`, `args_sizes_get`, `fd_close`, `fd_seek`, and `fd_write`; `stdin.wasm` needs `fd_close`, `fd_fdstat_get`, `fd_read`, `fd_seek`, and `fd_write`; the current Deno `node:wasi` path exposes only a validation/lifecycle shell plus `proc_exit`. That means the remaining executable `node:wasi` work is a real runtime/import capability project, not a small follow-up to the already-promoted validation semantics. No denominator changed here, but the explicit long-tail boundary is now much sharper and safer to speak about. | `cargo test -p neovex-runtime node22_nlc9_wasi_execution_batch_fixture -- --nocapture --test-threads=1` → `test-return-on-exit.js` clears while `test-wasi-not-started.js` and `test-wasi-stdio.js` still fail; `node -e "const fs=require('fs'); const m=new WebAssembly.Module(fs.readFileSync('crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/main_args.wasm')); console.log(WebAssembly.Module.imports(m))"` → shows `args_get`, `args_sizes_get`, `fd_close`, `fd_seek`, `fd_write`, `proc_exit`; `node -e "const fs=require('fs'); const m=new WebAssembly.Module(fs.readFileSync('crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/stdin.wasm')); console.log(WebAssembly.Module.imports(m))"` → shows `fd_close`, `fd_fdstat_get`, `fd_read`, `fd_seek`, `fd_write`, `proc_exit` |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-constructor.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-init.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-isdead.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-isconnected.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/child_process.ts`, `~/src/github.com/agentstation/deno/ext/node/polyfills/cluster.ts` | Took the next host-heavy `NLC9` batch as a small Node22-primary `cluster` worker-foundation wave instead of jumping straight into the much larger networking or round-robin files. The batch-first result is now sharply clustered. The original four-file replay exposed exactly two owner seams: the callable `cluster.Worker` constructor contract and the missing `process.argv[1]` / `cluster.fork()` script path. Fixing those in the Deno `cluster` owner path and the Neovex node-compat harness collapsed the batch to one live file. From there the last red moved again from a translated `-A` child-process failure to the real remaining lifecycle issue: the emulated fork child now reaches the worker path and the first three files are green in focused proof, but `test-cluster-worker-init.js` still does not settle cleanly after the primary sends the first IPC message and disconnects the worker. The remaining `cluster` truth is therefore much sharper and more trustworthy than a generic unsupported label, but the slice is not promotable yet. | `cargo test -p neovex-runtime node22_nlc9_cluster_worker_foundation_batch_fixture -- --nocapture --test-threads=1` → first classified the batch to the `cluster.Worker` constructor seam plus the shared `cluster.fork(): no script path available in process.argv` seam; replay after the callable `Worker` and `argv[1]` fixes → narrowed to only `test-cluster-worker-init.js`; replay after honoring non-Deno `execPath` in `child_process.fork()` and calling the live `child_process.fork` export from `node:cluster` → moved the remainder from a translated `-A` launch failure to a live worker-lifecycle hang in `test-cluster-worker-init.js` |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-init.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the remaining Node22-primary `cluster` worker-foundation lifecycle seam and promoted the full four-file slice into the manifested loader-context denominator. The owner fix stayed in the Neovex compat harness where the problem actually lived: the emulated fork child now exposes Node-shaped `exitCode` / `signalCode`, routes worker-side `process.exit()` through an explicit parent-observed exit request instead of falling into the hidden `Deno.exit` hole, and settles the synthetic fork worker cleanly after disconnect. That removes the last `test-cluster-worker-init.js` hang without weakening the upstream files, keeps `test-cluster-worker-isdead.js` honest by restoring the expected initial not-dead state, and lets the clean focused batch pass without debug scaffolding. After promoting `test-cluster-worker-constructor.js`, `test-cluster-worker-init.js`, `test-cluster-worker-isdead.js`, and `test-cluster-worker-isconnected.js`, the Node22 loader-context denominator rises from `198 / 155 / 160` to `202 / 155 / 160`; the Node20 and Node24 counts are carried forward unchanged because this promotion is intentionally Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_cluster_worker_foundation_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `202` official files green; `cargo fmt --all --check` → clean; `make clippy` → passed with only preexisting sibling Deno dead-code warnings; `git diff --check` → clean |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-not-started.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-return-on-exit.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-stdio.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/wasi.ts` | Closed the first Node22-primary executable `node:wasi` wave and promoted it through the manifested loader-context denominator. The owner fix stayed in the shared Deno `node:wasi` path instead of weakening the upstream fixtures: imported WASI calls now throw Node-shaped `ERR_WASI_NOT_STARTED` until `wasi.start()` wires memory, argv marshalling is real, logical WASI stdio descriptors map to the caller-provided `stdin` / `stdout` / `stderr` host fds, and the staged wasm payloads now have the first real fd `read` / `write` / `fdstat` / `seek` shell they need. Focused proof first narrowed the batch to the true stdio seam, then the final fd-mapping fix cleared `test-wasi-not-started.js`, `test-return-on-exit.js`, and `test-wasi-stdio.js` together on a clean rerun. After promoting that three-file Node22-primary wave, the broad loader-context replays now hold at `205 / 155 / 160`. The remaining WASI truth is narrower and more trustworthy: broader preopen/path/filesystem-backed behavior still stays explicit outside the denominator. | `cargo test -p neovex-runtime node22_nlc9_wasi_execution_batch_fixture -- --nocapture --test-threads=1` → first reclassified the batch from a broad import gap to the live stdio seam, then passed cleanly after the logical-stdio-to-host-fd owner fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `205` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/wasi.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/child_process.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi/input.txt`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi/input2.txt`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi/notadir`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-main_args.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-write_file.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-stat.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-readdir.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-notdir.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/main_args.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/write_file.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/stat.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/readdir.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/notdir.wasm`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the next coherent Node22-primary `node:wasi` slice as a shared filesystem/preopen foundation batch instead of mixing it with clocks, entropy, sockets, or threads. The staging is now resumable: the shared `common/wasi.js` helper, `fixtures/wasi-preview-1.js` child harness, `/sandbox` fixture directory, and five wasm/test pairs (`main_args`, `write_file`, `stat`, `readdir`, `notdir`) are all checked in. The classification result is already sharp and useful. This is not yet a real WASI filesystem syscall failure pocket. All five staged files stop at the same earlier seam because the shared `test/common/wasi.js` helper spawns the child with `--turbo-fast-api-calls`, and the Neovex subprocess bridge still rejects that flag outright. That means the next owner target is the compat subprocess-profile contract, not more speculative WASI syscall work. No denominator changed here. | `cargo test -p neovex-runtime node22_nlc9_wasi_filesystem_foundation_batch_fixture -- --nocapture --test-threads=1` → first failed on missing staged `test/common/child_process.js`, then re-ran and classified all five files to the same subprocess-profile boundary: `Error: unsupported node_compat subprocess flag '--turbo-fast-api-calls'` from the shared `wasi-preview-1.js` child path |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed and promoted the first Node22-primary `node:wasi` filesystem/preopen foundation wave after fixing the real shared owner seam. The upstream files themselves were already a coherent slice; the only blocker was earlier in the compat subprocess bridge, which still rejected Node's `--turbo-fast-api-calls` / `--no-turbo-fast-api-calls` flag pair used by the shared `test/common/wasi.js` helper. Once the subprocess path admitted those CLI flags, the staged upstream fixtures executed against the already-landed argv/preopen/fd shell and came back green without weakening the tests. That promoted `test-wasi-main_args.js`, `test-wasi-write_file.js`, `test-wasi-stat.js`, `test-wasi-readdir.js`, and `test-wasi-notdir.js` into the Node22-primary denominator and raised the carried loader-context truth from `205 / 155 / 160` to `210 / 155 / 160`. The broader WASI remainder is now narrower and more trustworthy again: the live boundary is no longer the subprocess-profile contract, but the broader preopen/path/filesystem/import-backed surface beyond this first foundation wave. | `cargo test -p neovex-runtime node22_nlc9_wasi_filesystem_foundation_batch_fixture -- --nocapture --test-threads=1` → first classified the batch to the shared subprocess-flag boundary, then passed cleanly after the compat subprocess parser fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `210` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-io.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-preopen_populates.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-fd_prestat_get_refresh.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-cant_dotdot.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/freopen.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/read_file.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/read_file_twice.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/preopen_populates.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/fd_prestat_get_refresh.wasm`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/cant_dotdot.wasm`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the next coherent `node:wasi` slice as a Node22-primary preopen/file-IO continuation batch instead of jumping to sockets, clocks, or threads. The classification result is already much sharper than the old generic “broader WASI” label. Three files are green in focused proof immediately: `test-wasi-preopen_populates.js`, `test-wasi-fd_prestat_get_refresh.js`, and `test-wasi-cant_dotdot.js`. The only live red is `test-wasi-io.js`, and even there the failure pocket is narrower than “file IO doesn’t work”: the first `freopen` subcase exits with status `0` but produces empty stdout instead of `hello from input2.txt\n`. Import inspection explains why that is the most likely owner seam. Compared with the rest of the slice, `freopen.wasm` is the only payload that newly imports `fd_renumber`, so the active next investigation target is the reopen/renumber path rather than the broader preopen metadata surface. No denominator changed here. | `cargo test -p neovex-runtime node22_nlc9_wasi_preopen_io_batch_fixture -- --nocapture --test-threads=1` → `1` failing fixture, only `test-wasi-io.js`; `node -e "const fs=require('fs'); const p='crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/wasm/freopen.wasm'; const m=new WebAssembly.Module(fs.readFileSync(p)); console.log(JSON.stringify(WebAssembly.Module.imports(m), null, 2));"` → shows the same core preopen imports as the green peers plus unique `fd_renumber`; matching import inspection for `cant_dotdot.wasm` and `read_file.wasm` confirms `fd_renumber` is the new differentiator in the red pocket |
| 2026-05-09 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-freopen-only.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/wasi/test-wasi-read-file-only.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Added two local focused controls to test whether the live `test-wasi-io.js` red was really `freopen`-specific. That control run materially changed the seam map. `test-wasi-read-file-only.js` fails the same way as `test-wasi-freopen-only.js`: both subprocesses exit with status `0` and empty captured stdout instead of the expected file contents. That falsifies the earlier narrow `fd_renumber` hypothesis as the sole explanation. The green official peers still matter: `test-wasi-preopen_populates.js`, `test-wasi-fd_prestat_get_refresh.js`, and `test-wasi-cant_dotdot.js` remain focused-green, so the active owner pocket is now the file-backed WASI stdout data path inside the compat subprocess / host-fd capture contract, not the broader preopen metadata surface. No denominator changed here. | `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` → `2` failing fixtures, `test-wasi-freopen-only.js` and `test-wasi-read-file-only.js`, both with status `0` and empty captured stdout instead of expected file contents |
| 2026-05-09 | NLC9 | `in_progress` | `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/wasi.ts`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js` | Took one more honest diagnostic pass on the live Node22-only `node:wasi` preopen/file-IO remainder instead of promoting guesses. The new evidence is stronger than the earlier “stdout/capture” label. Inside the embedded child runtime, the staged WASM payloads still report the full `wasi_snapshot_preview1` import set they need: `fd_close`, `fd_fdstat_get`, `fd_fdstat_set_flags`, `fd_prestat_get`, `fd_prestat_dir_name`, `fd_read`, `fd_seek`, `fd_write`, `path_open`, `proc_exit`, plus `fd_renumber` for `freopen.wasm`. But the live JS `node:wasi` object only exposes the smaller 8-function surface (`args_get`, `args_sizes_get`, `fd_close`, `fd_fdstat_get`, `fd_read`, `fd_seek`, `fd_write`, `proc_exit`), and temporary owner-side probes confirmed those JS `fd_read` / `fd_write` hooks do not drive the red `read_file` / `freopen` payloads before they fall through to the same empty-stdout status-0 result. I backed every temporary probe out and left only the sharper control-plane truth behind. No denominator changed here. | `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` → still `2` failing local controls with status `0` and empty stdout; temporary child-fixture import logging inside `wasi-preview-1.js` showed `freopen.wasm` imports `fd_close`, `fd_fdstat_get`, `fd_fdstat_set_flags`, `fd_prestat_get`, `fd_prestat_dir_name`, `fd_read`, `fd_renumber`, `fd_seek`, `fd_write`, `path_open`, `proc_exit`, while `read_file.wasm` imports the same set minus `fd_renumber`; temporary JS `node:wasi` logging inside `~/src/github.com/agentstation/deno/ext/node/polyfills/wasi.ts` showed the live `wasiImport` object still only exposes `args_get`, `args_sizes_get`, `fd_close`, `fd_fdstat_get`, `fd_read`, `fd_seek`, `fd_write`, `proc_exit`, which is why the next owner seam is now the embedded preview1 import-resolution layer below the JS polyfill rather than more JS-only patching |
| 2026-05-10 | NLC9 | `in_progress` | `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js` | Tightened the same Node22-only `node:wasi` preopen/file-IO seam one level further without landing speculative owner code. The strongest new signal is that the live original `wasiImport` object behaves differently from every plain-object equivalent even though the visible JS surface is the same. In the embedded child runtime, the staged WASM payloads still instantiate and run to the empty-stdout status-0 result when passed the original live `wasiImport` object, but three control variants all fail with the expected `LinkError`: an empty `wasi_snapshot_preview1` object, a plain hand-built 8-function object matching the visible JS surface, and a shallow clone of the live `wasiImport` object itself. That means the remaining owner seam is not just “missing visible imports” or generic stdout capture; it now points at hidden object identity/brand behavior on the original live `wasiImport` object in the embedded preview1 import-resolution path. I backed the temporary child-fixture probes back out after recording the result. No denominator changed here. | Temporary focused reruns of `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` showed: original live `wasiImport` path still reaches status `0` with empty stdout; `WebAssembly.instantiate(wasmModule, { wasi_snapshot_preview1: {} })` fails with `LinkError: Import #0 "fd_close": function import requires a callable`; a plain hand-built 8-function object fails with `LinkError: Import #2 "fd_fdstat_set_flags": function import requires a callable`; and a shallow clone of the live `wasiImport` surface fails with that same `Import #2 "fd_fdstat_set_flags"` `LinkError`, proving the difference is tied to hidden identity/brand on the original object rather than the visible JS properties |
| 2026-05-10 | NLC9 | `in_progress` | `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took one more lower-layer diagnostic pass on the same Node22-only `node:wasi` preopen/file-IO pocket after the original-object/clone split was already proven. The follow-up result removes the last plausible visible-JS explanation: the live `wasiImport` object is an ordinary `Object.prototype` object with no hidden preview1 methods on itself or its prototype, a speculative Deno-owner override on the visible JS `fd_write` surface left the red pocket unchanged and was backed out, and temporary child-fixture probes immediately around the original-object instantiate/start path never observed the expected post-instantiate/post-start markers before they were backed out too. The remaining `test-wasi-io.js` blocker is therefore now explicitly below the JS property layer in the embedded preview1 runtime path itself rather than another plain `node:wasi` polyfill seam. No denominator changed here. | `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` → still `2` failing local controls with status `0` and empty stdout; temporary child-fixture prototype probe showed `Object.getPrototypeOf(wasiImport) === Object.prototype`, `Object.getOwnPropertyNames(wasiImport)` stayed the same visible `8`-function surface, and `'fd_fdstat_set_flags' in wasiImport`, `'path_open' in wasiImport`, and `'fd_prestat_get' in wasiImport` all remained false; temporary Deno-owner `fd_write` override left the same two red controls unchanged before it was backed out; `git -C /Users/jack/src/github.com/agentstation/deno diff --check -- ext/node/polyfills/wasi.ts` → clean after backing the owner probe out; `rg -n "neovex-wasi-probe|file-stdout|before-start|after-start" crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js` → no matches after cleanup |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-events.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-exit.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-disconnect.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-forced-exit.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-kill.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Started the next coherent Node22-only `cluster` slice instead of staying pinned to the lower-layer WASI blocker: a 5-file worker lifecycle/teardown wave. The first classification pass is already useful and honest even without promotion. `test-cluster-worker-events.js` exposed a real worker-side exit bridge seam: worker `process.exit()` still falls through `process.reallyExit()` to a missing `Deno.exit` path. Restoring the staged upstream `common.isAlive()` helper removed the next fixture gap and moved `test-cluster-worker-exit.js` onto a real lifecycle mismatch, where the expected disconnect/exit must-calls still do not all arrive. `test-cluster-worker-disconnect.js` currently hangs in teardown, which blocks the last two lifecycle files from classifying in the same batch replay. No denominator changed here, but the broader post-foundation cluster remainder is now a concrete lifecycle seam map instead of an unstaged unknown. | `cargo test -p neovex-runtime node22_nlc9_cluster_worker_lifecycle_batch_fixture -- --nocapture --test-threads=1` → first classified `test-cluster-worker-events.js` to `process.reallyExit()` / missing-`Deno.exit` and `test-cluster-worker-exit.js` to a must-call settle mismatch, then hung on `test-cluster-worker-disconnect.js`; restoring `common.isAlive()` removed the earlier `TypeError: common.isAlive is not a function` gap and kept `test-cluster-worker-exit.js` on the real lifecycle seam on replay; host `ps -ax` confirmation showed the replay was genuinely hung before it was stopped so the worktree could continue serialized; `git diff --check -- crates/neovex-runtime/src/runtime/tests/node_compat.rs crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-events.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-exit.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-disconnect.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-forced-exit.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-cluster-worker-kill.js docs/architecture/runtime/node-lts-compat/failures/loader-context.md docs/plans/node-lts-compatibility-plan.md docs/plans/node-lts-compatibility-execution-log.md` |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed and promoted the first Node22-primary `node:cluster` worker lifecycle/teardown wave after fixing the real emulated-fork owner seams instead of weakening the upstream files. The retained harness work stayed local where the behavior diverged: the primary-side cluster shim now preserves Node-shaped worker `state` / `exitedAfterDisconnect` transitions before `disconnect` and `exit` events fire, the emulated fork child now distinguishes raw `child_process.disconnect()` from `cluster.Worker.disconnect()`, natural child exit now carries a parent-observed disconnect before exit, signal kills now surface disconnect plus `signalCode`, and the child side now forwards `listening` back to the primary worker so the staged lifecycle files can execute their real contracts. The scoped `process.reallyExit()` sentinel is now applied only to the two primary-side cluster fixtures that use `process.exit()` purely as a completion signal. With those seams closed, `test-cluster-worker-events.js`, `test-cluster-worker-exit.js`, `test-cluster-worker-disconnect.js`, `test-cluster-worker-forced-exit.js`, and `test-cluster-worker-kill.js` all passed in focused proof and promoted into the Node22-primary loader-context denominator, raising the carried truth from `210 / 155 / 160` to `215 / 155 / 160`. The next sharp `NLC9` owner seam is back on the lower-layer `node:wasi` preview1 import-resolution boundary, not `cluster`. | `cargo test -p neovex-runtime node22_nlc9_cluster_worker_lifecycle_batch_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `215` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-10 | NLC9 | `in_progress` | `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js` | Corrected the stale `node:wasi` preopen/file-IO seam story instead of chasing the wrong owner layer. A temporary child-fixture probe proved the live `wasiImport` object still has only the visible 8-function surface and no symbol keys, but it also disproved the earlier hidden-object-identity theory: both a descriptor-preserving clone and a plain spread clone of that same visible surface now instantiate and run to the same empty-stdout status-`0` outcome as the original object. The follow-up visible-I/O probe sharpened the real boundary further: wrapping the visible JS `fd_read` and `fd_write` functions produced no markers at all in the failing `freopen` and `read_file` controls before the probe was backed out, so the red pocket is executing below the visible `node:wasi` JS I/O hooks. The last instantiate-path marker pass made that sharper again: the failing controls reach `before_instantiate`, but never reach `after_instantiate` or `before_start` before the child exits `0`, so the live seam is now specifically in the embedded preview1 `WebAssembly.instantiate(...)` path for the file-backed WASI payloads rather than in a post-start stdout capture path. No denominator changed here; this row exists to keep the control plane truthful and to steer the next owner work back toward the embedded preview1 runtime path itself instead of more stale clone/identity or JS-only WASI patching. | Temporary focused reruns of `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` showed: the shape probe surfaced only `args_get`, `args_sizes_get`, `fd_close`, `fd_fdstat_get`, `fd_read`, `fd_seek`, `fd_write`, and `proc_exit` with `symbolCount: 0`; the descriptor-preserving clone probe still reached status `0` with empty stdout for both `freopen` and `read_file`; the plain spread-clone probe reproduced that same status-`0` / empty-stdout outcome; the visible-I/O wrapper probe produced no `fd_read` / `fd_write` markers at all before being backed out; the final instantiate marker probe printed only `__neovex_before_instantiate__` for both local reds and never reached `__neovex_after_instantiate__` or `__neovex_before_start__`; and a final clean replay after probe removal still showed the same two local reds with empty stdout. `git diff --check -- crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js` → clean after backing the probe out |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/wasi.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Corrected the script-mode compat child harness so staged top-level async Node fixtures cannot falsely look green just because `runMain()` returned before the main promise settled. The harness now explicitly awaits `globalThis.__neovexMainScriptPromise` when a staged script exposes one, `wasi-preview-1.js` publishes that promise, and the shared WASI helper order is back to the upstream turbo-first / no-turbo-second contract. That correction materially changed the live `node:wasi` truth: the old Node22-primary filesystem/preopen wave was overstated. After rerunning the coherent focused batches, only `test-wasi-main_args.js` remains promotable from that broader wave. The broader pocket is now classified by exact preview1 import gaps instead of empty-stdout guesses: `test-wasi-write_file.js`, `test-wasi-io.js`, `test-wasi-cant_dotdot.js`, plus the local `freopen` and `read_file` controls all stop at missing `fd_fdstat_set_flags`; `test-wasi-stat.js` stops at missing `fd_filestat_get`; `test-wasi-readdir.js` stops at missing `environ_get`; and `test-wasi-notdir.js` stops at missing `fd_prestat_get`. To keep the control plane truthful, the four false-promoted Node22-only filesystem files were removed from the manifested denominator, narrowing the carried loader-context truth from `215 / 155 / 160` to `211 / 155 / 160` pending broad-lane replay on the corrected harness. | `cargo test -p neovex-runtime node22_nlc9_wasi_execution_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` → `2` failing local controls, both now failing during `WebAssembly.instantiate(...)` on missing `fd_fdstat_set_flags`; `cargo test -p neovex-runtime node22_nlc9_wasi_preopen_io_batch_fixture -- --nocapture --test-threads=1` → `test-wasi-preopen_populates.js` and `test-wasi-fd_prestat_get_refresh.js` green, `test-wasi-io.js` and `test-wasi-cant_dotdot.js` red on missing `fd_fdstat_set_flags`; `cargo test -p neovex-runtime node22_nlc9_wasi_filesystem_foundation_batch_fixture -- --nocapture --test-threads=1` → `test-wasi-main_args.js` green, `test-wasi-write_file.js` red on missing `fd_fdstat_set_flags`, `test-wasi-stat.js` red on missing `fd_filestat_get`, `test-wasi-readdir.js` red on missing `environ_get`, `test-wasi-notdir.js` red on missing `fd_prestat_get`; `git diff --check -- crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs crates/neovex-runtime/src/runtime/tests/node_compat.rs crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/wasi.js crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js docs/architecture/runtime/node-lts-compat/failures/loader-context.md docs/plans/node-lts-compatibility-plan.md docs/plans/node-lts-compatibility-execution-log.md` |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the last control-plane drift from the same WASI reclassification pass. The first harness correction had narrowed the docs and the focused batch, but the broad Node22 manifested subset still directly carried the four old filesystem entries, so the first replay still surfaced them. Removing that duplicate inclusion point from `node_compat.rs` made the manifested batch definition match the narrowed support claim. Fresh broad replays on the corrected state are now green again at `211 / 155 / 160`, so the plan, manifest, failure inventory, public surface matrix, and the executable denominator are back in sync on the same truthful WASI contract: validation, executable basics, and `test-wasi-main_args.js` are promoted; the broader preview1 filesystem/import surface remains explicit. | `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `211` official files green after removing the stale direct inclusion; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/wasi.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/wasi-preview-1.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/wasi.ts` | Closed and promoted the broader Node22-primary `node:wasi` filesystem and preopen/file-IO waves instead of leaving them as a lingering explicit boundary. The shared Deno owner fixes filled the missing preview1 fd/path/import surface, added the permissive host-open fallback when libc passes zero rights, restored Node-shaped rights propagation for preopens, directories, and files, and corrected file-backed stdio routing so only logical stdio mapped to real host fds `1` / `2` uses `process.stdout` / `process.stderr`. With that owner path in place, the focused filesystem batch, the focused preopen/file-IO batch, the executable batch, and the local `freopen` / `read_file` controls all went green, and the promoted Node22 denominator widened from `211 / 155 / 160` to `219 / 155 / 160` without reopening the Node20 or Node24 lanes. The control plane now reflects the broader truthful Node22 WASI support story: validation, executable basics, argv, filesystem, and preopen/file-IO behavior are promoted, while broader unstaged `node:wasi` families remain future work by omission rather than an active explicit failure pocket. | `cargo test -p neovex-runtime node22_nlc9_wasi_filesystem_foundation_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc9_wasi_preopen_io_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc9_wasi_execution_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc9_wasi_io_subcase_watchpoint_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `219` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-aliases.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-typechecking.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-custom-assertions.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the first coherent `node:test` family slice under `NLC9` instead of leaving the completion-gate family unstaged. The first three-file Node22-primary foundation batch already produced a clean seam map. `test-runner-aliases.js` is green and is now promoted into the broad loader-context denominator, proving the first narrow `node:test` export-alias contract. The two remaining files are explicit rather than fuzzy: `test-runner-typechecking.js` still fails because the compat script-mode runner leaves the upstream returned promise pending instead of executing and settling the registered tests through a Node-shaped script-mode runner path, while `test-runner-custom-assertions.js` fails immediately because the module-level `node:test`.`assert.register` contract is still missing. The broad Node22 denominator therefore widens from `219 / 155 / 160` to `220 / 155 / 160` without overstating broader `node:test` support. | `cargo test -p neovex-runtime node22_nlc9_test_runner_foundation_batch_fixture -- --nocapture --test-threads=1` → classified `1` green fixture (`test-runner-aliases.js`) and `2` explicit failures (`test-runner-typechecking.js` pending-promise runner seam, `test-runner-custom-assertions.js` missing `assert.register` seam); `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `220` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-get-test-context.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-assert.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Widened the same Node22-primary `node:test` slice with the next two helper-surface files instead of guessing at an owner fix from the first three-file batch alone. That replay did not yield another promotable file, but it materially sharpened the family map. `test-runner-get-test-context.js` proves the public `node:test.getTestContext()` export is still missing entirely, not just missing some fields, and `test-runner-assert.js` proves the current `t.assert` surface still lacks the Node-shaped helper keys `CallTracker`, `snapshot`, and `fileSnapshot` even though the copied core assertion methods are present. With those two files added to the same batch, the remaining staged `node:test` pocket is now four explicit seams: script-mode promise settlement, module-level `assert.register`, current-context export/propagation, and the helper-key surface on `t.assert`. No denominator changed here. | `cargo test -p neovex-runtime node22_nlc9_test_runner_foundation_batch_fixture -- --nocapture --test-threads=1` → `1` green fixture (`test-runner-aliases.js`) and `4` explicit failures: `test-runner-typechecking.js` pending-promise runner seam, `test-runner-custom-assertions.js` missing module-level `assert.register`, `test-runner-get-test-context.js` missing `getTestContext()` export, and `test-runner-assert.js` missing `CallTracker` / `snapshot` / `fileSnapshot` on `t.assert` |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-get-test-context.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-assert.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Closed the remaining staged Node22-primary `node:test` helper seams on the shared Deno owner path instead of leaving the family half-classified. The owner patch made top-level ignored tests settle the returned promise in the checked-in compat runner, added the shared module-level `node:test`.`assert.register` registry, exposed `getTestContext()`, made current test context propagate across awaited test bodies and subtests, added plan-counted `t.assert` wrappers with the Node-shaped helper keys `CallTracker`, `snapshot`, and `fileSnapshot`, and restored the built-in `ok(...)` stack-source behavior through the existing `innerOk(...)` helper. With those owner fixes in place, the full five-file helper wave (`test-runner-aliases.js`, `test-runner-typechecking.js`, `test-runner-custom-assertions.js`, `test-runner-get-test-context.js`, and `test-runner-assert.js`) went green and was promoted into the broad Node22 loader-context denominator. The carried truth is now `224 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_foundation_batch_fixture -- --nocapture --test-threads=1` → passed with all `5` staged `node:test` helper fixtures green; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `224` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-test-fullname.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-test-filepath.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Widened the same Node22-primary `node:test` family with the next direct context-metadata slice instead of jumping to `test.run()` or CLI-mode coverage. The first classification replay immediately split the batch into two nearby owner seams: module-level `before` was still missing from the public `node:test` export object, and suite callbacks still were not receiving or propagating a parent context, which flattened `fullName` and left the basic metadata surface incomplete. The shared Deno owner patch closed that whole cluster in one pass by wiring `before` / `after` onto the public `test` export, implementing root-hook registration and once-only root hook execution around the checked-in compat harness, injecting suite context, propagating the suite context as the parent for nested tests and nested suites, and adding the current basic metadata getters needed by upstream (`filePath`, `passed`, `attempt`, and the existing `diagnostic()` / `fullName()` contract). With those fixes in place, `test-runner-test-fullname.js` and `test-runner-test-filepath.js` both went green in focused proof and were promoted into the broad Node22 loader-context denominator. The carried truth is now `226 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_context_metadata_batch_fixture -- --nocapture --test-threads=1` → first classified `before` export wiring plus suite-parent-context propagation, then passed with both staged fixtures green after the owner fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `226` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-test-id.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-filetest-location.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/index.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/test-id-fixture.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Took the next coherent `node:test` slice as the first in-process `test.run()` event-stream metadata batch instead of jumping to the larger planning, reporter, or CLI families. The first classification replay first flushed out missing staged payloads, then reduced to one shared owner seam: `test.run()` was still a hard stub. The shared Deno owner patch replaced that stub with a narrow in-process file-backed runner for the checked-in compat harness: it now returns an event stream with `on(...)` plus async iteration, defers execution so listeners attach before any events fire, loads file-backed CJS fixtures through `createRequire(...)`, runs them against the existing embedded `Deno.test` queue, emits per-test `enqueue` / `dequeue` / `start` / `pass` / `fail` / `complete` events with stable `testId`s, and surfaces root file-load failures with the Node-shaped location metadata expected by upstream. It also stopped treating normal failing tests inside `run()` as root file-load failures, so intentional red tests like the `e2e` subtest in `test-id-fixture.js` now stay inside the event stream instead of collapsing the whole run. With those owner fixes in place, `test-runner-test-id.js` and `test-runner-filetest-location.js` both went green in focused proof and were promoted into the broad Node22 loader-context denominator. The carried truth is now `228 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_run_event_metadata_batch_fixture -- --nocapture --test-threads=1` → first classified missing staged `test/fixtures/test-runner/*` payloads, then classified the live `test.run()` owner seam, then passed with both staged fixtures green after the owner fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `228` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-option-validation.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Widened the same Node22-primary `node:test` family with the first direct runner-option validation slice instead of jumping ahead to planning, reporter, or CLI semantics. The focused replay proved there was a real quick win here: upstream `test-runner-option-validation.js` narrowed cleanly to `prepareOptions(...)` missing the Node-shaped validation contract for `timeout` and `concurrency`. The shared Deno owner patch now validates finite numeric `timeout` values with the expected range semantics, accepts `Infinity` as the upstream fixture requires, and validates `concurrency` as either a boolean or an unsigned integer through the same option-preparation path used by the existing compat runner. With that owner fix in place, `test-runner-option-validation.js` went green in focused proof, was promoted into the broad Node22 loader-context denominator, and turned the current `node:test` support claim into a helper, context-metadata, `run()` event-metadata, and first narrow option-validation wave instead of leaving runner options as an all-or-nothing future bucket. The carried truth is now `229 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_option_validation_batch_fixture -- --nocapture --test-threads=1` → passed with the staged fixture green; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `229` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-plan.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/index.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/common/fixtures.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/plan/*.mjs`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Took the next coherent `node:test` slice as the first staged `t.plan()` / planning batch instead of leaping straight to reporter or CLI semantics. The first replay was a pure staging failure, so the batch was tightened before any owner claims: the ESM upstream file `test-runner-plan.mjs` needed `test/common/index.mjs`, `test/common/fixtures.mjs`, and the full `test/fixtures/test-runner/plan/*.mjs` payload set. Once that was in place, the batch yielded a real chain of owner seams instead of fuzzy red noise. The shared Deno owner path now validates `t.plan(count, options)` like upstream for `options` and `options.wait`, the Neovex restricted Node22 ESM builtin gate now admits the claimed `node:stream` family (`node:stream`, `node:stream/consumers`, `node:stream/promises`, and `node:stream/web`) for file-backed `.mjs` compat loads, and the suite-backed `node:test` execution path now supports callback-style tests that use `(t, done)` instead of silently dropping the callback. After those fixes the batch got far enough to expose the first live planning seam honestly: even the simple `match.mjs` subcase still emits `test:fail` unexpectedly through the current `test.run()` / `t.plan()` integration path, so the broader planning family is not promotable yet and the denominator stays `229 / 155 / 160`. | `cargo test -p neovex-runtime node22_nlc9_test_runner_plan_batch_fixture -- --nocapture --test-threads=1` → first failed on missing `test/common/index.mjs`; second failed on missing `t.plan()` input validation; third failed on the restricted ESM `node:stream` gate for `streaming.mjs`; fourth failed on missing suite-backed callback-style `done` support; latest clean replay still fails in the live planning seam because `match.mjs` unexpectedly emits `test:fail` through the current `test.run()` / `t.plan()` path even after the staging and owner fixes |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/module_loader.rs`, `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Closed and promoted the first Node22-primary `node:test` planning wave instead of leaving the family parked between focused proof and denominator truth. The final owner pass fixed the real shared seams in the Deno `node:test` bridge: `run()` executions are now serialized so concurrent outer tests cannot share one `activeRunState`; the shared `TestPlan` path now supports `options.plan`, `options.wait`, and the Node-shaped `t.plan()` contract; parent plans count subtest creation like upstream; delayed `wait:true` planning keeps the embedded event loop alive until the plan resolves or fails; custom assertion failures still count during synchronous test bodies; and `test.run()` now asks the embedded flush loop to continue across file-local failing tests so later planned files still execute and report their events. The Neovex side kept the earlier ESM `node:stream` allowlist widening and staged-fixture materialization, and the full planning file `test-runner-plan.mjs` is now promoted into the manifested Node22 denominator. A broad replay caught and then cleared one shared regression in `test-runner-custom-assertions.js`, which is why this row includes both the focused planning proof and the follow-up foundation rerun before the final broad-lane evidence. The carried truth is now `230 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_plan_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_nlc9_test_runner_foundation_batch_fixture -- --nocapture --test-threads=1` → passed after the custom-assertion counting fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `230` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-enqueue-file-syntax-error.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-run-files-undefined.mjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/syntax-error-test.mjs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/ext/node/polyfills/testing.ts` | Took the next adjacent `node:test.run()` edge slice instead of jumping to a new family and kept the batch-first classification honest. The first two-file focused batch (`test-runner-enqueue-file-syntax-error.js` plus `test-runner-run-files-undefined.mjs`) immediately split into two owner seams: the Deno `node:test` bridge was surfacing root file-load failures with a synthetic non-Error payload and no root-file enqueue event, and the Neovex subprocess bridge still rejected `--input-type=module` for `-e` eval children. Those two seams were fixed directly: `testing.ts` now preserves the real caught error object on root file-load failures, emits the root syntax-error enqueue/fail pair, and exposes a narrow `compose(...)` bridge on the run stream, while the Neovex subprocess parser now accepts `--input-type=module` and evaluates module-mode `-e` payloads through a generated `.mjs` path instead of plain `eval(...)`. The follow-up replay then cleanly separated the remaining held-out seam instead of papering it over: `test-runner-run-files-undefined.mjs` now narrows to the missing `node:test/reporters` builtin family rather than the earlier eval/profile gap, so it was moved into an explicit ignored watchpoint while the green syntax-error file-load contract was promoted into the Node22 denominator. The carried truth is now `231 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_run_edge_batch_fixture -- --nocapture --test-threads=1` → first classified the two-file batch to the root file-load error-shape / enqueue seam plus the `--input-type=module` subprocess seam; second replay narrowed to the root enqueue contract plus the missing `node:test/reporters` builtin family; final replay passed with the reduced syntax-error batch green; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `231` official files green; `cargo test -p neovex-runtime node22_nlc9_test_runner_reporters_watchpoint -- --nocapture --test-threads=1 --ignored` → expected failure on missing `node:test/reporters`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-import-no-scheme.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno/libs/node_resolver/builtin_modules.rs`, `~/src/github.com/agentstation/deno/libs/node_resolver/resolution.rs`, `~/src/github.com/agentstation/deno/libs/resolver/lib.rs`, `~/src/github.com/agentstation/deno/libs/resolver/graph.rs`, `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js` | Closed and promoted the first Node22-primary `node:test` reporter-edge wave instead of leaving it parked as a stale missing-builtin watchpoint. The focused two-file reporter batch (`test-runner-run-files-undefined.mjs` plus `test-runner-import-no-scheme.js`) first proved the shared Deno owner seam had moved from CommonJS to the ESM bare-builtin resolver path: CommonJS `require("test")` and `require.resolve("test")` were already fixed once the schemeless blocklist stopped forcing builtin substitution after `_findPath(...)`, but dynamic bare `import("test")` inside the compat eval child still short-circuited to the builtin before package lookup. The shared Deno owner fix made the Rust resolver distinguish full builtins from bare-importable builtins, added `test/reporters` to the supported builtin set for the `node:` path, and kept the JS require loader aligned with the same scheme-only contract. The Neovex subprocess owner fix then closed the last remaining child-bundle seam by awaiting returned thenables from the CommonJS `-e` eval shim, which let `import("test").then(...)` settle before the child exits. With those two seams closed and the temporary probes removed, the focused reporter batch went green and the broad Node22 manifested loader-context lane stayed green too, so `test-runner-run-files-undefined.mjs` and `test-runner-import-no-scheme.js` are now promoted into the carried denominator. The carried truth is now `233 / 155 / 160`; Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_reporters_batch_fixture -- --nocapture --test-threads=1` → initially reduced to the lone `import("test")` empty-stdout seam, then passed after the resolver and eval-shim fixes; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `233` official files green; temporary debug probes in `~/src/github.com/agentstation/deno/ext/node/polyfills/01_require.js` and `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-import-no-scheme.js` were removed before the final proof |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/default-behavior/index.test.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed and promoted the first Node22-primary `node:test` reporter-output wave instead of treating the remaining `test-runner-reporters.js` failure as a vague timeout or child-process flake. The first focused replay proved the staged corpus was still missing `test/fixtures/test-runner/default-behavior/index.test.js`; staging that file removed the last materialization gap but kept the same live seam: async custom-reporter failures were still escaping as raw runtime-level uncaught exceptions with exit code `1` instead of the Node-shaped reporter failure exit `7`. The owner fix stayed local and honest. The compat child harness in `test_runtime.rs` now arms explicit async-fatal capture for reporter runs across `uncaughtException`, `unhandledRejection`, and browser-style error events, holds the child open through a narrow late-settle window when reporter failure classification is armed, and maps those late async fatals back into the Node-shaped reporter stderr/exit contract. The Node22 bootstrap `setImmediate(...)` wrapper now also routes thrown callback errors through `process._fatalException(...)` before rethrowing, which keeps late async reporter throws on the same Node-shaped fatal path instead of leaking them as raw runtime failures. With those owner fixes in place, both `test-runner-reporters.js` and `test-runner-error-reporter.js` went green in focused proof and then stayed green through the broad carried-lane replays. The carried truth is now `235 / 155 / 160`; the Node20 validation and Node24 preview lanes both remained fully green, so this reporter-output promotion is safe across the shared harness. | `cargo test -p neovex-runtime node22_nlc9_test_runner_reporter_output_batch_fixture -- --nocapture --test-threads=1` → first isolated the remaining async custom-reporter exit-code seam after staging `test/fixtures/test-runner/default-behavior/index.test.js`, then passed with both `test-runner-reporters.js` and `test-runner-error-reporter.js` green after the owner fixes; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `235` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `155` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `160` staged official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed and promoted the first Node22-primary `node:test` CLI-options wave instead of leaving the synthetic `node --test` child path as a partially truthful future bucket. The focused batch around `test-runner-cli-concurrency.js` and `test-runner-cli-timeout.js` first proved the shared owner seam was the compat subprocess profile itself: synthetic `--test` mode still lacked default test-file discovery, did not parse `--test-timeout` or `--test-isolation`, and could not emit the `NODE_DEBUG=test_runner` diagnostics that upstream treats as part of the visible CLI contract. The local Neovex owner patch kept that work tightly scoped to the checked-in harness. The synthetic child bridge in `test_runtime.rs` now parses `--test-timeout[=N]` and `--test-isolation[=mode]`, carries those options through the `RuntimeTestSpawnMode::TestRunner` envelope, discovers the default `test/` corpus when no explicit patterns are supplied, and emits Node-shaped `TEST_RUNNER ...` configuration and worker-assignment diagnostics when `NODE_DEBUG=test_runner` is active. The same path now also handles the host `availableParallelism()` gap safely so those diagnostics and option-driven worker counts remain truthful inside the embedded runtime. With those fixes in place, the focused CLI-options batch went green and the broad Node22 manifested loader-context lane lifted cleanly from `235 / 155 / 160` to `237 / 155 / 160`. Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_cli_options_batch_fixture -- --nocapture --test-threads=1` → passed with `test-runner-cli-concurrency.js` and `test-runner-cli-timeout.js` green after the synthetic `--test` discovery / option / debug-contract fix; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `237` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/js/node22_runtime_bootstrap.js`, `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-cli-randomize.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/randomize/internal-order.cjs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/rerun-state.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/shards/*.cjs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed and promoted the first Node22-primary `node:test` CLI-randomize wave instead of leaving seeded ordering parked as an unstaged future seam after the CLI-options work. The staged upstream file `test-runner-cli-randomize.js` first proved the materialization side was sufficient and the top owner seam was the synthetic child profile itself: `--test-randomize` and `--test-random-seed` were still rejected as unsupported flags before any runner semantics could execute. The local Neovex owner fix kept that work on the checked-in synthetic `--test` path. `test_runtime.rs` now parses `--test-randomize`, `--test-random-seed`, `--watch`, and `--test-rerun-failures`; validates out-of-range `--test-random-seed` values with the Node-shaped CLI message; carries the seed/watch/rerun profile through `RuntimeTestSpawnMode::TestRunner`; shuffles discovered test files deterministically when randomization is active; emits the TAP seed banner; and gives this nested-subprocess `node:test` sweep the same honest longer compat runtime window already used by the reporter sweep. The embedded Node22 bootstrap now also honors a narrow compat-only root-test randomization side channel so `require("node:test").run(...)` can replay a file’s queued root tests in deterministic seeded order without widening the broader builtin claim. The follow-up replay then exposed and closed the remaining lifecycle seam honestly: the synthetic child still was not surfacing `process.on('exit')` output from the staged `internal-order.cjs` fixture, so the local child runner now emits a final `beforeExit` / `exit` cycle after its reporter loop. With those fixes in place, the focused randomize batch went green and the broad Node22 manifested loader-context lane lifted cleanly from `237 / 155 / 160` to `238 / 155 / 160`. Node20 and Node24 remain carried forward unchanged because this promotion is Node22-primary only. | `cargo test -p neovex-runtime node22_nlc9_test_runner_cli_randomize_batch_fixture -- --nocapture --test-threads=1` → first classified the unsupported `--test-randomize` flag seam, then narrowed to the missing `process.on('exit')` marker after the seed/shuffle/conflict fixes, then passed once the synthetic child emitted the final lifecycle drain under the longer honest runtime window; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `238` official files green |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/parallel/test-runner-test-rerun-failures.js`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/test/fixtures/test-runner/rerun.js`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Staged the next adjacent Node22-primary `node:test` CLI watchpoint, `test-runner-test-rerun-failures.js`, and used it to classify the next owner seam instead of guessing from the earlier randomize wave. The first replay proved the compat child parser still treated `--test-rerun-failures <file> <fixture>` as plain script mode, so the local synthetic child path now promotes the test-runner-only flags (`--test-reporter*`, `--test-concurrency`, `--test-timeout`, `--test-randomize`, `--test-random-seed`, `--test-rerun-failures`, and `--test-isolation`) into test-runner mode even without an explicit `--test`. The follow-up replay materially sharpened the remaining gap: the child now runs under the test runner and produces Node-shaped reporter output, but the broader rerun-state contract is still missing. The live seams are shared rerun-attempt/state-file support in `testing.ts`, continued sibling subtest execution after a failing child, and the local synthetic summary emission of suite counts. No denominator changed here; the file remains an explicit watchpoint rather than a fuzzy red. | `cargo test -p neovex-runtime node22_nlc9_test_runner_cli_rerun_failures_batch_fixture -- --nocapture --test-threads=1` → first classified plain-script mode on `--test-rerun-failures` without `--test`, then reclassified to the deeper rerun-state seam after the parser fix, with live output proving the current runner still omits one passing sibling subtest and does not emit the expected `suites 2` summary line |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node20/test/parallel/test-os-constants-signals.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Finished the last staged cross-lane `node:constants` materialization gap instead of leaving the family artificially half-open. The earlier widening had already proved `test-constants.js`, `test-binding-constants.js`, `test-process-constants-noatime.js`, and `test-uv-binding-constant.js` across the non-Node22 lanes, but `test-os-constants-signals.js` was still excluded on Node20 only because the lane-local fixture copy had never been staged. Adding the official Node20 fixture, wiring it into both the focused constants batch and the broad manifested loader-context batch, and replaying the proofs showed there was no deeper runtime seam hiding behind that omission. The focused Node20 constants batch stayed fully green, and the broad Node20 validation lane lifted cleanly from `158` to `159` without changing the existing Node22 `238` or Node24 `163` truths. The staged five-file `node:constants` tranche is now green across all three carried lanes, so it no longer belongs in the active `NLC9` remainder. | `cargo test -p neovex-runtime node20_nlc9_constants_foundation_batch_fixture -- --nocapture --test-threads=1` → passed with `test-os-constants-signals.js` included; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `159` official files green and the constants tranche carried in the live denominator |
| 2026-05-10 | NLC9 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/node24/test/parallel/test-domain-promise.js`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the staged `node:domain` family cross-lane instead of leaving it as a stale Node22-only tranche. The source-truth sweep showed the widening was mostly materialization, not new runtime work: all 16 official Node20 files match the staged Node22 copies byte-for-byte, and only the Node24 `test-domain-promise.js` file differs. I widened the domain batch and the manifested loader-context batch accordingly, reusing the shared official Node22 fixture path for the unchanged files and staging only the Node24-specific promise fixture. The focused Node20 and Node24 domain foundation replays both stayed fully green, which proved there was no hidden cross-version runtime seam behind the omission. The broad carried lanes then lifted cleanly from `238 / 159 / 163` to `238 / 175 / 179`, so the 16-file `node:domain` tranche is now green across all three carried lanes and no longer belongs in the active `NLC9` remainder. | `cargo test -p neovex-runtime node20_nlc9_domain_foundation_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node24_nlc9_domain_foundation_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `175` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `179` staged official files green |
| 2026-05-10 | NLC9 | `done` | `crates/neovex-runtime/src/runtime/bootstrap/ops/test_runtime.rs`, `docs/architecture/runtime/node-lts-compat/manifests/loader-context.md`, `docs/architecture/runtime/node-lts-compat/failures/loader-context.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed `NLC9` truthfully instead of forcing the last staged `node:test` CLI rerun file through a leaky harness. The earlier shared `testing.ts` rerun-state owner work had already made `test-runner-test-rerun-failures.js` promotable, but the first broad Node22 replay after that promotion exposed a different local regression: the synthetic `node --test` child bridge in `test_runtime.rs` was still passing `rerunFailuresFilePath: ""` into ordinary reporter and CLI child runs, which forced unrelated `node:test` reporter/CLI files down the rerun-failures path and triggered the late `Promise resolution is still pending but the event loop has already resolved` cluster. Making that run option conditional closed the shared local leak without weakening any upstream fixtures. The focused `node:test` reporter-output and CLI-randomize batches went green again, the broad Node22/Node20/Node24 loader-context lanes all replayed cleanly at `239 / 175 / 179`, and the control-plane inventory now shows the long-tail families in a truthful closed state: `cluster`, `repl`, `test`, `sqlite`, `wasi`, `sea`, `domain`, `trace_events`, `sys`, and `constants` each have checked-in manifests, explicit failure inventories, and measured support claims that match reality. `NLC9` is therefore complete, and `NLC10` is now the active control item for dashboards, canaries, framework smoke, and public closeout. | `cargo test -p neovex-runtime node22_nlc9_test_runner_reporter_output_batch_fixture -- --nocapture --test-threads=1` → passed after the conditional `rerunFailuresFilePath` fix; `cargo test -p neovex-runtime node22_nlc9_test_runner_cli_randomize_batch_fixture -- --nocapture --test-threads=1` → passed; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `239` official files green; `cargo test -p neovex-runtime node20_validation_lane_executes_official_loader_context_subset -- --nocapture --test-threads=1` → `1 passed` with `175` official files green; `cargo test -p neovex-runtime node24_preview_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1 --ignored` → `1 passed` with `179` staged official files green |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_metadata.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/schema.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/lanes/node20.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/lanes/node22.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/lanes/node24.json`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Started `NLC10` with a concrete evidence-layer slice instead of leaving the closeout phase as a pure roadmap heading. The first activation step is machine-readable lane provenance for the existing carried Node fixture lines: a checked-in schema plus explicit Node20 / Node22 / Node24 lane metadata now records the pinned upstream tags, lane roles, public-contract roles, runtime execution target, runtime-limits profile, vendored fixture roots, and the live family manifest/failure docs that those lanes feed today. The new runtime test keeps this honest by parsing the schema and all three lane files and asserting that the vendored fixture roots and cited docs actually exist in the live worktree. In parallel, the companion `node-compat-test-infrastructure` plan is now active at `Phase 1.1` rather than remaining a passive research memo. This does not yet claim per-family manifest-driven execution or dashboards, but it establishes the first checked-in NLC10 corpus-metadata substrate that later per-module slices, reports, and canaries can consume. | `cargo test -p neovex-runtime node_compat_lane_metadata_schema_is_valid_json_and_documents_required_fields -- --nocapture --test-threads=1`; `cargo test -p neovex-runtime node_compat_lane_metadata_files_parse_and_point_at_real_roots -- --nocapture --test-threads=1`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_metadata.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/schema.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/preludes.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/core-semantics.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/process-and-timing.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/streams-and-local-io.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/networking.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/loader-context.json`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Advanced the first real Phase 1.1 topology slice instead of leaving the new manifest root as lane-only metadata. The checked-in schema now documents three file shapes: lane metadata, carried family catalog files, and named harness behaviors. The manifest root now carries five family catalog seed files for the current NLC3-through-NLC7 carried families plus a `preludes.json` registry for the default prelude/postlude behaviors the harness already relies on. On the Rust side, the default raw-string prelude/postlude match map in `node_compat.rs` is now represented as named behavior data for the carried defaults, which makes the focused metadata tests prove something real about the live harness instead of only about static JSON. The topology proof now covers both file content and discovery semantics: the family files point at real manifest/failure docs, still anchor to live subset batch names in `node_compat.rs`, the named-behavior registry matches the current default fixture mappings plus the implicit `--pending-deprecation` flag behavior, and the manifest directory layout loads from disk in a deterministic order. This still does not claim per-fixture manifest-driven execution, but it moves the NLC10 infrastructure substrate from simple lane provenance to the first real topology and behavior registry layer. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `6 passed`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/schema.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/core-semantics.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/process-and-timing.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/streams-and-local-io.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/networking.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/loader-context.json`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Took the next seed-stage Phase 1.2 step instead of stopping at catalog existence. The carried family manifest files now model the first explicit execution-class and capability axes (`sequential`, `bundle-root-fs`, and `loopback-net` for the networking seed), and the focused topology harness now includes a real loader that composes those family catalogs from disk in deterministic order. The validation path is no longer passive: it rejects duplicate family ids, duplicate lane entries, duplicate profiles, and duplicate capabilities, and it uses typed enum parsing so unknown execution classes or capability names fail immediately instead of drifting in as free text. The new proof also keeps the seed-stage scope honest: this is still family-catalog infrastructure, not per-fixture manifest execution yet. What is verified now is that the first manifest loader/profile/capability substrate exists, composes deterministically from the checked-in directory layout, and refuses ambiguous seed state before that ambiguity can turn into misleading support claims later. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `8 passed`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_catalog.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_metadata.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Finished the next moduleization slice for the Phase 1 manifest seed layer instead of leaving the first loader/validation pass stranded inside a single proof file. The lane/family/named-behavior seed types, deterministic on-disk discovery, and ambiguity validation now live in a dedicated `node_compat_manifest_catalog` module, while the metadata and topology tests consume that module as clients. That turns the previous proof-only step into the first concept-owned harness substrate: the manifest seed layer is no longer just asserted by tests, it is loaded and validated through a reusable module that can support later per-fixture manifest execution. The focused proofs stayed green after the extraction, so this checkpoint is a real structural gain rather than a docs-only reshuffle. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `8 passed`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_catalog.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/schema.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/core-semantics.json`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Advanced from family-level manifest seeds to the first real per-fixture manifest slice instead of leaving Phase 1 at topology-only structure. The `core-semantics` catalog now carries a narrow `assert-and-buffer-foundation` fixture seed wave with explicit lane-source mappings for ten carried fixtures, including shared, split, and Node22-only cases. The runtime-side proof is stronger than a schema parse: `node_compat.rs` now exposes a batch snapshot helper for `CORE_SEMANTICS_BATCH`, and the focused topology test cross-checks each seeded fixture entry against the live batch definition so the manifest seed cannot silently drift from the executed denominator. The schema and catalog loader also now model fixture-seed lane-source objects and reject empty lane-source seeds. This is still intentionally narrow; it does not claim the whole `core-semantics` family or other carried families are fully per-fixture-manifested yet. It does prove that the new catalog module can carry real fixture-level truth for a live family slice, which is the next foundation needed before broader per-fixture migration and report generation. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `9 passed`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/process-and-timing.json`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Widened the first per-fixture manifest substrate into a second carried family instead of leaving Phase 1 stranded on a single `core-semantics` seed wave. The `process-and-timing` catalog now participates in the same batch-backed proof style through a narrow `process-foundation` fixture seed slice, and the focused topology test cross-checks those ten fixture entries against the live `PROCESS_AND_TIMING_BATCH` definitions just as strictly as the earlier `core-semantics` wave. That proof immediately paid off by catching a real lane-source drift in the seed JSON: `test-process-next-tick.js` had been miswired to the Node20 source for the Node22 lane, and the checkpoint now records the corrected Node22 fixture path instead of silently carrying wrong provenance forward. This keeps the Phase 1 manifest layer honest: it is still seed-stage infrastructure, but it now proves real per-fixture truth across two carried families rather than one. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `10 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/streams-and-local-io.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/networking.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/loader-context.json`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Finished the Phase 1.1 carried-family seed baseline instead of stopping after two families. `node_compat.rs` now exposes batch snapshot helpers for `STREAMS_AND_LOCAL_IO_BATCH`, `NETWORKING_BATCH`, and `LOADER_CONTEXT_BATCH`, and the focused topology harness consumes those live batch definitions to prove three additional narrow per-fixture seed waves: `streams-and-local-io` `os-tty-readline-foundation`, `networking` `dns-net-foundation`, and `loader-context` `module-and-async-foundation`. Each new seed file contributes ten explicit fixture entries, so all five carried family catalogs now have a real fixture-seed foothold rather than only family-level metadata. The proof stays stronger than a JSON parse: the loader-composition test now asserts all five seed counts, and dedicated topology tests cross-check every carried seed entry against the live batch snapshots, including Node24 omissions where the batch really carries them. This does not yet claim manifest-driven execution, but it completes the first all-carried-family evidence layer the later per-fixture loader and report-generation work can build on. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `13 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_catalog.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_resolution.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted the manifest seed layer from topology-only proof into the first reusable query/validation substrate. The dedicated `node_compat_manifest_resolution` tests now prove that loaded catalogs validate across files, reject duplicate fixture ids across family catalogs, reject wrong-phase named-behavior links, and resolve deterministic family/slice fixture lists through the new `resolve_fixture_seed_slice(...)` path on the catalog loader. This is the first `NLC10` slice that later execution-plan and structured-reporting code can call directly instead of reimplementing manifest joins inside proof tests. The scope is still intentionally narrow: it resolves seed fixture lists, not live execution plans yet. But it gives the harness a real deterministic manifest query surface and enforces the cross-file topology rules Phase 1.2 called for. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `18 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_catalog.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_resolution.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Extended the same manifest-resolution seam into the first deterministic lane execution-plan builder instead of stopping at slice lookup. The catalog loader now turns a resolved family/slice into ordered per-lane plans anchored to the family subset-test identifiers, with each lane carrying only the fixture sources that actually exist for that lane. The focused proof uses the `networking` `dns-net-foundation` slice to show the contract clearly: the plan preserves `node20`/`node22`/`node24` lane order, keeps the expected subset-test function for each lane, and retains the explicit Node24 omission for `test-stream-pipeline.js` instead of silently backfilling or flattening the source map. That gives later execution/reporting work a real plan object to consume rather than forcing it to rediscover lane grouping from raw seed JSON. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `19 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Pushed the manifest execution-plan seam one step further into the first structured reporting substrate instead of leaving dashboards and closeout evidence to reimplement counting logic later. The new report module serializes a versioned plan-report shape from the deterministic lane execution plan, carrying family, NLC item, slice, execution class, profiles, capabilities, and ordered per-lane summaries with subset-test anchors plus fixture counts/ids. The focused proof keeps that report shape honest with two checks: the schema/version test verifies deterministic serialization for the `networking` `dns-net-foundation` slice, and the summary test proves the per-lane counts preserve the real Node24 omission for `test-stream-pipeline.js` rather than flattening or inventing coverage. This is still planning/reporting substrate, not live runtime result aggregation yet, but it gives `NLC10` the first factual machine-readable summary layer built directly from the manifest-driven execution plan. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `21 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Tightened the first structured reporting slice so it now matches the plan’s lane/slice/profile shape more closely instead of stopping at raw lane rows. The report model now carries an explicit slice summary with unique-fixture, lane-count, total-lane-entry, and min/max lane coverage fields, plus profile summaries derived from the manifest plan metadata. The focused proof still uses the `networking` `dns-net-foundation` execution plan so the numbers stay grounded in a real omission case: the slice summary reports `10` unique fixtures and `29` total lane entries across `3` lanes, while the single `Application` profile summary carries the same deterministic counts without inventing runtime outcomes that the harness has not measured yet. This remains planning/reporting substrate rather than result aggregation, but it closes the first honest lane/slice/profile reporting loop on top of the manifest-driven execution plan. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `21 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Extended the structured reporting seam from single-slice reports into the first cross-family catalog summary over the current seeded baseline. The report module now aggregates all five carried family seed slices into a deterministic catalog report with total unique fixture-seed counts, total lane-entry counts, ordered per-lane totals, and the underlying slice reports. The focused proof did real work here instead of only rubber-stamping the design: the first replay failed because `unique_fixture_count` was incorrectly derived from the first lane, which undercounted slices with intentional Node20 or Node24 omissions. The fix now computes unique fixture ids across all lane entries, so the catalog report carries the honest `50` unique fixture seeds and `147` total lane entries across the current seeded corpus. This remains seed-stage reporting rather than live pass/fail result aggregation, but it gives `NLC10` the first machine-readable cross-family summary layer that later dashboards and closeout evidence can consume. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `22 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted the reporting layer from planned seeded coverage into the first measured-outcome aggregation path. The report module can now consume explicit observed lane fixture results on top of a deterministic manifest execution plan and aggregate pass/skip/fail/missing counts into lane, slice, and profile summaries. The focused proof uses a partial observed result set over the `networking` `dns-net-foundation` slice so the counts stay honest about missing entries: the resulting report shows `29` expected lane-fixture slots, `4` observed results, and a `2/1/1/25` pass/skip/fail/missing split while preserving the Node24 omission for `test-stream-pipeline.js`. The same slice also now rejects duplicate observed results and unknown lane/fixture combinations, which keeps later live harness integration from silently flattening or inventing results. This is still seeded-slice reporting rather than full harness output, but it is the first `NLC10` path that can represent measured outcomes instead of only planned coverage. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `24 passed`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Extended the same reporting substrate into the first catalog-level profile rollups instead of leaving multi-profile evidence implicit. The plan-report and observed-report catalog shapes now carry explicit per-profile summaries alongside the existing per-lane and per-slice counts, so later dashboards and closeout docs can read profile truth directly rather than reverse-engineering it from nested slice rows. The focused proof stays intentionally honest about scope: the checked-in seed baseline remains `Application`-only, but the new mutated-catalog test temporarily marks the carried `process-and-timing` slice as `Application` + `Tooling` and proves both catalog summary paths aggregate those profile facts deterministically without changing the public compatibility denominator or overstating live support claims. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `27 passed`; `cargo fmt --all`; `cargo fmt --all --check`; `make clippy`; `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Pushed the same report layer one step closer to real harness output instead of stopping at in-memory summaries. The manifest report module now has deterministic JSON artifact writers for slice-plan, catalog-plan, and catalog-observed outputs, all built on the already versioned report schema. The focused proof writes those artifacts to a temporary root, verifies the stable file names (`slice-plan-networking-dns-net-foundation.json`, `catalog-plan.json`, and `catalog-observed.json`), and round-trips the emitted JSON back into structured assertions. This still is not a top-level `make node-compat-report` entrypoint yet, but it is the first honest artifact-emission bridge the later dashboard/CI wiring can reuse instead of inventing its own serialization path. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `28 passed`; `cargo fmt --all`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `Makefile`, `scripts/node-compat-report.sh`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Landed the first real repo-owned harness entrypoint on top of the new report artifact writers instead of leaving artifact emission as a proof-only helper. The report module now emits a seeded slice bundle with a stable nested root under `target/node-compat/<family>/<slice>/`, including `slice-plan`, `slice-observed`, and `catalog-plan` JSON files. That path is exposed through a checked-in shell helper, `bash scripts/node-compat-report.sh --family <family> --slice <slice>`, plus the matching `make node-compat-report FAMILY=<family> SLICE=<slice>` wrapper, both backed by an ignored Rust harness hook so later dashboards and CI can reuse the same path without editing test code. The normal unit proof still keeps the behavior honest by asserting the nested root, file names, and JSON contents for the seeded `networking` `dns-net-foundation` slice. This is still seeded artifact emission, not live lane-result reporting yet, but it closes the first stable `target/node-compat/` entrypoint the plan has been aiming at. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `29 passed`, `1 ignored`; `bash scripts/node-compat-report.sh --family networking --slice dns-net-foundation` → emitted `/Users/jack/src/github.com/agentstation/neovex/target/node-compat/networking/dns-net-foundation/{slice-plan-networking-dns-net-foundation.json,slice-observed-networking-dns-net-foundation.json,catalog-plan.json}` via the ignored harness entrypoint; `make node-compat-report FAMILY=networking SLICE=dns-net-foundation` → same emitted bundle through the documented wrapper; `cargo fmt --all`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `Makefile`, `scripts/node-compat-report.sh`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Extended the same repo-owned report entrypoint from seeded empty-observed output into the first measured observed-result ingestion path. The report module now parses optional observed-results JSON, converts it into the existing manifest report model, and emits both `slice-observed` and `catalog-observed` artifacts with real pass/skip/fail/missing counts under the same stable `target/node-compat/<family>/<slice>/` root. The focused Rust proof covers that ingestion path directly by writing a two-entry observed-results file and asserting the emitted networking slice now reports `2` observed results with the expected `1 pass / 1 skip / 27 missing` split. The shell helper and `make` wrapper both now accept that same observed-results input, and a live replay of the generated `slice-observed-networking-dns-net-foundation.json` confirmed the emitted artifact carries the same measured counts instead of the old all-missing placeholder. This still is not automatic capture from a broad harness lane yet, but it is the first honest bridge from manifest planning to measured artifact emission on the checked-in entrypoint. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `30 passed`, `1 ignored`; `bash scripts/node-compat-report.sh --family networking --slice dns-net-foundation --observed-results <temp-json>` → emitted `slice-observed` plus `catalog-observed` under `target/node-compat/networking/dns-net-foundation/`; `make node-compat-report FAMILY=networking SLICE=dns-net-foundation OBSERVED_RESULTS=<temp-json>` → same observed-result bundle through the documented wrapper; `sed -n '1,220p' target/node-compat/networking/dns-net-foundation/slice-observed-networking-dns-net-foundation.json` → confirmed `total_observed_results: 2` and counts `passed: 1`, `skipped: 1`, `failed: 0`, `missing: 27`; `cargo fmt --all`; `cargo fmt --all --check`; `make clippy` (passed with the same preexisting sibling Deno dead-code warnings); `git diff --check` |
| 2026-05-11 | NLC10 | `in_progress` | `Makefile`, `scripts/node-compat-report.sh`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Promoted the report entrypoint from JSON-fed observed results into the first automatic live-harness capture path. The checked-in helper can now run the real seeded family/slice fixtures through the existing manifest-backed Node-compat runner, record per-lane pass/skip/fail states, and emit measured `slice-observed` plus `catalog-observed` artifacts under the same stable `target/node-compat/<family>/<slice>/` root. The focused proof stayed honest about environment truth: the first in-sandbox replay of `networking:dns-net-foundation` captured five false-red `0.0.0.0` bind failures across the three lanes, which established that the artifact path preserves host restrictions instead of flattening them away; the same helper rerun outside the sandbox then produced the clean host result set with `29 / 29` observed passes. That closes the plan’s next promised bridge from seeded planning to measured real-harness artifact emission without widening any runtime support claims implicitly. | `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `30 passed`, `1 ignored`; `bash scripts/node-compat-report.sh --family networking --slice dns-net-foundation --capture-live` (sandboxed) → emitted observed artifacts with `29` observed results and counts `passed: 24`, `failed: 5`, `missing: 0`, exposing sandbox-only `0.0.0.0` bind denials on `test-stream-finished.js` / `test-stream-pipeline.js`; `bash scripts/node-compat-report.sh --family networking --slice dns-net-foundation --capture-live` (outside sandbox) → emitted the same artifact bundle with `29` observed results and counts `passed: 29`, `failed: 0`, `missing: 0`; `sed -n '1,220p' target/node-compat/networking/dns-net-foundation/slice-observed-networking-dns-net-foundation.json` → confirmed the final host replay now reports all `29` expected results as passes |
| 2026-05-11 | NLC10 | `in_progress` | `Makefile`, `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_canary_registry.rs`, `scripts/node_compat/canary_registry.py`, `scripts/node_compat/canaries-bootstrap.sh`, `scripts/node_compat/canaries-run.sh`, `scripts/node_compat/validate-claims.sh`, `tests/node-compat/canary-registry.json`, `tests/node-compat/networking-canaries/README.md`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Landed the first canonical Phase 4.1 canary trust slice on top of the measured artifact substrate instead of leaving package-level evidence as a README-only convention. The repo now has a checked-in canary registry that maps the current Application-profile networking claims (`express`, `fastify`, `socket.io`, `undici`, and `axios`) to pinned versions, lane coverage, NLC family dependency, public claim ids, and the canonical ignored Rust batch tests. There is also a focused Rust proof layer now: `node_compat_canary_registry` validates that the registry parses, points at real roots/bundles/docs, matches the pinned package versions in `tests/node-compat/networking-canaries/package.json`, and that every active claim maps to at least one active canary without crossing profile/family boundaries. On top of that, the repo now has canonical trust entrypoints through `make node-compat-canaries-bootstrap`, `make node-compat-canaries`, and `make node-compat-validate-claims`, backed by checked-in `scripts/node_compat/*` helpers instead of ad hoc `npm ci` plus hand-run ignored tests. The focused proof again stayed environment-aware: the sandboxed `make node-compat-canaries PROFILE=application` replay failed on `127.0.0.1` bind denial for the Node22 batch, while the same canonical command rerun outside the sandbox went green for both the Node22 primary and Node20 validation application canary lanes. | `cargo test -p neovex-runtime node_compat_canary_registry_ -- --nocapture --test-threads=1` → `2 passed`; `bash scripts/node_compat/canaries-bootstrap.sh --profile application` → confirmed the current networking root is already bootstrapped; `bash scripts/node_compat/validate-claims.sh` → `validated 5 active claim mappings against 5 registered canaries`; `make node-compat-canaries PROFILE=application` (sandboxed) → failed on sandbox-only `127.0.0.1` bind denial in `application_node22_networking_package_canary_batch`; `make node-compat-canaries PROFILE=application` (outside sandbox) → `application_node22_networking_package_canary_batch` green and `application_node20_networking_validation_canary_batch` green; `make node-compat-canaries-bootstrap PROFILE=application` → confirmed canonical bootstrap wrapper; `make node-compat-validate-claims` → confirmed canonical claim-validation wrapper |
| 2026-05-11 | NLC10 | `in_progress` | `Makefile`, `scripts/node_compat/canary_registry.py`, `tests/node-compat/networking-canaries/README.md`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Extended the same canonical canary entrypoint from “green or red” console output into the first machine-readable canary evidence bundle. `scripts/node_compat/canary_registry.py run` now writes a stable JSON report under `target/node-compat/canaries/`, and `make node-compat-canaries PROFILE=application` now emits `profile-application.json` with profile-tagged lane summaries plus per-canary pass/fail rows for every active networking claim. The host replay stayed green for both the Node22 primary and Node20 validation application canary batches, and the emitted report now records all seven active lane-canary rows as passes (`5` on Node22, `2` on Node20). This keeps the trust layer aligned with the broader manifest/report direction instead of inventing a second opaque reporting path for canaries. | `make node-compat-canaries PROFILE=application` (outside sandbox) → emitted `/Users/jack/src/github.com/agentstation/neovex/target/node-compat/canaries/profile-application.json` after both application canary batches passed; `sed -n '1,240p' target/node-compat/canaries/profile-application.json` → confirmed `canary_count: 7`, `passed: 7`, `failed: 0`, Node20 lane summary `2/2`, and Node22 lane summary `5/5` |
| 2026-05-11 | NLC10 | `in_progress` | `Makefile`, `crates/neovex-runtime/src/runtime.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_oracle.rs`, `scripts/node_compat/oracle-run.sh`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Landed the first Phase 3.3 shadow-oracle substrate without pretending we had representative real-Node evidence yet. The harness now has a dedicated `node_compat_oracle` module with deterministic drift classes, a checked-in manual artifact entrypoint, and the repo-owned `make node-compat-oracle` / `bash scripts/node_compat/oracle-run.sh` surface. The critical owner fix was in the bundle writer itself: oracle materialization now uses a separate bundle mode that preserves real Node `process.execPath` / `argv` / `gc` semantics instead of reusing the embedded runtime’s Rust-test-binary `execPath` rewrite and host-hook `gc` override. Focused proof kept the environment story strict: classification and major-version guardrails are green, the documented helper is callable, and the canonical oracle entrypoint now fails explicitly and correctly against the only local `node` binary because it reports `v25.9.0` instead of the required Node22 contract. That means the trust surface is live, but representative host-oracle evidence is still honestly blocked on explicit `NEOVEX_NODE20_BIN` / `NEOVEX_NODE22_BIN` / `NEOVEX_NODE24_BIN` wiring. | `cargo test -p neovex-runtime node_compat_oracle_ -- --nocapture --test-threads=1` → `2 passed`, `1 ignored`; `bash scripts/node_compat/oracle-run.sh --help` → documented canonical entrypoint surfaced the expected usage with `--lane`, `--fixture`, `--output-root`, and `--node-bin`; `make node-compat-oracle LANE=node22 SAMPLE=test/parallel/test-buffer-alloc.js NODE_BIN=/opt/homebrew/bin/node` → failed explicitly at the version guard with `oracle lane \`node22\` requires a Node 22 binary, but \`/opt/homebrew/bin/node\` reported \`v25.9.0\`` |
| 2026-05-11 | NLC10 | `in_progress` | `.github/workflows/node-compat-nightly.yml`, `Makefile`, `scripts/node_compat/dashboard.py`, `tests/node-compat/README.md`, `tests/node-compat/networking-canaries/README.md`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Added the first canonical dashboard and CI-facing retention layer on top of the measured slice/canary/oracle artifacts instead of leaving NLC10 evidence spread across separate file roots. The new `scripts/node_compat/dashboard.py` / `make node-compat-dashboard` entrypoint aggregates seeded slice reports, canary reports, and oracle artifacts into stable JSON + Markdown summaries under `target/node-compat/dashboard/`. The first generated dashboard kept the closeout state honest: it recorded the current green networking slice and Application canary evidence, zero local oracle artifacts, and the still-missing Tooling canary claim set (`jest`, `tsx`, `ts-node`, `prisma`, and `next`) as explicit gaps instead of silently treating the current registry as complete. The same measured-output vocabulary is now wired into `.github/workflows/node-compat-nightly.yml`, a dedicated scheduled/manual evidence workflow that runs the five carried seeded slice replays, the Application canary lane, the representative Node22 oracle sample, the dashboard build step, and uploads the retained `target/node-compat/` artifact bundle. This closes the CI/dashboard vocabulary seam without overstating the still-missing Tooling canaries or local version-matched oracle binaries. | `python3 scripts/node_compat/dashboard.py` → emitted `target/node-compat/dashboard/dashboard-summary.json` and `target/node-compat/dashboard/dashboard-summary.md`; `sed -n '1,220p' target/node-compat/dashboard/dashboard-summary.md` → confirmed the generated dashboard reports one green networking slice, one green Application canary report, zero oracle artifacts, and explicit required canary gaps for `jest`, `tsx`, `ts-node`, `prisma`, and `next`; `make node-compat-dashboard` → same dashboard bundle through the documented wrapper; `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/node-compat-nightly.yml"); puts "yaml-ok"'` → basic workflow YAML parse check passed |
| 2026-05-11 | NLC10 | `done` | `.github/workflows/node-compat-nightly.yml`, `docs/architecture/runtime/deno-vs-neovex-node-compat.md`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `tests/node-compat/README.md` | Closed `NLC10` truthfully instead of leaving the trust layer split between fresh host evidence and stale control-plane prose. The final closeout pass synchronized three concrete seams. First, the host Tooling canary wave is now part of the measured baseline rather than scaffold-only intent: the canonical `profile-tooling.json` report is green for `jest`, `tsx`, `ts-node`, `prisma`, and `next`, and the regenerated dashboard now renders both Application and Tooling claim sets as observed passes instead of `missing_observation`. Second, representative host-oracle evidence now exists locally for the primary lane: `make node-compat-oracle` emitted a real Node22 oracle artifact through the version-matched Homebrew `node@22` binary, so the dashboard can aggregate a concrete oracle report locally while the nightly workflow owns the broader Node20 / Node22 / Node24 sweep through `actions/setup-node`. Third, the nightly evidence workflow is now aligned with the finished support claim rather than the earlier interim state: it caches both canary roots, boots and runs both canary profiles, executes the five carried seeded slice replays, performs the representative multi-lane oracle sweep, rebuilds the dashboard, and uploads the retained `target/node-compat/` bundle. With those pieces in place, the NLC10 completion gate is satisfied: CI has per-module seeded Node test slices, nightly dashboards, package/framework smoke lanes, pinned upstream corpus metadata, and public docs/matrices synchronized to measured artifacts. Future Node26+ harness scale work remains useful follow-on, but it is not part of the closed Node LTS compatibility control item. | `make node-compat-oracle LANE=node22 SAMPLE=test/parallel/test-buffer-alloc.js NODE_BIN=/opt/homebrew/Cellar/node@22/22.22.2_2/bin/node` → emitted `target/node-compat/oracle/node22/test-parallel-test-buffer-alloc-js/oracle-node22-test-parallel-test-buffer-alloc-js.json`; `make node-compat-dashboard` → regenerated `target/node-compat/dashboard/dashboard-summary.{json,md}` with `2` canary reports and `1` oracle report; `sed -n '1,220p' target/node-compat/dashboard/dashboard-summary.md` → confirmed both Application and Tooling claims now render `passed`; `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/node-compat-nightly.yml"); puts "yaml-ok"'` → workflow remains parseable after adding Tooling canaries and the Node20 / Node22 / Node24 oracle sweep |
| 2026-05-11 | NLC10 | `done` | `Cargo.toml`, `Cargo.lock`, `docs/plans/README.md`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md`, `~/src/github.com/agentstation/deno`, `~/src/github.com/agentstation/rusty_v8` | Completed the post-closeout cleanup pass so the finished `NLC10` state is anchored to immutable published fork revisions rather than the local path-patched proof baseline. The canonical `agentstation/deno` worktree is now published and clean at `v2.7.14-locker.40`, the matching `agentstation/rusty_v8` worktree is published and clean at `v147.4.0-locker.2`, and Neovex is repinned back from the sibling path overrides to those tags in `Cargo.toml` and `Cargo.lock`. The remaining stale plan prose is trimmed too: the main Node LTS plan no longer claims `NLC10` is the active control item, and the companion harness plan now reads as a finished baseline with successor-scope follow-on work instead of an interrupted in-progress log. Focused runtime verification stayed green on the repinned baseline, so the cleanup did not silently regress the measured trust layer while restoring the normal published-fork dependency posture. | `git describe --tags --always` in `~/src/github.com/agentstation/deno` → `v2.7.14-locker.40`; `git describe --tags --always` in `~/src/github.com/agentstation/rusty_v8` → `v147.4.0-locker.2`; `git status --short` in both sibling worktrees → clean; `cargo test -p neovex-runtime node_compat_canary_registry_ -- --nocapture --test-threads=1` → `2 passed`; `cargo test -p neovex-runtime node_compat_oracle_ -- --nocapture --test-threads=1` → `2 passed`, `1 ignored`; `make node-compat-validate-claims` → `validated 10 active claim mappings against 10 registered canaries`; `rg -n "v2\\.7\\.14-locker\\.40|v147\\.4\\.0-locker\\.2" Cargo.lock` → confirmed the published git-tag sources are locked in; `cargo fmt --all --check` → clean; `make clippy` → passed |
| 2026-05-11 | — | `done` | `.cargo/config.toml`, `crates/neovex-runtime/src/limits.rs`, `crates/neovex-runtime/src/runtime_capabilities.rs`, `crates/neovex-runtime/src/runtime/tests/basic_invocation.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Completed a post-closeout audit remediation pass to realign the runtime with the already-published support contract. The first correction fixes the published-fork release boundary: `.cargo/config.toml` now pins `RUSTY_V8_VERSION` to `147.4.0-locker.2`, matching the repinned `agentstation/rusty_v8` tag instead of requesting stale `147.0.0-locker.2` artifacts on clean builds. The second correction closes the Application-profile subprocess regression without weakening the compat harness: Application bundles now default to `RuntimeSubprocessPolicy::Denied`, Tooling keeps the staged-binary discovery policy, and the internal Node-compat runner uses the new narrow `RuntimeSelfExecOnly` seam only for respawn-based fixture families so the harness can keep driving copied `process.execPath` children without reopening host subprocess access. The third correction removes the metadata-path ancestor exception so `stat()` / `lstat()` no longer probe filesystem ancestors outside approved roots. Focused denial proofs are now explicit in `basic_invocation` and `runtime_capabilities`, the full Node22 `node:test` respawn batches stayed green, the Node22 `node:wasi` batches stayed green, and the carried Node22 loader-context denominator still replayed clean at `239 passed / 0 failed`. One unrelated existing red remains outside this audit slice: `node22_primary_lane_executes_manifested_process_and_timing_subset` still fails on `test-process-load-env-file.js`, and that path uses the dedicated host spawn op (`common.spawnPromisified`) rather than the corrected subprocess capability surface. | `cargo test -p neovex-runtime runtime_self_exec_subprocess_policy_requires_node22_target -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_profile_has_no_run_targets_and_denies_subprocess_queries -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_self_exec_subprocess_policy_only_allows_compat_exec_target -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime ensure_read_metadata_path_denies_ancestor_of_approved_root -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_node22_reads_local_files_hides_non_allowlisted_env_and_denies_escape_writes -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime application_node22_denies_child_process_spawn_even_for_process_exec_path -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node_compat_runtime_limits_only_grant_self_exec_to_known_respawn_fixtures -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_nlc9_test_runner_ -- --nocapture --test-threads=1` → `11 passed`, `1 ignored`; `cargo test -p neovex-runtime node22_nlc9_wasi_ -- --nocapture --test-threads=1` → `5 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_loader_context_subset -- --nocapture --test-threads=1` → `239 passed / 0 failed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → unrelated existing `test-process-load-env-file.js` failure persists; `cargo fmt --all --check` → clean; `make clippy` → passed; `git diff --check` → clean |
| 2026-05-11 | — | `done` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/preludes.json`, `docs/plans/node-lts-compatibility-plan.md`, `docs/plans/node-lts-compatibility-execution-log.md` | Closed the last stale post-closeout cleanup seam instead of carrying a known red forward into the companion infrastructure follow-on. The remaining `test-process-load-env-file.js` failure turned out to be a checkout-shape assumption inside the top-level upstream fixture: it expects a repo-root-like `process.cwd()` with a real `lib/` child before it probes the missing-default-`.env` branch. Neovex’s node-compat harness now models that contract explicitly and narrowly. A new named prelude behavior, `checkout_root_cwd`, moves that fixture onto the compat bundle root and seeds an empty `lib/` directory there, while the new dedicated `node22_process_load_env_file_fixture` watchpoint keeps the seam isolated from the broader process-and-timing denominator. With that harness correction in place, both the focused watchpoint and the full Node20 / Node22 manifested `NLC4` lanes are green again, the named-behavior manifest registry accepts the new prelude cleanly, and the stale `P3` phase row in the main Node LTS plan is now synchronized to the already-finished `NLC10` state. | `cargo test -p neovex-runtime node22_process_load_env_file_fixture -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node22_primary_lane_executes_manifested_process_and_timing_subset -- --nocapture --test-threads=1` → `48 passed, 0 failed`; `cargo test -p neovex-runtime node20_validation_lane_executes_official_process_and_timing_subset -- --nocapture --test-threads=1` → `46 passed, 0 failed`; `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `30 passed, 1 ignored` |
| 2026-05-11 | — | `done` | `crates/neovex-runtime/src/runtime/tests/node_compat.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_catalog.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_report.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifest_topology.rs`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/loader-context-supplementary-global-injection.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_manifests/fixtures/loader-context-supplementary-module-bridge.json`, `crates/neovex-runtime/src/runtime/tests/node_compat_fixtures/supplementary/`, `docs/architecture/runtime/node-compat-supplementary.md`, `docs/architecture/runtime/node-compat-supplementary-failures.md`, `docs/architecture/runtime/node-compat-surface-matrix.md`, `docs/plans/node-compat-test-infrastructure-plan.md`, `docs/plans/node-compat-future-lanes-and-correctness-plan.md`, `tests/node-compat/README.md` | Closed the last archival audit gaps in the completed Node-compat plan set instead of weakening the control plane to match an underspecified baseline. The supplementary correctness tier now includes the missing `module_resolution_bridge` and `global_injection_fidelity` categories as checked-in manifest families, focused lane tests, and live report artifacts, so the companion plan’s stated completion gate is now true by evidence rather than aspiration. The support-facing docs were synchronized at the same time: the supplementary docs now list three green successor slices plus the explicit `process-release-shape` expected-failure watchpoint, and the public surface matrix no longer speaks about an “active” Node LTS closeout or a “current `NLC10`” trust layer after those plans finished. With those additions in place, the successor Node-compat baseline is internally consistent across code, manifests, reports, and docs, and no remaining closeout item appears to require a new runtime feature wave before archival. | `cargo test -p neovex-runtime node_compat_supplementary_module_bridge_node22 -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node_compat_supplementary_global_injection_node22 -- --nocapture --test-threads=1` → `1 passed`; `cargo test -p neovex-runtime node_compat_manifest_ -- --nocapture --test-threads=1` → `36 passed`, `1 ignored`; `cargo test -p neovex-runtime node_compat_supplementary_ -- --nocapture --test-threads=1` → `12 passed`; `bash scripts/node-compat-report.sh --family loader-context-supplementary-module-bridge --slice supplementary-module-resolution-bridge --capture-live` → emitted green live artifacts; `bash scripts/node-compat-report.sh --family loader-context-supplementary-global-injection --slice supplementary-global-injection-fidelity --capture-live` → emitted green live artifacts |
