# Nimbus Full-Codebase Review Remediation Log - 2026-06-09

Source review: `docs/code-review/2026-06-09-full-codebase-review.md`

Protocol: each row starts as `PENDING`, then moves to `CONFIRMED`, `ALREADY-FIXED`, `NOT-A-DEFECT`, or `DEFERRED-BLOCKED` only after re-reading the cited code, tests, and callers. Confirmed findings are not marked fixed until focused verification records concrete output.

Verification strategy: each confirmed fix starts with the narrowest behavioral tests that would have caught the defect, followed by scoped crate clippy/checks for the touched surface. Cargo verification is serialized so one focused lane can reuse warmed artifacts instead of competing for locks or multiplying build output. Workspace `make clippy` / `make check` gates run at cluster or seam boundaries, and full provider-heavy crate sweeps or `make ci` are reserved for changes whose blast radius requires them and for final closeout. If a broad command enters unrelated slow lanes, stop it and record it as aborted rather than treating it as proof. Broad Cargo gates are followed by main-checkout target cleanup when they grow `target/` above 25 GiB, leave less than 200 GiB free, or complete a subsystem cluster, unless the next queued same-graph focused gate would immediately reuse those artifacts and free space remains above 200 GiB; preserve unrelated worktree targets and Cargo git checkout state unless explicitly cleaning those lanes after a live-use check.

Disk hygiene checkpoint 2026-06-09: `/` had 246 GiB free, the main checkout `target/` was 18 GiB (`target/debug/deps` 12 GiB, `target/debug/incremental` 7.0 GiB), Cargo git checkouts were 12 GiB, `/private/tmp` was 257 MiB, and sibling targets were `deno/target` 9.5 GiB, `rusty_v8/target` 6.3 GiB, `nimbus-libkrun/target` 454 MiB, and `bun/target` 69 MiB. No cleanup was run because the main checkout remained below the 25 GiB cleanup trigger and free space remained above 200 GiB; the next cleanup should run after D1-2 or any broad gate if either threshold is crossed.

Disk hygiene checkpoint 2026-06-10: `/` had 240 GiB free, the main checkout `target/` was 23 GiB, Cargo git checkouts were 12 GiB, no `/private/tmp/nvx-*` directories existed, and sibling targets were `deno/target` 9.5 GiB, `rusty_v8/target` 6.3 GiB, `nimbus-libkrun/target` 454 MiB, and `bun/target` 69 MiB. `ps -ax -o pid,comm` showed no active Cargo, rustc, clippy, or nextest process. No cleanup was run because the main checkout remained below the 25 GiB cleanup trigger and free space remained above 200 GiB; continue using focused crate/test-name verification until a subsystem-cluster gate or threshold crossing.

Post-H2-2 disk checkpoint 2026-06-10: `/` had 256 GiB free, the main checkout `target/` was 5.6 GiB, and Cargo git checkouts were 12 GiB after the H2-1 cleanup and H2-2 verification rebuild. No cleanup was run because the active checkout target was below the 25 GiB trigger and preserving warm artifacts avoids paying another cold Deno/V8 rebuild on the next focused checks.

Post-I1-2 disk checkpoint 2026-06-10: `/` had 240 GiB free, the main checkout `target/` was 23 GiB, and Cargo git checkouts were 12 GiB after the encrypted-redb rotation storage/bin verification. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor; continue with focused checks and clean this checkout only after the next threshold crossing or subsystem cluster gate.

Post-I1-5 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 24 GiB, and Cargo git checkouts were 12 GiB after the machine API client/API route tests plus `nimbus-bin` clippy. No cleanup was run because the active checkout target remained just below the 25 GiB trigger and free space remained above the 200 GiB floor; avoid parallel Cargo invocations and clean this checkout after the next threshold crossing.

Post-I1-7 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 24 GiB, and Cargo git checkouts were 12 GiB after the start-config tests and scoped `nimbus-bin` clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-I2-3 disk checkpoint 2026-06-10: `/` had 238 GiB free, the main checkout `target/` was 24 GiB, and Cargo git checkouts were 12 GiB after the `nimbus-machine` crate tests and scoped clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Operator-requested disk checkpoint 2026-06-10: `/` had 238 GiB free, the main checkout `target/` was 24 GiB, Cargo git checkouts were 12 GiB, `/private/tmp` was 1.2 GiB, and sibling targets were `deno/target` 9.5 GiB, `rusty_v8/target` 6.3 GiB, `nimbus-libkrun/target` 454 MiB, and `bun/target` 69 MiB. `ps -ax -o pid,comm` showed no active Cargo, rustc, or rustdoc process. No cleanup was run because the active checkout target stayed below the 25 GiB cleanup trigger and free space stayed above the 200 GiB floor; resume with focused MongoDB tests and clean this checkout immediately if the next verification crosses the threshold.

Post-E1-10 disk checkpoint 2026-06-10: MongoDB verification grew the main checkout `target/` to 29 GiB and left `/` at 233 GiB free, so the 25 GiB cleanup trigger fired after the scoped verification completed. A live `ps -ax -o pid,comm` check showed another Cargo/runtime process under the sibling NDS worktree target, not this checkout. `cargo clean` was run only in this checkout and removed 53,077 files / 33.2 GiB. Post-clean `/` had 261 GiB free, this checkout's `target/` was absent, Cargo git checkouts remained 12 GiB, and sibling targets were preserved (`deno/target` 9.5 GiB, `rusty_v8/target` 6.3 GiB, `nimbus-libkrun/target` 454 MiB, `bun/target` 69 MiB).

Post-E1-11 disk checkpoint 2026-06-10: `/` had 254 GiB free, the main checkout `target/` was 7.5 GiB, and Cargo git checkouts were 12 GiB after rebuilding the MongoDB crate from the post-E1-10 cleanup and running scoped tests/clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E1-8 disk checkpoint 2026-06-10: `/` had 254 GiB free, the main checkout `target/` was 7.6 GiB, and Cargo git checkouts were 12 GiB after focused MongoDB aggregation tests, crate tests, clippy, and formatting checks. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E2-1 disk checkpoint 2026-06-10: `/` had 251 GiB free, the main checkout `target/` was 11 GiB, and Cargo git checkouts were 12 GiB after the focused Firebase timestamp-boundary proof rebuilt Firebase/runtime dependencies. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E2-3 disk checkpoint 2026-06-10: `/` had 250 GiB free, the main checkout `target/` was 11 GiB, and Cargo git checkouts were 12 GiB after the focused Firebase BatchWrite parser tests and crate clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E2-4 disk checkpoint 2026-06-10: `/` had 241 GiB free, the main checkout `target/` was 20 GiB, and Cargo git checkouts were 12 GiB after focused Firebase crate tests plus two focused `nimbus-server` gRPC tests rebuilt the server stack. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E2-5 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after the focused Firebase transaction-token tests, full Firebase crate tests, and crate clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor; an optional `ps ... | rg ...` process glance was sandbox-blocked and not escalated because cleanup was not triggered.

Post-E2-6 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after focused Firebase duplicate-helper tests, full Firebase crate tests, and crate clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E2-7 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after focused Firebase poison-recovery tests, full Firebase crate tests, and crate clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E4-1 disk checkpoint 2026-06-10: Cloud Functions dependency verification grew the main checkout `target/` to 27 GiB and left `/` at 234 GiB free, so the 25 GiB cleanup trigger fired after scoped verification. `ps -ax` showed no active Cargo, rustc, clippy, or nextest workload for this checkout. `cargo clean` was run only in this checkout and removed 46,576 files / 31.1 GiB. Post-clean `/` had 257 GiB free, this checkout's `target/` was 3.0 GiB, Cargo git checkouts remained 12 GiB, and sibling targets were preserved.

Post-E4-2 disk checkpoint 2026-06-10: `/` had 250 GiB free, the main checkout `target/` was 11 GiB, and Cargo git checkouts were 12 GiB after the focused Convex dependency-scope verification rebuilt the runtime-adjacent stack from the post-E4-1 cleanup. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E4-3 disk checkpoint 2026-06-10: `/` had 250 GiB free, the main checkout `target/` was 11 GiB, and Cargo git checkouts were 12 GiB after the warmed Convex duplicate-dependency verification. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E4-5/E4-4 disk checkpoint 2026-06-10: `/` had 244 GiB free, the main checkout `target/` was 16 GiB, and Cargo git checkouts were 12 GiB after focused Cloud Functions Firestore Admin runtime-extension tests, full Cloud Functions crate tests, and scoped clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E4-6/E4-7 disk checkpoint 2026-06-10: `/` had 244 GiB free, the main checkout `target/` was 16 GiB, and Cargo git checkouts were 12 GiB after focused Convex subscription-bound tests, full Convex crate tests, and scoped clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-E4-8/E4-9 disk checkpoint 2026-06-10: `/` had 244 GiB free, the main checkout `target/` was 16 GiB, and Cargo git checkouts were 12 GiB after focused Convex transform tests, full Convex crate tests, and scoped clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-A1-1/A1-2 disk checkpoint 2026-06-10: `/` had 241 GiB free, the main checkout `target/` was 20 GiB, and Cargo git checkouts were 12 GiB after focused redb tenant-event journal tests, full Storage crate tests, and scoped clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-A2-5 disk checkpoint 2026-06-10: `/` had 237 GiB free, the main checkout `target/` was 23 GiB, and Cargo git checkouts were 12 GiB after focused range/historical tests, full Storage crate tests, and scoped Storage/Engine clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-A2-7 disk checkpoint 2026-06-10: `/` had 237 GiB free, the main checkout `target/` was 24 GiB, and Cargo git checkouts were 12 GiB after focused historical-start-key tests, full Storage crate tests, and scoped Storage clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-A2-8 disk checkpoint 2026-06-10: `/` had 236 GiB free, the main checkout `target/` was 24 GiB, and Cargo git checkouts were 12 GiB after focused composite-range tests, full Storage crate tests, and scoped Storage clippy. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-A4-2 disk checkpoint 2026-06-10: `/` had 229 GiB free, the main checkout `target/` was 31 GiB, and Cargo git checkouts were 12 GiB after focused document-codec/scan-pushdown tests and scoped Storage clippy. A live process check showed an active Cargo/runtime lane in the sibling `node-default-runtime-support-hardening` worktree, not this checkout target. `cargo clean` was run only in this checkout and removed 53,256 files / 38.9 GiB. Post-clean `/` had 259 GiB free, this checkout's `target/` was 175 MiB, and Cargo git checkouts remained 12 GiB.

Post-A4-3 disk checkpoint 2026-06-10: `/` had 254 GiB free and the main checkout `target/` was 5.8 GiB after rebuilding the Storage crate from the post-A4-2 cleanup and running focused async-read cancellation tests plus scoped Storage clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-A4-4 disk checkpoint 2026-06-10: `/` had 253 GiB free and the main checkout `target/` was 6.6 GiB after focused async-storage helper tests and scoped Storage clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-A4-6 disk checkpoint 2026-06-10: `/` had 251 GiB free and the main checkout `target/` was 8.3 GiB after deterministic libsql freshness-barrier tests and scoped Storage clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B1-4 disk checkpoint 2026-06-10: `/` had 245 GiB free, the main checkout `target/` was 15 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` was 7.5 GiB after the focused atomic-write timestamp tests and scoped Engine/Storage clippy rebuilt the warmed engine stack. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B1-5 disk checkpoint 2026-06-10: `/` had 243 GiB free, the main checkout `target/` was 17 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` was 7.5 GiB after focused execution-unit dependency tests and scoped Engine clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B1-6 disk checkpoint 2026-06-10: `/` had 242 GiB free, the main checkout `target/` was 17 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` was 7.5 GiB after focused mutation-result helper tests, direct mutation tests, and scoped Engine clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B1-7 disk checkpoint 2026-06-10: `/` had 242 GiB free, the main checkout `target/` was 18 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after the focused journal-recovery clip tests and scoped Engine clippy. `du` also reported permission-denied notices for three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B2-2 disk checkpoint 2026-06-10: `/` had 241 GiB free, the main checkout `target/` was 19 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after focused cursor-auth tests and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B2-3 disk checkpoint 2026-06-10: `/` had 240 GiB free, the main checkout `target/` was 19 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after focused ordering tests and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B2-4 disk checkpoint 2026-06-10: `/` had 240 GiB free, the main checkout `target/` was 20 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after focused shadow-materializer/consistency tests and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B2-6 disk checkpoint 2026-06-10: `/` had 240 GiB free, the main checkout `target/` was 20 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after focused range-planner tests and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B2-7 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 20 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after focused planner-score tests and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B3-5 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 21 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 7.5 GiB after focused subscription/trigger worker tests and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B3-6 disk checkpoint 2026-06-10: `/` had 237 GiB free, the main checkout `target/` was 23 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 10 GiB after the focused concurrent subscription-overflow regression test and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor, but the checkout is close enough to the trigger that the next verification should stay narrow or clean if it crosses 25 GiB.

Post-B3-7 disk checkpoint 2026-06-10: `/` had 236 GiB free, the main checkout `target/` was 23 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after the focused subscription-registry visibility check and scoped Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-B4-6 disk checkpoint 2026-06-10: `/` had 236 GiB free, the main checkout `target/` was 23 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after the focused MySQL fault-forwarding test plus scoped Storage/Engine clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-C1-2 disk checkpoint 2026-06-10: Runtime verification grew the main checkout `target/` to 27 GiB and left `/` at 232 GiB free, so the 25 GiB cleanup trigger fired after focused runtime tests and scoped runtime clippy. A live `ps -ax -o pid,comm,args` check showed no active Cargo, rustc, clippy, or nextest workload for this checkout. `cargo clean` was run only in this checkout and removed 41,260 files / 37.7 GiB. Post-clean `/` had 259 GiB free, this checkout's `target/` was absent, Cargo git checkouts remained 12 GiB, and `/private/tmp` reported 9.5 GiB with the same three permission-denied mount notices.

Post-C1-4/C1-6 disk checkpoint 2026-06-10: `/` had 253 GiB free, the main checkout `target/` was 5.7 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused runtime exec-path tests and one scoped runtime clippy gate rebuilt the runtime stack from the post-C1-2 cleanup. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-C2-1 disk checkpoint 2026-06-10: `/` had 253 GiB free, the main checkout `target/` was 6.0 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused admission-permit tests and scoped runtime clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-C2-2 disk checkpoint 2026-06-10: `/` had 253 GiB free, the main checkout `target/` was 6.2 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused cooperative-shutdown tests and scoped runtime clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-C2-3 disk checkpoint 2026-06-10: `/` had 253 GiB free, the main checkout `target/` was 6.4 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused cancellation/watchdog tests and scoped runtime clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-C2-4 disk checkpoint 2026-06-10: `/` had 253 GiB free, the main checkout `target/` was 6.5 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused blocking-bridge tests and scoped runtime clippy. `du` again reported permission-denied notices for the same three `/private/tmp/tmp-mount-*` entries while still returning the aggregate size; no cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-C2-5/C2-6/C3-9 disk checkpoint 2026-06-10: `/` had 251 GiB free, the main checkout `target/` was 8.1 GiB, and Cargo git checkouts were 12 GiB after focused router, cooperative executor, Bun/JSC scaffold-cache tests plus scoped runtime clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-E3-2 disk checkpoint 2026-06-10: `/` had 241 GiB free, the main checkout `target/` was 18 GiB, and Cargo git checkouts were 12 GiB after focused DynamoDB query/stream/TTL tests and scoped DynamoDB/Storage/Engine clippy rebuilt the adapter-adjacent stack. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-F1-1/F1-5 disk checkpoint 2026-06-10: `/` had 238 GiB free, the main checkout `target/` was 20 GiB, and Cargo git checkouts were 12 GiB after focused tenant external-policy tests plus scoped `nimbus-tenant` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor; keep the warmed target for the remaining tenant-security findings.

Post-F1-2 disk checkpoint 2026-06-10: `/` had 238 GiB free, the main checkout `target/` was 21 GiB, and Cargo git checkouts were 12 GiB after focused operator-policy validation/runtime-profile tests plus scoped `nimbus-tenant` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor; keep the warmed target for the remaining tenant-security findings.

Post-F1-3 disk checkpoint 2026-06-10: `/` had 237 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after focused tenant principal-claim tests plus scoped tenant/server/Firebase/Cloud Functions check and clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor; keep the warmed target for the remaining security findings.

Post-F1-4 disk checkpoint 2026-06-10: `/` had 237 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after focused operator-policy host validation tests plus scoped `nimbus-tenant` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-F2-1 disk checkpoint 2026-06-10: `nimbus-node` verification grew the main checkout `target/` to 26 GiB and left `/` at 233 GiB free, so the 25 GiB cleanup trigger fired after scoped verification. A live process check found active `nimbus_runtime-*` test binaries in the sibling `nimbus-worktrees/node-default-runtime-support-hardening` cwd, not this checkout. `cargo clean` was run only in this checkout and removed 54,750 files / 30.3 GiB. Post-clean `/` had 259 GiB free, this checkout's `target/` was 85 MiB, Cargo git checkouts remained 12 GiB, and the sibling NDS target was preserved at 45 GiB.

Post-F2-2 disk checkpoint 2026-06-10: `/` had 249 GiB free, the main checkout `target/` was 10 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after rebuilding `nimbus-system` from the post-F2-1 cleanup and running focused system-table tests plus scoped check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor; `/private/tmp` was not cleaned because this pass had not proven ownership/live-use of its contents.

Post-F2-3 disk checkpoint 2026-06-10: `/` had 249 GiB free, the main checkout `target/` was 10 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused subscription-projection tests plus scoped `nimbus-system` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-F2-4 disk checkpoint 2026-06-10: `/` had 249 GiB free, the main checkout `target/` was 10 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused key-collision tests plus scoped `nimbus-system` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-F2-5 disk checkpoint 2026-06-10: `/` had 245 GiB free, the main checkout `target/` was 14 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused direct-process tests plus scoped `nimbus-node` check/clippy rebuilt the node/runtime stack. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-F2-6 disk checkpoint 2026-06-10: `/` had 245 GiB free, the main checkout `target/` was 14 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after warmed direct-process tests plus scoped `nimbus-node` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-F2-7 disk checkpoint 2026-06-10: `/` had 245 GiB free, the main checkout `target/` was 14 GiB, Cargo git checkouts were 12 GiB, and `/private/tmp` reported 9.5 GiB after focused system cleanup tests plus scoped `nimbus-system` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-I1-3 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 20 GiB, and Cargo git checkouts were 12 GiB after warmed `nimbus-bin` compose-loader tests plus scoped check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor; preserving these artifacts keeps the next nearby CLI finding cheap.

Post-I1-4 disk checkpoint 2026-06-10: `/` had 239 GiB free, the main checkout `target/` was 20 GiB, and Cargo git checkouts were 12 GiB after focused compose service-catalog tests plus scoped `nimbus-bin` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-I1-6 disk checkpoint 2026-06-10: `/` had 237 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after focused encryption-rotation tests plus scoped `nimbus-bin` check/clippy and `make deny`. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-I1-8 disk checkpoint 2026-06-10: `/` had 236 GiB free, the main checkout `target/` was 22 GiB, and Cargo git checkouts were 12 GiB after focused machine unsafe-surface tests plus scoped `nimbus-bin` check/clippy. No cleanup was run because the active checkout target stayed below the 25 GiB trigger and free space stayed above the 200 GiB floor.

Post-I2-2/I2-5 disk checkpoint 2026-06-10: the batched operator/session verification rebuilt the server stack, grew the main checkout `target/` to 33 GiB, and left `/` at 226 GiB free, so the 25 GiB cleanup trigger fired after scoped verification completed. A live `ps -ax -o pid,comm` check showed no active Cargo, rustc, or clippy process. `cargo clean` was run only in this checkout and removed 54,457 files / 38.0 GiB. Post-clean `/` had 257 GiB free, this checkout's `target/` was 40 MiB, and Cargo git checkouts remained 12 GiB.

Post-I2-4/I2-6/I2-7/I1-11/I2-8 disk checkpoint 2026-06-10: the batched CLI/machine/assets/operator verification rebuilt the `nimbus-bin` runtime-adjacent graph once from the post-I2-2/I2-5 cleanup. Post-gate `/` had 242 GiB free, this checkout's `target/` was 16 GiB, and Cargo git checkouts remained 12 GiB. No cleanup was run because the active checkout target stayed below the 25 GiB cleanup trigger and free space stayed above the 200 GiB floor.

Post-J-1/J-2/J-4/J-5/J-6/J-7/J-8 disk checkpoint 2026-06-10: the focused `nimbus-bridge` suite, check, and clippy gate rebuilt the bridge dependency stack from the previous cleanup. Post-gate `/` had 235 GiB free, this checkout's `target/` was 21 GiB, and Cargo git checkouts remained 12 GiB. No cleanup was run because the active checkout target stayed below the 25 GiB cleanup trigger and free space stayed above the 200 GiB floor.

Post-J-3/J-9 disk checkpoint 2026-06-10: focused Convex bridge delegation and websocket-fixture verification grew this checkout's `target/` to 34 GiB and left `/` at 225 GiB free, while Cargo git checkouts remained 12 GiB. An escalated live-use check, `ps -ax -o pid=,command= | rg "(cargo|rustc|clippy)"`, matched only the check itself. `cargo clean` was run only in this checkout and removed 54,133 files / 39.5 GiB; post-clean `/` had 259 GiB free, `target/` was absent, and Cargo git checkouts remained 12 GiB. The added J-3 server proof and scoped clippy rebuild then left `/` at 246 GiB free with this checkout's `target/` at 13 GiB, so no second cleanup was run.

Post-E2-8/E2-9 disk checkpoint 2026-06-10: the focused Firebase aggregation parity test and scoped `nimbus-firebase` clippy reused the warmed server/runtime graph and left `/` at 241 GiB free, this checkout's `target/` at 18 GiB, and Cargo git checkouts at 12 GiB. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-D2-6/D3-3/D3-4/D3-5/D4-7 disk checkpoint 2026-06-10: the batched operator/server info verification reused the warmed graph and left `/` at 239 GiB free, this checkout's `target/` at 20 GiB, and Cargo git checkouts at 12 GiB. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-A1-3/A1-4/A3-10/A3-9/A4-5 disk checkpoint 2026-06-10: the focused storage-info batch rebuilt the storage dependency stack once, then reused it for targeted storage tests and scoped clippy. Post-gate `/` had 236 GiB free, this checkout's `target/` was 22 GiB, and Cargo git checkouts remained 12 GiB. No cleanup was run because the active checkout target remained below the 25 GiB trigger and free space remained above the 200 GiB floor.

Post-D1-1/D1-3/D1-4/D1-5/D1-6/D1-7/D2-2/D2-3/D2-4/D2-5/D3-1/D3-2/D4-4/D4-5 disk checkpoint 2026-06-10: the server-low batch deliberately reused the runtime/server graph instead of cleaning between focused tests. Post-gate `/` had 229 GiB free, this checkout's `target/` was 29 GiB, and Cargo git checkouts remained 12 GiB. Cleanup was deferred because free space stayed above the 200 GiB floor and the remaining info/final gates can reuse the warmed Deno/V8/server artifacts; clean this checkout after those gates or sooner if free space drops below 200 GiB.

Post-G-5/G-6/H1-7 disk checkpoint 2026-06-10: the final info batch reused the warmed server/runtime graph and left `/` at 227 GiB free, this checkout's `target/` at 32 GiB, and Cargo git checkouts at 12 GiB. Cleanup was deferred because final broad gates can reuse these artifacts and free space remains above the 200 GiB floor; clean this checkout after final verification or immediately if free space drops below 200 GiB.

Final closeout disk cleanup 2026-06-10: after final verification, `/` had 170 GiB free and the main checkout `target/` measured 86 GiB. `ps -ax -o pid,comm,args | rg 'cargo|rustc|clippy|nextest|/Users/jack/src/github.com/nimbus/nimbus/target'` showed no active Cargo/rustc/clippy/nextest process or target user for this checkout. `cargo clean` was run only in this checkout and removed 158,168 files / 123.3 GiB. Post-clean `/` had 256 GiB free, this checkout's `target/` was absent, Cargo git checkouts remained 12 GiB, and `/private/tmp` measured 13 GiB.

## Summary

| Verdict | Count |
|---|---:|
| PENDING | 0 |
| CONFIRMED/FIXED | 180 |
| ALREADY-FIXED | 2 |
| NOT-A-DEFECT | 11 |
| DEFERRED-BLOCKED | 0 |

## Final Verification Addendum - 2026-06-10

Final focused regression gates after the last remediation batch:

- `cargo test -p nimbus-server --test reactive_loop -- --nocapture`: 32 passed, 0 failed.
- `cargo test -p nimbus-server tenant_isolation_conformance_suite_covers_runtime_services_storage_and_system_control -- --nocapture`: 1 passed; suite output covered 21 tenant-isolation scenarios (12 allowed, 9 denied).
- `cargo test -p nimbus-server convex_http_demo_ -- --nocapture`: 5 passed, 0 failed.
- `cargo test -p nimbus-bridge host_call_session_validation_ -- --nocapture`: 2 passed, 13 filtered.
- `cargo test -p nimbus-artifacts redaction_ -- --nocapture`: 3 passed, 6 filtered.
- `cargo test -p nimbus-server machine_config_routes_create_update_delete_and_project_system_state -- --nocapture`: 1 passed, 444 filtered.
- `cargo test -p nimbus-tenant external_policy_engine_timeout_fails_closed_without_waiting_for_backend -- --nocapture`: 1 passed, 83 filtered.
- `cargo test -p nimbus-engine engine_update_and_delete_drive_subscription_updates -- --nocapture`: 1 passed, 302 filtered.
- `cargo test -p nimbus-engine subscription_snapshots_expose_covered_sequence_and_commit_timestamp_metadata -- --nocapture`: 1 passed, 302 filtered.

Final broad and repo gates:

- `NIMBUS_DISABLE_IMPLICIT_EXTERNAL_PROVIDER_FIXTURES=1 cargo nextest run --workspace --exclude nimbus-runtime --no-fail-fast --status-level fail --final-status-level fail -- --skip machine_status_detects_reachable_machine_api_socket`: 2937 passed, 0 failed, 19 skipped.
- `cargo fmt --all --check`: pass.
- `git diff --check`: pass.
- `cargo clippy -p nimbus-artifacts -p nimbus-bridge -p nimbus-cloud-functions -p nimbus-operator -p nimbus-runtime -p nimbus-server --all-targets -- -D warnings`: pass.
- `cargo clippy -p nimbus-tenant --all-targets -- -D warnings`: pass.
- `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass.
- `npm run typecheck && npm run test && npm run build`: pass; UI workspace reported 42 test files and 278 tests passed during `npm run test`.
- `make check`: pass; embedded package build/stage completed and workspace `cargo check` finished successfully.
- `make clippy`: pass; workspace all-targets clippy completed with `-D warnings`.
- `make deny`: pass; advisories, bans, licenses, and sources all ok.
- `make verify-third-party-attribution`: pass; no guarded crates exist yet, so the gate has nothing to enforce until the guest/libkrun crates land.

`make ci` was not rerun as a final green gate in this closeout. The last full `make ci` attempt failed only in the `nimbus-runtime` Node capability/bootstrap lane (Node permission/bootstrap expectations such as local filesystem/env/tmp/spawn access). Those failures are excluded from this remediation closeout by operator instruction because Node capability/bootstrap work is not part of the reviewed finding plan. The non-runtime broad lane and all repo gates above are green.

## Ledger

| ID | Sev | Subsystem | Verdict | What changed | Tests added | Verification result | Commit |
|---|---|---|---|---|---|---|---|
| `E1-1` | critical | Adapters | CONFIRMED/FIXED | Re-read `commands/mod.rs`, connection SCRAM state, listener tests, and server wiring. `dispatch` now rejects data/DDL/session commands until `ConnectionState.authenticated` is true; authenticated commands derive a MongoDB principal from SCRAM identity. | `dispatch_rejects_data_command_before_authentication`; server wire CRUD test asserts unauthenticated insert is `Unauthorized`; MongoDB spec runner authenticates before seeding/ops. | `cargo test -p nimbus-mongodb`: 245 passed; `cargo test -p nimbus-server mongodb -- --nocapture`: 6 passed; `cargo test -p nimbus-server --test mongodb_spec -- --nocapture`: 23 passed; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `E1-2` | critical | Adapters | CONFIRMED/FIXED | Re-read CRUD/filter/aggregation/collection/index/session call paths plus engine authz rules. Removed MongoDB-originated `PrincipalContext::system()` use; authenticated SCRAM identity is threaded through reads, writes, DDL, aggregation, session, and transaction paths. | `command_principal_enforces_owner_access_policy` proves MongoDB writes/reads obey principal-claim table policy; existing CRUD/aggregation/session tests updated to pass explicit principals. | `cargo test -p nimbus-mongodb`: 245 passed; `cargo test -p nimbus-server mongodb -- --nocapture`: 6 passed; `cargo test -p nimbus-server --test mongodb_spec -- --nocapture`: 23 passed; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `E1-3` | high | Adapters | CONFIRMED/FIXED | Re-read `_id` fast path in `crud/filter.rs`, residual-filter matcher, find tests, and CRUD callers. `matches_simple_filters` now evaluates `Gt`/`Gte`/`Lt`/`Lte` with the same comparator used by sorting instead of treating them as unconditional matches. | `find_id_fast_path_applies_residual_range_filters` covers `_id` plus false/true `$gt`, `$gte`, `$lt`, `$lte`, and missing-field residual predicates. | `cargo test -p nimbus-mongodb find_id_fast_path_applies_residual_range_filters`: 1 passed; `cargo test -p nimbus-mongodb`: 246 passed; `cargo clippy -p nimbus-mongodb --all-targets -- -D warnings`: pass; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `E1-5` | high | Adapters | CONFIRMED/FIXED | Re-read `AuthConfig::default`, listener startup, and `MongoDbConfig` constructors. Removed hard-coded `admin`/`admin` default and require explicit `Arc<AuthConfig>` for MongoDB listener configuration. | Listener and wire/spec tests now construct explicit test SCRAM credentials; package smoke tests require `--smoke-username` and `--smoke-password` when exercising a live listener. | `cargo test -p nimbus-mongodb`: 245 passed; `cargo test -p nimbus-server mongodb -- --nocapture`: 6 passed; `cargo test -p nimbus-server --test mongodb_spec -- --nocapture`: 23 passed; `npm run test --workspace @nimbus/mongodb`: package exports, bundle, mongoUri, typecheck passed; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `A2-1` | high | Storage | CONFIRMED/FIXED | Re-read `index/scan/range.rs`, `index/bounds.rs`, encoding tags, storage tests, and engine planner residual behavior. Single-field and composite range scans now build bounded redb keys scoped to the bound value's encoded JSON type tag, preventing open-ended range scans from admitting other JSON types. Removed the obsolete full-prefix byte-filter scan helper path. | `index_scan_open_ended_range_excludes_other_json_types`; `composite_index_range_scan_excludes_other_json_types_on_range_field`; existing range/index suites exercise the new bounded helper. | Focused new storage tests: 3 passed; `cargo test -p nimbus-storage`: 306 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass; `make clippy`: pass; `make check`: pass; `cargo fmt --all --check`: pass. | Final batch commit |
| `A2-2` | high | Storage | CONFIRMED/FIXED | Re-read `index/tests.rs` and confirmed the prior numeric-only range test missed mixed-type and negative-number scan behavior. Added mixed-type single-field/composite range tests and a negative-to-positive range-ordering test. | `index_scan_open_ended_range_excludes_other_json_types`; `index_scan_range_orders_negative_and_positive_numbers`; `composite_index_range_scan_excludes_other_json_types_on_range_field`. | Focused new storage tests: 3 passed; `cargo test -p nimbus-storage`: 306 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass; `make clippy`: pass; `make check`: pass; `cargo fmt --all --check`: pass. | Final batch commit |
| `B1-1` | high | Engine | CONFIRMED/FIXED | Re-read direct store mutation helpers, queued journal apply, execution-unit commit path, async/sync document reads, document-cache facade, and subscription cache tests. Direct store mutations now invalidate document cache and materialized reads while still holding the mutation sequence guard, after durable-head publication and before `applied_head` publication. | `direct_mutations_invalidate_document_cache_before_applied_head_is_visible` pauses cache invalidation and proves an async read remains pending until invalidation completes, then returns the updated document instead of stale cached data. | `cargo test -p nimbus-engine direct_mutations_invalidate_document_cache_before_applied_head_is_visible -- --nocapture`: 1 passed, 275 filtered; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass; `cargo test -p nimbus-engine`: 274 passed, 2 ignored, doc-tests 0; `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 17.38s; `make check`: pass, workspace check finished in 7.80s. | Final batch commit |
| `B1-2` | high | Engine | CONFIRMED/FIXED | Re-read `execution_units/commit.rs`, execution-unit begin/snapshot flow, dependency recording, conflict-intersection logic, storage CAS backstops, and execution-unit conflict tests. `commit()` now performs schema-change checks, OCC conflict scanning, storage append, durable-head publication, cache/materialized invalidation, and applied-head publication inside one `lock_mutation_sequence` critical section; observer fanout remains outside the serialized writer section. | `mutation_execution_unit_conflict_scan_and_append_are_sequence_atomic` proves a second execution unit cannot pass the phantom-conflict scan while the first has scanned but not appended; `mutation_execution_unit_conflicts_with_durable_unapplied_write` now asserts execution-unit commits wait behind the queued journal writer's sequence gate before conflicting. | `cargo test -p nimbus-engine mutation_execution_unit_conflict_scan_and_append_are_sequence_atomic -- --nocapture`: 1 passed, 276 filtered; `cargo test -p nimbus-engine mutation_execution_unit_conflicts_with_durable_unapplied_write -- --nocapture`: 1 passed, 276 filtered; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass; `cargo test -p nimbus-engine`: 275 passed, 2 ignored, doc-tests 0; `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 16.40s; `make check`: pass, workspace check finished in 6.41s. | Final batch commit |
| `B2-1` | high | Engine | CONFIRMED/FIXED | Re-read evaluator pagination, prepared-query index planning, cursor encode/decode, query tests, and subscription/read-surface callers. Indexed residual-query paths now sign cursors with the canonical planned paginated query while still executing the residual/effective query for filtering and ordering, so cursor signatures remain stable when the planner switches between index and fallback plans. | `paginated_query_cursor_survives_index_plan_change_between_pages` creates a cursor under an indexed plan, removes the index, and proves the same user query can resume the next page under the fallback plan without a signature mismatch. | `cargo test -p nimbus-engine paginated_query_cursor_survives_index_plan_change_between_pages -- --nocapture`: 1 passed, 277 filtered; `cargo test -p nimbus-engine cursor -- --nocapture`: 15 passed, 263 filtered; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass; `cargo test -p nimbus-engine`: 276 passed, 2 ignored, doc-tests 0, finished in 1330.63s; `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 13.49s; `make check`: pass, workspace check finished in 6.28s. | Final batch commit |
| `B3-1` | high | Engine | CONFIRMED/FIXED | Re-read `tenant/lifecycle.rs`, `TenantRuntime` operation guards, sync/async tenant deletion, and existing lifecycle tests. `release_operation` now holds the same `zero_active_lock` used by `begin_delete_blocking` while decrementing the active-operation predicate and notifying the `Condvar`, closing the lost-wakeup window for sync tenant deletion. | `release_operation_notifies_blocking_delete_after_waiter_registers` forces the private condvar interleaving and would fail if release could notify before the waiter parks; `delete_tenant_waits_for_in_flight_sync_operation_and_rejects_new_work` covers the public sync `Engine::delete_tenant` path with an in-flight sync query and blocked new work. | `cargo test -p nimbus-engine release_operation_notifies_blocking_delete_after_waiter_registers -- --nocapture`: 1 passed, 278 filtered; `cargo test -p nimbus-engine lifecycle -- --nocapture`: 8 passed, 272 filtered, finished in 360.03s; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass; `make clippy`: pass, workspace clippy finished in 13.01s; `make check`: pass, workspace check finished in 6.11s. | Final batch commit |
| `B4-1` | high | Engine | CONFIRMED/FIXED | Re-read trigger execution bootstrap, durable trigger state transitions, execution worker claiming, pending/retry restart tests, and test hooks. Bootstrap now re-enqueues durable `Running` trigger invocations at `Timestamp(0)`, and the worker treats recovered `Running` records as already-claimed attempts instead of dropping them or trying to begin a second attempt. | `installing_executor_replays_running_invocations_after_restart` persists a durable `Running` invocation across an engine restart and proves installing the executor replays it to `Completed` at attempt 1. Adjacent pending and retry restart tests were rerun to guard existing bootstrap behavior. | `cargo test -p nimbus-engine installing_executor_replays_running_invocations_after_restart -- --nocapture`: 1 passed, 280 filtered; `cargo test -p nimbus-engine installing_executor_bootstraps_pending_invocations_after_restart -- --nocapture`: 1 passed, 280 filtered; `cargo test -p nimbus-engine installing_executor_bootstraps_due_retry_invocations_after_restart -- --nocapture`: 1 passed, 280 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass; `make clippy`: pass, workspace clippy finished in 12.22s; `make check`: pass, workspace check finished in 5.84s. | Final batch commit |
| `C3-1` | high | Runtime | CONFIRMED/FIXED | Re-read Bun/JSC linked backend admission, linked FFI call, adapter contract, V8 backend watchdog wiring, run-to-completion permit lifecycle, and Bun/JSC backend tests. Confirmed the permit portion is already covered by the shared invocation lifecycle, but timeout enforcement is fail-open for linked Bun/JSC. The runtime now fails closed before guest entry for linked Bun/JSC adapters when `execution_timeout` is nonzero; raw linked-adapter seam tests must use an explicit no-timeout policy until the ABI exposes enforceable deadlines/cancellation. | `bun_jsc_linked_backend_rejects_timeout_policy_before_guest_entry` proves a default Bun/JSC timeout policy is rejected before adapter invocation or lifecycle entry. Existing linked-seam and adapter-cancellation tests now exercise only explicit no-timeout policies; the pre-cancelled test confirms cancellation still returns before the timeout guard. | `cargo test -p nimbus-runtime bun_jsc_linked_backend_rejects_timeout_policy_before_guest_entry -- --nocapture`: 1 passed, 547 filtered; `cargo test -p nimbus-runtime bun_jsc_runtime_backend_dispatches_through_no_timeout_linked_adapter_seam -- --nocapture`: 1 passed, 547 filtered; `cargo test -p nimbus-runtime bun_jsc_linked_backend_records_cancelled_adapter_then_tears_down -- --nocapture`: 1 passed, 547 filtered; `cargo test -p nimbus-runtime bun_jsc_linked_backend_rejects_pre_cancelled_invocation_before_guest_entry -- --nocapture`: 1 passed, 547 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass; `make clippy`: pass, workspace clippy finished in 15.42s; `make check`: pass, workspace check finished in 7.58s. | Final batch commit |
| `D4-1` | high | Server | CONFIRMED/FIXED | Duplicate of `E1-1`, confirmed from the server/listener side. The listener can no longer dispatch MongoDB data-plane commands before SCRAM auth completes. | Server listener test rejects unauthenticated data command; wire CRUD roundtrip authenticates before insert/find. | `cargo test -p nimbus-server mongodb -- --nocapture`: 6 passed; `cargo test -p nimbus-server --test mongodb_spec -- --nocapture`: 23 passed; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `I1-1` | high | CLI | CONFIRMED/FIXED | Re-read HTTP image materialization, `MachineImageSource` parsing/serialization, machine create/status callers, server/system machine source display, and image tests. HTTP machine image sources now require a canonical `#sha256=<64 hex>` integrity suffix, materialization verifies the staged raw boot disk before `persist`, plaintext remote HTTP is rejected before download, and all machine image display/status reducers preserve the digest. | `http_image_source_rejects_sha256_mismatch_before_persisting_disk`; `http_image_source_rejects_plaintext_non_loopback_before_download`; `machine_image_source_parse_rejects_http_sources_without_sha256`; existing raw/gzip HTTP materialization tests now prove digest-checked success. | `cargo test -p nimbus-bin http_image_source_ -- --nocapture`: 3 passed, 570 filtered; `cargo test -p nimbus-bin http_gzip_image_source_materializes_decompressed_disk_into_reserved_path -- --nocapture`: 1 passed, 572 filtered; `cargo test -p nimbus-bin machine_image_source_parse_ -- --nocapture`: 2 passed, 571 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-bin -p nimbus-server -p nimbus-system --all-targets -- -D warnings`: pass, finished in 32.22s; `make clippy`: pass, workspace clippy finished in 11.33s; `make check`: pass, workspace check finished in 7.41s. | Final batch commit |
| `E1-6` | medium | Adapters | CONFIRMED/FIXED | Re-read MongoDB update lowering, existing update operator tests, core write-batch transform types, engine transform application, and execution-unit batch callers. Added engine-owned atomic transforms for append, pop, multiply, and bitwise operations; MongoDB `$push`, `$pop`, `$mul`, and `$bit` now lower to those transforms instead of reading `current_doc` into patches. | `atomic_write_batch_applies_array_multiply_and_bitwise_transforms`; `update_read_modify_write_operators_compose_from_stale_snapshots` builds two stale-snapshot writes per affected operator and proves all effects compose through the engine. Existing MongoDB update/operator tests were rerun in the full crate suite. | `cargo test -p nimbus-engine atomic_write_batch_applies_array_multiply_and_bitwise_transforms -- --nocapture`: 1 passed, 281 filtered; `cargo test -p nimbus-engine atomic_write_batch_transform_write_creates_missing_document_and_returns_ordered_results -- --nocapture`: 1 passed, 281 filtered; `cargo test -p nimbus-mongodb update_read_modify_write_operators_compose_from_stale_snapshots -- --nocapture`: 1 passed, 246 filtered; `cargo test -p nimbus-mongodb`: 247 passed; `cargo test -p nimbus-core`: 127 passed, doc-tests 0; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-core -p nimbus-engine -p nimbus-mongodb --all-targets -- -D warnings`: pass, finished in 17.31s; `make clippy`: pass, workspace clippy finished in 31.10s; `make check`: pass, workspace check finished in 26.39s. | Final batch commit |
| `E1-7` | medium | Adapters | CONFIRMED/FIXED | Re-read MongoDB aggregation execution, CRUD filter lowering, aggregation tests, and the engine `Query` surface. Aggregation now derives an initial engine query from a pushable leading `$match` sequence plus leading `$limit`s, while preserving pipeline execution and falling back to full materialization for unpushable `_id` matches or unsupported operators. The shared CRUD filter translator is visible only within `commands`. | `aggregate_initial_query_pushes_leading_match_and_limit`; `aggregate_initial_query_keeps_match_after_limit_in_pipeline`; `aggregate_initial_query_keeps_id_match_in_pipeline`; existing aggregation behavior suite rerun. | `cargo test -p nimbus-mongodb aggregate_ -- --nocapture`: 27 passed, 225 filtered; `cargo test -p nimbus-mongodb`: 252 passed; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-mongodb --all-targets -- -D warnings`: pass, finished in 12.51s; `make clippy`: pass, workspace clippy finished in 8.90s; `make check`: pass, workspace check finished in 4.45s. | Final batch commit |
| `E1-9` | medium | Adapters | CONFIRMED/FIXED | Re-read MongoDB per-connection cursor/session stores, find/aggregation cursor callers, and session command dispatch. Added per-connection caps for retained overflow cursors and logical sessions; fully consumed zero-id cursors do not consume capacity, overflow cursor creation is fallible, and `startSession` now rejects new sessions at the cap with `TooManyLogicalSessions`. | `cursor_store_rejects_new_overflow_cursor_at_connection_cap`; `start_session_rejects_new_session_at_connection_cap`. | `cargo test -p nimbus-mongodb cursor_store_rejects_new_overflow_cursor_at_connection_cap -- --nocapture`: 1 passed, 248 filtered; `cargo test -p nimbus-mongodb start_session_rejects_new_session_at_connection_cap -- --nocapture`: 1 passed, 248 filtered; `cargo test -p nimbus-mongodb`: 249 passed; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-mongodb --all-targets -- -D warnings`: pass, finished in 11.45s; `make clippy`: pass, workspace clippy finished in 8.33s; `make check`: pass, workspace check finished in 8.92s. | Final batch commit |
| `E2-2` | medium | Adapters | CONFIRMED/FIXED | Re-read Firebase error mapping, structured-query index enforcement, core `Error`, and downstream error-envelope/adapter mappers. Added typed `Error::MissingIndex { fields }`; the engine now produces that variant, Firebase matches it directly instead of parsing prose, and bridge/server/Convex/DynamoDB/CLI boundaries map it explicitly. | `compound_structured_queries_require_matching_schema_indexes`; `firebase_rest_error_uses_failed_precondition_for_missing_index`; `firebase_rest_error_does_not_parse_missing_index_from_invalid_input_text`; `runtime_host_error_envelope_preserves_missing_index_fields`; `maps_each_core_error_class_to_the_expected_dynamodb_code`; `missing_index_error_round_trips_through_runtime_encoding`; `missing_index_maps_to_precondition_failed_with_fields`; `machine_client_missing_index_error_maps_to_invalid_spec`. | `cargo test -p nimbus-engine compound_structured_queries_require_matching_schema_indexes -- --nocapture`: 1 passed, 281 filtered; `cargo test -p nimbus-firebase missing_index -- --nocapture`: 2 passed, 41 filtered; `cargo test -p nimbus-core`: 127 passed, doc-tests 0; `cargo test -p nimbus-firebase`: 43 passed; `cargo clippy -p nimbus-core -p nimbus-engine -p nimbus-firebase --all-targets -- -D warnings`: pass, finished in 18.67s; `cargo test -p nimbus-bridge runtime_host_error_envelope_preserves_missing_index_fields -- --nocapture`: 1 passed, 8 filtered; `cargo test -p nimbus-dynamodb maps_each_core_error_class_to_the_expected_dynamodb_code -- --nocapture`: 1 passed, 244 filtered; `cargo test -p nimbus-convex missing_index_error_round_trips_through_runtime_encoding -- --nocapture`: 1 passed, 13 filtered; `cargo test -p nimbus-server missing_index_maps_to_precondition_failed_with_fields -- --nocapture`: 1 passed, 431 filtered; `cargo test -p nimbus-bin machine_client_missing_index_error_maps_to_invalid_spec -- --nocapture`: 1 passed, 574 filtered; `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 8.53s; `make check`: pass, workspace check finished in 22.38s. | Final batch commit |
| `E3-1` | medium | Adapters | CONFIRMED/FIXED | Re-read DynamoDB `PutItem`/`DeleteItem`/`UpdateItem`, `BatchWriteItem`, stream capture, and the `TransactWriteItems` atomic precedent. Moved reusable stream-change folding into `stream.rs`; single-item and batch-write handlers now execute the base item write plus stream event/counter writes in one `AtomicWriteBatch`, with bounded retry only for verified stream sequence contention. Removed the obsolete post-write `remove_item` path. | `single_item_writes_roll_back_when_stream_event_cannot_commit` covers `PutItem`, `DeleteItem`, and `UpdateItem`; `batch_write_requests_roll_back_when_stream_event_cannot_commit` covers batch put/delete rollback; existing stream-record emission tests were rerun. | `cargo test -p nimbus-dynamodb single_item_writes_roll_back_when_stream_event_cannot_commit -- --nocapture`: 1 passed, 244 filtered; `cargo test -p nimbus-dynamodb batch_write_requests_roll_back_when_stream_event_cannot_commit -- --nocapture`: 1 passed, 244 filtered; `cargo test -p nimbus-dynamodb stream_records -- --nocapture`: 3 passed, 242 filtered; `cargo test -p nimbus-dynamodb`: 245 unit tests, 7 failure-injection tests, 1 ground-truth test, 1 soak test, 4 tenant-isolation tests passed; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-dynamodb --all-targets -- -D warnings`: pass, finished in 22.60s; `make clippy`: pass, workspace clippy finished in 12.37s; `make check`: pass, workspace check finished in 8.18s. | Final batch commit |
| `E3-2` | medium | Adapters | CONFIRMED/FIXED | Re-read `attribute_value.rs`, `lib.rs`, `expression.rs`, `dispatch.rs`, `tenant.rs`, `query.rs`, `stream.rs`, `ttl.rs`, DynamoDB tests, and the storage/engine read seams. Removed the unused public AttributeValue sidecar `StoredValue` codec and stale re-exports; kept the live wire-JSON `Document.fields` codec. Corrected the auth-mode comment to reflect strict-by-default behavior. Added bounded table-id prefix and lower-bound read primitives through Storage/Engine; DynamoDB Query now reads the encoded partition prefix instead of enumerating the whole table, and Streams GetRecords reads from the iterator sequence with a page limit instead of materializing every event. Unknown stream event names now return a typed error, expired stream records are reclaimed through one engine-owned atomic write batch, TTL config upsert uses `atomic_overwrite`, and `query.rs`/`stream.rs` production files were split below the 1,500-line threshold by moving tests into concept-owned sibling modules. | `query_partition_does_not_decode_unrelated_partition_rows`; `get_records_does_not_decode_events_before_iterator_start`; `shape_record_rejects_corrupt_event_name`. Existing query partition, stream reclaim, TTL update, and wire-JSON codec tests were rerun after the sidecar removal and bounded-read changes. | `cargo check -p nimbus-dynamodb`: pass, finished in 5.82s; initial `cargo test -p nimbus-dynamodb query_partition -- --nocapture` failed before running tests on a missing test-only `BTreeSet` import after sidecar cleanup and is not proof; after fixing the import, final `cargo test -p nimbus-dynamodb query_partition -- --nocapture`: 2 passed, 243 filtered, integration binaries 0 matching, finished in 0.03s after a 6.41s compile; `cargo test -p nimbus-dynamodb get_records_does_not_decode_events_before_iterator_start -- --nocapture`: 1 passed, 244 filtered, integration binaries 0 matching, finished in 0.01s; `cargo test -p nimbus-dynamodb get_records_skips_expired_events_and_reclaims_their_storage -- --nocapture`: 1 passed, 244 filtered, integration binaries 0 matching, finished in 0.01s; `cargo test -p nimbus-dynamodb shape_record_rejects_corrupt_event_name -- --nocapture`: 1 passed, 244 filtered; `cargo test -p nimbus-dynamodb update_enables_ttl_and_describe_reports_it -- --nocapture`: 1 passed, 244 filtered; `cargo test -p nimbus-dynamodb item_roundtrips_through_wire_json_fields -- --nocapture`: 1 passed, 244 filtered; initial `cargo fmt --all --check` found formatting-only diffs from the mechanical test move and is not proof; final `cargo fmt --all --check`: pass; initial scoped clippy found a new needless-borrow warning in the query regression and is not proof; final `cargo clippy -p nimbus-dynamodb -p nimbus-storage -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 2.25s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 18 GiB, Cargo git checkouts 12 GiB, 241 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-5` | medium | Adapters | CONFIRMED/FIXED | Re-read Cloud Functions Firestore Admin runtime-extension dispatch, sync/cancellable/async entrypoints, server executor callers, bridge host capability helpers, and existing Cloud Functions tests. Confirmed the surface had no direct unit coverage. Added a fake `RuntimeCapabilityHost` backed by a real engine and mutation execution unit, then exercised Firestore Admin get/set/update/delete through sync, cancellable, and async dispatch. | `firestore_admin_sync_dispatch_round_trips_crud`; `firestore_admin_cancellable_dispatch_round_trips_crud`; `firestore_admin_async_dispatch_round_trips_crud`; `firestore_admin_rejects_non_default_database_id_on_reads_and_writes`; `firestore_admin_reports_validation_and_contract_errors`. | Initial focused test compile caught a test-only move in the unsupported-operation assertion; fixed by borrowing the contract message. Final `cargo test -p nimbus-cloud-functions firestore_admin_ -- --nocapture`: 5 passed, 21 filtered, finished in 4.48s; `cargo test -p nimbus-cloud-functions`: 26 passed; `cargo clippy -p nimbus-cloud-functions --all-targets -- -D warnings`: pass, finished in 24.19s; initial `cargo fmt --all --check` caught wrapping in the new test module; `cargo fmt --all` applied it; final `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, 244 GiB free, so no cleanup triggered. | Final batch commit |
| `A2-3` | medium | Storage | CONFIRMED/FIXED | Re-read live index key parsing, scan loops, and historical-index corruption handling. `doc_id_from_index_key` and its trailer parser now return typed `StorageErrorKind::Corruption` errors for missing trailers, length underflow, non-UTF8 document IDs, and invalid document IDs; live scan loops propagate those errors instead of panicking on corrupted storage keys. | `doc_id_from_index_key_round_trips_valid_trailer`; `doc_id_from_index_key_rejects_missing_length_trailer`; `doc_id_from_index_key_rejects_length_underflow`; `doc_id_from_index_key_rejects_non_utf8_document_id`; `doc_id_from_index_key_rejects_invalid_document_id`. | `cargo test -p nimbus-storage doc_id_from_index_key_ -- --nocapture`: 5 passed, 308 filtered; `cargo test -p nimbus-storage index_insert_and_eq_scan -- --nocapture`: 1 passed, 312 filtered; `cargo test -p nimbus-storage index_scan_range_on_numbers -- --nocapture`: 1 passed, 312 filtered; `cargo test -p nimbus-storage index_ -- --nocapture`: 53 passed, 260 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 13.83s; `make clippy`: pass, workspace clippy finished in 16.49s; `make check`: pass, workspace check finished in 8.18s. | Final batch commit |
| `A2-4` | medium | Storage | CONFIRMED/FIXED | Re-read the cited single-field range path, bounded helper, scan reader, public `TenantStore`/`TenantReadSnapshot` range callers, and mixed-type range tests. This was the same defect class fixed under `A2-1`: single-field range scans now derive concrete `single_field_range_scan_bounds` and delegate to `scan_documents_for_index_key_bounds_in_read_txn`, which performs a real bounded redb `range(start_key..end_key)` when an upper bound exists. | Covered by the `A2-1`/`A2-2` range tests: `index_scan_open_ended_range_excludes_other_json_types`; `index_scan_range_orders_negative_and_positive_numbers`; `composite_index_range_scan_excludes_other_json_types_on_range_field`; existing `index_scan_range_on_numbers`. | `cargo test -p nimbus-storage index_scan_range_on_numbers -- --nocapture`: 1 passed, 312 filtered; `cargo test -p nimbus-storage index_scan_open_ended_range_excludes_other_json_types -- --nocapture`: passed during focused new storage tests; `cargo test -p nimbus-storage`: 306 passed, 2 ignored, doc-tests 0; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `A2-6` | medium | Storage | CONFIRMED/FIXED | Re-read `index/maintenance/rebuild.rs`, module exports, caller search, and the live journal/schema rebuild path. Deleted the unused `clear_table_indexes` / `rebuild_table_indexes` maintenance module instead of preserving a non-atomic three-transaction API; live schema/index rebuild remains in `store/journal.rs` inside the active write transaction. | No new test was needed for deleted dead API. Existing live schema maintenance tests were rerun to prove the supported atomic paths still rebuild and clear index entries. | Caller search `rg -n "rebuild_table_indexes|clear_table_indexes|collect_index_keys_for_prefix" crates --glob '*.rs'`: no matches; `cargo test -p nimbus-storage replace_table_schema_rebuilds_indexes_and_persists_schema -- --nocapture`: 1 passed, 312 filtered; `cargo test -p nimbus-storage delete_table_schema_clears_schema_and_indexes -- --nocapture`: 1 passed, 312 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 4.25s; broad `cargo test -p nimbus-storage` was aborted after entering unrelated slow `libsql_provider` lanes and is not counted as proof; `make clippy`: pass, workspace clippy finished in 17.97s; `make check`: pass, workspace check finished in 8.87s. | Final batch commit |
| `B1-3` | medium | Engine | CONFIRMED/FIXED | Re-read atomic write precondition enforcement, `WritePrecondition`, Firebase REST/gRPC lowering, and existing precondition tests. The engine now enforces `update_time` preconditions by comparing the loaded document's `update_time`, returning `DocumentNotFound` for missing targets and `PreconditionFailed` for stale timestamps instead of the placeholder "not executable yet" rejection. | `atomic_write_batch_enforces_update_time_preconditions` covers matching update-time patch success, stale update-time rollback, missing-document failure, and matching update-time delete success. Existing Firebase parser test proves REST commit still lowers `updateTime` into the shared precondition type. | `cargo test -p nimbus-engine atomic_write_batch_enforces_update_time_preconditions -- --nocapture`: 1 passed, 282 filtered; `cargo test -p nimbus-engine atomic_write_batch_rolls_back_on_precondition_failure -- --nocapture`: 1 passed, 282 filtered; `cargo test -p nimbus-firebase parses_update_patch_delete_verify_and_transaction_bytes -- --nocapture`: 1 passed, 42 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 13.85s; `make clippy`: pass, workspace clippy finished in 13.05s; `make check`: pass, workspace check finished in 6.49s. | Final batch commit |
| `B2-5` | medium | Engine | CONFIRMED/FIXED | Re-read the cited evaluator pagination rejection test, query planner/prepared pagination path, and the `B2-1` regression coverage. This missing-coverage finding was fixed by the `B2-1` change: the test now mints a cursor under an indexed plan, drops the index from the schema, and resumes with the same user query under the fallback plan. | `paginated_query_cursor_survives_index_plan_change_between_pages`. | `cargo test -p nimbus-engine paginated_query_cursor_survives_index_plan_change_between_pages -- --nocapture`: 1 passed, 282 filtered; broader `B2-1` verification previously recorded `cargo test -p nimbus-engine cursor -- --nocapture`: 15 passed, 263 filtered; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `B3-3` | medium | Engine | CONFIRMED/FIXED | Re-read subscription queue overflow fallback, delivery worker, dispatch publication, registry delivery state, and overflow tests. Final subscription publication now goes through the registry under its write lock, rechecking staleness immediately before `try_send` and advancing dependencies/watermark in the same critical section, so an older worker/fallback result cannot be sent after a newer sequence has already published. | `publishing_rechecks_staleness_before_sending_update` proves stale final publication is skipped before reaching the receiver; existing `subscription_delivery_queue_overflow_falls_back_without_regressing_monotonicity` proves overflow fallback still reaches the latest state and skips older queued deliveries. | `cargo test -p nimbus-engine publishing_rechecks_staleness_before_sending_update -- --nocapture`: 1 passed, 283 filtered; `cargo test -p nimbus-engine subscription_delivery_queue_overflow_falls_back_without_regressing_monotonicity -- --nocapture`: 1 passed, 283 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 7.21s; `make clippy`: pass, workspace clippy finished in 21.69s; `make check`: pass, workspace check finished in 13.13s. | Final batch commit |
| `B3-4` | medium | Engine | CONFIRMED/FIXED | Re-read trigger candidate worker test/prod loops, queue draining, trigger materialization, bootstrap replay, and mutation-journal trigger tests. The worker now tracks per-commit progress across drained batches; on a transient error it requeues the failing commit and unprocessed suffix at the front with backoff instead of dropping them until restart. Added a deterministic materialization fault point at the engine/storage seam without weakening the atomic storage transaction. | `trigger_candidate_worker_retries_transient_materialization_failure` injects a first materialization failure and proves the live worker retries in-process, materializes the durable invocation, advances the cursor to sequence 1, and publishes candidates exactly once. | `cargo test -p nimbus-engine trigger_candidate_worker_retries_transient_materialization_failure -- --nocapture`: 1 passed, 284 filtered; `cargo test -p nimbus-engine trigger_candidates_publish_only_after_journal_apply -- --nocapture`: 1 passed, 284 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-storage -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 15.59s; `make clippy`: pass, workspace clippy finished in 30.69s after waiting for external target cleanup/build to drain; `make check`: pass, workspace check finished in 34.08s. | Final batch commit |
| `B4-2` | medium | Engine | CONFIRMED/FIXED | Re-read cron schedule advancement, HTTP/engine cron creation callers, scheduler tick callers, and the scheduled-job one-shot sibling path. `CronSchedule::next_after` now uses saturating multiplication and addition so pathological intervals cannot wrap or panic into a past/never-fire timestamp. | `cron_next_after_saturates_interval_multiplication`; `cron_next_after_saturates_timestamp_addition`. | `cargo test -p nimbus-core cron_next_after -- --nocapture`: 3 passed, 126 filtered; `cargo clippy -p nimbus-core --all-targets -- -D warnings`: pass, finished in 5.21s; B4 cluster `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 33.71s; `make check`: pass, workspace check finished in 34.74s. | Final batch commit |
| `B4-3` | medium | Engine | CONFIRMED/FIXED | Re-read sync/async scheduler due-job processing, scheduled-job result/complete bookkeeping, running-job recovery, and scheduler tests. Per-job bookkeeping errors now warn and isolate the failing job instead of aborting the whole claimed batch and stranding the tail until restart/tenant reload. Added deterministic scheduled-job bookkeeping fault points. | `scheduler_continues_claimed_batch_after_result_bookkeeping_failure` proves a first result-bookkeeping failure does not prevent the second claimed job from executing and completing. | `cargo test -p nimbus-engine scheduler_continues_claimed_batch_after_result_bookkeeping_failure -- --nocapture`: 1 passed, 285 filtered; `cargo test -p nimbus-engine scheduler_async_write_path_round_trips_pending_running_and_history_state -- --nocapture`: 1 passed, 285 filtered; B4 cluster `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 33.71s; `make check`: pass, workspace check finished in 34.74s. | Final batch commit |
| `B4-4` | medium | Engine | CONFIRMED/FIXED | Re-read engine scheduler claim wrappers, tenant persistence write trait, redb/SQLite/libsql/Postgres/MySQL claim implementations, and scheduler storage/provider tests. Claim APIs now require an explicit `max_jobs`; the engine applies a production batch cap before pending jobs move to running. Redb stops its due range at the cap, Postgres/MySQL use `LIMIT`, and SQLite/libsql persist an ordered `run_at` key so bounded due batches are selected in storage without materializing all pending jobs. | `claim_due_jobs_respects_batch_limit_and_leaves_remainder_pending`; `sqlite_claim_due_jobs_respects_batch_limit_and_due_order`; `scheduler_claims_bounded_due_job_batch`. | `cargo check -p nimbus-storage -p nimbus-engine`: pass, finished in 44.84s; `cargo test -p nimbus-storage claim_due_jobs_respects_batch_limit -- --nocapture`: 2 passed, 313 filtered; `cargo test -p nimbus-storage claim_due_jobs_includes_u64_max_boundary -- --nocapture`: 2 passed, 313 filtered; `cargo test -j 1 -p nimbus-engine scheduler_claims_bounded_due_job_batch -- --nocapture`: 1 passed, 286 filtered; `cargo test -p nimbus-storage sqlite_scheduler_state_round_trips_results_crons_and_recovery -- --nocapture`: 1 passed, 314 filtered; B4 cluster `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 33.71s; `make check`: pass, workspace check finished in 34.74s. | Final batch commit |
| `B4-5` | medium | Engine | CONFIRMED/FIXED | Re-read the tenant-load gate field, sync lifecycle callers, async tenant-load/scheduler callers, and tenant-load recovery tests. Replaced the `tokio::sync::Mutex` try-lock/yield loop with a private shared sync/async gate: blocking callers wait on a `Condvar`, async callers await a `Notify`, and both coordinate on the same held bit without holding a blocking mutex across `.await`. | `blocking_lock_parks_until_guard_drops`; `async_lock_waits_for_blocking_guard`. | `cargo check -p nimbus-engine`: pass, finished in 1.03s; `cargo test -p nimbus-engine tenant_load_gate -- --nocapture`: 2 passed, 287 filtered; `cargo test -p nimbus-engine load_tenants_with_scheduled_work_recovers_running_jobs -- --nocapture`: 1 passed, 288 filtered; B4 cluster `cargo fmt --all --check`: pass; `make clippy`: pass, workspace clippy finished in 33.71s; `make check`: pass, workspace check finished in 34.74s. | Final batch commit |
| `C1-1` | medium | Runtime | CONFIRMED/FIXED | Re-read runtime-local `stat`/`readLink` ops, runtime path policy, symlink creation policy, and Node capability tests. `stat` with `follow_symlink` now canonicalizes and rechecks the resolved metadata target before statting it, while `readLink` canonicalizes the link object's parent and verifies the returned target resolves inside read roots before exposing it. Non-following metadata keeps lstat-style link-object semantics. | `application_node22_confines_symlink_stat_and_readlink_targets` creates in-root and escaping symlinks, proves in-root `stat`/`readlink` still work, and proves async/sync `stat` plus async/sync `readlink` deny an out-of-root target. | `cargo test -p nimbus-runtime application_node22_confines_symlink_stat_and_readlink_targets -- --nocapture`: 1 passed, 548 filtered, integration binaries 0 matching tests; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 47.39s. | Final batch commit |
| `C1-3` | medium | Runtime | CONFIRMED/FIXED | Re-read runtime-local env ops, bootstrap op-state installation, worker-thread spawn/bootstrap, JS `SHARE_ENV` adapter, env grants, and capability tests. Removed the process-global shared-worker env map. Shared env is now a per-runtime handle installed in op state, cloned into worker runtimes, and configured from the runtime env policy; reads/snapshots are constrained by `env_read`, writes/deletes by `env_write`, and ungranted read probes resolve absent like the normal `process.env` surface. | `application_node22_shared_worker_env_is_runtime_scoped_and_grant_gated` proves a granted runtime can round-trip shared env, a real `Worker` with `SHARE_ENV` shares and mutates the same per-runtime handle, a separate runtime cannot read the first runtime's value, and a read-only runtime cannot write. | `cargo test -p nimbus-runtime application_node22_shared_worker_env_is_runtime_scoped_and_grant_gated -- --nocapture`: 1 passed, 549 filtered, integration binaries 0 matching tests; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 5.81s. | Final batch commit |
| `C3-2` | medium | Runtime | CONFIRMED/FIXED | Re-read the production `ScopedNodeRequireLoader`, Node init service wiring, runtime permission construction, CommonJS package tests, and the query/mutation permission-profile tests. Removed the loader's Err-arm permission bypass. The loader now owns a narrowly scoped Deno module-read `PermissionsContainer` built from runtime read roots and propagates denial from that container instead of falling back to the embedder path policy alone; direct runtime FS permission profiles are unchanged. | `require_loader_propagates_deno_permission_denial_inside_runtime_roots` injects a deny-all module-read permission container for an otherwise path-policy-approved module and proves the loader returns the Deno read denial; existing `application_node22_commonjs_package_can_require_core_semantics_builtins` proves legitimate CommonJS package loading still works. | `cargo test -p nimbus-runtime require_loader_propagates_deno_permission_denial_inside_runtime_roots -- --nocapture`: 1 passed, 550 filtered, integration binaries 0 matching tests; `cargo test -p nimbus-runtime application_node22_commonjs_package_can_require_core_semantics_builtins -- --nocapture`: 1 passed, 550 filtered, integration binaries 0 matching tests; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.77s. | Final batch commit |
| `C3-3` | medium | Runtime | CONFIRMED/FIXED | Re-read `test_runtime/render.rs`, `bundle.rs`, `test_runtime.rs`, parser/types, and nearby Node spawn/eval fixtures. Confirmed `render.rs` was a 1,630-line composition root with eval rendering embedded in the top-level function. Extracted eval-mode rendering into concept-owned `render_eval.rs` sub-renderers for module eval, CommonJS inline eval, dynamic-import eval, print-result output, and eval-file writes; `render.rs` is now 1,434 lines. | `module_eval_writes_rewritten_test_import_module`; `commonjs_dynamic_import_eval_writes_require_backed_module`; `commonjs_inline_eval_rewrites_source_bundle_paths_when_unrestricted`. | `cargo test -p nimbus-runtime render_eval -- --nocapture`: 3 passed, 551 filtered, integration binaries 0 matching tests; `cargo fmt --all --check`: pass; `git diff --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 4.21s. Disk threshold check after verification: main `target/` 8.6 GiB, 256 GiB free, so no cleanup triggered. | Final batch commit |
| `C3-5` | medium | Runtime | CONFIRMED/FIXED | Re-read `worker_threads.rs`, worker state wiring, runtime op registration, worker-thread capability tests, and the review's duplicate note for `C1-2`. Both CPU-usage fast ops now return before indexing when the JS-supplied output buffer has fewer than two slots; the normal two-slot path is unchanged. The guarded writes live in private helpers because the `op2(fast)` macro does not expose a normal Rust-callable function signature for direct unit tests. | `current_thread_cpu_usage_ignores_short_output_buffers`; `host_worker_cpu_usage_ignores_short_output_buffers`. | Initial direct-op unit-test attempt failed to compile because `op2(fast)` rewrites the Rust-callable signature; replaced it with helper-level tests used by the ops. `cargo test -p nimbus-runtime cpu_usage -- --nocapture`: 2 passed, 554 filtered, integration binaries 0 matching tests; `cargo fmt --all --check`: pass; `git diff --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.68s. Disk threshold check after verification: main `target/` 8.7 GiB, 256 GiB free, so no cleanup triggered. | Final batch commit |
| `D1-2` | medium | Server | CONFIRMED/FIXED | Re-read service, session, sandbox HTTP authorization modules, service-manager route tests, local-server credential extraction, and the existing service-grants helper precedent. Extracted duplicated principal-class parsing, principal-claim lookup, permission action matching, RFC3339 millis formatting, and normalized operator-route credential extraction into `http::authz`; service/session/sandbox routes keep their route-specific audit and response semantics while sharing the common authorization primitives. | `principal_class_prefers_verified_claims_and_rejects_operator_identity`; `permission_claim_values_flattens_arrays_and_preserves_scalar_permissions`; `format_millis_rfc3339_saturates_invalid_timestamps_to_epoch`; `operator_route_access_treats_missing_security_as_missing`. Existing service, service-definition, session, and sandbox route authorization tests were rerun as behavioral regression coverage. | `cargo check -p nimbus-server`: pass, finished in 31.04s; `cargo test -p nimbus-server authz -- --nocapture`: 4 passed, 432 filtered, plus 0 matching integration tests; `cargo test -p nimbus-server principal_class -- --nocapture`: 5 passed, 431 filtered; `cargo test -p nimbus-server service_definition_permissions -- --nocapture`: 1 passed, 435 filtered; `cargo test -p nimbus-server session_routes_reject_service_sessions_without_exact_grants_and_unsupported_channels -- --nocapture`: 1 passed, 435 filtered; `cargo test -p nimbus-server sandbox_routes_enforce_owner_authority_and_backend_admission -- --nocapture`: 1 passed, 435 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-server --all-targets -- -D warnings`: pass, finished in 28.43s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, 243 GiB free, so no cleanup triggered. | Final batch commit |
| `D2-1` | medium | Server | CONFIRMED/FIXED | Re-read `authorize_deploy_admin_bearer`, local-admin/session token verification precedent, and the HTTP deploy caller. The deploy bearer comparison now uses the same `ring::hmac::verify` constant-time style as the local-admin token path instead of direct `!=` string comparison. | Strengthened `deploy_admin_bearer_is_separate_from_local_admin_header_gate` to assert exact-match success plus wrong-token, prefix-only, and longer-token rejection. | `cargo test -p nimbus-operator deploy_admin_bearer_is_separate_from_local_admin_header_gate -- --nocapture`: 1 passed, 29 filtered; `cargo test -p nimbus-operator`: 30 passed; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 2.86s; `make clippy`: pass, workspace clippy finished in 13.79s; `make check`: pass, workspace check finished in 8.24s. | Final batch commit |
| `D4-3` | medium | Server | CONFIRMED/FIXED | Re-read MongoDB config/server construction and DynamoDB loopback precedent. MongoDB listener config now requires explicit credentials and `run_server` rejects non-loopback MongoDB bind addresses before serving. | `listener_rejects_non_loopback_bind_address`; listener tests use explicit auth config. | `cargo test -p nimbus-server mongodb -- --nocapture`: 6 passed; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `H1-3` | medium | Sandbox | CONFIRMED/FIXED | Re-read the container and krun conmon lifecycle/status paths, persisted state projections, restart helper tests, and sandbox resource model docs. Confirmed krun honored `SandboxLifecycleSpec.restart_policy` while the container backend never restarted and projected `restart_count: 0`. Moved restart-policy/backoff/time helpers into shared `backends::conmon::lifecycle`, wired krun through it, added container restart state (`restart_count`, `next_restart_at_millis`), made execute-mode container inspect schedule or perform restart before declaring failure, and made container state views report the persisted restart count. | `restart_decision_keeps_failed_container_starting_until_backoff_elapses`; `restart_decision_counts_due_failed_container_restart`; strengthened `state_view_lists_manifest_backed_summaries_and_skips_missing_manifest_dirs` to prove container restart counts are projected. Existing krun restart policy/backoff tests now exercise the shared helper. | `cargo test -p nimbus-sandbox restart_decision -- --nocapture`: 2 passed, 165 filtered, integration binaries 0 matching tests; `cargo test -p nimbus-sandbox state_view_lists_manifest_backed_summaries_and_skips_missing_manifest_dirs -- --nocapture`: 2 passed, 165 filtered; `cargo test -p nimbus-sandbox restart_policy -- --nocapture`: 1 passed, 166 filtered; `cargo test -p nimbus-sandbox restart_backoff -- --nocapture`: 1 passed, 166 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 2.80s after fixing a test-helper argument smell without an allow; `cargo test -p nimbus-sandbox -- --nocapture`: 167 lib tests passed, 2 bin tests passed, integration tests 0, doc-tests 0; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, 240 GiB free, so no cleanup triggered. | Final batch commit |
| `H1-4` | medium | Sandbox | CONFIRMED/FIXED | Re-read the cited container and krun conmon lifecycle/spec helpers, sandbox backend tests, and shared OCI conmon launch code. Confirmed the duplicate helper set still existed after H1-3 except for restart helpers. Extended `backends::conmon::lifecycle` and added `backends::conmon::spec_resolve`; container and krun now share Linux host gating, stop signal/timeout normalization, runtime status probing, process signaling, pid/exit/status-file handling, command execution, slug generation, rootfs defaulting, process defaulting, and env override merging. Backend-specific readiness, networking, guest-user switching, and VM config behavior remains local. | Added shared owner tests: `configured_stop_signal_trims_and_defaults`; `configured_stop_timeout_prefers_spec_override`; `slugify_normalizes_operator_facing_names`; `resolve_root_spec_uses_image_defaults_and_preserves_explicit_rootfs`; `resolve_process_spec_merges_overrides_without_losing_defaults`; `resolve_process_spec_keeps_default_env_when_request_does_not_override`. Existing krun stop/defaulting and container restart tests were rerun against the shared helpers. | Duplicate-helper scan now finds the listed helpers only under `backends/conmon`; `cargo fmt --all --check`: pass; `cargo test -p nimbus-sandbox conmon -- --nocapture`: 9 passed, 164 filtered, plus integration/doc harnesses 0 matching; `cargo test -p nimbus-sandbox configured_stop -- --nocapture`: 4 passed, 169 filtered; `cargo test -p nimbus-sandbox plan_start_with_launch_defaults -- --nocapture`: 2 passed, 171 filtered; `cargo test -p nimbus-sandbox restart_decision -- --nocapture`: 2 passed, 171 filtered; `cargo clippy -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 2.80s; `cargo test -p nimbus-sandbox -- --nocapture`: 173 lib tests passed, 2 bin tests passed, integration tests 0, doc-tests 0; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, 240 GiB free, so no cleanup triggered. | Final batch commit |
| `H2-1` | medium | Sandbox | CONFIRMED/FIXED | Re-read `RuntimeServiceRegistry`, `ServiceInstanceCatalog`, `ServiceManager` registry/catalog/handle paths, HTTP tenant deletion, runtime service lookup call sites, cloud-functions snapshot call sites, and existing manager tests. Confirmed the sync manager registry/catalog paths used `futures::executor::block_on` for backend inspect/stop/artifact futures from async contexts. The registry trait now exposes `teardown_tenant_async`; HTTP tenant deletion awaits it; ServiceManager teardown awaits sandbox backend futures directly; sync service snapshots and sync `resolve_service_binding` are snapshot-only cached-state reads with no backend I/O; backend inspect remains on async refresh/activation paths. | `resolve_service_binding_uses_cached_snapshot_without_backend_inspect` proves cached sync lookup projects without calling backend inspect or mutating snapshots; existing `ensure_service_binding_sync_lookup_stays_snapshot_only_for_missing_service` covers missing cached bindings without activation; `teardown_tenant_stops_tracked_sandboxes_and_clears_tenant_resources` now exercises async teardown. | `cargo fmt --all --check`: pass; `cargo test -p nimbus-services resolve_service_binding_uses_cached_snapshot_without_backend_inspect -- --nocapture`: 1 passed, 35 filtered; `cargo test -p nimbus-services teardown_tenant_stops_tracked_sandboxes_and_clears_tenant_resources -- --nocapture`: 1 passed, 35 filtered; `cargo test -p nimbus-services ensure_service_binding_sync_lookup_stays_snapshot_only_for_missing_service -- --nocapture`: 1 passed, 35 filtered; `cargo test -p nimbus-services -- --nocapture`: 36 passed; `cargo clippy -p nimbus-services --all-targets -- -D warnings`: pass, finished in 24.00s; `cargo check -p nimbus-server`: pass, finished in 10.80s; `cargo test -p nimbus-server delete_tenant_returns_no_content_and_removes_it_from_listing -- --nocapture`: 1 passed, 435 filtered, integration bins 0 matching; `cargo clippy -p nimbus-server --all-targets -- -D warnings`: pass, finished in 18.80s; `git diff --check`: pass. Disk cleanup triggered after verification because main `target/` reached 29 GiB; `cargo clean` removed 60,535 files / 35.7 GiB from this checkout only. Post-clean checkpoint: `/` 262 GiB free, main `target/` absent, Cargo git checkouts 12 GiB, sibling targets preserved (`deno/target` 9.5 GiB, `rusty_v8/target` 6.3 GiB, `nimbus-libkrun/target` 454 MiB, `bun/target` 69 MiB). | Final batch commit |
| `H2-2` | medium | Sandbox | CONFIRMED/FIXED | Re-read `ServiceManager::teardown_tenant_async`, sandbox backend stop/artifact cleanup calls, manager state snapshots, service/session cleanup behavior, and existing teardown tests. Confirmed teardown still aborted on the first backend stop error and only cleared tenant state on total success. Teardown now attempts every tracked service and standalone sandbox stop, attempts tenant artifact cleanup even after stop failures, records successful service stops, removes only successfully stopped service/resource/session state on partial failure, retains failed standalone resources for retry, and returns an aggregate best-effort cleanup error. | `teardown_tenant_attempts_all_stops_and_clears_successes_before_returning_errors` configures one service and one standalone sandbox with the standalone stop failing, then proves both stops are attempted, artifact cleanup still runs, successful service state is cleared, the failed standalone resource/session is retained, and the returned error names the failed sandbox plus best-effort cleanup. Existing full-success teardown coverage was rerun. | `cargo fmt --all --check`: pass; `cargo test -p nimbus-services teardown_tenant_attempts_all_stops_and_clears_successes_before_returning_errors -- --nocapture`: 1 passed, 36 filtered; `cargo test -p nimbus-services teardown_tenant_stops_tracked_sandboxes_and_clears_tenant_resources -- --nocapture`: 1 passed, 36 filtered; `cargo test -p nimbus-services -- --nocapture`: 37 passed; initial `cargo clippy -p nimbus-services --all-targets -- -D warnings` caught two needless borrows in the new partial-cleanup loop and they were fixed; final clippy pass finished in 1.33s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 5.6 GiB, Cargo git checkouts 12 GiB, 256 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-2` | medium | CLI | CONFIRMED/FIXED | Re-read encrypted redb DEK rotation, manifest writes, key resolution, redb open callers, and adjacent migration publish code. Confirmed the old rotation path renamed the newly encrypted database over the original before writing the matching manifest, leaving a crash window where the database and manifest could disagree. Added a storage-owned staged commit/recovery protocol: stage the reencrypted database and rotated manifest, durably mark the committed rotation, publish both artifacts with recovery support, and have redb key resolution recover any committed interruption before reading the manifest. The CLI now recovers before rotation, writes both staged artifacts, and commits through the storage protocol. | `commit_staged_redb_dek_rotation_replaces_database_and_manifest_pair`; `recover_redb_dek_rotation_finishes_before_any_artifact_is_published`; `recover_redb_dek_rotation_finishes_after_database_publish`; `recover_redb_dek_rotation_removes_marker_after_both_artifacts_are_published`; `resolve_redb_key_recovers_committed_rotation_before_reading_manifest`; `rotate_redb_dek_publishes_reencrypted_database_with_matching_manifest`. | `cargo test -p nimbus-storage redb_dek_rotation -- --nocapture`: 4 passed, 316 filtered; `cargo test -p nimbus-storage resolve_redb_key_recovers_committed_rotation_before_reading_manifest -- --nocapture`: 1 passed, 319 filtered; `cargo test -p nimbus-bin rotate_redb_dek_publishes_reencrypted_database_with_matching_manifest -- --nocapture`: 1 passed, 575 filtered; `cargo test -p nimbus-bin encryption::rotate -- --nocapture`: 6 passed, 570 filtered; `cargo test -p nimbus-storage -- --nocapture`: 318 passed, 2 ignored, doc-tests 0; `cargo check -p nimbus-bin -p nimbus-storage`: pass; `cargo fmt --all --check`: pass; initial clippy caught a manual error-propagation branch and was fixed; final `cargo clippy -p nimbus-storage -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 7.59s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, 240 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-5` | medium | CLI | CONFIRMED/FIXED | Re-read `MachineApiClient`, Unix-socket request/response parsing, machine API route handlers, and client/API tests. Confirmed the read loop treated `WouldBlock`/`TimedOut` as if the peer had closed the response. The client now returns an explicit timeout error unless a declared `Content-Length` response is complete; it rejects EOF before the declared body length, preserves close-delimited reads for `Connection: close`, and shares header/body parsing with JSON body extraction. While verifying the client module, the service-sandbox inspect/stop routes exposed a manifest-gate bug for backend-owned handles, so inspect and stop now consult the backend directly while preserving missing/standalone `404` behavior. | `client_accepts_complete_content_length_response_without_socket_eof`; `client_reports_timeout_before_declared_response_body_completes`; `client_rejects_eof_before_declared_response_body_completes`; existing `client_round_trips_service_sandbox_operations_when_backend_is_available` and `service_sandbox_id_routes_ignore_standalone_sandbox_records` cover the route-side correction. | Three new focused client tests each passed individually; `cargo test -p nimbus-bin machine::client::tests -- --nocapture`: 15 passed, 564 filtered; `cargo test -p nimbus-bin service_sandbox_id_routes_ignore_standalone_sandbox_records -- --nocapture`: 1 passed, 578 filtered; `cargo test -p nimbus-bin machine::api::tests -- --nocapture`: 12 passed, 567 filtered; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 25.45s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 24 GiB, Cargo git checkouts 12 GiB, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-7` | medium | CLI | CONFIRMED/FIXED | Re-read start persistence config structs, env loading, CLI fields, resolved-input merge, and start config tests. Confirmed the file/env/resolved paths repeated the same persistence field set and hand-threaded every source through a long merge. Replaced the parallel file/env structs with one optional `PersistenceInputs` carrier reused as `PersistenceFileConfig` and `PersistenceEnv`; added a CLI constructor and a single precedence fallback merge before applying defaults into `ResolvedPersistenceInputs`. The CLI `StartCommand` fields remain explicit for clap's public surface. | `persistence_sources_merge_with_command_env_file_precedence` asserts CLI overrides env/file, env fills missing values ahead of file, and file still supplies provider-specific defaults. Existing persistence and encryption start-config suites cover provider routing, pool settings, config-file parsing, and encryption validation through the shared input carrier. | `cargo check -p nimbus-bin`: pass, finished in 3.45s; `cargo test -p nimbus-bin persistence_sources_merge_with_command_env_file_precedence -- --nocapture`: 1 passed, 579 filtered; `cargo test -p nimbus-bin start::tests::persistence -- --nocapture`: 9 passed, 571 filtered; `cargo test -p nimbus-bin start::tests::encryption -- --nocapture`: 8 passed, 572 filtered; `cargo test -p nimbus-bin start::tests -- --nocapture`: 60 passed, 520 filtered; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 5.76s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 24 GiB, Cargo git checkouts 12 GiB, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `I2-1` | medium | CLI | CONFIRMED/FIXED | Duplicate of `D2-1`, confirmed from the CLI/operator-token angle. The deploy admin bearer no longer uses a short-circuiting string comparison and now follows the crate's HMAC verification pattern. | Same strengthened deploy-bearer unit test covers exact, wrong, prefix-only, and longer-token cases. | `cargo test -p nimbus-operator deploy_admin_bearer_is_separate_from_local_admin_header_gate -- --nocapture`: 1 passed, 29 filtered; `cargo test -p nimbus-operator`: 30 passed; `cargo clippy -p nimbus-operator --all-targets -- -D warnings`: pass; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `I2-3` | medium | CLI | CONFIRMED/FIXED | Re-read `nimbus-machine` crate source, crate file list, downstream callers/tests, and the review anchors. Confirmed the crate still had no in-crate tests despite owning image-source parsing, volume parsing, XDG/HOME/runtime-root resolution, shared-parent layout behavior, and directory creation. Added an injected environment lookup seam for root resolution so tests do not mutate process-global env; removed now-dead private resolver wrappers. | Added 11 crate-local tests covering HTTP/docker/local/implicit image-source parsing, invalid HTTP integrity cases, volume parse success and malformed/relative-path errors, shared-parent data/cache derivation, unshared-root fallback, injected XDG/runtime env resolution, HOME fallback/default runtime, missing-home error, directory creation success, and directory-creation failure path context. | First `cargo test -p nimbus-machine -- --nocapture` caught lifetime issues from passing `env::var_os` directly and then dead private wrappers; both were fixed. Final `cargo test -p nimbus-machine -- --nocapture`: 11 passed; `cargo clippy -p nimbus-machine --all-targets -- -D warnings`: pass, finished in 4.43s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 24 GiB, Cargo git checkouts 12 GiB, 238 GiB free, so no cleanup triggered. | Final batch commit |
| `E1-10` | low | Adapters | CONFIRMED/FIXED | Re-read `crud/filter.rs`, compound-sort call sites, residual range matching, the review anchor, and find tests. Confirmed BSON NaN cannot survive into `serde_json::Number`, but the comparator still returned `Equal` for arrays, objects, and same-rank fallback values. `compare_json_values` now provides deterministic MongoDB-subset ordering for missing/null, numbers, strings, objects, arrays, and booleans; arrays compare lexicographically and objects compare by sorted key/value pairs. | `compare_json_values_orders_json_type_subset`; `compare_json_values_orders_arrays_lexicographically`; `compare_json_values_orders_objects_by_key_then_value`; `find_compound_sort_distinguishes_structured_values`. | `cargo test -p nimbus-mongodb compare_json_values -- --nocapture`: 3 passed, 253 filtered; `cargo test -p nimbus-mongodb find_compound_sort_distinguishes_structured_values -- --nocapture`: 1 passed, 255 filtered; `cargo test -p nimbus-mongodb find_with_compound_sort -- --nocapture`: 1 passed, 255 filtered; `cargo clippy -p nimbus-mongodb --all-targets -- -D warnings`: pass, finished in 21.68s; `cargo test -p nimbus-mongodb`: 256 passed; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk cleanup triggered after verification: `cargo clean` removed 53,077 files / 33.2 GiB from this checkout, restoring `/` to 261 GiB free while preserving Cargo git checkouts and sibling targets. | Final batch commit |
| `E1-11` | low | Adapters | CONFIRMED/FIXED | Re-read the review anchor, CRUD test harness, `_id` fast-path tests, unauthenticated dispatch tests, MongoDB update lowering, command wrapper write-error behavior, and engine numeric-transform guards. Confirmed the original coverage gaps were real; `_id` range and unauthenticated data-command coverage were already added by `E1-3`/`E1-1`, while non-finite numeric update operands were still only rejected indirectly by the engine. `bson_to_numeric_value` now rejects NaN/Infinity at the MongoDB adapter boundary as `BadValue` before building a write. | `update_inc_rejects_nan_operand_without_mutating_document`; `update_mul_rejects_infinite_operand_without_mutating_document`; current proof also reran `find_id_fast_path_applies_residual_range_filters` and `dispatch_rejects_data_command_before_authentication`. | Initial `cargo test -p nimbus-mongodb update_rejects -- --nocapture` matched 0 tests and was not used as proof; `cargo test -p nimbus-mongodb rejects_ -- --nocapture`: 9 passed, 249 filtered, including both new numeric-update tests and unauthenticated dispatch; `cargo test -p nimbus-mongodb find_id_fast_path_applies_residual_range_filters -- --nocapture`: 1 passed, 257 filtered; `cargo clippy -p nimbus-mongodb --all-targets -- -D warnings`: pass, finished in 1m 07s; `cargo test -p nimbus-mongodb`: 258 passed; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 7.5 GiB, Cargo git checkouts 12 GiB, 254 GiB free, so no cleanup triggered. | Final batch commit |
| `E1-8` | low | Adapters | CONFIRMED/FIXED | Re-read the `$group` implementation, aggregation tests, accumulator state, and BSON key equality/hash behavior. Confirmed grouping used `format!("{group_key:?}")` as a string key even though BSON already provides structured equality/hash semantics. `execute_group` now keys groups by the original `bson::Bson` value, preserving the emitted `_id` and avoiding debug-string identity. | `aggregate_group_uses_structured_bson_identity_for_array_and_document_keys` groups documents by structured document and array `_id` values and asserts the distinct grouped counts. | `cargo test -p nimbus-mongodb aggregate_group_uses_structured_bson_identity_for_array_and_document_keys -- --nocapture`: 1 passed, 258 filtered; `cargo test -p nimbus-mongodb aggregate_group_ -- --nocapture`: 7 passed, 252 filtered; `cargo clippy -p nimbus-mongodb --all-targets -- -D warnings`: pass, finished in 3.05s; `cargo test -p nimbus-mongodb`: 259 passed; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 7.6 GiB, Cargo git checkouts 12 GiB, 254 GiB free, so no cleanup triggered. | Final batch commit |
| `E2-1` | low | Adapters | NOT-A-DEFECT | Re-read the Firestore gRPC timestamp codecs, REST timestamp parser/formatter, `TypedScalarValue::Timestamp`, server-timestamp transform storage, document projection, and Firebase serializer tests. The review's reachability claim does not hold: raw Firestore `timestampValue` can parse into the intermediate `FirestoreValue`, but ordinary REST/gRPC field lowering rejects it as a Firebase-only value; persisted timestamp fields are emitted only from `TypedScalarValue::Timestamp { value: nimbus_core::Timestamp }`, which is the same millisecond timestamp type used by document metadata and preconditions. Server-issued metadata round-trips losslessly at millisecond precision, and no stored field-value path preserves independent nanoseconds beside millisecond metadata. | No test added; no code change because the finding was not confirmed. | Initial `cargo test -p nimbus-firebase firestore_value_rejects_firestore_only_types_for_nimbus_conversion encode_document_field_to_grpc_rejects_foreign_typed_scalars_as_internal -- --nocapture` was malformed because Cargo accepts one test-name filter and is not proof; `cargo test -p nimbus-firebase firestore_value_rejects_firestore_only_types_for_nimbus_conversion -- --nocapture`: 1 passed, 42 filtered; `cargo test -p nimbus-firebase encode_document_field_to_grpc_rejects_foreign_typed_scalars_as_internal -- --nocapture`: 1 passed, 42 filtered. Disk threshold check after verification: main `target/` 11 GiB, Cargo git checkouts 12 GiB, 251 GiB free, so no cleanup triggered. | Final batch commit |
| `E2-3` | low | Adapters | CONFIRMED/FIXED | Re-read `batch_write_request.rs`, parser tests, REST/gRPC BatchWrite entrypoints, and generated Firestore BatchWrite labels shapes. Confirmed the private `_labels` field was never read, and under `rename_all = "camelCase"` it would bind the wrong key if read later. Removed the misleading field so labels are intentionally treated as ignored request metadata. | Strengthened `parses_writes_and_ignores_labels` by sending a non-map `labels` payload; the parser must still accept the request and preserve the write/database result, proving labels are ignored rather than captured in dead state. | `cargo test -p nimbus-firebase parses_writes_and_ignores_labels -- --nocapture`: 1 passed, 42 filtered; `cargo test -p nimbus-firebase`: 43 passed; `cargo clippy -p nimbus-firebase --all-targets -- -D warnings`: pass, finished in 21.19s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 11 GiB, Cargo git checkouts 12 GiB, 250 GiB free, so no cleanup triggered. | Final batch commit |
| `E2-4` | low | Adapters | CONFIRMED/FIXED | Re-read the four Firebase database-match helpers in REST BatchGet, REST Commit, gRPC unary BatchGet, and gRPC write-stream lowering plus their tests and server callers. Confirmed the same request-vs-resource database predicate was duplicated with transport-specific error wording. Added `resource_names::ensure_database_match` and a canonical database-name mismatch error; all REST/gRPC call sites now delegate to the shared predicate while preserving local error mapping. | `ensure_database_match_reports_expected_and_actual_database_names`; `rejects_documents_from_a_different_database`; strengthened `rejects_invalid_operation_shapes_and_database_mismatches` to assert the Commit mismatch message keeps the expected database. | Initial `cargo test -p nimbus-firebase ensure_database_match_reports_expected_and_actual_database_names rejects_documents_from_a_different_database rejects_invalid_operation_shapes_and_database_mismatches -- --nocapture` was malformed because Cargo accepts one test-name filter and is not proof; `cargo test -p nimbus-firebase database -- --nocapture`: 10 passed, 35 filtered; `cargo test -p nimbus-firebase`: 45 passed; `cargo clippy -p nimbus-firebase --all-targets -- -D warnings`: pass, finished in 2.77s; `cargo test -p nimbus-server firebase_grpc_batch_get_documents_reads_found_missing_and_rolls_back_sessions -- --nocapture`: 1 passed, 435 filtered, plus 0 matching integration tests; `cargo test -p nimbus-server firebase_write_stream_handshakes_and_applies_ordered_writes -- --nocapture`: 1 passed, 435 filtered, plus 0 matching integration tests; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, 241 GiB free, so no cleanup triggered. | Final batch commit |
| `E2-5` | low | Adapters | CONFIRMED/FIXED | Re-read REST BatchGet, RunQuery, and Commit transaction-token parsing plus their parser tests. Confirmed all three modules duplicated the same standard base64 decode policy and only differed in local error wrapping. Added a crate-local `transaction_token` owner module and routed all three request parsers through the shared decoder with local error mapping. | `decodes_standard_base64_transaction_tokens`; `rejects_invalid_transaction_tokens_with_base64_context`; existing parser tests cover BatchGet/Commit/RunQuery valid transaction bytes and RunQuery invalid base64. | `cargo test -p nimbus-firebase transaction_token -- --nocapture`: 2 passed, 45 filtered; `cargo test -p nimbus-firebase transaction -- --nocapture`: 9 passed, 38 filtered; `cargo test -p nimbus-firebase`: 47 passed; `cargo clippy -p nimbus-firebase --all-targets -- -D warnings`: pass, finished in 4.33s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `E2-6` | low | Adapters | CONFIRMED/FIXED | Re-read the duplicated `special_double_from_firestore` helpers in `serializer.rs` and gRPC `write_stream.rs`, the duplicated `firestore_document_name` helpers in `response.rs` and REST BatchGet, and their callers/tests. Confirmed both pairs were exact duplicated policy. `serializer` now owns Firestore-special-double conversion for both REST and gRPC paths; `response::firestore_document_name` now owns canonical document-name formatting for BatchGet and response serialization. | `decode_numeric_value_from_grpc_maps_special_doubles`; strengthened `parses_documents_mask_and_transaction_and_elides_duplicates` to assert the canonical BatchGet document name. | `cargo test -p nimbus-firebase decode_numeric_value_from_grpc_maps_special_doubles -- --nocapture`: 1 passed, 47 filtered; `cargo test -p nimbus-firebase parses_documents_mask_and_transaction_and_elides_duplicates -- --nocapture`: 1 passed, 47 filtered; `cargo test -p nimbus-firebase`: 48 passed; initial `cargo clippy -p nimbus-firebase --all-targets -- -D warnings` caught a test-only `SpecialDouble` import in the main module; moved it into the test module; final clippy pass finished in 1.71s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `E2-7` | low | Adapters | CONFIRMED/FIXED | Re-read the Firebase write-stream and retained-listen registry mutex sites plus stream/listen tests. Confirmed both registries used `.expect(...)` on `std::sync::Mutex` locks, so one panic while holding a registry lock would make subsequent stream operations panic. The guarded state is plain registry maps, so both registries now recover poisoned locks with `into_inner()` and continue returning normal per-request results. | `write_stream_registry_recovers_after_poisoned_lock`; `retained_listen_registry_recovers_after_poisoned_lock`. | `cargo test -p nimbus-firebase poisoned_lock`: 2 passed, 48 filtered; `cargo test -p nimbus-firebase`: 50 passed; `cargo clippy -p nimbus-firebase --all-targets -- -D warnings`: pass, finished in 3.01s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-1` | low | Adapters | CONFIRMED/FIXED | Re-read the Cloud Functions crate manifest, source modules, tests, and callers. Confirmed `tokio` was declared in production dependencies but never referenced by the crate; async functions are caller/runtime-driven and do not need a direct Tokio dependency. Removed the unused production dependency. | No test added; manifest-only dependency cleanup covered by existing Cloud Functions behavior tests. | `rg -n "\btokio\b" crates/nimbus-cloud-functions`: no matches after removal; `cargo check -p nimbus-cloud-functions`: pass, finished in 29.10s; `cargo test -p nimbus-cloud-functions`: 21 passed; `cargo clippy -p nimbus-cloud-functions --all-targets -- -D warnings`: pass, finished in 13.01s; first sandboxed `make deny` failed on a read-only advisory DB lock and is not proof; escalated `make deny`: pass (`advisories ok`, `bans ok`, `licenses ok`, `sources ok`); `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk cleanup triggered after verification: `cargo clean` removed 46,576 files / 31.1 GiB from this checkout, restoring `/` to 257 GiB free while preserving Cargo git checkouts and sibling targets. | Final batch commit |
| `E4-2` | low | Adapters | CONFIRMED/FIXED | Re-read the Convex crate manifest, auth metadata verifier test, crate public surface, and server/adapters callers. Confirmed `tokio` was a direct production dependency but the crate only names Tokio in one `#[tokio::test]`; production async APIs are runtime-agnostic and await caller/runtime-provided futures. Moved `tokio` to `[dev-dependencies]`. | No test added; manifest-only dependency-scope cleanup covered by the existing auth metadata cache test that requires the dev Tokio test macro. | `rg -n "\btokio\b" crates/nimbus-convex/Cargo.toml crates/nimbus-convex/src`: only dev-dependency line and `#[tokio::test]`; `cargo tree -p nimbus-convex --depth 1 --edges normal`: direct normal dependencies no longer include `tokio`; `cargo check -p nimbus-convex`: pass, finished in 28.35s; `cargo test -p nimbus-convex auth_metadata_is_cached_and_refresh_can_replace_stale_jwks -- --nocapture`: 1 passed, 13 filtered, finished in 1m 46s; `cargo clippy -p nimbus-convex --all-targets -- -D warnings`: pass, finished in 14.45s; first sandboxed `make deny` failed on a read-only advisory DB lock and is not proof; escalated `make deny`: pass (`advisories ok`, `bans ok`, `licenses ok`, `sources ok`); `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 11 GiB, Cargo git checkouts 12 GiB, 250 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-4` | low | Adapters | CONFIRMED/FIXED | Re-read Firestore Admin get/set/update/delete handlers, database-id validation in `firebase_admin_bound_key`, target resolution, and Firebase database-id validation. Confirmed reads deserialized `database_id` but never validated it, while writes rejected non-`(default)` database ids. Added a shared Firestore Admin database-id validator and call it from both sync and async get paths before resolving or reading the document. | `firestore_admin_rejects_non_default_database_id_on_reads_and_writes` seeds a default-database document, proves a non-default get returns `op.invalid_input`, and asserts no storage read is recorded before also proving writes reject the same invalid database id. | `cargo test -p nimbus-cloud-functions firestore_admin_ -- --nocapture`: 5 passed, 21 filtered; `cargo test -p nimbus-cloud-functions`: 26 passed; `cargo clippy -p nimbus-cloud-functions --all-targets -- -D warnings`: pass, finished in 24.19s; `cargo fmt --all --check`: pass after formatting; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, 244 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-6` | low | Adapters | CONFIRMED/FIXED | Re-read `bounds.rs`, `derive_index_read`, and the server wrapper. Confirmed cross-type scalar pairs return `None` from the value comparator and previously replaced existing lower/upper bounds. Incomparable candidates now leave the existing tracked bound in place instead of silently narrowing to a different JSON type. | `incomparable_candidates_do_not_replace_existing_bounds` covers number-vs-string lower and upper bound candidates plus absent-current-bound behavior. | `cargo test -p nimbus-convex bounds -- --nocapture`: 3 passed, 14 filtered; final-state `cargo test -p nimbus-convex`: 14 passed, 3 ignored; `cargo clippy -p nimbus-convex --all-targets -- -D warnings`: pass, finished in 1.61s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, 244 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-7` | low | Adapters | CONFIRMED/FIXED | Re-read the JSON-number comparator and read-bound caller. Confirmed the old `as_f64` comparison could collapse distinct large integer bounds. Number comparison now orders `i64`/`u64` values exactly first, handles mixed integer/finite-float comparisons explicitly, and only falls back to float comparison for genuine float pairs. | `large_integer_bounds_compare_without_f64_precision_loss`; `mixed_integer_float_bounds_compare_by_numeric_value`. | `cargo test -p nimbus-convex bounds -- --nocapture`: 3 passed, 14 filtered; final-state `cargo test -p nimbus-convex`: 14 passed, 3 ignored; `cargo clippy -p nimbus-convex --all-targets -- -D warnings`: pass, finished in 1.61s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, 244 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-8` | low | Adapters | CONFIRMED/FIXED | Re-read `builtins.rs`, crate re-exports, and server runtime-transform dispatch. Confirmed crate-public callers could construct a runtime-backed transform and reach the builtin handler's `unreachable!`. Runtime-backed variants now return the handler's existing `Result` error instead of panicking. | `runtime_transforms_return_error_from_builtin_handler` asserts both runtime named-query variants return the exact resolution-required error. | `cargo test -p nimbus-convex transform`: 7 passed, 13 filtered; final-state `cargo test -p nimbus-convex`: 17 passed, 3 ignored; `cargo clippy -p nimbus-convex --all-targets -- -D warnings`: pass, finished in 1.42s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, 244 GiB free, so no cleanup triggered. | Final batch commit |
| `E4-9` | low | Adapters | CONFIRMED/FIXED | Re-read `state.rs`, `selection.rs`, transform call sites, and the auth metadata cache's poison-recovery precedent. Confirmed transform state used panic-on-poison for simple in-memory maps while auth metadata recovered. Transform writes now share a recovery helper, and runtime selection uses the same recovered write path when promoting pending transforms. | `transform_state_mutators_recover_after_poisoned_lock` covers every public transform-state mutator with concrete map assertions; `resolve_subscription_transform_recovers_poisoned_lock_and_promotes_pending_transform` covers poisoned selection promotion from pending request to subscription id. | `cargo test -p nimbus-convex transform`: 7 passed, 13 filtered; final-state `cargo test -p nimbus-convex`: 17 passed, 3 ignored; `cargo clippy -p nimbus-convex --all-targets -- -D warnings`: pass, finished in 1.42s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, 244 GiB free, so no cleanup triggered. | Final batch commit |
| `A1-1` | low | Storage | CONFIRMED/FIXED | Re-read the Windows `MoveFileExW` manifest path, adjacent unsafe-code convention in `encryption/key.rs`, and storage encryption tests. Confirmed the tracked manifest unsafe block lacked a safety note. Added a local safety comment documenting the NUL-terminated UTF-16 buffers, synchronous call lifetime, and checked return value. | No new behavioral test; this is an unsafe-contract documentation fix. Existing manifest/rotation/storage tests cover the surrounding file-write paths. | `cargo test -p nimbus-storage`: 319 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 22.96s; `cargo fmt --all --check`: pass after formatting; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, 241 GiB free, so no cleanup triggered. | Final batch commit |
| `A1-2` | low | Storage | CONFIRMED/FIXED | Re-read `append_commit`, `append_tenant_event`, `next_sequence`, `write_next_sequence`, latest-sequence reads, and journal tests. Confirmed `next_sequence` advanced `NEXT_SEQUENCE_KEY` before `append_tenant_event` advanced the same cursor again. `next_sequence` now only reads the reserved sequence; `append_tenant_event` remains the single owner that writes the next cursor with `saturating_add`. | `tenant_event_journal_advances_next_sequence_once_per_commit` commits two metadata-only tenant events, asserts sequences 1 and 2, verifies `latest_sequence()` after each commit, and checks durable journal order. | Initial `cargo test -p nimbus-storage tenant_event_journal_advances_next_sequence_once_per_commit -- --nocapture` failed to compile after the first edit (`E0308` raw `u64` from `next_sequence`) and is not proof; after fixing, the same focused test passed: 1 passed, 320 filtered. `cargo test -p nimbus-storage tenant_event_journal -- --nocapture`: 7 passed, 314 filtered; final formatted-state `cargo test -p nimbus-storage tenant_event_journal -- --nocapture`: 7 passed, 314 filtered; `cargo test -p nimbus-storage`: 319 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 22.96s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, 241 GiB free, so no cleanup triggered. | Final batch commit |
| `A2-5` | low | Storage | CONFIRMED/FIXED | Re-read the live range adapters, bounded-scan helpers, query-read traits, engine planner boundary, historical scan constructors, provider wrappers, and SQL-builder callers. Replaced `(Option<&Value>, bool)` range-bound pairs with `IndexRangeBound`/`Bound`, mapped from planner `PlannedRangeBound` only at the engine boundary, changed SQLite SQL planning to `Bound<()>`, grouped historical pagination/key bounds and backend owned bounds, and removed the relevant `too_many_arguments` suppressions instead of re-allowing clippy. | Existing live range, historical range/composite pagination, and SQLite SQL-plan tests now compile against typed bounds; the SQL-plan tests assert included/excluded bounds still generate the expected `>=` / `<` predicates. No new behavior-only test was needed for this representational cleanup. | `cargo test -p nimbus-storage index_scan_range_on_numbers -- --nocapture`: 1 passed, 320 filtered; `cargo test -p nimbus-storage historical_index -- --nocapture`: 10 passed, 311 filtered, with external provider tests using their no-Docker skip path; `cargo check -p nimbus-storage`: pass; `cargo check -p nimbus-engine`: pass; final `cargo test -p nimbus-storage`: 319 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 6.01s; initial `cargo clippy -p nimbus-engine --all-targets -- -D warnings` caught a stale bench SQL-helper caller and is not proof; after fixing it, final engine clippy passed in 2.34s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, 237 GiB free, so no cleanup triggered. | Final batch commit |
| `A2-7` | low | Storage | CONFIRMED/FIXED | Re-read `index/history_scan.rs`, the duplicate redb historical helper in `store/index_versions.rs`, `prefix_end`, and the SQL/redb historical scan consumers. Confirmed both exclusive-start helpers still fell back to the beginning of the keyspace when `prefix_end` could not advance a max byte key. The shared historical plan now carries an explicit empty-range state, SQL-backed providers short-circuit empty plans after page validation, and the redb path uses an explicit empty key-bound state instead of `Vec::new()`. | `historical_exclusive_start_at_max_key_is_empty`; `redb_historical_exclusive_start_at_max_key_is_empty` exercise synthetic all-`0xff` exclusive-start keys plus inclusive and unbounded edge cases. | `cargo test -p nimbus-storage historical_exclusive_start_at_max_key_is_empty -- --nocapture`: 2 passed, 321 filtered; `cargo test -p nimbus-storage historical_index -- --nocapture`: 10 passed, 313 filtered, with external provider tests using their no-Docker skip path; `cargo test -p nimbus-storage`: 321 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 6.07s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 24 GiB, Cargo git checkouts 12 GiB, 237 GiB free, so no cleanup triggered. | Final batch commit |
| `A2-8` | low | Storage | CONFIRMED/FIXED | Re-read `composite_range_scan_bounds`, the shared single-field range helper, live range scan callers, and the redb historical composite caller. Confirmed empty ranges were still encoded as `(match_prefix, Vec::new(), Some(Vec::new()))` and every caller had to remember the in-band `start_key.is_empty()` contract. Replaced the tuple alias with `IndexRangeScanBounds::{Empty, Bounds { ... }}` and updated callers to match on the type-level empty case. | `composite_range_bounds_reports_empty_as_enum` asserts the helper returns `IndexRangeScanBounds::Empty` for cross-type impossible ranges; existing live composite-index and historical composite pagination tests cover caller behavior. | `cargo test -p nimbus-storage composite_range_bounds_reports_empty_as_enum -- --nocapture`: 1 passed, 323 filtered; `cargo test -p nimbus-storage composite_range -- --nocapture`: 6 passed, 318 filtered, with external provider tests using their no-Docker skip path; `cargo test -p nimbus-storage composite_index_range -- --nocapture`: 2 passed, 322 filtered; `cargo test -p nimbus-storage`: 322 passed, 2 ignored, doc-tests 0; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 5.87s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 24 GiB, Cargo git checkouts 12 GiB, 236 GiB free, so no cleanup triggered. | Final batch commit |
| `A2-9` | low | Storage | CONFIRMED/FIXED | Re-read the live byte encoder, historical index scalar encoder, storage callers, and number-ordering tests. Confirmed the same f64 order-preserving transform was duplicated in storage and core. Added a canonical pure `order_preserving_number_bits` helper in `nimbus-core`, switched live storage bytes to use it, and corrected the duplicated signed-zero edge so `-0.0` sorts after negative finite numbers instead of before negative infinity. | `order_preserving_number_bits_matches_numeric_sort_order` covers negative infinity, negative finite values, signed zero, positive finite values, and positive infinity. `index_key_encoding_preserves_number_sort_order` now also asserts live storage numeric bytes are the canonical core bits in big-endian form. | Initial `cargo test -p nimbus-core order_preserving_number_bits_matches_numeric_sort_order -- --nocapture` failed and exposed the signed-zero edge, so it is not proof. Final `cargo test -p nimbus-core order_preserving_number_bits_matches_numeric_sort_order -- --nocapture`: 1 passed, 129 filtered; `cargo test -p nimbus-storage index_key_encoding_preserves_number_sort_order -- --nocapture`: 1 passed, 323 filtered; `cargo test -p nimbus-core`: 130 passed, doc-tests 0; `cargo clippy -p nimbus-core -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 5.26s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 24 GiB, Cargo git checkouts 12 GiB, 236 GiB free, so no cleanup triggered. | Final batch commit |
| `A3-1` | low | Storage | CONFIRMED/FIXED | Re-read `KeyDirectoryProvider::key_file_path`, subject descriptors/derivation contexts, manifest AAD, runtime manifest validation, logical-name validation, and key-directory tests. Confirmed the residual defect: lossy `:`/`/` replacement made structurally distinct subjects share one wrapping-key file. Replaced the filename stem with a collision-free lowercase hex encoding of the full subject descriptor; manifest AAD continues to authenticate the original descriptor before unwrap. | `key_file_path_distinguishes_delimiter_colliding_subjects` reproduces the old filename collision and proves the second subject now gets a distinct missing-key failure. `key_file_path_encodes_full_descriptor_without_delimiters` asserts deterministic full-descriptor hex naming. `subject_descriptor_is_authenticated_during_unwrap` proves a wrapped DEK cannot be unwrapped under a different subject descriptor even when both subjects' key files contain the same key bytes. | `cargo test -p nimbus-storage key_directory -- --nocapture`: 11 passed, 315 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 4.31s; `cargo fmt --all --check`: pass after formatting; `git diff --check`: pass. Disk threshold check after verification: main `target/` 25 GiB, Cargo git checkouts 12 GiB, 236 GiB free; at watch threshold but no cleanup triggered because free space remains healthy and no broad gate ran. | Final batch commit |
| `A3-2` | low | Storage | CONFIRMED/FIXED | Re-read `GeneratedDatabaseKey::into_wrapped`, key-type tests, manifest integration call sites, and the local key-provider trait. Confirmed `into_wrapped` copied the `[u8; 32]` plaintext, forgot the original owner, and zeroized only the copy. Replaced the unsafe `ptr::read`/`mem::forget` path with a takeable wrapped-key slot; the private take path zeroizes the actual stored plaintext field before returning the wrapped key. | `taking_wrapped_key_zeroizes_stored_plaintext` calls the private take path and asserts the real stored plaintext bytes are zeroed and the wrapped slot is consumed. Existing `into_wrapped_returns_wrapped_key` covers the public consuming API, and the encryption integration lane covers manifest construction call sites. | `cargo test -p nimbus-storage key::tests -- --nocapture`: 5 passed, 322 filtered; `cargo test -p nimbus-storage encryption::tests -- --nocapture`: 17 passed, 310 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 3.14s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 25 GiB, Cargo git checkouts 12 GiB, 236 GiB free; no cleanup triggered because free space remains healthy and no broad gate ran. | Final batch commit |
| `A3-3` | low | Storage | CONFIRMED/FIXED | Re-read the local key-provider trait, runtime manifest unwrap/generate paths, SQLite/redb/libsql encryption consumers, `nimbus-bin` migration/rotation callers, and key tests. Confirmed plaintext DEKs escaped key management as copyable `[u8; 32]` values. Added a non-`Clone`/non-`Copy` `DataEncryptionKey` wrapper with redacted debug output and `ZeroizeOnDrop`, changed provider unwrap and runtime resolve APIs to return it, moved generated plaintext out as that wrapper after manifest persistence, and kept only storage-engine handoff APIs borrowing raw key bytes. Long-lived SQLite tenant stores now retain `Option<DataEncryptionKey>` instead of `Option<[u8; 32]>`. | `data_encryption_key_debug_redacts_plaintext`; `generated_key_can_move_plaintext_without_copyable_array_escape`; existing key tests cover public wrapped extraction and stored-plaintext zeroization; encryption integration tests cover manifest generation/unwrap; redb rotation and `nimbus-bin` rotation test targets cover runtime resolution consumers. | `cargo check -p nimbus-storage`: pass; initial `cargo check -p nimbus-storage --all-targets` failed on stale test comparisons and is not proof; final `cargo check -p nimbus-storage --all-targets`: pass; initial `cargo check -p nimbus-bin --all-targets` failed on one stale rotation assertion and is not proof; final `cargo check -p nimbus-bin --all-targets`: pass; `cargo test -p nimbus-storage key::tests -- --nocapture`: 7 passed, 322 filtered; `cargo test -p nimbus-storage encryption::tests -- --nocapture`: 17 passed, 312 filtered; `cargo test -p nimbus-storage redb_dek_rotation -- --nocapture`: 4 passed, 325 filtered; initial `cargo clippy -p nimbus-storage -p nimbus-bin --all-targets -- -D warnings` failed on an AWS-KMS test comparison and is not proof; final clippy command passed, finished in 13.86s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 28 GiB, Cargo git checkouts 12 GiB, 232 GiB free; cleanup decision deferred until the encryption cluster boundary because free space remains healthy. | Final batch commit |
| `A3-4` | low | Storage | CONFIRMED/FIXED | Re-read `KeyDirectoryProvider::read_wrapping_key`, wrap/unwrap callers, key-directory tests, and the `MasterKeyFileProvider` wrapping-key zeroize pattern. Confirmed key-directory wrapping keys were returned as bare `[u8; 32]` values and the file-read `Vec<u8>` was not scrubbed. `read_wrapping_key` now returns `Zeroizing<[u8; 32]>`, zeroizes the intermediate file bytes on success and invalid-size errors, and wrap/unwrap borrow from the zeroizing owner. | `read_wrapping_key_returns_zeroizing_owner` calls the private key-directory reader, pins the fixed return owner type, and asserts the read bytes are preserved for use. Existing key-directory wrap/unwrap/missing/wrong-key tests cover behavior after the owner change. | `cargo test -p nimbus-storage key_directory -- --nocapture`: 12 passed, 318 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 6.92s; `cargo fmt --all --check`: pass after formatting; `git diff --check`: pass. Disk threshold check after verification: main `target/` 28 GiB, Cargo git checkouts 12 GiB, 232 GiB free; cleanup not run because free space remains healthy and the next storage findings can reuse the artifacts. | Final batch commit |
| `A3-5` | low | Storage | CONFIRMED/FIXED | Re-read `capabilities`, `backend_feature_support`, backend `storage_capabilities` callers, diagnostic tests, and the review's backend-implementation evidence. Confirmed the uniform feature support is currently accurate for the shared tenant-event journal backends, but the ignored `_backend` argument falsely implied backend-specific dispatch and was a latent drift hazard. Renamed the function to `shared_journal_backend_feature_support` and removed the unused backend parameter. | `shared_journal_backend_feature_support_is_enterprise_complete` asserts all seven shared-journal backend features are supported and derive `EnterpriseComplete`. Existing `storage_health_diagnostic_reports_backend_layout_and_heads` covers the health diagnostic consumer. | `cargo test -p nimbus-storage shared_journal_backend_feature_support_is_enterprise_complete -- --nocapture`: 1 passed, 330 filtered; `cargo test -p nimbus-storage storage_health_diagnostic_reports_backend_layout_and_heads -- --nocapture`: 1 passed, 330 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 3.31s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 28 GiB, Cargo git checkouts 12 GiB, 232 GiB free; cleanup not run because free space remains healthy and the next storage diagnostics finding can reuse the artifacts. | Final batch commit |
| `A3-6` | low | Storage | CONFIRMED/FIXED | Re-read the five `storage_health_diagnostic*` impls, their per-backend `storage_capabilities`, and the existing health diagnostic tests. Confirmed the diagnostic assembly body was duplicated across `TenantStore`, `SqliteTenantStore`, `PostgresTenantStore`, `MySqlTenantStore`, and `LibsqlReplicaTenantStore`. Introduced `impl_storage_health_diagnostic!` to own the shared method bodies once while preserving each backend's explicit `storage_capabilities` implementation. | Existing `storage_health_diagnostic_reports_backend_layout_and_heads` covers the generated method body through the redb store; clippy all-targets type-checks the macro expansion for all five backend store types. No new behavior-only test was needed for this source-level consolidation. | `cargo test -p nimbus-storage storage_health_diagnostic -- --nocapture`: 1 passed, 330 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 3.79s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 28 GiB, Cargo git checkouts 12 GiB, 232 GiB free; cleanup not run because free space remains healthy. | Final batch commit |
| `A3-7` | low | Storage | CONFIRMED/FIXED | Re-read the finding text and current `GeneratedDatabaseKey` definition after the A3-3 wrapper change. Confirmed the original redundant `#[zeroize(drop)]` field attribute was real before this pass; it was removed while introducing `DataEncryptionKey`. `GeneratedDatabaseKey` still derives `ZeroizeOnDrop`, and the wrapped field remains `#[zeroize(skip)]`. | Covered by A3-3 key tests: `taking_wrapped_key_zeroizes_stored_plaintext`, `generated_key_can_move_plaintext_without_copyable_array_escape`, and redacted-debug tests. | `rg -n "zeroize\\(drop\\)|derive\\(ZeroizeOnDrop\\)|GeneratedDatabaseKey" crates/nimbus-storage/src/encryption/key.rs`: no `zeroize(drop)` remains; `cargo test -p nimbus-storage key::tests -- --nocapture`: 7 passed, 322 filtered; `cargo clippy -p nimbus-storage -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 13.86s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `A3-8` | low | Storage | CONFIRMED/FIXED | Re-read `block_on_future`, AWS KMS provider call sites, and the feature-gated KMS tests. Confirmed the bridge spawned a fresh OS thread whenever called inside Tokio. Replaced the thread bridge with `tokio::task::block_in_place` on multi-thread runtimes, kept the owned current-thread runtime path for synchronous callers outside Tokio, and fail closed with a configuration error inside current-thread Tokio runtimes instead of hiding another spawned thread. | `block_on_future_runs_without_existing_runtime`; `block_on_future_uses_multithread_runtime_without_spawning_bridge_thread`; `block_on_future_rejects_current_thread_runtime`; existing `generate_and_unwrap_round_trip_with_bound_encryption_context` covers the mock KMS provider path under the new bridge. | `cargo test -p nimbus-storage block_on_future -- --nocapture`: 0 tests because `aws-kms` is feature-gated, not proof; `cargo test -p nimbus-storage --features aws-kms block_on_future -- --nocapture`: 3 passed, 333 filtered; `cargo test -p nimbus-storage --features aws-kms generate_and_unwrap_round_trip_with_bound_encryption_context -- --nocapture`: 1 passed, 335 filtered; `cargo clippy -p nimbus-storage --features aws-kms --all-targets -- -D warnings`: pass, finished in 39.48s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 30 GiB, Cargo git checkouts 12 GiB, 230 GiB free; no active Cargo/rustc seen in the process snapshot, so main-target cleanup is appropriate before continuing if approved. | Final batch commit |
| `A4-2` | low | Storage | CONFIRMED/FIXED | Re-read the document MessagePack codec, `Document` field order and typed-field sidecar, scan pushdown probe, read-scan caller, and scan-pushdown tests. Confirmed the probe hard-coded the `Document` array layout with local `5`/`6` field counts and four manual skips. Moved the layout constants and supported field-count predicate beside `document_codec`; scan pushdown now skips to the fields map using the codec-owned field index. | `document_msgpack_layout_constants_match_plain_and_typed_documents`; `scan_pushdown_probe_matches_full_decode_for_document_msgpack_layouts` covers plain rows with extra nested fields, missing target fields, typed-field rows, negative zero, and fractional numeric range edges against full decode. | `cargo test -p nimbus-storage document_msgpack_layout_constants_match_plain_and_typed_documents -- --nocapture`: 1 passed, 332 filtered; `cargo test -p nimbus-storage scan_pushdown_probe_matches_full_decode_for_document_msgpack_layouts -- --nocapture`: 1 passed, 332 filtered; `cargo test -p nimbus-storage scan_pushdown -- --nocapture`: 3 passed, 330 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 3.79s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk cleanup triggered after verification: `cargo clean` removed 53,256 files / 38.9 GiB from this checkout, restoring `/` to 259 GiB free while preserving Cargo git checkouts and sibling targets. | Final batch commit |
| `A4-3` | low | Storage | CONFIRMED/FIXED | Re-read redb/SQLite, libsql, PostgreSQL, and MySQL read executors, the shared `TenantReadStorage` cancellation contract, scan callers that poll `check_cancel`, and async fault tests. Confirmed started `spawn_blocking` reads cannot be preempted by `JoinHandle::abort`; cancellation is cooperative and permits remain held until the blocking task polls or completes. Removed the misleading read-side `handle.abort()` calls across all storage read executors and documented the cooperative permit-release contract on the trait. | `started_async_read_cancellation_is_cooperative_and_releases_permit_after_poll`; existing `queued_canceled_async_read_never_begins_real_storage_execution` covers cancellation before a queued read acquires a permit. | `cargo test -p nimbus-storage started_async_read_cancellation_is_cooperative_and_releases_permit_after_poll -- --nocapture`: 1 passed, 333 filtered; `cargo test -p nimbus-storage queued_canceled_async_read_never_begins_real_storage_execution -- --nocapture`: 1 passed, 333 filtered; `cargo test -p nimbus-storage async_faults -- --nocapture`: 5 passed, 329 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 23.00s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 5.8 GiB, 254 GiB free, so no cleanup triggered. | Final batch commit |
| `A4-4` | low | Storage | CONFIRMED/FIXED | Re-read `async_storage/helpers.rs`, libsql/Postgres/MySQL backend mapper definitions, storage executor call sites, provider bridge call sites, and helper tests. Confirmed join/permit errors used duplicated mapper bodies and divergent operator messages across the same async executor seam. Hoisted the taxonomy into `async_storage::helpers::{map_executor_join_error,map_executor_permit_error}`, exported those helpers crate-internally, removed backend-local mapper bodies, and left each backend with only a short executor-context label. | `executor_join_mapper_preserves_cancelled_join`; `executor_permit_mapper_reports_context_and_closed_semaphore`. | `cargo test -p nimbus-storage executor_ -- --nocapture`: 2 passed, 334 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 4.52s; `rg -n "fn map_join_error|fn map_permit_error" crates/nimbus-storage/src`: only `async_storage/helpers.rs` defines the wrappers; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 6.6 GiB, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `A4-6` | low | Storage | CONFIRMED/FIXED | Re-read the libsql replica freshness state machine, read/write refresh callers, freshness metrics, and existing network-gated provider coverage. Confirmed the core Notify/reschedule/fallback paths had no deterministic unit coverage. Added a test-only refresh override on `LibsqlReplicaTenantStore`, keeping production refresh behavior unchanged, and used a real local SQLite cache with scripted empty durable records to drive freshness progress without a remote libsql fixture. | `freshness_barrier_waits_for_background_refresh_completion`; `freshness_barrier_falls_back_to_sync_refresh_after_background_error`; `freshness_background_refresh_reschedules_for_mid_refresh_sequence_bump`. | Initial `cargo test -p nimbus-storage freshness_ -- --nocapture` attempts exposed test-harness issues (disabled `libsql` local builder/TLS connector and a one-connection local cache) and are not proof; final `cargo test -p nimbus-storage freshness_ -- --nocapture`: 3 passed, 336 filtered; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 4.15s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 8.3 GiB, 251 GiB free, so no cleanup triggered. | Final batch commit |
| `B1-4` | low | Engine | CONFIRMED/FIXED | Re-read `batch.rs` set/patch/transform paths, execution-unit commit finalization, storage execution-unit batch appenders across redb/SQLite/libsql/Postgres/MySQL, Firestore response serialization, transaction staging callers, and the existing server-timestamp test. Confirmed each atomic write path minted independent timestamps for transforms, lifecycle metadata, pending results, and durable commit. `prepare_atomic_write_batch` now captures one batch timestamp, threads it through all atomic write applications, and immediate atomic-batch execution commits with that same timestamp through the existing storage transaction path. | Strengthened `atomic_write_batch_applies_server_timestamp_as_typed_scalar_metadata` with an advancing test clock; it now asserts the server timestamp transform, stored typed field, stored document `update_time`, write-result `update_time`, outcome `commit_time`, and durable `CommitEntry.timestamp` all share one value. Existing atomic batch tests cover set/patch/delete/verify/mixed/staged behavior after the timestamp plumbing. | `cargo test -p nimbus-engine atomic_write_batch_applies_server_timestamp_as_typed_scalar_metadata -- --nocapture`: 1 passed, 288 filtered, finished in 2m02s after cold rebuild; `cargo test -p nimbus-engine atomic_write_batch -- --nocapture`: 13 passed, 276 filtered, finished in 1.31s; `cargo clippy -p nimbus-engine -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 26.75s; initial `cargo fmt --all --check` reported formatting-only diffs and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 15 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB, 245 GiB free, so no cleanup triggered. | Final batch commit |
| `B1-5` | low | Engine | CONFIRMED/FIXED | Re-read `stage_write`, read-side snapshot dependency recording, `TenantPersistenceSnapshot::table_id`, dependency-set intersection, table-lifecycle replacement helpers, schema deletion, and execution-unit tests. Confirmed writes used the live store's table identity while reads used the captured execution-unit snapshot. `stage_write` now resolves table identity from `self.snapshot`, keeping write and read dependency identities in the same snapshot epoch. | `mutation_execution_unit_write_dependencies_use_snapshot_table_identity` starts an execution unit, activates a replacement live table identity, stages a snapshot-era update, and asserts write dependencies contain the original snapshot table id and not the later live replacement. Existing `mutation_execution_unit_` tests cover surrounding staged-write conflict and lifecycle behavior. | Initial focused test run failed because the test assumed redb while the default fixture used SQLite and is not proof; after handling both embedded providers, `cargo test -p nimbus-engine mutation_execution_unit_write_dependencies_use_snapshot_table_identity -- --nocapture`: 1 passed, 289 filtered, finished in 14.50s; `cargo test -p nimbus-engine mutation_execution_unit_ -- --nocapture`: 13 passed, 277 filtered, finished in 1.01s; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 7.07s; initial `cargo fmt --all --check` reported import-order formatting only and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 17 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB, 243 GiB free, so no cleanup triggered. | Final batch commit |
| `B1-6` | low | Engine | CONFIRMED/FIXED | Re-read `mutations/direct/types.rs`, direct mutation API/execution callers, scheduled mutation call sites, and existing mutation tests. Confirmed `expect_immediate_result` and `expect_scheduled_applied` still panicked with `unreachable!` on wrong-variant results. Both helpers now return `Result` and map wrong variants to `Error::Internal`, with callers propagating via `?` instead of assuming the invariant. | `expect_immediate_result_reports_scheduled_variant_as_internal_error`; `expect_scheduled_applied_reports_immediate_variant_as_internal_error`; `expect_result_helpers_return_matching_variants`. Existing `mutation_` test filter exercises direct immediate/scheduled caller propagation. | `cargo test -p nimbus-engine expect_ -- --nocapture`: 3 passed, 290 filtered, finished in 17.61s; `cargo test -p nimbus-engine mutation_ -- --nocapture`: 57 passed, 236 filtered, finished in 1.13s, with external provider tests taking their no-Docker skip path; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 3.69s; initial `cargo fmt --all --check` reported formatting-only wrapping and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 17 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB, 242 GiB free, so no cleanup triggered. | Final batch commit |
| `B1-7` | low | Engine | CONFIRMED/FIXED | Re-read `engine/mutations/journal.rs`, commit-batch processing, document-cache/materialized-read invalidation, mutation-journal visibility tests, and backend durable-apply/recovery paths. Confirmed the recovered `applied_head` was computed but the full prepared commit batch still fed cache/materialized invalidation and later subscription/trigger dispatch. The queued journal path now clips the applied commit list to commits whose sequence is at or below the recovered applied head before invalidation, applied-head publication, and downstream batch processing. | `retain_commits_through_applied_head_clips_recovered_batches` covers recovered heads before the batch, in the middle of the batch, and beyond the batch. Existing `mutation_journal_returns_only_after_apply_visibility`, `sync_subscription_bootstrap_does_not_miss_lagged_applied_commit`, and `trigger_candidates_publish_only_after_journal_apply` were rerun for the adjacent visibility, subscription, and trigger paths. | `cargo test -p nimbus-engine retain_commits_through_applied_head_clips_recovered_batches -- --nocapture`: 1 passed, 293 filtered, finished in 10.08s before the test-module move and 4.52s after the move; `cargo test -p nimbus-engine mutation_journal_returns_only_after_apply_visibility -- --nocapture`: 1 passed, 293 filtered, finished in 0.13s; `cargo test -p nimbus-engine sync_subscription_bootstrap_does_not_miss_lagged_applied_commit -- --nocapture`: 1 passed, 293 filtered, finished in 0.11s; `cargo test -p nimbus-engine trigger_candidates_publish_only_after_journal_apply -- --nocapture`: 1 passed, 293 filtered, finished in 0.01s; initial `cargo fmt --all --check` reported formatting-only wrapping and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; initial `cargo clippy -p nimbus-engine --all-targets -- -D warnings` failed on `items_after_test_module` and is not proof; after moving the test module, final clippy command passed, finished in 2.54s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 18 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 242 GiB free, so no cleanup triggered. | Final batch commit |
| `B2-2` | low | Engine | CONFIRMED/FIXED | Re-read `queries/authorization.rs`, `queries/prepared.rs`, evaluator cursor/pagination code, execution-unit pagination reads, access-rule filter compilation, and existing plan-change cursor tests. Confirmed the B2-1 fix made cursor signatures plan-independent but still signed `ReadAuthorization::merge_query` output, so principal-derived planner filters remained in cursor identity; execution-unit dependency recording also decoded cursors against the authorization-merged query. Prepared pagination now carries a separate stable `cursor_query` copied from the user query before auth filters are merged, store/doc evaluators accept a separate cursor-query argument, and execution-unit pagination uses the user query for cursor encode/decode while keeping the authorization-merged query for filtering and dependency tracking. | `paginated_query_cursor_signature_excludes_principal_auth_filters` proves a cursor minted under Alice with an auth-filtered indexed plan is accepted under Bob after an index-drop plan flip, while Bob's read policy still constrains results; `mutation_execution_unit_paginated_cursor_excludes_principal_auth_filters` covers the V8/runtime execution-unit host-op path and its dependency recorder. Existing `paginated_query_cursor_survives_index_plan_change_between_pages` and evaluator cursor tests were rerun. | Initial `cargo test -p nimbus-engine paginated_query_cursor_signature_excludes_principal_auth_filters -- --nocapture` failed to compile on stale internal re-exports and is not proof; after removing stale re-exports, the test passed: 1 passed, 295 filtered, finished in 0.03s. Initial `cargo test -p nimbus-engine mutation_execution_unit_paginated_cursor_excludes_principal_auth_filters -- --nocapture` failed with `InvalidInput("invalid cursor")`, confirming the execution-unit dependency-recorder gap; after decoding dependencies with the stable cursor query, the test passed: 1 passed, 295 filtered, finished in 0.03s. `cargo test -p nimbus-engine paginated_query_cursor -- --nocapture`: 2 passed, 294 filtered, finished in 0.02s; final post-format `cargo test -p nimbus-engine cursor -- --nocapture`: 17 passed, 279 filtered, finished in 0.20s; initial `cargo fmt --all --check` reported formatting-only import/wrapping diffs and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 3.76s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 19 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 241 GiB free, so no cleanup triggered. | Final batch commit |
| `B2-3` | low | Engine | CONFIRMED/FIXED | Re-read `evaluator/ordering.rs`, structured-query `finalize.rs`, shared comparison helpers, and ordering tests. Confirmed both plain and structured sort paths still called `.expect("ordering inputs should be validated before sorting")` inside sort closures, relying on separate validators to keep fallible comparisons unreachable. Plain and structured sorting now precompute validated sort keys and validate mixed/unsupported domains before sorting; the sort closures compare only total key types and tie-breakers, so invalid data returns `Error::InvalidInput` before sorting instead of preserving a panic edge. | Added `structured_query_rejects_ordering_on_boolean_fields` for the structured finalizer. Existing `evaluator_rejects_ordering_on_boolean_fields`, `evaluator_rejects_mixed_order_value_types`, `paginate_with_filters_and_ordering`, `paginated_fallback_scan_preserves_cursor_and_ordering`, and `supports_document_id_filters_and_implicit_name_ordering` cover plain/structured success and error behavior after the key refactor. | `cargo test -p nimbus-engine ordering -- --nocapture`: 5 passed, 292 filtered, finished in 0.17s before formatting and 0.17s after formatting; `cargo test -p nimbus-engine evaluator_rejects_mixed_order_value_types -- --nocapture`: 1 passed, 296 filtered, finished in 0.15s; `rg -n 'ordering inputs should be validated before sorting' crates/nimbus-engine/src/evaluator/ordering.rs crates/nimbus-engine/src/engine/queries/structured/finalize.rs`: no matches; initial `cargo fmt --all --check` reported formatting-only wrapping and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 6.61s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 19 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 240 GiB free, so no cleanup triggered. | Final batch commit |
| `B2-4` | low | Engine | CONFIRMED/FIXED | Re-read `queries/snapshot.rs`, `queries/verification.rs`, shadow materializer callers, consistency verifier callers, and consistency tests. Confirmed `build_shadow_materializer_async` still duplicated the durable-journal suffix paging loop that `verify_consistency_async` already delegated to `read_durable_journal_suffix_to_sequence_async`. `build_shadow_materializer_async` now calls the shared suffix helper and no longer owns a second no-progress/error loop. | No new behavior test was needed for this consolidation; existing `shadow_materializer_queries_match_live_engine_path`, `shadow_materializer_schema_aware_queries_match_live_engine_path`, and `online_consistency_verifier_matches_authoritative_shadow_and_replica_state` exercise the shadow builder and verifier through the shared helper. | `cargo test -p nimbus-engine shadow_materializer_queries_match_live_engine_path -- --nocapture`: 1 passed, 296 filtered, finished in 0.04s; `cargo test -p nimbus-engine shadow_materializer_schema_aware_queries_match_live_engine_path -- --nocapture`: 1 passed, 296 filtered, finished in 0.02s; `cargo test -p nimbus-engine online_consistency_verifier_matches_authoritative_shadow_and_replica_state -- --nocapture`: 1 passed, 296 filtered, finished in 0.31s; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 2.93s; `rg -n 'stream_durable_journal_async|read_durable_journal_suffix_to_sequence_async|journal stream made no progress' crates/nimbus-engine/src/engine/queries/verification.rs crates/nimbus-engine/src/engine/queries/snapshot.rs`: `verification.rs` has only shared-helper calls and `snapshot.rs` owns the stream/no-progress loop; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 240 GiB free, so no cleanup triggered. | Final batch commit |
| `B2-6` | low | Engine | CONFIRMED/FIXED | Re-read `queries/planner/range.rs`, planner tests, and range-plan call sites. Confirmed the planner owned non-trivial tightest-bound selection, inclusive/exclusive tie-breaking, and `Neq` residual behavior without direct unit coverage. Added focused assertions for competing lower bounds, equal-value inclusive/exclusive tie-breaking, and `Neq` preservation on the range field. | `plan_query_range_index_selects_tightest_lower_bound_and_drops_looser_residuals`; `plan_query_range_index_prefers_exclusive_equal_value_bounds`; `plan_query_range_index_preserves_neq_filter_on_range_field_as_residual`. | `cargo test -p nimbus-engine plan_query_range_index -- --nocapture`: 3 passed, 297 filtered, finished in 0.00s after a 9.39s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 2.96s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 240 GiB free, so no cleanup triggered. | Final batch commit |
| `B2-7` | low | Engine | CONFIRMED/FIXED | Re-read `queries/planner/scoring.rs`, exact/range candidate construction, `plan_query_inner`, and planner-selection tests. Confirmed `PlanCandidate::score()` encoded the planner priority contract as an unlabeled positional tuple. Replaced the tuple with a named `PlanScore` and explicit `Ord` chain documenting the ranking order: consume more indexed fields, preserve requested order, prefer longer exact prefixes, then exact scans over range scans. | `plan_score_orders_priority_fields_explicitly` pins the score comparison priority. Existing `plan_query_selects_*` planner-selection tests were rerun to prove exact/range choices stayed stable. | `cargo test -p nimbus-engine plan_score_orders_priority_fields_explicitly -- --nocapture`: 1 passed, 300 filtered, finished in 0.00s after a 24.99s compile before formatting and 0.00s after a 4.62s compile post-format; `cargo test -p nimbus-engine plan_query_selects -- --nocapture`: 4 passed, 297 filtered, finished in 0.00s; initial `cargo fmt --all --check` reported formatting-only wrapping and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 6.82s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `B3-5` | low | Engine | CONFIRMED/FIXED | Re-read subscription delivery worker/tests/facade, trigger candidate worker/tests/callers, and the prior trigger retry path. Confirmed both workers still carried duplicated `#[cfg(test)]` and `#[cfg(not(test))]` `start_inner`/loop bodies. Collapsed each worker to one `start_inner` and one loop body with cfg-gated test hook parameters; production and test builds now share the queue drain, merge/retry, dispatch, and shutdown logic. | No new runtime behavior test was needed for the modularity consolidation; existing `subscription_delivery` and `trigger_candidate` tests were rerun to cover the pause hooks, queue merge/overflow behavior, trigger publication, bootstrap replay, and in-process retry through the shared bodies. | `cargo test -p nimbus-engine subscription_delivery -- --nocapture`: 4 passed, 297 filtered, finished in 0.25s after formatting; `cargo test -p nimbus-engine trigger_candidate -- --nocapture`: 4 passed, 297 filtered, finished in 0.04s after formatting; initial `cargo fmt --all --check` reported formatting-only cfg-attribute wrapping and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 3.03s; `git diff --check`: pass; `rg -n '#\\[cfg\\((test|not\\(test\\))\\)\\]\\s*$|fn start_inner|fn run_delivery_worker|fn run_trigger_candidate_worker' crates/nimbus-engine/src/tenant/subscription_delivery/worker.rs crates/nimbus-engine/src/tenant/trigger_candidates.rs`: one `start_inner` and one worker-loop definition remain for each worker. Disk threshold check after verification: main `target/` 21 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 7.5 GiB with three permission-denied mount notices, 239 GiB free, so no cleanup triggered. | Final batch commit |
| `B3-6` | low | Engine | CONFIRMED/FIXED | Re-read the overflow monotonicity test, subscription delivery queue/worker, `dispatch_subscription_work`, registry publication, and overflow fallback caller. Confirmed the old test paused the worker before dispatch and did not create the worker-vs-sync-fallback publication race. Added a test-only subscription delivery publish pause at the final publication edge, then added a regression test that pauses the worker after evaluating an older delivery, forces a newer delivery through the overflow sync fallback, and asserts every observed delivery sequence increases until the latest snapshot arrives. | `subscription_delivery_overflow_fallback_does_not_regress_while_worker_is_publishing` covers the concurrent worker/fallback interleaving missing from the prior overflow test. | Initial focused runs exposed test assumptions and are not proof: first assumed fallback snapshots always retain per-commit metadata, second guessed the next delivery sequence, third assumed the fallback update would always arrive before a valid queued middle update. After correcting the test to observe the paused sequence and assert monotonic covered-sequence order across valid interleavings, `cargo test -p nimbus-engine subscription_delivery_overflow_fallback_does_not_regress_while_worker_is_publishing -- --nocapture`: 1 passed, 301 filtered, finished in 0.22s after a 19.78s compile; `cargo test -p nimbus-engine subscription_delivery -- --nocapture`: 5 passed, 297 filtered, finished in 0.25s; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 7.34s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 10 GiB with three permission-denied mount notices, 237 GiB free, so no cleanup triggered. | Final batch commit |
| `B3-7` | low | Engine | CONFIRMED/FIXED | Re-read `subscriptions/dependencies.rs`, the public subscription re-export surface, and all `affected_subscription_ids` callers. Confirmed `affected_subscription_ids` was public on the re-exported `SubscriptionRegistry` while its only consumer was internal commit processing. Demoted the method to `pub(crate)` to keep commit-dispatch matching inside the engine seam. | No new behavior test was needed for this visibility-only seam fix; compile, clippy, and a visibility grep prove the public API contraction and internal caller still compile. | `cargo check -p nimbus-engine`: pass, finished in 16.28s; `rg -n 'pub fn affected_subscription_ids\\(|pub\\(crate\\) fn affected_subscription_ids\\(' crates/nimbus-engine/src/subscriptions/dependencies.rs`: only `pub(crate) fn affected_subscription_ids` remains; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 3.11s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 236 GiB free, so no cleanup triggered. | Final batch commit |
| `B4-6` | low | Engine | CONFIRMED/FIXED | Re-read `persistence/tenant/reads.rs`, MySQL store/provider definitions, and backend fault hook usage. Confirmed the engine persistence enum returned `Ok(())` for MySQL read-side fault checks while MySQL write/journal paths already used the provider fault injector. Added `MySqlTenantStore::check_fault` and routed the `TenantPersistence::MySql` arm through it. | `mysql_tenant_store_check_fault_delegates_to_provider_injector` constructs a non-connecting MySQL tenant store with a failing test injector and asserts the selected fault point is observed. | `cargo test -p nimbus-storage mysql_tenant_store_check_fault_delegates_to_provider_injector -- --nocapture`: 1 passed, 339 filtered, finished in 0.00s after an 18.47s compile; `cargo check -p nimbus-engine`: pass, finished in 2.89s; `rg -n 'Self::MySql\\(_store\\) => Ok\\(\\(\\)\\)|Self::MySql\\(store\\) => store.check_fault\\(point\\)|pub fn check_fault\\(&self, point: FaultPoint\\)' crates/nimbus-engine/src/persistence/tenant/reads.rs crates/nimbus-storage/src/mysql.rs`: no no-op arm remains and the MySQL store forwarder is present; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-storage -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 6.62s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 23 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 236 GiB free, so no cleanup triggered. | Final batch commit |
| `C1-2` | low | Runtime | CONFIRMED/FIXED | Re-read `runtime/bootstrap/ops/worker_threads.rs`, the fast-op CPU usage entrypoints, helper functions, and local unit tests. Confirmed the original unchecked `out[0]`/`out[1]` writes existed in `HEAD`, while the current remediation routes both worker and current-thread CPU usage through helpers that return without writing when the caller provides fewer than two output slots. | `current_thread_cpu_usage_ignores_short_output_buffers` asserts a one-slot current-thread output buffer is preserved and a two-slot buffer receives finite non-negative CPU counters; `host_worker_cpu_usage_ignores_short_output_buffers` asserts a one-slot worker output buffer is preserved and a missing worker writes `[0.0, 0.0]` only when two slots are present. | `cargo test -p nimbus-runtime cpu_usage -- --nocapture`: 2 unit tests passed, 554 filtered, integration test binaries `bun_jsc_linked_adapter`, `engine_proofs`, and `locker_smoke` had 0 matching tests, finished in 0.00s after a 47.05s compile; `cargo fmt --all --check`: pass; `git diff --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 25.36s. Disk threshold check after verification: main `target/` 27 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 232 GiB free, so the cleanup trigger fired; `cargo clean` removed 41,260 files / 37.7 GiB and post-clean `/` had 259 GiB free with this checkout's `target/` absent. | Final batch commit |
| `C1-4` | low | Runtime | CONFIRMED/FIXED | Re-read `runtime/bootstrap/ops/worker_threads.rs` around the macOS, Linux, and Windows CPU-accounting helpers and confirmed the unsafe FFI blocks had no local safety rationale. Added `SAFETY:` comments for `mach_thread_self`, zero-initialized `ThreadBasicInfo`, `thread_info`, the macOS extern declarations, Linux `syscall`/`sysconf`, and Windows `GetCurrentThreadId`/`OpenThread`/`GetThreadTimes`/`CloseHandle`/`assume_init`. | No behavior test was needed for this comment-only auditability fix; the existing CPU-usage tests from `C1-2` cover the helper outputs, and the C1-4 proof is structural plus compilation/linting. | `rg -n 'unsafe \\{|unsafe extern|SAFETY:' crates/nimbus-runtime/src/runtime/bootstrap/ops/worker_threads.rs`: every unsafe block/extern block in the CPU-accounting section now has an adjacent `SAFETY:` comment; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 52.32s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 5.7 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `C1-6` | low | Runtime | CONFIRMED/FIXED | Re-read `runtime_local/bootstrap.rs`, `RuntimePathPolicy`, Node22 bootstrap/reset code, and exec-path tests/callers. Confirmed `op_nimbus_runtime_exec_path` returned the real host executable path without consulting runtime grants. The op now returns a stable synthetic path by default, returns the staged compat self-exec path only when `$runtime_self_exec` is granted, and returns the real host executable only when `$runtime_host_exec` is explicitly granted. The per-invocation bootstrap reset refreshes `process.execPath` and `process.argv[0]` after the runtime contract is installed so pooled/snapshot runtimes do not retain the pre-contract synthetic seed; that reset refresh is block-scoped so warm runtime reuse can execute it repeatedly without lexical redeclaration. | Added `node22_exec_path_uses_synthetic_path_without_run_grant`, `node22_self_exec_grant_exposes_staged_exec_path`, and `node22_host_exec_grant_is_required_to_expose_host_exec_path`; updated `node22_target_exposes_minimal_node_globals` to assert the synthetic default instead of the old host-path leak. Adjacent cooperative warm-reuse coverage also pins repeated reset execution. | Initial `cargo test -p nimbus-runtime node22_self_exec_grant_exposes_staged_exec_path -- --nocapture` failed after the first op change because `Deno.execPath()` saw the self-exec grant but `process.execPath` still held the snapshot-time synthetic value; this was not proof and led to the reset refresh fix. Final focused runs passed: `cargo test -p nimbus-runtime node22_exec_path_uses_synthetic_path_without_run_grant -- --nocapture`: 1 passed, 558 filtered, integration test binaries had 0 matching tests, finished in 1.80s after the reset fix; `cargo test -p nimbus-runtime node22_self_exec_grant_exposes_staged_exec_path -- --nocapture`: 1 passed, 558 filtered, finished in 1.96s after a 3.82s compile; `cargo test -p nimbus-runtime node22_host_exec_grant_is_required_to_expose_host_exec_path -- --nocapture`: 1 passed, 558 filtered, finished in 1.87s; `cargo test -p nimbus-runtime node22_target_exposes_minimal_node_globals -- --nocapture`: 1 passed, 558 filtered, finished in 1.85s. Later C2-2 adjacent verification exposed that the reset script still used a top-level `const` and failed warm reuse with `Identifier '__nimbusRuntimeExecPath' has already been declared`; after block-scoping the reset refresh, `cargo test -p nimbus-runtime cooperative_execution_model_ -- --nocapture`: 4 passed, 561 filtered, integration test binaries had 0 matching tests, finished in 0.08s after a 4.85s compile, and `cargo test -p nimbus-runtime exec_path -- --nocapture`: 4 passed, 561 filtered, integration test binaries had 0 matching tests, finished in 7.40s. `cargo fmt --all --check`: pass; final `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.61s; `git diff --check`: pass. Disk threshold check after C2-2 verification: main `target/` 6.2 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `C2-1` | low | Runtime | CONFIRMED/FIXED | Re-read `executor/admission/permit.rs`, `RuntimeExecutorAdmission`/dispatch handles, runtime policy semaphores, metrics counters, and cooperative permit callers. Confirmed both initial acquisition and async-host resume incremented `queued_invocations` before fallible active/runtime semaphore awaits, and semaphore-closed errors could return before the decrement. Added a `QueuedInvocationMetricGuard` that increments on entry and decrements exactly once on success or any early return after queue accounting begins. | `acquire_initial_decrements_queue_when_active_permit_is_closed`; `acquire_initial_decrements_queue_when_runtime_semaphore_is_closed`; `complete_async_host_call_decrements_queue_when_active_permit_is_closed`; `complete_async_host_call_decrements_queue_when_runtime_semaphore_is_closed`. Each closes the target semaphore, asserts the expected error branch, and proves `queued_invocations` returns to 0. | Initial `cargo clippy -p nimbus-runtime --all-targets -- -D warnings` failed on `items_after_test_module` after the new tests and is not proof; after moving the test module to EOF, final verification passed. `cargo test -p nimbus-runtime decrements_queue_when -- --nocapture`: 4 passed, 559 filtered, integration test binaries had 0 matching tests, finished in 0.01s after a 7.10s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 4.65s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 6.0 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `C2-2` | low | Runtime | CONFIRMED/FIXED | Re-read `worker_loop/cooperative/run.rs`, `scheduler.rs`, `execution.rs`, executor invoke/drop paths, cooperative runtime-slot finalization, and existing cooperative tests. Confirmed shutdown cancelled the worker loop before retained runnable/parked scheduler slots were completed, so callers observed the dropped-result contract error. The scheduler now drains retained runnable/parked slots on shutdown, unfinished cooperative runtime slots can be finalized with an explicit result, and the worker completes drained slots through the existing `finish_invocation` plus `queue.complete_job` path with `NimbusRuntimeError::Cancelled`. | `cooperative_execution_model_cancels_parked_invocations_on_shutdown` parks real async host work, triggers the executor shutdown/queue-close path, and asserts the caller receives `Cancelled` and in-flight cancellation metrics are recorded; `scheduler_drains_retained_runnable_and_parked_slots` proves the scheduler drains retained runnable/parked payloads while excluding an in-flight slot. | `cargo test -p nimbus-runtime cooperative_execution_model_cancels_parked_invocations_on_shutdown -- --nocapture`: 1 passed, 564 filtered, integration test binaries had 0 matching tests, finished in 0.05s after a 10.14s compile; `cargo test -p nimbus-runtime scheduler_drains_retained_runnable_and_parked_slots -- --nocapture`: 1 passed, 564 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 4.47s compile; initial `cargo clippy -p nimbus-runtime --all-targets -- -D warnings` failed on a test-only `drop_non_drop` assertion shape and is not proof; after rewriting the test assertion, clippy passed. Adjacent `cargo test -p nimbus-runtime cooperative_execution_model_ -- --nocapture` initially failed in the warm-reuse test on the C1-6 reset-script top-level `const` redeclaration and is not proof; after block-scoping that reset refresh, the same command passed with 4 passed, 561 filtered, integration test binaries had 0 matching tests, finished in 0.08s after a 4.85s compile. `cargo test -p nimbus-runtime exec_path -- --nocapture`: 4 passed, 561 filtered, integration test binaries had 0 matching tests, finished in 7.40s; `cargo fmt --all --check`: pass; final `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.61s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 6.2 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `C2-3` | low | Runtime | CONFIRMED/FIXED | Re-read `watchdog.rs`, `HostCallCancellation`, runtime invocation-driver cancellation registration, executor cancellation callers, and watchdog tests. Confirmed external cancellation registrations were still stored in a map polled every 10 ms whenever any cancellation handler was active. `HostCallCancellation` now supports one-shot cancel listeners, and watchdog cancellation registrations enqueue a `CancellationFired` command into the watchdog queue instead of forcing periodic map scans; stale fired events after disarm are ignored by the existing handler removal. `next_wait` is deadline-only again, so registered external cancellations do not wake the watchdog unless a cancellation actually fires. | `host_call_cancellation_notifies_listeners_once_and_immediate_after_cancel` pins listener firing, single-shot behavior, preserved first cancellation cause, and immediate listener invocation after cancellation; `external_cancellation_registration_fires` covers the evented watchdog cancellation path; `disarm_prevents_external_cancellation_callback` proves a later fired cancellation event is ignored after disarm; `next_wait_uses_deadlines_only` pins removal of the 10 ms cancellation poll wake. The existing `runtime_external_cancellation_stops_infinite_loops` also ran under the `external_cancellation` filter. | `cargo test -p nimbus-runtime host_call_cancellation_notifies_listeners_once_and_immediate_after_cancel -- --nocapture`: 1 passed, 567 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 12.02s compile; `cargo test -p nimbus-runtime external_cancellation -- --nocapture`: 3 passed, 565 filtered, integration test binaries had 0 matching tests, finished in 0.06s; `cargo test -p nimbus-runtime next_wait_uses_deadlines_only -- --nocapture`: 1 passed, 567 filtered, integration test binaries had 0 matching tests, finished in 0.00s; initial `cargo fmt --all --check` reported formatting-only wrapping/blank-line diffs and is not proof; after `cargo fmt --all`, final `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 5.61s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 6.4 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `C2-4` | low | Runtime | CONFIRMED/FIXED | Re-read `executor/invoke.rs`, bridge helper tests, blocking invocation callers, and worker invocation lifecycle tests. Confirmed `bridge_blocking_invocation` still matched `RuntimeFlavor::CurrentThread | _`, where the wildcard already covers current-thread and future non-multithread flavors. Replaced the arm with a plain fallback `_` so the helper states the actual policy: multithread runtimes use `block_in_place`; every other runtime flavor uses a scoped fallback thread. | No new behavior test was needed for the code-smell simplification; existing `blocking_bridge_stays_on_current_thread_for_multi_thread_runtime` and `blocking_bridge_spawns_fallback_thread_for_current_thread_runtimes` pin the two routing outcomes. | `cargo test -p nimbus-runtime blocking_bridge -- --nocapture`: 2 passed, 566 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 6.55s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.68s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 6.5 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices, 253 GiB free, so no cleanup triggered. | Final batch commit |
| `C3-4` | low | Runtime | ALREADY-FIXED | Re-read the cited worker-thread CPU-accounting FFI section and the `C1-4` remediation row. The current file already carries adjacent `SAFETY` rationale for `mach_thread_self`, `ThreadBasicInfo` zeroing and `thread_info`, macOS extern declarations, Linux `syscall` and `sysconf`, and Windows `GetCurrentThreadId`, `OpenThread`, `GetThreadTimes`, `CloseHandle`, and `assume_init`. This is the same unsafe-comment defect remediated under `C1-4`, so no additional code change was needed. | No new test added; duplicate comment-only unsafe-audit finding already covered by the `C1-4` structural proof and runtime clippy. | `rg -n 'unsafe \\{|unsafe extern|SAFETY:' crates/nimbus-runtime/src/runtime/bootstrap/ops/worker_threads.rs`: every unsafe block and unsafe extern block in the cited CPU-accounting section has adjacent `SAFETY` rationale; latest `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.68s during `C2-4` verification; latest `git diff --check`: pass. | Final batch commit |
| `C3-6` | low | Runtime | NOT-A-DEFECT | Re-read `metrics/global.rs`, the public `RuntimeMetrics` facade, cancellation lifecycle callers, worker-loop queued-cancel callers, and the existing metrics tests. `canceled_invocations` is a total invocation count, while `queued_canceled_invocations`/`in_flight_canceled_invocations` are phase counters and `disconnect_canceled_invocations`/`explicit_canceled_invocations` are cause dimensions recorded alongside the same invocation. The cause recorders are private and are reachable only from the queued/in-flight recorders, so a disconnected queued cancellation is already counted once in `canceled_invocations`; adding cause counters to the aggregate would double-count. | No test added; no code defect exists after re-deriving the metric model and call graph. Existing `tenant_metrics_snapshot_tracks_distributions_and_cancellations` demonstrates phase and cause counters are both recorded for the same cancellations. | `rg -n 'record_canceled_invocation_cause|record_disconnect_canceled_invocation|record_explicit_canceled_invocation|HostCallCancellationCause' crates/nimbus-runtime/src crates/nimbus-runtime/tests`: cause recorders are private and called only from queued/in-flight cancellation recorders; code read confirmed `run_invocation_lifecycle`, direct worker invoke, blocking worker invoke, and run-to-completion worker paths record a queued or in-flight cancellation before recording a cause. | Final batch commit |
| `C3-7` | low | Runtime | CONFIRMED/FIXED | Re-read the Bun/JSC linked host-bridge callback, Rust caller overflow handling, linked-backend tests, and linked-adapter feature wiring. Confirmed `bun_jsc_host_bridge_call_json` wrote `*output_len` before proving the caller buffer could hold the response. The callback now checks capacity first, reports the required length only with status `307`, and writes the success length only after the bounded-copy invariant is established. The feature-aware clippy gate also exposed a large `BunJscAdapterDiscoveryError` error variant in the same linked-adapter seam; boxed diagnostics so the touched feature builds cleanly without suppressing the lint. | `host_bridge_callback_reports_required_length_without_copy_on_overflow` asserts status `307`, required-length reporting, and no output-buffer write on overflow; `host_bridge_callback_sets_output_length_after_successful_capacity_check` asserts the success length and copied JSON bytes. | Initial default-feature `cargo test -p nimbus-runtime host_bridge_callback_ -- --nocapture`: 0 matching tests, so not proof for the feature-gated file; `cargo test -p nimbus-runtime --features bun-jsc-linked-adapter host_bridge_callback_ -- --nocapture`: 2 passed, 590 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 4.52s compile; initial `cargo clippy -p nimbus-runtime --features bun-jsc-linked-adapter --all-targets -- -D warnings` failed on pre-existing `clippy::result_large_err` in the linked-adapter discovery error path and is not proof; after boxing diagnostics, final `cargo clippy -p nimbus-runtime --features bun-jsc-linked-adapter --all-targets -- -D warnings`: pass, finished in 7.06s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 7.8 GiB, 251 GiB free, so no cleanup triggered. | Final batch commit |
| `C3-8` | low | Runtime | CONFIRMED/FIXED | Re-read the worker-thread raw post-message ops, host/worker port lifecycle, Node worker-thread bootstrap JS, and worker-thread tests. Confirmed both raw paths still converted `tx.send(...)` failures into `Ok(())`. Added a shared raw-send helper that preserves the intentional no-op for absent/disentangled ports but returns a JS-visible `MessagePortError` when a sender exists and the receiving half is closed; both host-to-worker and worker-to-parent raw ops now use that helper. The first JS fixture tried to prove this through `parentPort.close()`, but rerunning it showed that path is a Node capability/bootstrap behavior rather than the cited raw-channel defect, so it was replaced with a deterministic raw-send unit regression beside the helper. | `raw_message_send_reports_closed_receiver` drops the receiver side of a Tokio unbounded channel and asserts the raw-send mapper returns the closed raw-receiver `MessagePortError`; `application_node22_shared_worker_env_is_runtime_scoped_and_grant_gated` was rerun as adjacent happy-path worker raw-message coverage. | Earlier `cargo test -p nimbus-runtime application_node22_worker_post_message_reports_closed_raw_receiver -- --nocapture` passed once but failed on closeout rerun with `postAfterClose.ok == true`, so that fixture is recorded as non-proof and removed. Final proof: `cargo test -p nimbus-runtime raw_message_send_reports_closed_receiver -- --nocapture`: 1 passed, 572 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 15.36s compile; `cargo test -p nimbus-runtime application_node22_shared_worker_env_is_runtime_scoped_and_grant_gated -- --nocapture`: 1 passed, 568 filtered, integration test binaries had 0 matching tests, finished in 9.05s after a 0.90s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 28.73s; `git diff --check`: pass. | Final batch commit |
| `D1-1` | low | Server | CONFIRMED/FIXED | Re-read `HostCallCancellation::cancelled`, `cancel_with_cause`, runtime cancellation callers, and the WS pending unsubscribe path. Confirmed the `is_cancelled()`-then-`notified().await` sequence could miss a `notify_waiters` wakeup. `cancelled()` now creates and enables the `Notified` future before the second cancellation check, using the standard Tokio pattern. | `host_call_cancellation_waiter_completes_after_cancel` constructs and enables a waiter, cancels immediately, and asserts the waiter completes instead of sleeping forever. | `cargo test -p nimbus-runtime host_call_cancellation_waiter_completes_after_cancel -- --nocapture`: 1 passed, 572 filtered, integration binaries 0 matching, finished in 0.00s after a 56.84s compile; `cargo clippy -p nimbus-runtime -p nimbus-server -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 44.00s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `D1-3` | low | Server | CONFIRMED/FIXED | Re-read `ws/socket/pending.rs`, its callers, and the review's AppState recovery comparison. Confirmed the registry used repeated `.expect` lock acquisitions while the crate already uses poison recovery for long-lived shared state. Added one poison-recovering `lock()` helper and routed all pending-registry lock sites through it. | The new pending-registry tests exercise the same state machine through the helper: linked cancellation, finish cleanup, and clear behavior. | `cargo test -p nimbus-server ws::socket::pending::tests -- --nocapture`: 3 passed, 441 filtered, integration binaries 0 matching, finished in 0.00s after a 57.98s compile; scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D1-4` | low | Server | NOT-A-DEFECT | Re-read `spawn_socket_writer`, `ServerMessage::to_text`, `to_v2_json`, `PublicError`, and outbound message construction. The writer still breaks on serialization errors, but the asserted pathological payload is not reachable today: messages are first lowered to `serde_json::Value` and `PublicError` contains ordinary strings/static fields/`Value`, so `serde_json::to_string(Value)` has no fallible custom serializer path. Changing the writer would not fix a live defect. | None. | Evidence read: `transport.rs` still breaks on `to_text` error, while `protocol.rs` serializes a constructed `serde_json::Value` from server-owned fields; no behavioral test added because the claimed error path is not constructible. | Final batch commit |
| `D1-5` | low | Server | NOT-A-DEFECT | Re-read `ServerMessage::to_v2_json`, `packages/nimbus/src/browser.ts`, and `packages/convex/src/selftest.mjs`. The `AuthError` wire shape intentionally uses `type: "error"` because the JS client treats that frame as the auth-refresh signal while `op.error` remains request-scoped. Emitting `auth.error` only server-side would break the current client protocol instead of resolving ambiguity. | None. | Evidence read: `browser.ts` handles `type: "error"` during socket authentication and the Convex selftest sends `{ type: "error", error: { message: "expired" } }` to force token refresh; no server change made. | Final batch commit |
| `D1-6` | low | Server | CONFIRMED/FIXED | Re-read `deploy_app`, staging helpers, deploy route wiring, and deploy artifact tests. Confirmed synchronous filesystem staging ran inside the async handler. `deploy_app` now moves artifact staging into `tokio::task::spawn_blocking`, and staging cleanup is owned by `tempfile::TempDir` instead of a custom synchronous `Drop` cleanup path. | `stage_deploy_artifacts_uses_private_randomized_directory` also covers the refactored staging path by asserting the staged manifest exists in the generated directory. | Initial focused staging test failed because the random tempdir was `0755`, exposing an incomplete D2-4 fix; after enforcing `0700`, `cargo test -p nimbus-server stage_deploy_artifacts_uses_private_randomized_directory -- --nocapture`: 1 passed, 443 filtered, integration binaries 0 matching, finished in 0.00s after a 19.05s compile; scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D1-7` | low | Server | CONFIRMED/FIXED | Re-read `ws/socket/pending.rs`, session cancellation callers, and the reactive-loop coverage gap. Confirmed the pure pending registry had no in-module tests. Added focused unit tests for linked cancel, finish cleanup, and clear semantics. | `cancel_subscription_cancels_linked_request_once`; `finish_request_removes_request_and_linked_subscription`; `clear_drops_pending_requests_without_cancelling_them`. | `cargo test -p nimbus-server ws::socket::pending::tests -- --nocapture`: 3 passed, 441 filtered, integration binaries 0 matching, finished in 0.00s; scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D2-2` | low | Server | CONFIRMED/FIXED | Re-read `route_family_gate_middleware`, `shutdown_system`, `rotate_local_admin_token`, local-admin tests, and production router wiring. Confirmed `shutdown_system` relied on middleware and did not defend itself when local server security was absent. `shutdown_system` now self-authorizes with configured local server security before recording the shutdown request and fails closed when security is unconfigured. | The D2-3 negative tests cover the fixed posture, including the unconfigured-security branch. | `cargo test -p nimbus-server system_shutdown_endpoint_rejects -- --nocapture`: 2 passed, 442 filtered, integration binaries 0 matching, finished in 0.06s; `cargo test -p nimbus-server system_shutdown_endpoint_stops_live_server -- --nocapture`: 1 passed, 443 filtered, integration binaries 0 matching, finished in 0.40s; scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D2-3` | low | Server | CONFIRMED/FIXED | Re-read `local_admin.rs`, the existing happy-path shutdown test, and local server security fixtures. Confirmed there was no missing/invalid token test and no `local_server_security == None` coverage for the destructive shutdown route. Added both negative cases while preserving the existing valid-bearer happy path. | `system_shutdown_endpoint_rejects_missing_and_invalid_credentials`; `system_shutdown_endpoint_rejects_when_local_security_unconfigured`. | `cargo test -p nimbus-server system_shutdown_endpoint_rejects -- --nocapture`: 2 passed, 442 filtered, integration binaries 0 matching, finished in 0.06s; `cargo test -p nimbus-server system_shutdown_endpoint_stops_live_server -- --nocapture`: 1 passed, 443 filtered, integration binaries 0 matching, finished in 0.40s; scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D2-4` | low | Server | CONFIRMED/FIXED | Re-read `stage_deploy_artifacts`, deploy artifact writes, route wiring, and temp cleanup behavior. Confirmed the old staging path was predictable and inherited default permissions. Staging now uses `tempfile::Builder` for an unpredictable `nimbus-deploy-` directory and explicitly sets Unix permissions to `0700` before writing artifacts. | `stage_deploy_artifacts_uses_private_randomized_directory` asserts the random Nimbus prefix, staged manifest, and `0700` mode on Unix. | Initial `cargo test -p nimbus-server stage_deploy_artifacts_uses_private_randomized_directory -- --nocapture` failed with mode `0755`, proving the platform default was insufficient; after the explicit permission fix, final run passed: 1 passed, 443 filtered, integration binaries 0 matching, finished in 0.00s after a 19.05s compile. Scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D2-5` | low | Server | NOT-A-DEFECT | Re-read `validate_origin`, `LocalServerRouteFamily::requires_origin_allowlist`, local-server middleware ordering, and local-server security tests. Confirmed absent `Origin` intentionally returns `Ok(())` so native/non-browser clients work, and protected non-browser routes still require server-access credentials through the next middleware. Present non-loopback origins are rejected before auth. This is the documented native boundary, not a defect to patch locally. | None. | `cargo test -p nimbus-server bad_origin_returns_forbidden_before_local_admin_auth -- --nocapture`: 1 passed, 443 filtered, integration binaries 0 matching, finished in 0.09s; `cargo test -p nimbus-operator validate_origin_rejects_non_loopback_and_pna_preflights -- --nocapture`: 1 passed, 31 filtered, finished in 0.00s. | Final batch commit |
| `D3-1` | low | Server | CONFIRMED/FIXED | Re-read the Convex read-tracking builder locks, subscription-status locks, and socket/read-tracking callers. Confirmed the panic-on-poison policy remained spread across repeated `.expect` sites. Centralized the policy behind `lock_query_builders`, `read_subscription_statuses`, and `write_subscription_statuses`, so the message and invariant live in one place per shared state owner. | No new behavior test was needed for this lock-policy centralization; existing socket and server compile coverage exercise the helper call sites. | `cargo test -p nimbus-server ws::socket::pending::tests -- --nocapture`: 3 passed, 441 filtered; `cargo clippy -p nimbus-runtime -p nimbus-server -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 44.00s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `D3-2` | low | Server | CONFIRMED/FIXED | Re-read direct invocation pagination, query read tracking, and subscription bootstrap. Confirmed paginated reads are intentionally post-execution because the returned page defines the window bounds, and failed subscription bootstrap installs no subscription. Added an explanatory comment at the post-execution read tracking site. | No behavior test was needed for this documentation-only maintainability fix; the existing subscription/cancellation tests continue to prove failed bootstrap does not install read-tracking state. | `cargo test -p nimbus-server runtime_cancellable_db_get_short_circuits_before_dispatch -- --nocapture`: previously in this remediation pass, 1 passed, 437 filtered; current scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D4-4` | low | Server | CONFIRMED/FIXED | Re-read `FirestoreGrpcService::app_state`, stateless constructors, router construction, and the gRPC tests. Confirmed the branch represented a misconstructed service, not an unimplemented feature. The error is now `Status::internal("Firestore gRPC server constructed without application state")`, and tests assert the honest internal-misconfiguration contract. | `firestore_grpc_unary_stub_reports_missing_application_state`; `firestore_grpc_server_streaming_stub_reports_missing_application_state`. | `cargo test -p nimbus-server firestore_grpc_ -- --nocapture`: 3 passed, 441 filtered, integration binaries 0 matching, finished in 0.00s; scoped clippy/fmt/diff checks passed as recorded for D1-1. | Final batch commit |
| `D4-5` | low | Server | CONFIRMED/FIXED | Re-read `system/version_check.rs` hostname collection and confirmed the `libc::gethostname` call was correct but lacked a local safety rationale. Added a `SAFETY:` comment documenting the fixed stack buffer, length match, rc check, and NUL handling. | No behavior test was needed for the unsafe-auditability comment; compilation and linting are the proof. | `cargo clippy -p nimbus-runtime -p nimbus-server -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 44.00s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `F1-1` | low | Security | CONFIRMED/FIXED | Re-read `operator_policy/external.rs`, `evaluation.rs`, external-policy tests, and repo callers. Confirmed `recv_timeout` still returned while leaving a spawned backend worker detached until `evaluate` returned, while production paths only expose the optional public seam today. Added a bounded in-flight worker permit to `OperatorExternalPolicyEngine`; timed-out workers keep a slot until they finish, and a saturated cap fails closed before spawning another backend call. | `external_policy_engine_caps_workers_while_backend_is_hung` blocks a fake backend past timeout, proves the next admission fails closed at the worker cap, and asserts the backend call count stays at 1; `external_policy_engine_rejects_zero_worker_limit`; existing timeout test rerun. | `cargo test -p nimbus-tenant external_policy_engine -- --nocapture`: 3 passed, 79 filtered, finished in 0.25s after a 40.32s cold rebuild; `cargo check -p nimbus-tenant`: pass, finished in 20.17s; `cargo clippy -p nimbus-tenant --all-targets -- -D warnings`: pass, finished in 6.64s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, 238 GiB free, so no cleanup triggered. | Final batch commit |
| `F1-2` | low | Security | CONFIRMED/FIXED | Re-read `operator_policy/validation.rs`, `OperatorPolicyWorkload::validate`, runtime-profile tests, and downstream runtime admission. Confirmed `OperatorRuntimePolicy::validate` returned `Ok(())` on every branch while real grant enforcement lives in `TenantIsolationContext::admit_runtime_policy`. Removed the no-op validator, its call site, the dead `workload_key` parameter, and now-unused imports. | No new behavior test was needed for deleting inert code; existing policy-fixture validation and Node-profile admission tests pin the surrounding behavior. | `cargo test -p nimbus-tenant policy_fixture -- --nocapture`: 4 passed, 78 filtered, finished in 0.21s; `cargo test -p nimbus-tenant node_profile -- --nocapture`: 4 passed, 78 filtered, finished in 0.00s; `cargo check -p nimbus-tenant`: pass, finished in 1.65s; `cargo clippy -p nimbus-tenant --all-targets -- -D warnings`: pass, finished in 1.75s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 21 GiB, Cargo git checkouts 12 GiB, 238 GiB free, so no cleanup triggered. | Final batch commit |
| `F1-3` | low | Security | CONFIRMED/FIXED | Re-read `TenantIsolationContext` principal-claim helpers, decision admission, Convex/Firebase/Cloud Functions route call sites, strict sandbox/session/service route call sites, and the tenant principal-claim tests. Confirmed the permissive and strict helpers were intentionally different but too similarly named. Replaced the permissive public method with `admit_if_principal_claim_absent_or_matching`, added doc notes for permissive vs strict use, and routed both public methods through one private checker with an explicit missing-claim policy. | `application_context_rejects_mismatched_principal_tenant_claim`; `application_context_allows_matching_verified_principal_tenant_claim`; `application_context_can_require_tenant_claim_for_control_plane_routes` cover mismatch rejection, verified-claim precedence, permissive absent-claim admission, and strict absent-claim rejection. | `cargo test -p nimbus-tenant application_context -- --nocapture`: 4 passed, 78 filtered, finished in 0.00s; initial `cargo check -p nimbus-tenant -p nimbus-server` failed on two missed public call sites in `nimbus-firebase` and `nimbus-cloud-functions` and is not proof; after the repo-wide rename, `rg -n "validate_principal_claim_if_present" crates packages demos`: no matches; final `cargo check -p nimbus-tenant -p nimbus-server`: pass, finished in 8.15s; `cargo clippy -p nimbus-tenant -p nimbus-server -p nimbus-firebase -p nimbus-cloud-functions --all-targets -- -D warnings`: pass, finished in 32.57s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, 237 GiB free, so no cleanup triggered. | Final batch commit |
| `F1-4` | low | Security | CONFIRMED/FIXED | Re-read `operator_policy/validation.rs`, egress conversion, sandbox egress host validation, and policy validation tests. Confirmed operator network endpoint hosts only rejected empty/`*`/unspecified IPs and accepted embedded ports, schemes, userinfo, paths, brackets, whitespace, and invalid DNS labels. Tightened `validate_host` to require a concrete bare DNS hostname or IP literal, reject wildcard hostnames, reject URL/authority/path/bracket/port shapes, reject whitespace, and validate DNS label shape after IP parsing. | `policy_rejects_malformed_network_endpoint_hosts` covers embedded port, scheme, userinfo, path, bracketed IPv6, whitespace, wildcard DNS, and invalid DNS label; `policy_accepts_bare_network_endpoint_hosts` covers DNS, IPv4, and bare IPv6 literals. Existing policy fixture validation tests rerun. | `cargo test -p nimbus-tenant network_endpoint_hosts -- --nocapture`: 2 passed, 82 filtered, finished in 0.23s; `cargo test -p nimbus-tenant policy_fixture -- --nocapture`: 4 passed, 80 filtered, finished in 0.20s; `cargo check -p nimbus-tenant`: pass, finished in 1.99s; `cargo clippy -p nimbus-tenant --all-targets -- -D warnings`: pass, finished in 1.84s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, 237 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-1` | low | Security | CONFIRMED/FIXED | Re-read `systemd_transient/zbus_client/signals.rs`, the `ZbusSystemdClient` start/stop callers, and node systemd tests. Confirmed `wait_for_job` awaited the `JobRemoved` stream indefinitely. Added a default 30s systemd job-completion timeout, threaded it through start/stop signal correlation, exposed `with_job_completion_timeout` for callers/tests, and mapped elapsed waits to an internal error naming the phase, job path, and deadline. `tokio` is now an optional production dependency of the `systemd-dbus` feature because the live zbus client uses `tokio::time::timeout`. | `wait_for_job_result_times_out_when_matching_signal_never_arrives` drives a pending completion future with a 5ms deadline and asserts the error names the missing job path and timeout. | Initial `cargo test -p nimbus-node wait_for_job_result_times_out_when_matching_signal_never_arrives -- --nocapture` failed to compile because `tokio` was only a dev-dependency and is not proof; after wiring optional `tokio` into `systemd-dbus`, the same focused test passed: 1 passed, 30 filtered, live integration binary 0 matching, finished in 0.01s after 11.46s compile. `cargo check -p nimbus-node`: pass, finished in 16.79s; `cargo clippy -p nimbus-node --all-targets -- -D warnings`: pass, finished in 6.25s; `make deny`: pass (`advisories ok`, `bans ok`, `licenses ok`, `sources ok`); `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk cleanup triggered after verification: `cargo clean` removed 54,750 files / 30.3 GiB from this checkout and restored `/` to 259 GiB free while preserving Cargo git checkouts and the active sibling NDS target. | Final batch commit |
| `F2-2` | low | Security | CONFIRMED/FIXED | Re-read `records.rs`, `schema.rs`, system projection tests, and the projection call sites. Confirmed the table sink defect: schema generation and system projection writes/deletes/list scans were still selected with raw table string literals. The module-size half of the finding is not independently a repo-law defect: `records.rs` is about 1,064 lines, below the 1,500-line threshold, and remains a coherent system-tenant projection owner, so this fix kept the ownership boundary intact instead of mechanically splitting it. Added a `SystemTable` enum in the schema owner and routed schema generation plus system upsert/delete/list/insert projection paths through `SystemTable::table_name()` instead of call-site strings. | Strengthened `system_table_schemas_are_valid_and_cover_control_plane_contract` to assert the explicit reserved table-name contract, `SystemTable::ALL` coverage, and schema count; updated system projection tests to query reserved tables through `SystemTable` rather than rebuilding table strings. Existing idempotence and seed/projection tests were rerun. | `rg -n 'TableName::new\("|upsert_system_document_async\([^\n]*"|delete_system_document_if_exists_async\([^\n]*"|delete_stale_scheduler_documents_async\([^\n]*"' crates/nimbus-system/src/records.rs crates/nimbus-system/src/schema.rs crates/nimbus-system/src/tests.rs`: no matches for raw system-table sinks; `cargo test -p nimbus-system system_table_schemas_are_valid_and_cover_control_plane_contract -- --nocapture`: 1 passed, 7 filtered, finished in 0.00s after a 1m50s cold rebuild; `cargo test -p nimbus-system ensure_system_tenant_creates_reserved_tenant_and_schemas_idempotently -- --nocapture`: 1 passed, 7 filtered, finished in 0.08s; `cargo test -p nimbus-system prepare_system_tenant_seeds_network_and_adapter_posture_documents -- --nocapture`: 1 passed, 7 filtered, finished in 0.28s; initial `cargo check -p nimbus-system` failed on test-only `SystemTable::ALL` dead code and is not proof; after making `ALL` test-only, final `cargo check -p nimbus-system`: pass, finished in 1.21s; `cargo clippy -p nimbus-system --all-targets -- -D warnings`: pass, finished in 12.25s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB, 249 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-3` | low | Security | CONFIRMED/FIXED | Re-read the subscription projection functions, WebSocket subscription status callers, forwarding delivery/error paths, keys, and subscription projection tests. Confirmed the asymmetry still existed: delivery skipped `_nimbus`, but direct state and error writes could still create/update `_nimbus.subscriptions` for the system tenant. Hoisted the skip policy into the system projection layer: `record_subscription_state_async` and `record_subscription_error_async` now return `Ok(())` for the system tenant, and delivery delegates to the state path so the three record paths cannot drift separately. | `subscription_projection_skips_system_tenant_state_delivery_and_error` prepares `_nimbus`, calls state, delivery, and error projection for the system tenant, and asserts `_nimbus.subscriptions` remains empty. Existing `record_subscription_state_projects_live_subscription_document` rerun to keep the user-tenant happy path pinned. | `cargo test -p nimbus-system subscription_projection_skips_system_tenant_state_delivery_and_error -- --nocapture`: 1 passed, 8 filtered, finished in 0.21s after a 3.38s compile; `cargo test -p nimbus-system record_subscription_state_projects_live_subscription_document -- --nocapture`: 1 passed, 8 filtered, finished in 0.08s; `cargo check -p nimbus-system`: pass, finished in 1.58s; `cargo clippy -p nimbus-system --all-targets -- -D warnings`: pass, finished in 1.33s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB, 249 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-4` | low | Security | CONFIRMED/FIXED | Re-read `keys.rs`, inventory document-id call sites, system projection call sites/tests, and `DocumentId` validation. Confirmed `stable_key_segment` folded distinct separator/case/non-ASCII inputs into shared slug segments before building `_nimbus` document IDs. Replaced separator-folding with byte-level `~hh` escaping: ASCII alphanumerics remain readable and every other UTF-8 byte is encoded, preserving case and making projected ID segments collision-free for the helper's inputs while still avoiding slash/NUL in `DocumentId`s. | `stable_key_segment_is_injective_for_separator_and_case_variants` covers `foo.bar`, `foo bar`, `foo-bar`, `foo/bar`, case variants, escape-like input, Unicode, and empty input; `system_document_ids_do_not_collide_for_distinct_projected_names` proves service/function document IDs stay distinct and parse as `DocumentId`s; existing deployment projection test rerun with colon-bearing function names. | Initial `cargo test -p nimbus-system stable_key_segment system_document_ids_do_not_collide -- --nocapture` was rejected by Cargo because only one test-name filter is accepted and is not proof; `cargo test -p nimbus-system keys::tests -- --nocapture`: 2 passed, 9 filtered, finished in 0.00s after a 2.64s compile; `cargo test -p nimbus-system record_deployment_state_projects_neutral_bundle_and_functions -- --nocapture`: 1 passed, 10 filtered, finished in 0.11s; `cargo check -p nimbus-system`: pass, finished in 1.37s; `cargo clippy -p nimbus-system --all-targets -- -D warnings`: pass, finished in 1.22s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB, 249 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-5` | low | Security | CONFIRMED/FIXED | Re-read `direct_process.rs`, the `nimbus-node` public re-exports, direct-process tests, reconciler direct-process caller, and repo references. Confirmed `DirectProcessStatusSnapshot` was only declared and re-exported and had no constructor or production/test construction site. Deleted the dead public type and its `lib.rs` re-export; the existing backend still exposes the actually implemented `inspect`, `logs`, and `evidence` paths. | No new behavior test was needed for deleting a never-constructed public type. Existing direct-process lifecycle/evidence/fail-closed tests and the reconciler direct-process test were rerun to prove the implemented status/evidence/log paths still compile and behave. | `rg -n "DirectProcessStatusSnapshot" crates packages demos`: no matches; `cargo test -p nimbus-node direct_process -- --nocapture`: 5 passed, 26 filtered, live zbus integration binary 0 matching, finished in 0.00s after a 41.75s compile; `cargo check -p nimbus-node`: pass, finished in 24.08s; `cargo clippy -p nimbus-node --all-targets -- -D warnings`: pass, finished in 7.62s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 14 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB, 245 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-6` | low | Security | CONFIRMED/FIXED | Re-read `DirectProcessEvidence`, `HostLifecyclePlan`, node re-exports, direct-process tests, and repo references. Confirmed `HostPlatformDependencies` was only constructed through `HostPlatformDependencies::none()` and tests merely asserted every advertised dependency flag was false. Deleted the always-none dependency field, type, accessors, and public re-export until the direct-process backend has real platform dependency evidence to report. | No new behavior test was needed for deleting an always-false evidence stub. Existing direct-process lifecycle/evidence/fail-closed tests and the reconciler direct-process test were rerun; the deterministic evidence test still asserts process id, workload id, unit name, executable, args, and logs. | `rg -n "HostPlatformDependencies|requires_pid1|requires_dbus|requires_podman|requires_conmon|requires_kvm" crates packages demos`: no matches; `cargo test -p nimbus-node direct_process -- --nocapture`: 5 passed, 26 filtered, live zbus integration binary 0 matching, finished in 0.00s after a 4.41s compile; `cargo check -p nimbus-node`: pass, finished in 1.87s; `cargo clippy -p nimbus-node --all-targets -- -D warnings`: pass, finished in 1.62s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 14 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB, 245 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-7` | low | Security | CONFIRMED/FIXED | Re-read the three cleanup helpers, system schemas/indexes, engine query API, and projection tests. Confirmed `delete_service_port_documents_async`, `delete_stale_deployment_documents_async`, and `delete_stale_scheduler_documents_async` still listed whole system tables and filtered in memory. Added a local filtered-query helper that routes through `Engine::query_documents_async`, moved service-port deletion to the existing `ports.by_serviceId` index, moved bundle cleanup to `bundles.by_status` plus function cleanup through `functions.by_bundleId`, and added `by_tenantId_and_status` indexes for scheduled/cron cleanup. | `system_table_schemas_are_valid_and_cover_control_plane_contract` now asserts the scheduled/cron `by_tenantId_and_status` indexes; `record_service_handle_removes_only_stale_ports_for_that_service` proves stale ports are deleted only for the refreshed service; `record_deployment_state_projects_neutral_bundle_and_functions` rerun to prove stale bundle/function cleanup; `sync_scheduler_state_deletes_only_matching_tenant_pending_projection` proves tenant/status cleanup deletes only the synced tenant's stale pending row. | `rg -n "delete_service_port_documents_async|delete_stale_deployment_documents_async|delete_stale_scheduler_documents_async|query_system_documents_by_eq_async|list_documents_async" crates/nimbus-system/src/records.rs`: the three named helpers call `query_system_documents_by_eq_async`, with no `list_documents_async` in those helper bodies; `cargo test -p nimbus-system system_table_schemas_are_valid_and_cover_control_plane_contract -- --nocapture`: 1 passed, 12 filtered, finished in 0.00s after a 2.57s compile; `cargo test -p nimbus-system record_service_handle_removes_only_stale_ports_for_that_service -- --nocapture`: 1 passed, 12 filtered, finished in 0.14s; `cargo test -p nimbus-system record_deployment_state_projects_neutral_bundle_and_functions -- --nocapture`: 1 passed, 12 filtered, finished in 0.10s; `cargo test -p nimbus-system sync_scheduler_state_deletes_only_matching_tenant_pending_projection -- --nocapture`: 1 passed, 12 filtered, finished in 0.13s; `cargo check -p nimbus-system`: pass, finished in 2.62s; `cargo clippy -p nimbus-system --all-targets -- -D warnings`: pass, finished in 2.34s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 14 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB, 245 GiB free, so no cleanup triggered. | Final batch commit |
| `F2-8` | low | Security | CONFIRMED/FIXED | Re-read `existing_system_started_at_async`, `record_system_status_async`, the system-tenant preparation tests, and the status projection caller. Confirmed the `unwrap_or(unix_time_millis()?)` fallback was eagerly evaluated even when an existing `_nimbus.system_status` document already had a valid `startedAt`. Replaced the eager fallback with a small `started_at_or_else` helper that calls the clock only when `startedAt` is missing or invalid. | `started_at_or_else_uses_persisted_value_without_calling_fallback` proves a persisted value returns without evaluating the fallback; `started_at_or_else_calls_fallback_when_started_at_is_missing_or_invalid` covers missing and invalid fields; `prepare_system_tenant_seeds_network_and_adapter_posture_documents` rerun through the real system-status projection path. | `cargo test -p nimbus-system started_at_or_else -- --nocapture`: 2 passed, 13 filtered, finished in 0.00s after a 3.01s compile; `cargo test -p nimbus-system prepare_system_tenant_seeds_network_and_adapter_posture_documents -- --nocapture`: 1 passed, 14 filtered, finished in 0.27s; `cargo check -p nimbus-system`: pass, finished in 1.45s; `cargo clippy -p nimbus-system --all-targets -- -D warnings`: pass, finished in 1.25s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 14 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices while still reporting the aggregate, and 245 GiB free, so no cleanup triggered. | Final batch commit |
| `G-1` | low | Trust | CONFIRMED/FIXED | Re-read `crates/nimbus-core/src/typed_scalar.rs`, the public re-export in `nimbus-core/src/lib.rs`, the live numeric-transform guards in `nimbus-engine/src/engine/execution_units/batch.rs`, MongoDB/Firebase numeric decoders, and engine execution-unit tests. Confirmed `NumericValue::Double { value: f64 }` remained publicly constructible with non-finite values while the dead `NumericValue::projected_json` and `NumericValue::into_stored_value` methods would panic on such values. Deleted those unused public methods so the core crate no longer advertises a panicable conversion API; live storage conversion continues through the engine's finite/comparable numeric types. | Added `atomic_write_batch_rejects_non_finite_double_operands_before_storage`, which exercises both increment and maximum live transform paths with non-finite `NumericValue::Double` operands and asserts `InvalidInput` plus no document creation. Existing `typed_scalar` tests rerun to prove the supported typed projection surface remains intact. | `rg -n 'impl NumericValue|pub fn projected_json\\(&self\\)|pub fn into_stored_value\\(self\\)' crates/nimbus-core/src/typed_scalar.rs`: no `impl NumericValue` or `NumericValue` conversion methods remain, only `TypedScalarValue`/`StoredValue` projection methods; `cargo test -p nimbus-core typed_scalar -- --nocapture`: 15 passed, 115 filtered, finished in 0.00s after an 8.96s compile; `cargo test -p nimbus-engine atomic_write_batch_rejects_non_finite_double_operands_before_storage -- --nocapture`: 1 passed, 302 filtered, finished in 0.01s after a 2m15s compile; `cargo check -p nimbus-core -p nimbus-engine`: pass, finished in 42.16s; `cargo clippy -p nimbus-core -p nimbus-engine --all-targets -- -D warnings`: pass, finished in 1m19s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 8.7 GiB, Cargo git checkouts 12 GiB, 250 GiB free, so no cleanup triggered. | Final batch commit |
| `G-2` | low | Trust | CONFIRMED/FIXED | Re-read `crates/nimbus-artifacts/src/admission.rs`, `ArtifactVerificationPolicy`/`ArtifactVerificationEvidence` builders in `crates/nimbus-artifacts/src/lib.rs`, and artifact admission tests. Confirmed the existing provenance check was correct but duplicated the builder/source predicate across the empty-predicate and per-predicate branches. Refactored `ensure_artifact_policy_evidence` to filter attestations by builder/source once, fail if none match, and then require each demanded predicate within that filtered set. | Added `runtime_bundle_artifact_admission_rejects_predicate_from_wrong_attestation_scope`, which supplies one matching builder/source attestation with the wrong predicate, one wrong-builder attestation with the right predicate, and one wrong-source attestation with the right predicate; admission must still reject with the predicate-specific error. Existing runtime/guest admission accept/reject tests rerun. | `cargo test -p nimbus-artifacts artifact_admission -- --nocapture`: 5 passed, 2 filtered, finished in 0.00s after a 16.18s compile; `cargo check -p nimbus-artifacts`: pass, finished in 11.65s; `cargo clippy -p nimbus-artifacts --all-targets -- -D warnings`: pass, finished in 3.10s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 9.3 GiB, Cargo git checkouts 12 GiB, 249 GiB free, so no cleanup triggered. | Final batch commit |
| `G-3` | low | Trust | CONFIRMED/FIXED | Re-read `Timestamp::now`, `Document::new`/`Document::with_id`, core document tests, and timestamp/document creation call sites. Confirmed `Timestamp::now()` still used `duration_since(UNIX_EPOCH).expect(...)`, so a pre-epoch system clock could panic along document construction and dependency-test document helpers. Replaced the panic with a testable `Timestamp::from_system_time` helper that saturates pre-epoch instants to `Timestamp(0)` and preserves post-epoch milliseconds. | Added `timestamp_from_system_time_saturates_pre_epoch_to_zero` for the error edge and `timestamp_from_system_time_preserves_epoch_millis` for the normal path; reran existing document constructor/projection tests to keep the `Document::new`/`Document::with_id` path covered. | `cargo test -p nimbus-core timestamp_from_system_time -- --nocapture`: 2 passed, 130 filtered, finished in 0.00s after a 1.78s compile; `cargo test -p nimbus-core document::tests -- --nocapture`: 2 passed, 130 filtered, finished in 0.00s; `cargo check -p nimbus-core`: pass, finished in 2.81s; `cargo clippy -p nimbus-core --all-targets -- -D warnings`: pass, finished in 2.97s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 9.5 GiB, Cargo git checkouts 12 GiB, `/private/tmp` 9.5 GiB with three permission-denied mount notices while still reporting the aggregate, and 249 GiB free, so no cleanup triggered. | Final batch commit |
| `G-4` | low | Trust | CONFIRMED/FIXED | Re-read `redact_artifact_verifier_output`, artifact verifier error redaction, cosign/SLSA/SBOM/command-backend callers, and existing redaction tests. Confirmed the redactor only replaced lines containing sensitive keywords and would leak unlabeled secret-looking values such as bare JWTs, registry-auth blobs, long hex tokens, or AWS access key ids. Kept the shared line-redaction contract but documented its best-effort boundary and added value-shape detection for JWTs, AWS access key ids, long hex strings, and long base64/base64url-like blobs. | Added `redaction_covers_unlabeled_secret_value_shapes`, which feeds bare JWT, long base64 auth blob, long hex token, and AWS access-key shaped output without sensitive labels and asserts each line is redacted while ordinary diagnostics survive; existing keyword redaction test rerun. | `cargo test -p nimbus-artifacts redaction -- --nocapture`: 2 passed, 6 filtered, finished in 0.00s after a 1.81s compile; `cargo check -p nimbus-artifacts`: pass, finished in 1.11s; `cargo clippy -p nimbus-artifacts --all-targets -- -D warnings`: pass, finished in 1.81s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 9.7 GiB, Cargo git checkouts 12 GiB, and 249 GiB free, so no cleanup triggered. | Final batch commit |
| `H1-2` | low | Sandbox | CONFIRMED/FIXED | Re-read the review entry, `parse_ipv4_subnet_and_gateway`, its three callers (`bridge_gateway_addr`, netavark network construction, and IPAM allocation), `ContainerSandboxBackendConfig.network_subnet`, and existing OCI network tests. Confirmed the shared parser still derived the gateway with `octets[3] + 1`, so `10.0.0.255/24` could overflow in debug or wrap in release, and `/31`/`/32` bridge subnets were not rejected before allocation. Replaced the octet arithmetic with a shared integer CIDR parser that requires the configured address to be the network address, rejects prefixes without gateway plus container host space, canonicalizes the subnet string, and derives gateway/broadcast through checked `u32` math; IPAM now reuses the same parsed subnet instead of reparsing independently. | Added parser tests for the `.255` broadcast-base overflow case, `/31` no-room case, and `/30` smallest valid bridge subnet; added `allocate_container_ips_uses_only_container_slot_in_smallest_bridge_subnet` to prove `/30` allocates only the non-gateway host and then exhausts. Existing default allocation/deallocation tests rerun through the real IPAM path. | Initial `cargo test -p nimbus-sandbox bridge_subnet_parser -- --nocapture` caught a refactor miss (`prefix` reference left in IPAM) and was not counted as proof; after fixing it, `cargo test -p nimbus-sandbox bridge_subnet_parser -- --nocapture`: 3 passed, 174 filtered, integration binaries 0 matching, finished in 0.00s after a 4.88s compile; `cargo test -p nimbus-sandbox allocate_container_ips -- --nocapture`: 3 passed, 174 filtered, integration binaries 0 matching, finished in 0.00s; `cargo check -p nimbus-sandbox`: pass, finished in 5.56s; `cargo clippy -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 5.56s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, and 248 GiB free, so no cleanup triggered. | Final batch commit |
| `H1-5` | low | Sandbox | CONFIRMED/FIXED | Re-read the review entry, `oci/buildah/render.rs`, `oci/buildah/cli.rs`, `oci/network.rs`, `conmon/lifecycle.rs`, `oci/command.rs`, and the network/buildah/conmon command call sites. Confirmed the duplicated render helpers still existed, with two stderr-only copies plus the Buildah two-stream superset; the review's older container-runtime/krun anchors had drifted, but the duplicated command-diagnostics pattern remained. Moved the two-stream renderer into `backends::oci::command`, switched Buildah, Conmon, and Netavark callers to it, deleted local copies, and preserved Netavark's structured-stdout special case before falling back to the shared helper. | Added `render_command_failure_prefers_stderr` and `render_command_failure_falls_back_to_stdout_then_empty_message` in the shared command module; reran the existing Netavark structured-error test to prove that caller still prefers structured stdout. | `rg -n "fn render_command_failure|render_command_failure\\(" crates/nimbus-sandbox/src -g '*.rs'`: exactly one function definition remains in `backends/oci/command.rs`, with Conmon, Buildah, and Network callers using it; `cargo test -p nimbus-sandbox render_command_failure -- --nocapture`: 2 passed, 177 filtered, integration binaries 0 matching, finished in 0.00s after a 2.35s compile; `cargo test -p nimbus-sandbox netavark_failure_prefers_structured_stdout_error -- --nocapture`: 1 passed, 178 filtered, integration binaries 0 matching, finished in 0.00s; `cargo check -p nimbus-sandbox`: pass, finished in 0.96s; `cargo clippy -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 1.70s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, and 248 GiB free, so no cleanup triggered. | Final batch commit |
| `H1-6` | low | Sandbox | CONFIRMED/FIXED | Re-read the IPv6 classifier in `egress.rs`, resolved-IP SSRF checks, egress proxy resolver caller, and existing reserved/mapped address tests. Confirmed `is_non_global_or_internal_ipv6` only delegated IPv4-mapped addresses to the IPv4 classifier; NAT64 `64:ff9b::/96` and deprecated IPv4-compatible `::a.b.c.d` answers could carry link-local/private/reserved IPv4 targets while passing the ordinary IPv6 checks. Added `embedded_ipv4_for_ipv6` so IPv4-mapped, NAT64, and IPv4-compatible forms all flow through the existing IPv4 internal/reserved classifier. | Extended `sandbox_egress_policy_treats_reserved_and_mapped_addresses_as_internal` with `64:ff9b::169.254.169.254` and `::169.254.169.254` denial cases plus a global NAT64 allow control; reran the egress-proxy DNS-resolved-internal test to keep the resolver-answer caller path covered. | `cargo test -p nimbus-sandbox sandbox_egress_policy_treats_reserved_and_mapped_addresses_as_internal -- --nocapture`: 1 passed, 178 filtered, integration binaries 0 matching, finished in 0.00s after a 2.09s compile; `cargo test -p nimbus-sandbox egress_proxy_denies_dns_resolved_internal_targets -- --nocapture`: 1 passed, 178 filtered, integration binaries 0 matching, finished in 0.12s; `cargo check -p nimbus-sandbox`: pass, finished in 0.85s; `cargo clippy -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 1.57s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, and 248 GiB free, so no cleanup triggered. | Final batch commit |
| `H1-8` | low | Sandbox | CONFIRMED/FIXED | Re-read the review entry, OCI materializer unpack loop, `sanitize_archive_path`, whiteout handling, `remove_path_if_exists`, and existing happy-path materializer tests. Confirmed the adversarial coverage was missing; while adding it, the opaque-whiteout case exposed a real bug: `clear_directory_contents` used `path.is_dir()`, which follows a symlinked parent and could clear the target outside the rootfs. Replaced that check with `symlink_metadata`, removing a symlink parent itself and only iterating real directories. | Added `materializer_rejects_parent_directory_layer_paths` with a raw malformed tar fixture proving `../` entries are rejected and do not write outside; added Unix-gated `materializer_symlink_write_through_layer_does_not_write_outside_rootfs`; added Unix-gated `materializer_opaque_whiteout_removes_symlink_parent_without_following_it`, which asserts the symlink is removed and the outside directory sentinel survives. Reran the existing registry materialization happy path. | Initial `cargo test -p nimbus-sandbox materializer_rejects_parent_directory_layer_paths -- --nocapture` failed while building the test fixture because `tar::Builder::append_data` rejects `..` paths; replaced only the fixture with a raw tar writer and reran. `cargo test -p nimbus-sandbox materializer_rejects_parent_directory_layer_paths -- --nocapture`: 1 passed, 181 filtered, integration binaries 0 matching, finished in 0.00s after a 2.45s compile; `cargo test -p nimbus-sandbox materializer_symlink_write_through_layer_does_not_write_outside_rootfs -- --nocapture`: 1 passed, 181 filtered, integration binaries 0 matching, finished in 0.00s; `cargo test -p nimbus-sandbox materializer_opaque_whiteout_removes_symlink_parent_without_following_it -- --nocapture`: 1 passed, 181 filtered, integration binaries 0 matching, finished in 0.00s; `cargo test -p nimbus-sandbox materializer_pulls_and_extracts_image_rootfs_from_registry -- --nocapture`: 1 passed, 181 filtered, integration binaries 0 matching, finished in 0.21s; `cargo check -p nimbus-sandbox`: pass, finished in 0.99s; `cargo clippy -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 1.65s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 10 GiB, Cargo git checkouts 12 GiB, and 248 GiB free, so no cleanup triggered. | Final batch commit |
| `H2-3` | low | Sandbox | CONFIRMED/FIXED | Re-read the review entry, `manager/definitions.rs`, `manager/sandboxes.rs`, `manager/sessions.rs`, manager module wiring, and references/tests for resource versions. Confirmed the three `now_millis` helpers were still byte-identical and the three version helpers still shared the same saturating increment plus prefix-only formatting pattern. Extracted `manager/clock.rs` with shared `now_millis`, testable `millis_since_epoch`, and `next_version(next, prefix)`, then routed definitions, sandbox resources, and sessions through it. | Added `millis_since_epoch_saturates_pre_epoch_to_zero` and `next_resource_version_starts_at_one_and_saturates` to pin the shared helper behavior, including the `u64::MAX` saturation edge. | `cargo test -p nimbus-services clock -- --nocapture`: 2 passed, 37 filtered, finished in 0.00s after a 47.00s compile; `cargo check -p nimbus-services`: pass, finished in 24.45s; `cargo clippy -p nimbus-services --all-targets -- -D warnings`: pass, finished in 8.28s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, and 243 GiB free, so no cleanup triggered. | Final batch commit |
| `H2-4` | low | Sandbox | CONFIRMED/FIXED | Re-read the review entry, `manager.rs` module declarations, `manager/tests/definitions.rs`, and references to the test module/internal `TenantServiceKey`. Confirmed the same leaf name still existed for production `manager/definitions.rs` and test `manager/tests/definitions.rs`. Renamed only the test submodule/file to `definition_lifecycle`, leaving the production module stable; also imported `TenantServiceKey` once in the test module instead of spelling the long `super::super::types` path at each use. | No behavior change; reran the full renamed `definition_lifecycle` test module to prove all moved tests still run under the new path. | `rg -n "mod definitions|tests/definitions|tests::definitions|definition_lifecycle|TenantServiceKey" crates/nimbus-services/src/manager.rs crates/nimbus-services/src/manager/tests -g '*.rs'`: only production `mod definitions;` remains, test module is `mod definition_lifecycle;`, and no stale `tests::definitions` path remains; `cargo test -p nimbus-services definition_lifecycle -- --nocapture`: 6 passed, 33 filtered, finished in 0.22s after a 4.06s compile; `cargo check -p nimbus-services`: pass, finished in 1.35s; `cargo clippy -p nimbus-services --all-targets -- -D warnings`: pass, finished in 1.32s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, and 242 GiB free, so no cleanup triggered. | Final batch commit |
| `H2-5` | low | Sandbox | CONFIRMED/FIXED | Re-read the review entry, `wait_for_ready_handle_async`, `service_binding_from_handle`, registry binding tests, and activation wait tests. Confirmed the readiness condition still used `handle.status == Ready || service_binding_from_handle(&handle).is_some()`, while `service_binding_from_handle` itself returns `None` unless the handle status is `Ready`; the right-hand side was unreachable as a distinct readiness condition. Removed the redundant branch and the now-unused import so readiness is keyed directly on `SandboxStatus::Ready`. | No new behavior test was needed for deleting an unreachable condition. Reran the existing registry test that proves non-ready handles with endpoints stay hidden, plus the activation wait cancellation test that exercises the non-ready wait loop. | `cargo test -p nimbus-services snapshot_skips_sandboxes_without_ready_endpoints -- --nocapture`: 1 passed, 38 filtered, finished in 0.00s after a 2.14s compile; `cargo test -p nimbus-services ensure_service_binding_async_can_be_cancelled_while_waiting_for_readiness -- --nocapture`: 1 passed, 38 filtered, finished in 0.20s; `cargo check -p nimbus-services`: pass, finished in 1.37s; `cargo clippy -p nimbus-services --all-targets -- -D warnings`: pass, finished in 1.26s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 16 GiB, Cargo git checkouts 12 GiB, and 242 GiB free, so no cleanup triggered. | Final batch commit |
| `H2-6` | low | Sandbox | CONFIRMED/FIXED | Re-read the H2-6 entry, `ServiceManager::get_session`, `close_session`, `refresh_session_expiration`, the HTTP session get/close handlers, route authorization helpers, and session route tests. Confirmed manager lookup/close keyed only on `session_id`, while tenant isolation lived in caller-side HTTP checks. Changed manager get/close/refresh to require a `TenantId` and return `None` before exposing or mutating wrong-tenant sessions. Updated native HTTP get/close to scope manager calls by the authorized tenant; operator get/close now require explicit `tenantId` query context instead of using an unscoped manager lookup. Removed the caller-side `ensure_session_lookup_tenant_matches` guard because the manager now owns the invariant. | Added `session_lookup_and_close_are_tenant_scoped` to assert wrong-tenant lookup returns `None`, wrong-tenant close does not mutate the owning tenant's open session, and owning tenant close records the requested reason. Extended `session_routes_open_service_sessions_with_target_snapshot_and_audit` to assert operator get/close without `tenantId` fails `400`, wrong-tenant operator get/close returns `404`, and correct-tenant operator get succeeds. | `cargo test -p nimbus-services session_lookup_and_close_are_tenant_scoped -- --nocapture`: first run caught signature/import misses; after fix, 1 passed, 39 filtered, finished in 0.00s after a 2.58s compile; `cargo test -p nimbus-server session_routes_open_service_sessions_with_target_snapshot_and_audit -- --nocapture`: 1 passed, 435 filtered, plus 0 matching tests in `dynamodb_spec` (27 filtered), `mongodb_spec` (23 filtered), and `reactive_loop` (32 filtered), finished in 0.13s after a 1m39s compile; `cargo check -p nimbus-services -p nimbus-server`: pass, finished in 32.15s; `cargo clippy -p nimbus-services -p nimbus-server --all-targets -- -D warnings`: pass, finished in 29.40s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check: main `target/` reached 27 GiB with 231 GiB free; no active Cargo/rustc/clippy process was visible, so `cargo clean` in this checkout removed 55,353 files / 31.4 GiB. Post-clean: 258 GiB free, main `target/` removed, Cargo git checkouts 12 GiB. | Final batch commit |
| `H2-7` | low | Sandbox | CONFIRMED/FIXED | Re-read the H2-7 entry, `delete_service_definition_async`, the lifecycle-slot helper, update/delete generation checks, and the definition lifecycle tests. Confirmed the force-delete path checked `expected_generation`, waited for/claimed the lifecycle slot, refreshed/stopped the backend, recorded a stopped handle, and only then repeated the generation check; a generation change before the slot claim could therefore leave the service stopped while the definition remained. Added a post-claim precondition check for dynamic source and expected generation before live-session checks, backend refresh, or backend stop, and release the lifecycle slot on that new early failure. Left the final post-stop state check intact as a defensive guard. | Added `force_delete_revalidates_generation_before_stopping_backend`, which forces delete to wait on the lifecycle slot, advances the definition to generation 2 before delete claims the slot, installs a ready backend handle, and asserts the delete returns `PreconditionFailed`, performs zero backend stops, records no stopped handle evidence, and leaves the generation-2 definition plus ready cached handle intact. Reran the neighboring force-delete serialization test to preserve the existing wait/stop/delete behavior. | `cargo test -p nimbus-services force_delete_revalidates_generation_before_stopping_backend -- --nocapture`: 1 passed, 40 filtered, finished in 0.00s after a 1m21s compile; `cargo test -p nimbus-services delete_service_definition_serializes_with_in_flight_activation -- --nocapture`: 1 passed, 40 filtered, finished in 0.00s after a 1.00s warm build; `cargo check -p nimbus-services`: pass, finished in 47.84s; `cargo clippy -p nimbus-services --all-targets -- -D warnings`: pass, finished in 8.02s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 5.7 GiB, Cargo git checkouts 12 GiB, and 253 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-10` | low | CLI | CONFIRMED/FIXED | Re-read the I1-10 entry, `parse_compose_duration`, `duration_unit_nanos`, compose lifecycle tests, and duration callsites. Confirmed accepted duration units lived in one inline list while nanos conversion lived in a separate `match` with `unreachable!()` for drift. Replaced the split list/mapping with one `DURATION_UNITS` table used directly by the parser and removed `duration_unit_nanos`, eliminating the panic path. | Added `compose_duration_parser_accepts_every_supported_unit` for `ns`, `us`, both microsecond spellings, `ms`, `s`, `m`, `h`, and `1m30s`; added `compose_duration_parser_rejects_unknown_units_without_panicking` to assert an unsupported `d` unit returns the supported-units validation error. Reran the existing invalid stop-grace-period compose test. | `cargo test -p nimbus-bin compose_duration_parser -- --nocapture`: 2 passed, 580 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.00s after a 2m33s compile; `cargo test -p nimbus-bin compose_project_rejects_invalid_stop_grace_period -- --nocapture`: 1 passed, 581 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.00s after a 1.45s warm build; `cargo check -p nimbus-bin`: initial pass finished in 1m09s; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: first run caught `clippy::into_iter_on_ref`; changed the table iteration to `.iter()` and reran successfully in 7.37s; `cargo check -p nimbus-bin`: rerun after the clippy fix passed in 1.93s; `cargo fmt --all --check`: pass after applying rustfmt's one-line wrapping; `git diff --check`: pass. Disk threshold check after verification: main `target/` 18 GiB, Cargo git checkouts 12 GiB, and 241 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-3` | low | CLI | CONFIRMED/FIXED | Re-read the I1-3 entry, `compose/mod.rs`, `compose/execution.rs`, the compose forwarded-machine tests, the Linux krun smoke test, and all named loader callers. Confirmed the six compose loader wrappers were reachable only through each other or tests, and the execution selection wrapper existed only under a test helper. Deleted the dead wrappers, routed the test-only platform helper directly to the live admission loader, made `ResolvedComposeSelection::explicit` test-only, added an explicit test-support `SandboxBackend` import instead of inheriting it through deleted module imports, and rewired the ignored krun smoke test to exercise the production isolation-mode compose loader. | No new runtime behavior test was needed for removing inert wrappers; strengthened existing coverage by making the krun smoke test call the live compose loader path and reran the forwarded-machine host-loader test that exercises the remaining test helper. | `rg -n "load_service_definition_catalog\(|load_service_definition_catalog_for_selection|load_service_manager_for_selection|load_host_backed_service_manager_for_selection\(|load_host_backed_service_manager_for_selection_with_admission|load_host_backed_service_manager_for_platform_selection\(" crates/nimbus-bin/src/compose crates/nimbus-bin/src/start/tests/krun.rs`: no matches; initial focused test caught missing explicit test imports and non-test dead code, and is not proof; after the fix, `cargo test -p nimbus-bin host_loader_accepts_container_projects_with_ready_forwarded_machine_api_on_macos -- --nocapture`: 1 passed, 581 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.08s after an 8.06s compile; `cargo test -p nimbus-bin convex_runtime_query_starts_real_krun_service_from_compose_file_and_tears_it_down -- --ignored --nocapture`: 0 tests on this non-Linux host, 582 filtered, finished in 0.00s after a 0.61s warm build; `cargo check -p nimbus-bin`: pass, finished in 2.73s; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 4.23s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, and 239 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-4` | low | CLI | CONFIRMED/FIXED | Re-read the I1-4 entry, `ComposeProjectPlan::into_service_catalog`, the `ComposeServiceCatalog` `ServiceDefinitionCatalog` impl, `to_service_backend` / `to_sandbox_spec`, lifecycle callers, the service catalog trait, and compose catalog tests. Confirmed request-time catalog lookups still re-lowered services and used `.expect()` despite validation having already proved the same tenant-independent lowering. Added a precomputed `service_backends` template map to `ComposeServiceCatalog`; `into_service_catalog` now performs all fallible lowering once, and per-tenant lookup/list methods clone cached templates and stamp the requested tenant id into sandbox specs without panicking. | Added `compose_service_catalog_stamps_cached_backends_for_each_tenant`, covering all-backend lookup for two tenants and asserting both service names and tenant-stamped sandbox specs. Reran `compose_project_lowers_into_service_definition_catalog` to preserve the existing named lookup, build source, resource, lifecycle, and cross-tenant behavior. | `rg -n "validated compose services should keep lowering|to_service_backend\([^\n]*\)\.expect|service_backend_for_tenant\(|service_backends_for_tenant\(" crates/nimbus-bin/src/compose/file/lower.rs`: only trait method definitions remained, no `.expect()` lowering path; `cargo test -p nimbus-bin compose_service_catalog_stamps_cached_backends_for_each_tenant -- --nocapture`: 1 passed, 582 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.01s after an 11.27s compile; `cargo test -p nimbus-bin compose_project_lowers_into_service_definition_catalog -- --nocapture`: 1 passed, 582 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.00s after a 1.27s warm build; `cargo check -p nimbus-bin`: pass, finished in 3.23s; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 4.40s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 20 GiB, Cargo git checkouts 12 GiB, and 239 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-6` | low | CLI | CONFIRMED/FIXED | Re-read the I1-6 entry, `rotate.rs` SQLite/redb/libsql DEK rotation paths, redb page re-encryption, storage key-provider/runtime contracts, and existing zeroization types. The `current_dek` half was already improved in-tree because `unwrap_database_manifest_key` now returns `DataEncryptionKey`, which zeroizes on drop; confirmed the remaining gap that newly generated rotation DEKs were still bare arrays and redb decrypted page plaintext was a plain `Vec<u8>`. Added a direct `zeroize` dependency to `nimbus-bin`, introduced `generate_rotation_dek() -> Zeroizing<[u8; 32]>`, routed SQLite/redb/libsql fresh DEKs through it, wrapped each redb decrypted page in `Zeroizing<Vec<u8>>`, and explicitly zeroized the reusable redb page buffer before return. | Added `generate_rotation_dek_returns_zeroizing_filled_key`, asserting the rotation helper returns the intended zeroizing key wrapper and fills it from OS entropy. Reran `rotate_redb_dek_publishes_reencrypted_database_with_matching_manifest` to exercise page re-encryption, staged publication, manifest unwrap, and reopened-store behavior with zeroizing buffers. | `rg -n "let mut new_dek = \[0u8; 32\]|let plaintext = old_cipher|Zeroizing|zeroize" crates/nimbus-bin/src/encryption/rotate.rs crates/nimbus-bin/Cargo.toml`: no bare new-DEK allocation or unwrapped plaintext decrypt path remains; `cargo test -p nimbus-bin generate_rotation_dek_returns_zeroizing_filled_key -- --nocapture`: 1 passed, 583 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.00s after a 25.67s compile; `cargo test -p nimbus-bin rotate_redb_dek_publishes_reencrypted_database_with_matching_manifest -- --nocapture`: 1 passed, 583 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), re-encrypted 900 pages, finished in 4.32s after a 1.41s warm build; `cargo check -p nimbus-bin`: pass, finished in 6.23s; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 8.93s; `make deny`: pass (`advisories ok`, `bans ok`, `licenses ok`, `sources ok`); `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, and 237 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-8` | low | CLI | CONFIRMED/FIXED | Re-read the I1-8 entry, `machine/manager/stop.rs`, `machine/api/listener.rs`, and the rest of the production `unsafe` sites in `nimbus-bin` (`machine/manager/launch.rs`, `machine/manager.rs`, and the already-commented `start/boot.rs`). Confirmed the cited `kill` and inherited Unix fd conversions lacked `SAFETY` comments, and the production audit found two more unlabeled production unsafe blocks for child `pre_exec` and signal-hook registration. Added `SAFETY` comments documenting the `setsid` pre-exec constraint, signal-safe atomic callback and unregister lifecycle, systemd/test fd ownership transfer, SIGTERM/SIGKILL process signalling, and signal-0 liveness probe. The remaining unlabelled unsafe uses in `nimbus-bin` are test-only env/dup/getsid helpers. | No runtime behavior changed; reran focused tests that exercise the touched unsafe surfaces: socket activation fd validation/conversion, helper process session detachment, and machine stop cleanup/signalling. | `rg -n -C 3 "unsafe" crates/nimbus-bin/src/machine/manager/launch.rs crates/nimbus-bin/src/machine/manager.rs crates/nimbus-bin/src/machine/api/listener.rs crates/nimbus-bin/src/machine/manager/stop.rs crates/nimbus-bin/src/start/boot.rs` showed `SAFETY` comments adjacent to all production unsafe blocks; `cargo test -p nimbus-bin socket_activation_listener -- --nocapture`: 2 passed, 582 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.00s after a 6.36s compile; `cargo test -p nimbus-bin machine_command_spawn_detaches_helpers_into_new_session -- --nocapture`: 1 passed, 583 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.00s after a 0.61s warm build; `cargo test -p nimbus-bin stop_machine_uses_graceful_krunkit_stop_before_cleaning_up_helpers -- --nocapture`: 1 passed, 583 filtered, plus 0 matching tests in `server_discovery_serde` (2 filtered), finished in 0.45s after a 0.58s warm build; `cargo check -p nimbus-bin`: pass, finished in 2.39s; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 3.79s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 22 GiB, Cargo git checkouts 12 GiB, and 236 GiB free, so no cleanup triggered. | Final batch commit |
| `I1-9` | low | CLI | CONFIRMED/FIXED | Re-read `build_oci_client`, `is_loopback_registry`, local-registry tests, and the `start/network_bind.rs` loopback precedent. OCI registry plaintext downgrade now extracts the exact registry host, strips an optional port/bracketed IPv6 wrapper, and only accepts exact `localhost` or parsed loopback IP addresses. | `loopback_registry_detection_requires_exact_loopback_host` covers accepted localhost/IPv4/IPv6 loopback registries and rejects `localhost.evil.com`, `127.0.0.1.attacker.tld`, bracketed-IPv6 lookalikes, and normal remote registries. | `cargo test -p nimbus-bin loopback_registry_detection_requires_exact_loopback_host -- --nocapture`: 1 passed, 573 filtered; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-bin --all-targets -- -D warnings`: pass, finished in 27.21s; `make clippy`: pass, workspace clippy finished in 4.88s; `make check`: pass, workspace check finished in 2.11s. | Final batch commit |
| `I2-2` | low | CLI | CONFIRMED/FIXED | Re-read the I2-2 entry, `crates/nimbus-operator/src/access.rs`, session bootstrap callers, UI launch-ticket/session HTTP mapping, and operator/session tests. Confirmed session issuance still used infallible `.expect()` paths for OS RNG, token formatting, cookie serialization, and signing, so an entropy/serialization/signing failure could panic instead of returning a controlled bootstrap failure. Made session issuance fallible, added `SessionBootstrapFailure::SessionIssueFailed`, mapped HTTP UI issuance failures to internal server errors, and added a private token-generator seam for deterministic error injection. | `session_issue_failure_returns_error_without_registering_session` injects a token-generation failure, asserts a `SessionIssueFailed` bootstrap error, and proves no partial session is registered. Reran `launch_ticket_is_single_use` and UI session/launch-ticket integration tests to preserve the happy path. | Initial focused runs caught a bad Cargo filter and a syntax miss and are not proof. Final `cargo test -p nimbus-operator session_ -- --nocapture`: 4 passed, 28 filtered, finished in 0.04s after a 0.76s compile; `cargo test -p nimbus-operator launch_ticket_is_single_use -- --nocapture`: 1 passed, 31 filtered, finished in 0.01s after a 0.25s warm build; `cargo test -p nimbus-server valid_token_post_creates_session_cookie_and_cookie_auth_unlocks_ui_and_ws -- --nocapture`: 1 passed, 435 filtered, plus 0 matching tests in `dynamodb_spec`, `mongodb_spec`, and `reactive_loop`, finished in 0.16s after a 1m57s compile; `cargo test -p nimbus-server consume_ui_launch_ticket_sets_session_cookie_and_redirects_to_ui_root -- --nocapture`: 1 passed, 435 filtered, plus 0 matching integration tests, finished in 0.06s after a 1.49s warm build; `cargo check -p nimbus-operator -p nimbus-server`: pass, finished in 45.53s; after replacing `io::Error::new(ErrorKind::Other, ...)` with `io::Error::other`, `cargo clippy -p nimbus-operator -p nimbus-server --all-targets -- -D warnings`: pass, finished in 26.69s; `cargo fmt --all --check`: pass after running rustfmt; `git diff --check`: pass. Cleanup trigger fired after verification and `cargo clean` removed 54,457 files / 38.0 GiB from this checkout only. | Final batch commit |
| `I2-4` | low | CLI | CONFIRMED/FIXED | Re-read the I2-4 entry, `crates/nimbus-machine/src/lib.rs`, env/root resolution, directory-creation paths, all `MachineRootLayout::new` callers in `nimbus-bin`, later-discovered `nimbus-server` machine lifecycle tests, and machine layout tests. Confirmed `nimbus-machine` intentionally owns layout/env/fs helpers, but the public three-root `new` constructor still silently produced hard-coded `/tmp/nimbus-test-*` data/cache roots when roots did not share a parent. Deleted that constructor shape: `MachineRootLayout::new` now requires explicit config/state/data/cache/runtime roots, `from_sibling_roots` derives data/cache only after proving the three roots share a parent, and test call sites use a doc-hidden `test_sibling_roots` helper that fails on unshared roots instead of falling back to `/tmp`. | Added/strengthened `machine_root_layout_new_uses_explicit_roots`, `machine_root_layout_from_sibling_roots_derives_data_and_cache`, and `machine_root_layout_from_sibling_roots_rejects_unshared_roots`. Reran a `nimbus-bin` machine path caller test to prove cross-crate test callers no longer rely on the old constructor; during J-3 server verification, two stale `nimbus-server` machine lifecycle test call sites were also moved to `MachineRootLayout::test_sibling_roots`. | `cargo test -p nimbus-machine machine_root_layout -- --nocapture`: 6 passed, 6 filtered, finished in 0.00s after a 7.74s cold compile; `cargo test -p nimbus-bin machine_paths_use_short_runtime_root_and_typed_socket_layout -- --nocapture`: 1 passed, 583 filtered, plus 0 matching tests in `server_discovery_serde`, finished in 0.00s after a 2.33s warm build; `rg -n "/tmp/nimbus-test-(data|cache)|EmbeddedAsset|packet_enforcement_active" crates/nimbus-machine crates/nimbus-assets crates/nimbus-bin/src/sandbox_supervisor.rs`: no matches; `rg -n "MachineRootLayout::new\\(" crates/nimbus-bin/src/machine`: no matches; initial `cargo test -p nimbus-server runtime_mutation_bridge -- --nocapture` failed to compile on the stale server test call sites and is not proof; after fixing them, `cargo test -p nimbus-server runtime_mutation_bridge -- --nocapture`: 4 passed, 432 filtered, integration binaries had 0 matching tests, finished in 0.13s after a 50.90s compile; `cargo check -p nimbus-machine -p nimbus-bin -p nimbus-operator`: pass, finished in 46.76s; `cargo clippy -p nimbus-machine -p nimbus-bin -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 30.94s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `I2-5` | low | CLI | CONFIRMED/FIXED | Re-read the I2-5 entry, `authorize_session_cookie`, session revocation/rotation tests, and UI cookie-auth callers. Confirmed the validator decoded the unsigned cookie payload and consulted generation/session/expiry state before checking the HMAC, letting tampered cookies observe `Revoked` or `Expired` states instead of failing uniformly as invalid. Moved signature verification immediately after token/payload parsing and before generation, session-id, or expiry checks. | `session_cookie_signature_is_checked_before_payload_state` tampers an otherwise valid cookie payload to reference a revoked generation and asserts the result is `Invalid`, proving unsigned state no longer influences authorization. Updated the rotation test to assert old cookies are invalid after token rotation because the signature now fails before revocation state is consulted. | Same batched gate as I2-2. Final `cargo test -p nimbus-operator session_ -- --nocapture`: 4 passed, 28 filtered, including signature-before-state and issue-failure coverage; `cargo test -p nimbus-operator launch_ticket_is_single_use -- --nocapture`: 1 passed, 31 filtered; `cargo test -p nimbus-server valid_token_post_creates_session_cookie_and_cookie_auth_unlocks_ui_and_ws -- --nocapture`: 1 passed, 435 filtered, plus 0 matching integration tests; `cargo test -p nimbus-server consume_ui_launch_ticket_sets_session_cookie_and_redirects_to_ui_root -- --nocapture`: 1 passed, 435 filtered, plus 0 matching integration tests; `cargo check -p nimbus-operator -p nimbus-server`: pass; `cargo clippy -p nimbus-operator -p nimbus-server --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass after rustfmt; `git diff --check`: pass. Cleanup trigger fired after verification and `cargo clean` removed 54,457 files / 38.0 GiB from this checkout only, leaving `/` at 257 GiB free, `target/` at 40 MiB, and Cargo git checkouts at 12 GiB. | Final batch commit |
| `I2-6` | low | CLI | CONFIRMED/FIXED | Re-read `crates/nimbus-assets/src/ui.rs`, `js_packages.rs`, `lib.rs`, embedded asset tests, and crate references. Confirmed the crate exposed two public `EmbeddedAsset` structs with different byte ownership semantics. Renamed them without aliases: UI assets are `UiAsset`, and embedded JS/package assets are `PackageAsset`. | No new behavior test was needed for the public type rename; reran the full feature-backed `nimbus-assets` unit suite so UI, template, package manifest, checksum, and tooling materialization paths all compile and return bytes through the renamed wrappers. | `rg -n "EmbeddedAsset" crates/nimbus-assets`: no matches; `cargo test -p nimbus-assets --features all -- --nocapture`: 9 passed, 0 failed, doc-tests 0, finished in 3.14s after a 4.70s compile; `cargo clippy -p nimbus-assets --features all --all-targets -- -D warnings`: pass, finished in 5.07s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `I2-7` | low | CLI | CONFIRMED/FIXED | Re-read `MachineVolume::parse`, `MachineImageSource::parse`, the clap value-parser boundary in `nimbus-bin`, direct parse tests, and machine record-state tests. Confirmed the volume parser returned `String` only to satisfy clap while the sibling image-source parser returned the domain `nimbus_core::Error`. Changed `MachineVolume::parse` to return `Result<Self, Error>` and moved the `String` conversion into `parse_machine_volume` at the CLI boundary. | Existing parser tests now assert the same behavior through the domain error type: `machine_volume_parse_accepts_absolute_source_and_target`, `machine_volume_parse_rejects_missing_or_relative_paths`, and `machine_volume_requires_absolute_host_and_guest_paths`. | `cargo test -p nimbus-machine machine_volume_parse -- --nocapture`: 2 passed, 10 filtered, finished in 0.00s after a 0.41s warm build; `cargo test -p nimbus-bin machine_volume_requires_absolute_host_and_guest_paths -- --nocapture`: 1 passed, 583 filtered, plus 0 matching tests in `server_discovery_serde`, finished in 0.00s after a 2.13s warm build; `cargo check -p nimbus-machine -p nimbus-bin -p nimbus-operator`: pass, finished in 46.76s; `cargo clippy -p nimbus-machine -p nimbus-bin -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 30.94s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-1` | low | Misc | CONFIRMED/FIXED | Re-read `nimbus-bridge` atomic write capability helpers, sync fallback behavior, async insert/update/delete fallbacks, runtime host construction, and execution-unit batch path. Confirmed the async atomic batch path failed closed when no bootstrap mutation execution unit existed while the sync path created an engine execution unit. The async path now mirrors the sync fallback by calling `begin_mutation_execution_unit(...).execute_atomic_write_batch(batch)` after the cancellation check, preserving the single engine-owned mutation path. | `async_atomic_write_batch_without_bootstrap_unit_commits_through_engine_path` builds a query host with no bootstrap execution unit, executes an async atomic set batch, asserts a commit is returned, and reads the written document back through the engine. | Initial multi-filter `cargo test -p nimbus-bridge ...` was rejected by Cargo before running tests and is not proof. Final `cargo test -p nimbus-bridge -- --nocapture`: 14 passed, 0 failed, finished in 0.02s after a 1m00s rebuild; `cargo check -p nimbus-bridge`: pass, finished in 27.52s; `cargo clippy -p nimbus-bridge --all-targets -- -D warnings`: pass, finished in 12.91s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-2` | low | Misc | CONFIRMED/FIXED | Re-read `RuntimeHostState::validate_host_call_session`, bridge host-call call sites, Convex bridge sharing of the state object, and host-call payload shapes. Confirmed any non-empty token was accepted. Non-empty tokens must now match the active generated runtime host-call session id; missing tokens remain valid for host calls that intentionally do not carry a token, and empty tokens still fail closed. | `host_call_session_validation_binds_non_empty_tokens_to_active_session` asserts missing token, active token, empty token, and stale-token behavior with specific error checks. | Same focused bridge gate as J-1: `cargo test -p nimbus-bridge -- --nocapture`: 14 passed; `cargo check -p nimbus-bridge`: pass; `cargo clippy -p nimbus-bridge --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-3` | low | Misc | CONFIRMED/FIXED | Re-read `crates/nimbus-server/src/adapters/convex/host_bridge/db_ops/documents.rs`, the Convex bridge `RuntimeCapabilityHost` implementation, the Cloud Functions Firestore bridge-capability caller, and Convex authorization/cancellation tests. Confirmed Convex `ctx.db.get/insert/patch/delete` reimplemented the bridge document capability helpers instead of delegating, which made shared fixes like absent-read tracking depend on duplicated adapter branches. Routed Convex document get/insert/patch/delete through `nimbus_bridge::capabilities::{get_document, insert_document, update_document, delete_document}` and their async forms, while keeping Convex-specific table-id decoding, protocol id validation, and response-envelope shaping at the adapter boundary. | Added `runtime_host_bridge_get_records_missing_durable_document_reads`, which creates a durable table, reads a missing Convex document id through `ctx.db.get`, asserts the response is `null`, and proves the runtime read set records the raw storage document id under the stable `TableId`. Existing wrong-table and cancellation tests cover the preserved Convex adapter edges, and runtime mutation bridge tests cover staged write/read delegation. | Initial `cargo test -p nimbus-server runtime_mutation_bridge -- --nocapture` failed to compile on stale I2-4 server test call sites and is not proof; after fixing those call sites, `cargo test -p nimbus-server runtime_mutation_bridge -- --nocapture`: 4 passed, 432 filtered, integration binaries had 0 matching tests; `cargo test -p nimbus-server runtime_host_bridge_rejects_wrong_table_convex_document_ids -- --nocapture`: 1 passed, 435 filtered, integration binaries had 0 matching tests; `cargo test -p nimbus-server runtime_cancellable_db_get_short_circuits_before_dispatch -- --nocapture`: 1 passed, 435 filtered, integration binaries had 0 matching tests; after cleanup and the added proof, `cargo test -p nimbus-server runtime_host_bridge_get_records_missing_durable_document_reads -- --nocapture`: 1 passed, 436 filtered, integration binaries had 0 matching tests, finished after a 4m03s cold rebuild; `cargo clippy -p nimbus-server -p nimbus-testing --all-targets -- -D warnings`: pass, finished in 2m16s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-4` | low | Misc | CONFIRMED/FIXED | Re-read runtime read-set commit-intersection helper and its engine caller. Confirmed the parameter was still named `service` despite accepting `&nimbus_engine::Engine`. Renamed it to `engine` without changing behavior. | No new behavior test was needed for the local rename; existing bridge read-tracking/intersection tests compiled and ran through the same helper surface. | Same focused bridge gate as J-1: `cargo test -p nimbus-bridge -- --nocapture`: 14 passed; `cargo check -p nimbus-bridge`: pass; `cargo clippy -p nimbus-bridge --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-5` | low | Misc | CONFIRMED/FIXED | Re-read runtime subscription base-query synthesis and read-set tests. Confirmed the document-specific `queries.is_empty()` branch only pushed the same broad query as the immediately following fallback branch. Removed the redundant branch and kept the generic fallback. | `synthesize_runtime_subscription_base_queries_uses_broad_query_for_document_only_reads` pins the document-only read-set behavior to one broad table query. | Same focused bridge gate as J-1: `cargo test -p nimbus-bridge -- --nocapture`: 14 passed; `cargo check -p nimbus-bridge`: pass; `cargo clippy -p nimbus-bridge --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-6` | low | Misc | CONFIRMED/FIXED | Re-read bridge document get helpers, execution-unit reads, runtime read-set conversion, subscription dependency synthesis, and capability callers. Confirmed the bridge only recorded point reads when the document existed, so absent point reads could be missed by runtime-backed invalidation. Both sync and async get helpers now record the locator on successful lookup completion whether the document exists or not. | `get_document_records_absent_document_reads` creates an existing table, reads a missing document through the bridge, and asserts the missing document id is present in the dependency set. | Same focused bridge gate as J-1: `cargo test -p nimbus-bridge -- --nocapture`: 14 passed; `cargo check -p nimbus-bridge`: pass; `cargo clippy -p nimbus-bridge --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-7` | low | Misc | CONFIRMED/FIXED | Re-read `RuntimeHostContext::record_document_read`, `RuntimeHostState`, and runtime read-set storage. Confirmed every document-read record performed an engine table-id lookup even within the same invocation. Added a per-invocation table-id cache in `RuntimeHostState`, caching both present and missing table lookups before recording the document read. | `document_read_table_id_cache_memoizes_present_and_missing_tables` proves a present table id and a missing table are each resolved once per runtime host state. | Same focused bridge gate as J-1: `cargo test -p nimbus-bridge -- --nocapture`: 14 passed; `cargo check -p nimbus-bridge`: pass; `cargo clippy -p nimbus-bridge --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-9` | low | Misc | CONFIRMED/FIXED | Re-read `crates/nimbus-testing/src/websocket_fixture.rs`, all `next_message_with_timeout`/`next_json_with_timeout` callers, and websocket fixture tests. Confirmed `next_message_with_timeout` returned `None` both for timeout and a cleanly closed stream, causing wrapper methods to panic as if a closed peer were a timeout. Split timeout from stream completion: timeout remains `None`, while a closed stream now panics with `websocket closed before message`, and stream errors still panic with the underlying websocket error. | Added `message_from_websocket_next_reports_closed_stream_separately_from_timeout` and `message_from_websocket_next_returns_available_message` to pin the helper behavior. The first test catches the expected closed-stream panic and asserts its message, so the panic line in `--nocapture` output is intentional proof, not a failed test. | `cargo test -p nimbus-testing websocket_fixture -- --nocapture`: 2 passed, 4 filtered, finished in 0.00s after a 44.28s rebuild; `cargo check -p nimbus-server -p nimbus-testing`: pass, finished in 37.73s before the added J-3 server proof; final `cargo clippy -p nimbus-server -p nimbus-testing --all-targets -- -D warnings`: pass, finished in 2m16s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `E2-8` | info | Adapters | CONFIRMED/FIXED | Re-read `crates/nimbus-firebase/src/grpc/unary.rs`, the REST aggregation response encoder in `crates/nimbus-firebase/src/response.rs`, the shared Firestore serializers, and aggregation request/operation tests. Confirmed the gRPC and REST aggregation-result encoders intentionally target different wire shapes, but future value-type changes could drift without a shared parity assertion. Added a module-local parity test that normalizes the gRPC proto `Value` result into REST proto-JSON shape and compares representative aggregate fields. | `aggregation_result_grpc_and_rest_encoders_preserve_representative_values` covers integer, finite double, string, map, array, boolean, and null aggregate fields through both encoders and asserts equivalent Firestore proto semantics. | Initial `cargo test -p nimbus-firebase aggregation_result_grpc_and_rest_encoders_preserve_representative_values -- --nocapture` failed to compile because the new test compared `serde_json::Value` to `&serde_json::Value`; that test-only error was fixed and is not proof. Final `cargo test -p nimbus-firebase aggregation_result_grpc_and_rest_encoders_preserve_representative_values -- --nocapture`: 1 passed, 50 filtered, finished in 0.00s after a 6.21s rebuild; `cargo clippy -p nimbus-firebase --all-targets -- -D warnings`: pass, finished in 32.77s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `E2-9` | info | Adapters | NOT-A-DEFECT | Re-read the review entry, current `crates/nimbus-firebase/src/grpc/unary.rs`, `crates/nimbus-firebase/src/grpc.rs`, and the existing gRPC module layout. The file is currently 1,316 lines, below the repo's 1,500-line soft threshold and far below the 2,000-line split requirement; the finding's own fix direction says extraction is needed when the file next grows toward 1,500 lines. No current architecture invariant or repo law is violated, and forcing an extraction now would be churn unrelated to a present defect. | No test added; no code change warranted for a below-threshold composition root. E2-8 added the adjacent aggregation parity test so future encoder changes are covered without creating a new module prematurely. | `wc -l crates/nimbus-firebase/src/grpc/unary.rs`: 1,316 lines; `find crates/nimbus-firebase/src/grpc -maxdepth 2 -type f -name '*.rs' -print`: current gRPC children are `write_stream.rs`, `listen_stream.rs`, and `unary.rs`; `cargo clippy -p nimbus-firebase --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `E4-3` | info | Adapters | CONFIRMED/FIXED | Re-read the Convex manifest, production `TempDir` artifact guard, embedded bundle loader, and runtime-access tests. Confirmed `tempfile` is legitimately required as a normal dependency and the duplicate dev-dependency entry was redundant because normal dependencies are available to unit tests. Removed the duplicate `[dev-dependencies]` entry. | No test added; manifest-only duplicate cleanup covered by an existing runtime-lane test that imports `tempfile::tempdir` from a unit-test module. | `rg -n "\btempfile\b" crates/nimbus-convex/Cargo.toml crates/nimbus-convex/src`: one manifest normal-dependency entry plus production/test uses; `cargo tree -p nimbus-convex --depth 1 --edges normal`: direct normal dependencies include `tempfile`; `cargo tree -p nimbus-convex --depth 1 --edges dev`: only direct dev-dependency is `tokio`; `cargo check -p nimbus-convex`: pass, finished in 0.58s; `cargo test -p nimbus-convex convex_node_runtime_lanes_follow_lts_registry_targets -- --nocapture`: 1 passed, 13 filtered, finished in 0.77s; `cargo clippy -p nimbus-convex --all-targets -- -D warnings`: pass, finished in 0.57s; `make deny`: pass (`advisories ok`, `bans ok`, `licenses ok`, `sources ok`); `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk threshold check after verification: main `target/` 11 GiB, Cargo git checkouts 12 GiB, 250 GiB free, so no cleanup triggered. | Final batch commit |
| `A1-3` | info | Storage | NOT-A-DEFECT | Re-read the SQL commit paths in `sqlite/write.rs`, `postgres/write.rs`, `mysql/write.rs`, and `libsql/write.rs`, plus their write-serialization mechanisms. The schema-cache refresh/invalidations still happen after SQL `COMMIT`, but the review explicitly records this as an informational visibility window, not a correctness defect: writers are serialized per tenant and reads tolerate eventual cache refresh. | No test added; no code defect exists. The post-commit ordering remains visible in the commit methods and is covered by the existing SQL backend schema/write tests. | Code evidence: SQLite reloads the in-process schema cache after `COMMIT`; Postgres/MySQL invalidate the schema-cache handle after `COMMIT`; Postgres holds `pg_advisory_xact_lock`, MySQL gates tenant sequence with `FOR UPDATE`, and libsql opens an immediate transaction. | Final batch commit |
| `A1-4` | info | Storage | CONFIRMED/FIXED | Re-read `store/write/batch.rs`, redb direct delete paths, resource-path helpers, and provider resource-path tests. Confirmed updates upsert a supplied binding but intentionally do nothing when the update carries `None`; deletes own binding removal. Added an explicit comment at the update site documenting locator-stable bindings and a regression test proving an update without a replacement binding preserves the existing binding. | `update_without_resource_path_binding_keeps_existing_binding` inserts a bound document through an execution-unit batch, updates the same document with `resource_path_binding: None`, then asserts both locator and document-path lookup still return the original binding. | Initial storage test compile failed on four stale imports after the `helpers.rs` rename; that mechanical rename fallout was fixed and is not proof. Final `cargo test -p nimbus-storage update_without_resource_path_binding_keeps_existing_binding -- --nocapture`: 1 passed, 341 filtered, finished in 0.17s after a 25.82s compile; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 25.92s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `A3-10` | info | Storage | ALREADY-FIXED | Re-read `crates/nimbus-storage/src/tests/generated_history.rs`, the simulation harness exports, fault injectors, scenario signals, and verification seed corpus. Confirmed the report is a positive finding: the generated-history harness is live, assertion-heavy, and wired to dedicated verification lanes rather than dead code. | No code change needed. Freshly ran the dedicated required generated-history test by explicitly including ignored harness tests. | `cargo test -p nimbus-storage verification_harness_required_generated_history_seed_corpus_matches_model -- --nocapture`: 0 passed, 1 ignored, confirming the test is dedicated-lane gated; `cargo test -p nimbus-storage verification_harness_required_generated_history_seed_corpus_matches_model -- --ignored --nocapture`: 1 passed, 341 filtered, finished in 0.33s. | Final batch commit |
| `A3-9` | info | Storage | CONFIRMED/FIXED | Re-read `encryption/runtime.rs`, storage tracing usage, and the encryption manifest path. Confirmed encryption profiling still emitted `eprintln!` records containing protected database paths under ad hoc env gates. Replaced those stderr writes with `tracing::debug!` records using structured duration fields and no path field. The existing env gate remains as the opt-in switch, but profiling now uses the crate's tracing infrastructure. | `encryption_profile_trace_is_structured_and_path_free` installs a minimal test subscriber, emits an unwrap-profile event, and asserts the target/action/duration fields are present while no `path` field is recorded. | `cargo test -p nimbus-storage encryption_profile_trace_is_structured_and_path_free -- --nocapture`: 1 passed, 341 filtered, finished in 0.00s; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 25.92s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `A4-5` | info | Storage | CONFIRMED/FIXED | Re-read `async_storage/helpers.rs`, its module exports, and all async-storage callers. Confirmed the file only owned async executor join/permit error mapping, so the generic `helpers.rs` name violated the repo's concept-owned module convention. Renamed it to `task_error.rs` and updated internal imports. | Existing task-error tests continued to assert behavior after the rename: cancelled joins map to `Error::Cancelled`, and closed semaphore permits include the executor context. | Initial storage compile exposed four stale `super::helpers` imports after the rename; those were fixed and are not proof. Final `cargo test -p nimbus-storage executor_ -- --nocapture`: 2 passed, 340 filtered, finished in 0.00s; `cargo clippy -p nimbus-storage --all-targets -- -D warnings`: pass, finished in 25.92s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `C1-5` | info | Runtime | CONFIRMED/FIXED | Re-read `bootstrap/transpile.rs`, extension-transpiler wiring, and runtime bootstrap tests. Confirmed `maybe_transpile_source` still panicked on unsupported extension media types and unwrapped `Url::parse` inside a `Result`-returning path. Replaced both non-recoverable exits with `JsErrorBox::generic` errors so bad embedder extension inputs fail as runtime construction errors instead of aborting the process. | `extension_transpiler_rejects_unsupported_media_type_without_panicking`; `extension_transpiler_rejects_invalid_specifier_without_panicking`. The latter uses a relative `.ts` specifier to exercise the former `Url::parse(...).unwrap()` path. | Initial `cargo test -p nimbus-runtime extension_transpiler_ -- --nocapture` failed first on test-only `FastString` construction and then on an invalid test case that still parsed as a `node:` URL; neither run is proof. Final `cargo test -p nimbus-runtime extension_transpiler_ -- --nocapture`: 2 passed, 569 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 3.45s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.76s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 8.0 GiB, 251 GiB free, so no cleanup triggered. | Final batch commit |
| `C1-7` | info | Runtime | NOT-A-DEFECT | Re-read `op_nimbus_async_host_call`, `op_nimbus_sync_host_call`, `enforce_host_call_grants`, service-lookup host-bridge tests, and the runtime/server trust docs. The runtime gate is intentionally narrow: `CtxServiceLookup` is a runtime-local service grant check, while data/storage/identity host calls remain authorized by the trusted `HostBridge`/server implementation. This matches `docs/architecture/runtime/adapter-boundary.md`, which states runtime realm separation is not a substitute for server authorization and route policy resolves principal class and grants server-side. | No test added; no code defect exists. Existing host-bridge tests already assert denied service lookups fail before reaching the host bridge. | `sed -n '145,225p' crates/nimbus-runtime/src/runtime/bootstrap/ops/shared.rs`: only service lookup is runtime grant-checked before host dispatch; `sed -n '80,125p' docs/architecture/runtime/adapter-boundary.md`: server authorization remains authoritative; `rg -n 'runtime service grant denied|denied service lookup|CtxServiceLookup|service lookup' crates/nimbus-runtime/src/runtime/tests/host_bridge.rs`: existing coverage includes runtime-local service-grant denial. | Final batch commit |
| `C2-5` | info | Runtime | CONFIRMED/FIXED | Re-read `RuntimeWorkerRouter::choose_worker`, assignment/rollback/completion accounting, and the router unit tests. Confirmed the worker route decision intentionally reads `load` and `last_assigned_sequence` as separate atomics for a best-effort routing heuristic. Added an explicit comment so future readers do not infer an atomic `(load, sequence)` snapshot or exact balancing guarantee. | No new behavior test; comment-only clarification. Existing dispatch accounting test still guards counter consistency around assignment rollback. | `cargo test -p nimbus-runtime failed_dispatch_rolls_back_pre_send_worker_load -- --nocapture`: 1 passed, 570 filtered, integration test binaries had 0 matching tests, finished in 0.00s after a 7.50s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.73s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 8.1 GiB, Cargo git checkouts 12 GiB, 251 GiB free, so no cleanup triggered. | Final batch commit |
| `C2-6` | info | Runtime | CONFIRMED/FIXED | Re-read `CooperativeWorkerLoop::finish_invocation`, the failed-start branch in `admit_job`, and cooperative executor tests. Confirmed `finish_failed_start` was a one-call pass-through that only reordered arguments. Removed the wrapper and call `finish_invocation` directly from the failed-start path using the canonical argument order. | No new behavior test; no behavior changed. Existing cooperative executor test recompiles and exercises the worker-loop finishing path after the wrapper removal. | `rg -n 'finish_failed_start' crates/nimbus-runtime/src`: no remaining references; `cargo test -p nimbus-runtime cooperative_execution_model_processes_worker_invocations -- --nocapture`: 1 passed, 570 filtered, integration test binaries had 0 matching tests, finished in 0.04s after a 0.50s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.73s; `git diff --check`: pass. | Final batch commit |
| `C3-9` | info | Runtime | CONFIRMED/FIXED | Re-read Bun/JSC backend invocation, pool construction, lifecycle scaffold verification, no-link and linked-adapter tests. Confirmed `BunJscPool::verify_scaffold_contract()` still ran the full lifecycle simulation inside every invocation. Cached the scaffold self-check behind a process-local `OnceLock`, preserving fail-closed invocation behavior while reducing each invocation to a cheap cached-result check. | `bun_jsc_scaffold_contract_is_cached_across_invocations` uses test-only verifier instrumentation and two no-link invocations to assert the scaffold lifecycle simulation runs at most once while both invocations still fail closed and record admission/disabled metrics. | `cargo test -p nimbus-runtime bun_jsc_scaffold_contract_is_cached_across_invocations -- --nocapture`: 1 passed, 571 filtered, integration test binaries had 0 matching tests, finished in 0.01s after a 5.42s compile; `cargo fmt --all --check`: pass; `cargo clippy -p nimbus-runtime --all-targets -- -D warnings`: pass, finished in 3.73s; `git diff --check`: pass. Disk threshold check after verification: main `target/` 8.1 GiB, Cargo git checkouts 12 GiB, 251 GiB free, so no cleanup triggered. | Final batch commit |
| `D2-6` | info | Server | CONFIRMED/FIXED | Re-read `crates/nimbus-operator/src/token.rs`, token/access-policy tests, and local-admin token authorization callers. Confirmed `LocalAdminTokenRecord::authorize` still used the HMAC-of-self pattern for a direct token equality check. Replaced it with a local constant-time byte comparison and removed the now-misleading HMAC dependency from this path. | Strengthened `token_authorization_accepts_only_exact_token_matches` to reject unrelated, shortened, and longer candidates while accepting only the exact generated token; updated the source audit to prove the direct constant-time helper is present and the old HMAC import is absent. | Initial `cargo test -p nimbus-operator token -- --nocapture` runs exposed a self-referential source-test literal and are not proof. Final `cargo test -p nimbus-operator token -- --nocapture`: 10 passed, 22 filtered, finished in 0.06s after a 0.69s compile; `cargo clippy -p nimbus-operator -p nimbus-server --all-targets -- -D warnings`: pass, finished in 10.12s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `D3-3` | info | Server | NOT-A-DEFECT | Re-read the subscription key builders in `crates/nimbus-server/src/adapters/convex/subscriptions/socket/mod.rs` plus their direct, message, and runtime-backed callers. The `.expect` calls serialize server-owned `Query` values and `serde_json::Value`/primitive JSON objects into `String`; with the current serde_json string writer, this path has no fallible I/O or user-controlled formatter. Treating it as a `Result` would only push an impossible error through map-key construction. | No test added; no code defect exists. Existing subscription tests continue to exercise the key builders through registration and runtime-backed subscription flows. | Code evidence: `plain_subscription_query_key` serializes `&Query`; `named_subscription_query_key` serializes a `serde_json::json!` object built from the function name, args, page size, and cursor. No file/socket writer or custom serializer is involved. | Final batch commit |
| `D3-4` | info | Server | CONFIRMED/FIXED | Re-read the raw Convex query/mutation HTTP handlers, access-policy path, compatibility docs, and Convex AI guidelines. Confirmed the surface is intentionally protocol-compatible but under-documented: raw query/mutation requests lower into principal-bearing engine entrypoints and rely on table access policy, not function-registry authorization. Added an explicit compatibility section documenting that boundary and the operator responsibility to enforce table access policies when exposing raw requests. | No behavior test added for the documentation clarification; the existing handler/auth tests already cover the engine-path enforcement. | `docs/adapters/convex/compatibility.md` now documents raw query/mutation authorization semantics; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `D3-5` | info | Server | CONFIRMED/FIXED | Re-read `crates/nimbus-server/src/adapters/convex/tests/cancellation.rs`, runtime host-bridge cancellation, and read-tracking helpers. Confirmed `runtime_cancellable_db_get_short_circuits_before_dispatch` only asserted the cancellation error and did not prove dispatch/read tracking stayed untouched. Strengthened the test to assert every read-set dependency collection remains empty after a pre-cancelled `ctx.db.get`. | Updated `runtime_cancellable_db_get_short_circuits_before_dispatch` to assert empty table, missing-table, missing-predicate, document, index-range, predicate, and paginated-window dependencies. | `cargo test -p nimbus-server runtime_cancellable_db_get_short_circuits_before_dispatch -- --nocapture`: 1 passed, 437 filtered, integration binaries had 0 matching tests, finished in 0.04s; `cargo clippy -p nimbus-operator -p nimbus-server --all-targets -- -D warnings`: pass, finished in 10.12s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `D4-6` | info | Server | CONFIRMED/FIXED | Re-read MongoDB listener tests and confirmed they lacked authenticated data-path and unauthenticated rejection coverage. Added listener-level unauthenticated rejection and authenticated wire CRUD/spec coverage. | `listener_rejects_unauthenticated_data_command`; server wire CRUD unauthenticated/authenticated assertions; spec runner authenticates via SCRAM for seeded tests. | `cargo test -p nimbus-server mongodb -- --nocapture`: 6 passed; `cargo test -p nimbus-server --test mongodb_spec -- --nocapture`: 23 passed; `make clippy`: pass; `make check`: pass. | Final batch commit |
| `D4-7` | info | Server | CONFIRMED/FIXED | Re-read `crates/nimbus-server/src/artifact_verifier_effects/process.rs`, `ArtifactVerifierCommandBackend`, the command-runner trait, and artifact-verifier tests. Confirmed `ProcessArtifactVerifierCommandRunner` wrote all stdin before reading stdout/stderr, so a verifier that fills stdout before consuming stdin could deadlock until timeout. The process runner now writes stdin and drains stdout/stderr on dedicated threads while the child runs, then joins those threads after process exit. | `process_runner_drains_stdout_while_writing_stdin` runs a Unix subprocess that emits 256 KiB to stdout before reading stdin, then asserts exit 0, full stdout capture, and empty stderr. | `cargo test -p nimbus-server process_runner_drains_stdout_while_writing_stdin -- --nocapture`: 1 passed, 437 filtered, integration binaries had 0 matching tests, finished in 0.01s after a 32.39s compile; `cargo clippy -p nimbus-operator -p nimbus-server --all-targets -- -D warnings`: pass, finished in 10.12s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `F1-5` | info | Security | CONFIRMED/FIXED | Re-read the test-quality finding and confirmed it is the coverage half of `F1-1`: the external-policy suite lacked a deliberately-blocking backend regression proving worker growth is bounded under timeouts. Covered with the same F1-1 blocking-backend test rather than duplicating code. | `external_policy_engine_caps_workers_while_backend_is_hung` explicitly exercises the hung-backend timeout path and proves no second backend worker is spawned while the cap is occupied. | Same focused F1-1 gate: `cargo test -p nimbus-tenant external_policy_engine -- --nocapture`: 3 passed, 79 filtered; `cargo check -p nimbus-tenant`: pass; `cargo clippy -p nimbus-tenant --all-targets -- -D warnings`: pass; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `F2-10` | info | Security | CONFIRMED/FIXED | Re-read `crates/nimbus-auth/src/lib.rs`, `crates/nimbus-server/src/application_auth.rs`, Firebase auth contract docs, and the REST/WebSocket Firebase mock-user-token tests. Confirmed the unsigned JSON parser is intentionally reachable only behind `DeploymentState::firebase_config().allows_emulator_mock_user_token_auth()`, but the public helper name/docs did not fully advertise that safety boundary. Renamed it to `firebase_emulator_mock_user_principal_from_bearer` and added a doc comment stating it accepts unsigned Firebase emulator `mockUserToken` JSON and must only be called after explicit emulator opt-in; signed production bearer paths must use `ApplicationAuthVerifier`. | Existing behavior tests already cover the security boundary: `emulator_principal_normalizes_subject_aliases` covers the parser's subject normalization, `firebase_mock_user_token_requires_explicit_server_opt_in` proves REST rejects unsigned JSON without opt-in and accepts it with opt-in, and `firebase_listen_websocket_mock_user_token_requires_explicit_server_opt_in` proves the WebSocket subprotocol path has the same gate. | `rg -n 'emulator_principal_from_bearer' crates packages demos`: no matches; `cargo test -p nimbus-auth emulator_principal_normalizes_subject_aliases -- --nocapture`: 1 passed, 4 filtered, finished in 0.00s after a 31.91s compile; `cargo test -p nimbus-server firebase_mock_user_token_requires_explicit_server_opt_in -- --nocapture`: 1 passed, 435 filtered, integration test binaries had 0 matching tests, finished in 0.12s after a 1m34s compile; `cargo test -p nimbus-server firebase_listen_websocket_mock_user_token_requires_explicit_server_opt_in -- --nocapture`: 1 passed, 435 filtered, integration test binaries had 0 matching tests, finished in 0.07s; `cargo check -p nimbus-auth -p nimbus-server`: pass, finished in 28.61s; `cargo clippy -p nimbus-auth -p nimbus-server --all-targets -- -D warnings`: pass, finished in 27.79s; `cargo fmt --all --check`: pass; `git diff --check`: pass. Disk cleanup trigger fired after server verification grew this checkout's `target/` to 27 GiB with 232 GiB free; `ps -ax -c -o pid,comm` showed no Cargo/rustc/clippy, and escalated `lsof` confirmed active `nimbus_runtime-*` probes belonged to `/Users/jack/src/github.com/nimbus/nimbus-worktrees/node-default-runtime-support-hardening`, not this checkout. `cargo clean` in this checkout removed 48,946 files / 30.7 GiB; post-clean `/` had 259 GiB free, this checkout's `target/` was absent, Cargo git checkouts stayed 12 GiB, and the sibling NDS target stayed 45 GiB. | Final batch commit |
| `F2-9` | info | Security | NOT-A-DEFECT | Re-read the finding, `object_fields`, all `object_fields(...)` call sites in `crates/nimbus-system/src/records.rs`, and the exported system-recording callers. The helper is private to the system-record projection module, every production call passes an immediate `json!({ ... })` object literal, and externally-shaped inputs reach the public `record_*` functions as typed Rust structs before fields are projected into those literals. The `unreachable!` is therefore an internal literal-construction invariant, matching the review's "otherwise leave as-is" direction. | No test added; no behavior change is warranted for a private helper whose non-object case is unreachable under the confirmed call graph. | `rg -n 'object_fields\\(' crates/nimbus-system/src/records.rs`: all production call sites pass `json!({ ... })` object literals; `rg -n 'record_system_status_async|seed_system_documents_async|record_.*_async' crates/nimbus-system/src crates/*/src -g '*.rs'`: public callers enter typed record/projection APIs rather than raw `serde_json::Value`; `git diff --check`: pass after ledger update. | Final batch commit |
| `G-5` | info | Trust | CONFIRMED/FIXED | Re-read `RuntimeBundleInvocationOptions`, `admit_runtime_bundle_artifact`, Convex/Cloud Functions registry provenance setters, and every runtime invocation call site. Confirmed `None` silently skipped provenance admission. Replaced the raw `Option` field with explicit `RuntimeBundleProvenanceGate::{Disabled, Configured}`, kept configured gates fail-closed, and renamed optional registry wiring to `with_optional_runtime_bundle_provenance_gate` so absent provenance is an explicit opt-out at invocation construction. | `runtime_bundle_provenance_gate_disabled_state_skips_admission_explicitly` proves the disabled state skips admission only through the explicit opt-out path; existing provenance admission/rejection tests rerun through the configured state. | Initial `cargo test -p nimbus-server runtime_bundle_provenance_gate_ -- --nocapture` ran 5 tests and found a stale existing assertion expecting predicate wording for a wrong-builder failure; after tightening it to the builder-specific trust boundary, final run: 5 passed, 440 filtered, integration binaries 0 matching, finished in 0.00s after an 8.67s compile. `cargo clippy -p nimbus-core -p nimbus-server -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 21.31s; final `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `G-6` | info | Trust | CONFIRMED/FIXED | Re-read `TypedScalarValue::Binary`, `projected_json`, typed-scalar tests, MongoDB BSON bridge projection/reconstruction, and Firebase unsupported-type handling. Confirmed clean JSON projection intentionally drops the BSON subtype while the typed sidecar preserves it, but the intent was undocumented. Added a comment at the projection and a regression test pinning the lossy projection plus lossless serde roundtrip. | `binary_projection_is_lossy_but_roundtrip_preserves_subtype`. | `cargo test -p nimbus-core binary_projection_is_lossy_but_roundtrip_preserves_subtype -- --nocapture`: 1 passed, 132 filtered, finished in 0.00s after a 9.21s compile; scoped clippy/fmt/diff checks passed as recorded for G-5. | Final batch commit |
| `H1-7` | info | Sandbox | CONFIRMED/FIXED | Re-read `crates/nimbus-sandbox/src/process.rs`, the conmon lifecycle caller, and related process-liveness references. Confirmed Unix `kill(pid, 0)` and Windows `OpenProcess`/`GetLastError`/`GetExitCodeProcess`/`CloseHandle` blocks lacked `SAFETY:` rationale. Added comments documenting pointer/handle validity, close ownership, permission-probe semantics, and the best-effort/PID-reuse boundary. | No behavior test was needed for unsafe-audit comments; scoped clippy and formatting prove the comments compile cleanly. | `cargo clippy -p nimbus-core -p nimbus-server -p nimbus-sandbox --all-targets -- -D warnings`: pass, finished in 21.31s; `cargo fmt --all --check`: initial check reported formatting-only wrapping in `provenance.rs` and is not proof; after `cargo fmt --all`, final check passed; `git diff --check`: pass. | Final batch commit |
| `I1-11` | info | CLI | CONFIRMED/FIXED | Re-read `crates/nimbus-bin/src/sandbox_supervisor.rs`, the hidden CLI wiring, supervisor tests, and sandbox egress contract types. Confirmed the command is intentionally validation-only, but it validated loaded plans twice and advertised a hard-coded `packet_enforcement_active: false` field as if packet enforcement were runtime state. Kept the validation-only command, moved validation to the report constructor so the command validates once, and removed the hard-coded packet-enforcement field from the report/text output. | Updated `supervisor_command_rejects_invalid_contract_policy` to prove invalid contracts still fail closed through the command/report path, and reran the full `sandbox_supervisor` focused suite. | `cargo test -p nimbus-bin sandbox_supervisor -- --nocapture`: 6 passed, 578 filtered, plus 0 matching tests in `server_discovery_serde`, finished in 0.01s after a 3m01s cold compile; `rg -n "packet_enforcement_active" crates/nimbus-bin/src/sandbox_supervisor.rs`: no matches; `cargo check -p nimbus-machine -p nimbus-bin -p nimbus-operator`: pass, finished in 46.76s; `cargo clippy -p nimbus-machine -p nimbus-bin -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 30.94s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `I2-8` | info | CLI | CONFIRMED/FIXED | Re-read `crates/nimbus-operator/src/lib.rs`, the extraction proof `docs/plans/proof/server-crate-extraction-completion/fce4-operator.md`, and `nimbus-operator` module exports. Confirmed the name is intentional and not a controller/reconciler crate, but the crate docs did not state that distinction at the reader entrypoint. Added a crate-level doc note clarifying that "operator" means the administrator operating this host's server, not a Kubernetes-style controller or reconciler. | No behavior test was needed for a doc-only clarification; the grouped `nimbus-operator` check/clippy gate proves the crate docs compile and the public surface remains intact. | `cargo check -p nimbus-machine -p nimbus-bin -p nimbus-operator`: pass, finished in 46.76s; `cargo clippy -p nimbus-machine -p nimbus-bin -p nimbus-operator --all-targets -- -D warnings`: pass, finished in 30.94s; `cargo fmt --all --check`: pass; `git diff --check`: pass. | Final batch commit |
| `J-8` | info | Misc | NOT-A-DEFECT | Re-read `RuntimeReadSet` storage and mutation helpers. The confirmed hot-path pieces already use `HashSet` for table and document reads. The remaining vector-backed reads carry JSON/filter/order/window payloads whose canonical hash identity is not a simple local change; the current code preserves deterministic insertion order and deduplicates with equality. No production defect or testable incorrect behavior remains for this info-level optimization note. | No test added; no code change warranted. The J-5/J-6/J-7 bridge tests cover the adjacent read-set behavior changed in this batch. | `sed -n '1,260p' crates/nimbus-bridge/src/read_tracking/read_set.rs`: table/document reads are `HashSet`, range/predicate/window reads are equality-deduped vectors carrying nontrivial payloads; `git diff --check`: pass after ledger update. | Final batch commit |
