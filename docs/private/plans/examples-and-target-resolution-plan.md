# Examples And Target Resolution — Control Plane

Status: `active`
Owner branch: `examples-and-target-resolution` (single PR to `nimbus/nimbus` at closeout)
Control-plane updates (this file, plans README): commit direct to `main`.

## Mission

Give Nimbus a canonical, user-facing examples tree that (a) teaches developers
each adapter surface with high-quality runnable apps, (b) verifies our builds
in CI against workspace HEAD, and (c) is displayed, explained, and linked from
the public docs. In the same stroke, unify the CLI's deployment-target UX so an
example's run story is identical from laptop to single node to cluster: one
optional resolver string, local by default, with no node/cluster distinction.

## What This Plan Owns

- The `demos/` → `examples/` restructure: adapter-first tree, root README nav
  index, shared per-app behavior specs.
- The target-resolution CLI UX: one optional positional `TARGET` (URL or
  configured name) on target-taking commands, local default, unified through
  the existing `TargetSelector` seam.
- Canonical example apps (web + agent) and their headless smoke verification
  lane.
- Public docs pages that display/explain each example and link to its GitHub
  source for review.
- Cleanup of demo-era residue (parity notes, stale planned-next lists, script
  naming, insecure tenant-provisioning defaults).

Non-goals: no new adapter surfaces; no cluster features (HS plan owns those);
no separate examples repo pre-launch (mirror publishing is an EX7 row with a
default-defer decision); no changes to the engine mutation path or storage
semantics.

## Decision Records

### XD1 — Examples live in this monorepo; no separate repo pre-launch

The dual purpose (developer-facing + build verification) requires examples to
import workspace packages and run in CI against HEAD, which a separate repo
structurally cannot do. Exemplar precedent: Supabase/Next.js (in-monorepo
`examples/` + scaffolder fetch), Cloudflare (canonical templates + CI that runs
each one), versus Firebase's fragmented sample repos (the anti-pattern). A
`nimbus/examples` mirror, if ever needed, is an automated one-way publish with
released version pins (EX7.5) — never a hand-maintained second copy.

Copy-out hazard (2026-07-10 DX review finding; mechanism corrected by the
2026-07-11 foundation review): workspace imports mean a copied-out example's
deps don't resolve — and for Convex specifically the shape differs.
`packages/convex/package.json` deliberately names itself `convex` with a
`convex` bin (that is the compat point), which collides with the official npm
package: the convex apps depend on `"convex": "*"`, which resolves to Nimbus's
workspace package in-repo but to the **official Convex Cloud** package once
copied out and `npm install`ed. The failure is visible, not silent: the apps'
scripts run `convex codegen --app .`, and `--app` is a Nimbus-only flag the
official CLI rejects, so codegen fails loudly; and even past that the client is
pinned to `http://localhost:8080/convex/demo` with `skipConvexDeploymentUrlCheck`,
so it targets the local Nimbus server rather than quietly talking to Convex
Cloud. Consequences bound into this plan: the convex example's README carries a
loud "monorepo-only until the scaffolder ships" warning naming this failure
mode accurately (EX3.2 acceptance); the EX7.1 scaffolder must rewrite the
`convex` workspace dep to a published pin as its core job, not an afterthought;
the other adapter examples do not share the collision: unpublished `@nimbus/*`
workspace deps fail resolution with a visible install error, while the
Firebase app's stock `firebase` dependency installs the real upstream SDK from
the registry (the app still expects a Nimbus server to talk to).

### XD2 — Tree shape: adapter-first with a root nav index, renamed to `examples/`

`examples/README.md` explains the adapter surfaces and is the root nav index;
`examples/nimbus/*` (native), `examples/convex/*`, `examples/firebase/*`,
`examples/cloud-functions/*`, `examples/mongodb/*`, and `examples/dynamodb/*`
hold the apps — one directory per public adapter surface (the six documented
under `docs/developers/`), later joined by `examples/s3/*` for the S3 surface.
Every directory level carries a `README.md` that explains its section: the
root nav index, each adapter directory (what the surface is, its examples, the
supported subset, a link to its docs page), `examples/specs/`, and each
example app. This matches how developers arrive ("I'm a Firebase dev").
The tree is renamed from `demos/` because these are the user-facing product and
"demos" reads as internal; pre-launch, the rename is free (breaking changes
preferred). Internal parity fixtures that are not user-worthy stay but move out
of the user-facing README into internal notes (EX8).

### XD3 — App parity via shared specs

Cross-adapter parity is owned by `examples/specs/<app>.md`: one behavior spec
per canonical app (schema, flows, observable assertions). Each adapter
directory implements the spec for its supported subset and says so explicitly.
The spec is the unit the smoke scripts assert against. Canonical app set:
`tasks` (CRUD + live queries — the industry hello-world), `chat` (subscriptions,
presence, pagination), `agent-chat` (durable agent, web), `agent-worker`
(headless agent), `filedrop` (blob/S3), `jobs` (cron/scheduling). Tier 1 =
`tasks` + the two agent apps; the rest are EX7 rows.

### XD4 — Target resolution: one optional resolver string, local default

There is no `nimbus deploy --node <host>` vs `nimbus deploy --cluster <name>`.
Commands that act on a Nimbus resource take one optional positional `TARGET`:

- URL-shaped (`http`/`https`) → remote URL target.
- Otherwise → configured target name / resolver slug.
- Omitted → local discovery, exactly like `nimbus dev`'s local default.

Whether the resolved resource is a single node or a cluster is invisible to the
UX — it is just "a Nimbus resource." The rationale (recorded so this is
auditable when HS lands): a Nimbus resource presents one external endpoint
regardless of topology — the kubectl/Fly precedent — so the CLI never needs
node-vs-cluster awareness; cluster-internal concerns are HS-plan territory.
`nimbus dev` remains the watch-mode local development loop; `nimbus deploy`
with no argument targets local.

This rides the `TargetSelector` seam (`crates/nimbus-cli/src/target_context.rs`:
`LocalDiscovery | NamedTarget | RemoteUrl`, single-source ambiguity rule) —
but `NamedTarget` is today an unbacked stub (`run.rs` hard-errors "not yet
backed by a target registry", and `credentials.rs` keys connections by daemon
URL only). Named targets therefore need a real backend, scoped here as EX1.5:
`~/.config/nimbus/targets` (TOML, name → URL — mirroring the credentials-file
precedent) with `nimbus target add/list/remove`; a name resolves to its URL,
then credentials look up by URL exactly as today. `deploy` migrates onto the
seam (today `deploy.rs` hard-requires `--url`/`NIMBUS_DEPLOY_URL`).
Pre-launch: replace flags with the positional and rename the generic env
fallback `NIMBUS_DEPLOY_URL` → `NIMBUS_TARGET_URL` (it already applies to
run/sandbox, not just deploy); no alias flags, no alias env vars.

Reviewer dissent, recorded: the 2026-07-10 gpt-5.6-sol DX review recommended
keeping `deploy` fail-closed (explicit target source required) because a bare
`nimbus deploy` next to a running `nimbus dev` server silently activates a
generation locally when the user may have meant a remote. Owner decision
stands — omitted TARGET means local everywhere, deploy included — with a
required mitigation: every target-taking command prints a prominent
resolved-target line before acting (e.g. `Deploying to LOCAL
http://127.0.0.1:PORT (no TARGET given)` / `Deploying to prod
(https://…, from ~/.config/nimbus/targets)`), so the destination is never
implicit in the output even when it was implicit in the invocation.

### XD5 — Docs display, explain, and link each example

Public docs get example pages (Developers group per adapter; Agents group for
agent examples), each explaining what the example demonstrates and linking to
its GitHub source path on `nimbus/nimbus` for code review. Pages obey the docs
skill rules: one Diátaxis mode per page, source-available wording, no
`docs/private` links, source-map rows, no internal plumbing. Every example
README ends with the same run story: `nimbus dev` for the local loop, then
`nimbus deploy [TARGET]` — same app, any Nimbus resource.

## Invariants (binding on every band)

1. Examples import workspace packages (npm workspaces / repo paths), so a
   surface change that breaks an example breaks the same PR.
2. Every user-facing example has: README (what it shows, run story), a spec
   mapping, and a headless smoke script asserting observable behavior — not
   just compilation.
3. Every directory level under `examples/` has a `README.md` explaining that
   section: root index, per-adapter directories, `specs/`, and each app.
4. No `--node`/`--cluster` vocabulary anywhere: CLI help, example READMEs,
   docs pages.
5. Docs claims are source-verified; supported-subset caveats stated honestly.
6. Fail-closed or gate-adding changes run the full workspace suite, not a
   name-filtered subset.

## Band Ledgers

Status values: `todo`, `in_progress`, `done(evidence)`, `no-action(reason)`,
`blocked(reason)`; EX7 rows may also close `decision-recorded(reason)`.

Foundation review (2026-07-11, gpt-5.6-sol): EX0–EX2 verdict not-ready with 6
findings; all applied on branch `examples-and-target-resolution`. (1) tasks
spec + supported-subset tables + root README labeled target-state (EX3 apps /
EX5 smokes), not delivered; (2) public docs stripped of the deleted
`--url`/`--local`/`--target` flags (get-started/deploy.md, cloud-functions/migrate.md,
reference/cli.md sandbox synopsis), `nimbus auth login --url` left intact;
(3) `dirs::global_config_dir` now ignores empty/relative `XDG_CONFIG_HOME` per
the XDG spec (+3 unit tests); (4) convex copy-out warning rewritten to the real
failure mode (npm swaps in official `convex`, `convex codegen --app .` fails
loudly, client stays on localhost — not a silent Convex-Cloud talk); (5)
per-app READMEs added under every `examples/<adapter>/<app>/` + `index.html`
title fixed; (6) deploy unconfigured-name test made deterministic via injected
temp registry, target add/list/remove stdout + TOML shape asserted, run
stdout/stderr contract pinned.

### Band EX0 — Baseline inventory and decision confirmation

Exploration is the deliverable; record findings with `file:line` evidence in
the Appendix.

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX0.1 | Inventory the demos surface: `demos/*` apps, npm workspaces (`package.json:14-19`) and scripts (`:28-41`), Makefile convex-demos overlay (`Makefile:699`), server static serving (`crates/nimbus-server/src/router.rs`, `http/metadata.rs`, `tests/registry_and_license/routes.rs`), `compose.yaml`/`Containerfile` references | Appendix table lists every reference the EX2 rename must touch | n/a | done(Appendix EX0.1: 20 load-bearing path/route literals catalogued incl. router.rs:596-607, metadata.rs:148-150, cloud-functions/lib.rs:566-573, operator/policy.rs:31-91, package.json:14-19; compose.yaml/Containerfile/.github confirmed zero hits) |
| EX0.2 | Inventory CLI target seams: `TargetSelector` consumers (`run.rs`, `sandbox.rs`, `lib.rs`), `deploy.rs` `resolve_deploy_url` + flag set, how `NamedTarget` actually resolves to an endpoint+credentials today, `dev` local defaults | Appendix records the resolution chain and the exact flag/env surface EX1 changes | n/a | done(Appendix EX0.2: resolution chain traced — 3 flags→3 kinds+env; run has local fallback, sandbox does not, deploy bypasses TargetSelector via resolve_deploy_url; NamedTarget has NO registry/resolver today (errors in run.rs:290, stub in sandbox); LocalDiscovery wired via LocalServerHttpClient::base_url) |
| EX0.3 | Agent-surface inventory: which agent-relevant surfaces are usable via public SDK/CLI today (scheduler, sandbox/run workloads, sessions, egress policy) | EX4 scope note written: what agent-chat/agent-worker may use, with citations; nothing fabricated | n/a | done(Appendix EX0.3: scheduler(SDK real)/DB(SDK+CLI real)/sandbox-sessions-services(SDK only, CLI stubbed) usable; cron/run-exec/sandbox-CLI/egress-knob NOT public; agent-chat scope = query/mutation+ctx.scheduler.runAfter, no machine needed) |
| EX0.4 | Docs touchpoints: sidebar (`website/astro.config.mjs`), `docs/source-map.md`, existing `docs/developers/<adapter>/` pages, Agents group layout | EX6 page list recorded (path + Diátaxis mode per page) | n/a | done(Appendix EX0.4: Starlight sidebar :74-129, autogenerated per-adapter subgroups; existing developers/<adapter> pages listed; EX6 page list = developers/<adapter>/examples.md how-to + agents/agent-*.md; stale-doc debt for deleted --url flagged for EX6) |

### Band EX1 — Target resolution UX

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX1.1 | Extend `TargetSelector` with positional resolver-string parsing: URL-shaped → `RemoteUrl`, else `NamedTarget`; keep validation and the exactly-one-source rule; absent → `LocalDiscovery` implicit default for all consumers | Unit tests cover url/name/whitespace/empty/ambiguous/default-local paths; test names + counts recorded | ~150 + tests | done(target_context.rs: one optional positional `target`, `resolve()` returns LocalDiscovery/ImplicitLocalDefault when absent, http(s) prefix→RemoteUrl else NamedTarget, both-env-set→ambiguity error. 9 unit tests: absent_target_resolves_local_discovery, url_shaped_positional_resolves_remote_url, non_url_positional_resolves_named_target, named_target_rejects_whitespace, empty_positional_rejects, env_target_resolves_named_target_when_positional_absent, env_target_url_resolves_remote_url_when_positional_absent, positional_wins_over_env_fallbacks, ambiguous_env_sources_reject — all green; env source renamed EnvironmentTargetUrl for NIMBUS_TARGET_URL per EX1.5) |
| EX1.2 | Migrate `nimbus deploy` onto `TargetSelector`: optional positional `TARGET`, remove required `--url` (delete, no alias), local default uses the same local discovery as `run`; every target-taking command prints the prominent resolved-target line (destination + how it was resolved) before acting | `nimbus deploy` (no args) resolves local and says so loudly; `nimbus deploy https://…` and `nimbus deploy prod` resolve correctly (the latter via EX1.5); an unconfigured name fails with an error naming the targets file and `nimbus target add`; deploy tests updated and green | ~200 + tests | done(deploy.rs: `resolve_deploy_target_url` now returns `ResolvedDeployTarget { url, banner }`; `run_deploy_command` emits the banner via `emit_deploy_phase` before acting. NamedTarget resolves through the registry (injectable resolver so tests never read the real ~/.config); named→`targets::resolve_named_target_url` error names the file + `nimbus target add <name> <url>`. Banner shapes: `Deploying to LOCAL <url> (no TARGET given)` / `Deploying to <name> (<url>, from <targets-file>)` / `Deploying to <url> (from TARGET|NIMBUS_TARGET_URL)`. run.rs mirrors via `emit_run_target_banner` to STDERR (keeps JSON stdout clean). Tests: deploy_target_resolution_resolves_url_and_requires_token asserts url+banner for TARGET/env/named + unconfigured-name error; deploy_help_describes_positional_target asserts NIMBUS_TARGET_URL, no `--url <URL>`. FOUNDATION REVIEW 2026-07-11: unconfigured-name deploy test made deterministic (injects a temp registry via `resolve_named_target_url_at`, no longer tolerant of the machine's real config); run.rs banner+result split into `write_run_banner`/`write_run_result` with a test pinning banner-to-its-sink and clean-JSON-stdout; public docs (get-started/deploy.md, cloud-functions/migrate.md) stripped of the deleted `nimbus deploy --url` form) |
| EX1.3 | Unify `run`/`sandbox` (and any other `TargetSelector` consumer found in EX0.2) onto the positional form with one shared help-text vocabulary: "TARGET is a URL or a configured target name; omitted = local" | Help output for each command shows the shared vocabulary; no per-command drift | ~100 | done(shared `TARGET_ARG_HELP` const drives every consumer's positional help; run.rs dropped `has_explicit_run_target` + `--local`/`--target` (local default now in `resolve()`); sandbox.rs now local-defaults too (new test sandbox_list_without_target_defaults_to_local); deploy help + cli_ux examples rewritten to positional. Error strings updated to "pass a TARGET URL / omit TARGET for local". 823/823 nimbus-cli tests green; clippy clean (only third-party brotli warnings)) |
| EX1.4 | No node/cluster distinction audit: sweep CLI help, errors, and this repo's docs for `--node`/`--cluster`-style target vocabulary | `git grep` sweep recorded clean (or each hit justified as non-target usage) | n/a | done(`git grep -E '--node|--cluster|node <host>|cluster <name>'` over crates/nimbus-cli + docs: zero target-selection hits. Two incidental non-target hits justified: compose/file/lower.rs:673 "single-node placement" (compose deploy.placement note), docs/concepts/architecture/node-lifecycle.md:159 (architecture prose). No `--node`/`--cluster` target flag exists) |
| EX1.5 | Minimal target registry backing `NamedTarget` (today an unbacked stub): `~/.config/nimbus/targets` TOML (name → URL, mirroring `credentials.rs`'s DEP2 precedent), `nimbus target add/list/remove`, name→URL resolution feeding the existing URL-keyed credential lookup; rename env fallback `NIMBUS_DEPLOY_URL` → `NIMBUS_TARGET_URL` everywhere (delete old name) | `nimbus target add prod https://…` then `nimbus run/deploy prod` works end-to-end; `run.rs`'s "not yet backed by a target registry" error is gone; unit tests cover add/list/remove/resolve/missing-name; `git grep NIMBUS_DEPLOY_URL` clean | ~250 + tests | done(new `targets.rs`: `TargetsFile`/`TargetEntry` TOML at `~/.config/nimbus/targets` (via `dirs::global_config_dir`, XDG-honoring), atomic temp-file write mirroring credentials.rs DEP2; `nimbus target add/list/remove` subcommand wired in lib.rs (`Command::Target`); `resolve_named_target_url` reads name→URL then hands the URL to the existing URL-keyed credential lookup unchanged. Env fallback renamed everywhere: const `TARGET_URL_ENV="NIMBUS_TARGET_URL"`, source `EnvironmentTargetUrl`, cli_ux + docs (reference/cli.md, developers/cloud-functions/index.md, source-map.md) updated. run.rs's registry-stub error DELETED — NamedTarget now resolves. Tests: add_list_remove_round_trips, upsert_overwrites, add_rejects_{non_http,url_shaped,whitespace}, resolve_reads_configured_url, resolve_missing_name_names_file_and_add_command, target_subcommands_parse, banner_names_local_named_and_url_sources — all green (832/832). `git grep NIMBUS_DEPLOY_URL` clean across code + user docs; residual only in this plan's EX0.2 baseline inventory + XD4/EX1.5 rename callouts, justified like the EX2.1 demos/ residual. FOUNDATION REVIEW 2026-07-11: `dirs::global_config_dir` hardened to ignore empty/relative `XDG_CONFIG_HOME` per the XDG spec (a CI-set empty XDG previously wrote the registry under the CWD) + 3 unit tests (empty/relative/absolute); `run_target_command` refactored to an injectable path+writer (`run_target_command_at`) with a test asserting add/list/remove stdout and the on-disk `[target.<name>]`/`url = "…"` TOML shape; reference/cli.md sandbox synopsis rewritten to positional TARGET) |

### Band EX2 — Tree restructure (`demos/` → `examples/`)

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX2.1 | Rename `demos/` → `examples/` and update every reference from EX0.1: npm workspaces + scripts, Makefile overlay, server static route `/demos/` → `/examples/` + route tests, `index.html`, compose/container files | `git grep -n "demos/"` clean or each residual justified; route tests green; every `npm run *:example:*` command exercised | ~300 (mostly mechanical) | done(`git mv demos examples`; all EX0.1 refs updated — package.json workspaces+path-scripts, package-lock regenerated clean (0 demos/22 examples/0 extraneous), router.rs+metadata.rs+http/mod.rs route `/demos`→`/examples` + `examples_redirect`/`examples_dir`, operator policy `Examples` variant, cloud-functions reserved prefix, cli_ux+cli_surface help paths, build.mjs vendor path, vite.config/vanilla.html served paths, .gitattributes/.gitignore/.claude audit, verify-*.sh, source-map/server-transport/AGENTS docs, tests/demos.smoke.md→examples.smoke.md. Route test nimbus_demo_html_is_served serves `/examples/nimbus/html/` green. Residual `demos/` only in this plan's own rename text + plans-README entry. NOTE: `:demo:` script NAMES left to EX8.4; Makefile `convex-demo*` targets untouched (external upstream convex-demos overlay, not the in-repo tree). Verified: nimbus-server 455/0, nimbus-cli 823/0, operator 36/0, cloud-functions 33/0; npm typecheck + build green (vendor bundle regenerated at examples/convex/vendor); clippy clean. FIX ROUND 2026-07-11 (tsconfig self-containment): all ten `examples/**/tsconfig.json` decoupled from `../../../tsconfig.base.json` (copy-out hazard) into two self-contained flavors — browser (Bundler resolution, DOM lib) and Node (NodeNext resolution, `types: ["node"]`, no DOM), with a `tsconfig.smoke.json` split for four hybrid apps; `@types/node` + `engines: ">=22 <25"` added to the Node examples; README run-sections note the Node range. Root `tsconfig.base.json` untouched (`packages/*` still shares it). Surfaced and fixed two repo-wide root causes the NodeNext directive exposed: codegen's `_generated/{api,scheduled_functions}.ts` templates emitted extensionless `from "./dataModel"` (now `.js`-suffixed, `packages/codegen/src/emit/generated_files.mjs`), and `packages/codegen/src/parser/compile_bindings.mjs`'s known-import recognition suffix-matched import specifiers without stripping a `.js`/`.mjs`/`.jsx` extension, silently dropping `internalScheduledFunctions`/`api`/`internal` bindings for any NodeNext-flavored app — regression-tested (`testRuntimeOnlyMutationImportedScheduledFunctionsWithJsExtensionFixture`) and reproduced/fixed live via `nimbus/agent-chat`'s `agent-chat.schedule` smoke anchor. `npm run typecheck`/`build` green across every example workspace; full `make examples-verify` green except the pre-existing, local-only `firebase/tasks` Node v26 gap (this machine's Node dropped `--experimental-transform-types`; CI's Node 24 is in the supported `>=22 <25` range and unaffected)) |
| EX2.2 | Rewrite `examples/README.md` as the user-facing root nav index: explains each adapter surface, tables the examples, states the uniform run story (`nimbus dev`; `nimbus deploy [TARGET]`); internal parity/status notes moved out (to EX8.1 destination) | README contains no compiled-subset changelog notes; run story uses only the XD4 pattern | ~150 doc | done(examples/README.md rewritten: surfaces table (all six adapters + docs links), uniform run story `nimbus dev` then `nimbus deploy [TARGET]` (XD4 only), specs pointer, provisioning caveat. The 4B compiled-subset parity changelog moved to examples/convex/DEVNOTES.md (parked per task; EX8.1 owns final placement). No changelog notes remain in the README. AMEND (XD1 copy-out hazard): added a copy-out note pointing at the convex README's loud warning) |
| EX2.3 | Seed `examples/specs/` with `tasks.md` (schema, flows, observable assertions, per-adapter supported-subset table); flows are a named-anchor checklist (`tasks.create`, `tasks.live-update`, …) that smoke scripts reference by anchor name, so spec/smoke drift is mechanically detectable | Spec reviewed against each tier-1 adapter's real surface; every assertion has an anchor; smokes reference anchors by name | ~120 doc | done(examples/specs/tasks.md Flows section is now an anchor table: `tasks.create`/`tasks.list`/`tasks.toggle`/`tasks.delete`/`tasks.live-update`, one row per anchor with its observable assertion in the third column so every assertion has an anchor; anchors declared append-only. Supported-subset table keyed to the anchors (CRUD anchors + `tasks.live-update`; mongodb/dynamodb satisfy live by polling `tasks.list`, cloud-functions via `onDocumentCreated` after `tasks.create`). specs/README.md adds a "Flow anchors" section explaining the spec↔smoke contract and drift detection. convex/README.md subset table aligned to anchor headers. FOUNDATION REVIEW 2026-07-11 (prospective-vs-delivered honesty): tasks.md now opens with a "target state, not current coverage" note and labels its supported-subset table as the target the forthcoming apps/smokes meet; every adapter README `tasks` support section labeled "Planned, not yet in this directory" naming the current messages/chat demo; examples/README.md Surfaces section adds a "what exists today vs still being built" paragraph. No tasks apps built here — that is EX3) |
| EX2.4 | Per-directory `README.md` for every section: each `examples/<adapter>/` dir (what the surface is, its examples, supported subset, link to its docs page) and `examples/specs/` (what a spec is, how smokes consume it) | Every directory level under `examples/` has a README; adapter READMEs link to the matching `docs/developers/<adapter>/` page | ~60 doc/dir | done(READMEs added: examples/nimbus, examples/convex, examples/firebase, examples/mongodb (each: surface, apps, tasks-subset row, run story, link to docs/developers/<adapter>/index.md), examples/specs/README.md. examples/convex/showcase already had one (deploy cmd fixed to positional). cloud-functions/dynamodb dirs not yet created — EX3.5/EX3.6 create them with their apps+READMEs; root README tables them as coming soon. AMEND (XD1): examples/convex/README.md carries the loud "monorepo-only" copy-out warning naming the `convex` npm-name collision. FOUNDATION REVIEW 2026-07-11: over-claim corrected — only adapter-level READMEs existed; app-level READMEs were missing. Added examples/convex/{html,http,node}/README.md, examples/firebase/html/README.md, examples/mongodb/node/README.md, examples/nimbus/html/README.md (each: what the app shows today, run story, spec-subset pointer); every directory level under examples/ now has a README. Fixed examples/index.html <title> "Nimbus Demos"→"Nimbus Examples". Copy-out warning mechanism corrected (see EX2.2/XD1)) |

### Band EX3 — Tier-1 canonical app: `tasks` across all public adapter surfaces

Tier 1 covers every adapter surface documented under `docs/developers/`:
native, Convex, Firebase, Cloud Functions, MongoDB, DynamoDB. Promote/reshape
existing demos where sensible rather than writing parallel apps
(cloud-functions and dynamodb have no demo today and are written fresh);
parity-fixture behavior that is not user-worthy stays internal.

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX3.1 | `examples/nimbus/tasks` (from `demos/nimbus/html` or new): CRUD + live subscription via the native SDK | Runs against a `nimbus dev`-started server; smoke asserts spec flows; README complete | ~250 | done(commit 0af1cd45a. Native SDK app (`@nimbus/nimbus`) with WebSocket subscription; smoke.ts asserts all 5 anchors — tasks.create/list/toggle/delete/live-update — against a live `cargo run -p nimbus-bin -- start` server, real PASS output for each. `packages/nimbus/src/rest.ts` gained authenticated-WebSocket factory support so `NimbusSubscriptionClient` can attach the admin token during upgrade; smoke reads `NIMBUS_ADMIN_TOKEN` from env, never hardcoded. Root workspace registered, package-lock regenerated) |
| EX3.2 | `examples/convex/tasks` (from the React demo): clean tasks app authored via `convex/_generated/server` + schema | Same acceptance; parity-only exercises moved out of the user-facing app; README carries the loud copy-out warning (XD1: `convex` npm name collision — copied out of the monorepo, `npm install` silently substitutes the real Convex Cloud package; monorepo-only until EX7.1) | ~250 | done-with-exceptions(commit 2a894fccd + this closure commit. App built to spec: schema/functions/generated API, React/Vite UI, reactive headless smoke, README with the XD1 copy-out warning. **Gate + dev auto-provisioning verified live**: anonymous `ensureTenant()`/`tasks.create`/`tasks.list` PASS end-to-end against a real `nimbus dev` boot with zero `NIMBUS_CONVEX_*` env vars set — the #41 team-binding gate is no longer what blocks this app. **`toggle`/`remove`/`live-update` anchors blocked by three pre-existing runtime defects unrelated to this plan**, recorded as follow-ups below. **In-scope bug found+fixed**: `convex/tasks.ts`'s `toggle` handler cast `ctx.db.get(id) as Doc<\"tasks\"> | null`; Nimbus's Convex bundler does not strip TypeScript `as` casts before compiling handlers as runtime JS, so the cast produced `SyntaxError: Unexpected identifier 'as'` that broke the *entire* compiled bundle (every function, not just `toggle`) — fixed by dropping the redundant cast. **Live smoke, capped at 2 attempts**: attempt 1, standard `npm run smoke -w convex-tasks` (runs `npm run codegen` immediately before `smoke.ts`), failed verbatim at setup: `FAIL setup: nimbus request failed with 500` — this is defect #2 below. Attempt 2 worked around it by invoking `smoke.ts` directly against a fresh boot, skipping the redundant client codegen (the server's own boot preflight already generates `_generated/*`): `PASS tasks.create`, `PASS tasks.list`, then `FAIL tasks.toggle: nimbus request failed with 500` — this is defect #3 below, root-caused past the earlier attempt's generic `createApiError` fallback message down to the exact source line. `tasks.delete`/`tasks.live-update` remain untested — the script halts on the first failure, though `tasks.delete` is expected to hit the identical #3 root cause (see below). Net: 2/5 anchors (`tasks.create`, `tasks.list`) have real PASS evidence; `tasks.toggle` has a real, root-caused FAIL unrelated to this app or the #41 gate; 2/5 anchors blocked transitively. **Compose-sideline mechanism** (team-lead's option (d), executed and reverted at each server stop/restart this closure): `nimbus dev`'s compose-auto-discovery reads the local machine registry on boot, which hits an unrelated schema-version mismatch (`crates/nimbus-machine/src/image_source.rs::CURRENT_MACHINE_CONFIG_VERSION`, v1 on-disk vs v3 in this build) and refuses to boot with the repo's root `compose.yaml` present; worked around by `mv compose.yaml compose.yaml.smoke-bak` before booting, `mv` back after boot succeeds (restoring the file post-boot does not affect the already-running process — confirmed via a live HTTP probe after each restore). Pure boot-time workaround, not a fix; inert with respect to the smoke evidence above. **Flagged follow-ups** (none are #41-gate-related; all are new, pre-existing, repo-wide defects, first surfaced by this closure's live anonymous smoke): (1) **machine-config schema-version mismatch** [owner: `nimbus-machine`] — `nimbus dev` cannot boot against this repo's current root `compose.yaml` without the sideline workaround; severity: high (blocks any compose-using boot, not just this app); repro: boot `nimbus dev` from repo root with `compose.yaml` present. (2) **compose-auto-discovery-on-app-boot DX question** [owner: `nimbus-cli` `dev` command, product decision] — whether `nimbus dev` should attempt a machine-registry read at all for a plain example/app boot is a product DX question independent of fixing (1); severity: medium (workaround exists, but every app-only `nimbus dev` boot pays this cost). (3a) **non-deterministic boot-time bundle-hash race** [owner: `nimbus-convex`/`nimbus-runtime` bundle activation path] — the mutation-dispatch path can cache the wrong of two boot-time build passes' hashes (`\"running one-shot codegen preflight\"` then `\"generated app artifacts\"` are logged as two distinct passes), producing a deterministic-per-boot `runtime bundle integrity check failed: expected <hashA>, got <hashB>` on every mutation for that server's lifetime; severity: high but intermittent (not every boot hits it — one clean boot did not); repro: not reliably reproducible on demand, observed across multiple `nimbus dev` boots this closure. (3b) **codegen/live-server bundle race** [owner: `nimbus-cli` `dev` watch path, `crates/nimbus-cli/src/dev/watch.rs`] — running `npm run codegen`/`test`/`build`/`typecheck` (i.e. the client `convex codegen --app .` CLI) while a `nimbus dev` server for the same app is live silently overwrites that server's served `.nimbus/convex/bundle.mjs` with zero coordination; the server's watcher explicitly skips `.nimbus` as build output (`should_skip_watch_dir`) so it never notices or recovers, and serves the corrupted bundle to the next mutation. Severity: **severe** — fires on the exact command every developer runs (`npm run smoke`/`test`/`build` all invoke `codegen` first), 100% deterministic; repro: attempt 1 above, verbatim `FAIL setup: nimbus request failed with 500`, confirmed via bundle-hash before/after with zero corresponding watch-log lines. (3c) **`ctx.db.get`/`patch`/`delete` reject the combined Convex id-string calling convention** [owner: `nimbus-runtime`, `crates/nimbus-runtime/src/runtime/bootstrap/js/nimbus_context_contract.js:331-354` for `get` (analogous `patch`/`delete` at lines 368-386)] — severity: **severe, flagship-surface-breaking**: every Convex handler that calls `ctx.db.get(id)`, `ctx.db.patch(id, patch)`, or `ctx.db.delete(id)` with a single combined `\"table:id\"` string (the standard, only calling convention in real Convex and in every generated `Doc`/`Id` type) fails, because `nimbus_context_contract.js`'s host contract only accepts either two positional args `(table, id)` or one object arg `{table, id}` — a bare combined-string single arg falls through to `throw new Error(\"Nimbus runtime ctx.db.get currently requires table and id at runtime\")`. `patch`/`delete` share the identical two-positional-arg shape (`patch(table, id, patch)`, `delete(table, id)`) so `tasks.delete` (which calls `ctx.db.delete(id)`) is expected to hit the same failure, though not independently reproduced this session since the smoke script halts at `tasks.toggle`. This blocks any `v.id(...)`-typed mutation/query argument from working via `ctx.db.get`/`patch`/`delete` on every Convex app in the repo, not just this one — it is the single highest-severity finding of this closure. Live repro captured 2026-07-11 against a clean local `nimbus dev` boot (server left running for the investigator, see below), no auth token involved (anonymous Convex traffic): `curl -s -X POST http://localhost:8080/convex/demo/mutation -H \"Content-Type: application/json\" -d '{\"name\":\"tasks:toggle\",\"args\":{\"id\":\"tasks:01KX85T776PYMWKNN6AVT5FXFE\"}}'` → HTTP 500, `{\"error\":{\"code\":\"service.internal\",\"message\":\"internal error: convex runtime error: runtime JavaScript error: Error: Nimbus runtime ctx.db.get currently requires table and id at runtime (at tasks:40)\\n    at nimbusRemapHandlerError (...bundle.mjs:119:20)\\n    at ... executeMutationDefinition (...bundle.mjs:357:12)\\n    at async globalThis.__nimbusInvoke (...bundle.mjs:607:37)\", ...}}`. **Investigation handoff**: server intentionally left running for a dedicated specialist follow-up per team-lead's decision not to dig further into `nimbus-runtime` internals from this plan — `./target/debug/nimbus dev --app-dir examples/convex/tasks --port 8080 --data-dir /tmp/ex32-dev-data-final --no-open`, pid 12741, `http://localhost:8080/convex/demo`, no auth required for the Convex mutation/query surface itself. **RESOLUTION (2026-07-11, runtime-bridge follow-up session; handoff server pid 12741 stopped, compose.yaml restored, all work committed on this branch): all five smoke anchors now PASS live — `tasks.create`, `tasks.list`, `tasks.toggle`, `tasks.delete`, `tasks.live-update` — verified twice against fresh `nimbus dev` boots.** Three fixes: (i) **(3c) FIXED, and it was two defects layered.** First, the runtime ctx contract: `nimbus_context_contract.js` implemented internal `(table, id, …)` signatures while the public SDK contract (`packages/nimbus/src/server.ts:307-321`) is single table-scoped id. `delete(id)` bound the scoped id to `table` and sent `id: undefined`, which JSON drops, so serde deserialization of `RuntimeAsyncDbDeletePayload` (`crates/nimbus-runtime/src/host.rs:195`) surfaced `TypeError: missing field \`id\`` from the deno op layer (the `remove` flavor of the bug); `get(id)` hit the shim's own guard (the `toggle` flavor). NOT pre-handler arg deserialization — args arrived intact; the failure fired at the handler's first `ctx.db` call. Fix: arity-dispatched support for both conventions; single-id calls derive an advisory table from the scoped-id prefix and the server bridge re-validates it (`resolve_convex_document_id`), so the JS derivation is never trusted. Tests: `runtime_db_ops_accept_single_table_scoped_id_convention` + `runtime_db_single_id_ops_reject_ids_without_table_scope` (nimbus-runtime). Second, unmasked behind it: **compiled-plan subscription re-evaluation was lossy** — with a runtime bundle present every named subscription re-evaluates through the bundle (design intent, pinned by the `convex_runtime_*`/`uses_runtime_bundle_when_available` reactive-loop tests), but the generated bundle replayed compiled plans through the runtime query-builder ops, which cannot express index-backed ordering (`tasks:list`'s plan `order.field` from `withIndex("by_created_at")` was dropped → `ctx.db.query(...).order(...) requires withIndex(...) or filter(...)` on the first push). Fix in codegen emit (`packages/codegen/src/emit/runtime_bundle_query_helpers.mjs`): compiled query plans now replay wholesale through `op_nimbus_ctx_query` — the same `ConvexExecutableQuery` the native HTTP dispatch resolves plans into, read-recording intact — and the lossy builder-replay helpers are deleted. (A first attempt instead re-evaluated plan-backed subscriptions natively per-function; the smoke passed but it broke the four bundle-authoritative reactive-loop tests and was reverted for this design-faithful fix.) Also fixed en route: the no-bundle direct subscription leg delivered raw unscoped `_id`s over WS while HTTP delivers table-scoped ids — `forwarding.rs` now converts snapshot documents via `document_to_convex_json` and the planner's Get transform stores the scoped id it is compared against. (ii) **The "silent whole-bundle kill" from the `as`-cast saga is now loud at codegen time**: the bundle compiles every `runtime_handler` eagerly at module evaluation, so one handler with TypeScript-only syntax (extraction is verbatim; nothing is stripped) killed every function with no diagnostic. `packages/codegen/src/main.mjs` now parse-validates each extracted handler (same Function-constructor expression shape the preamble compiles, never invoked) and fails codegen naming function + module:line + the TypeScript-syntax caveat; negative selftest `testRuntimeHandlerWithTypeScriptSyntaxFailsLoudly`. Real type-stripping remains FSV6-9. (iii) **(3a) still open, root-cause notes**: within one `start` boot the preflight strictly precedes the registry load (`boot.rs:111`→`:130`), so the race is not there; but codegen writes `bundle.mjs` then `bundle.sha256` as two non-atomic writes (`packages/codegen/src/main.mjs`), so any reader interleaving them captures a mismatched pair, and `nimbus dev`'s watch/redetect loops run concurrently with server boot with only a comment (`dev/watch.rs:35`) asserting the preflight-first ordering. Sized fix (~80 LoC + tests): atomic bundle+hash pair (temp names + ordered rename, or fold hash into one manifest) and `RuntimeBundle` capturing bytes+hash from one read; content-addressed generation artifacts (FSV direction) subsume it. (3b) untouched. **Verification**: nimbus-runtime serial lane 432/0 (incl. the 2 new tests); nimbus-convex planner tests 3/0 (rewritten to pin bundle-authoritative reeval + no-bundle Identity); the 4 previously-red reactive-loop tests 4/0; full workspace nextest 4253 run / 4251 passed / 31 skipped with the only 2 failures (`nimbus-cli machine::tests::{startup_failures::machine_start_reports_oci_materialization_failure_for_unreachable_registry_image, records_state::machine_set_rejects_running_machine}`) reproduced IDENTICALLY on clean HEAD with all changes stashed — pre-existing/environmental on this laptop (a live krunkit `default` machine is running), not regressions; codegen selftests green (incl. new negative case); `cargo fmt --all --check` + clippy on touched crates clean; live smoke 5/5 twice. **Known-red inherited gate, NOT fixed here**: the branch's `npm run typecheck` fails at `examples/convex/tasks/convex/tasks.ts:45` (TS18048/TS2339) — pre-existing at HEAD since the `as`-cast removal; the generic `ctx.db.get` returns `{}`-typed values, and every typecheck-clean JS narrowing (e.g. `typeof`) hard-fails the compile-time planner (`unsupported compile-time resolver expression "TypeOfExpression"`) instead of falling back to a runtime handler, while inline annotations/casts are TypeScript-only syntax the verbatim extraction cannot carry (now rejected loudly by (ii)). Real fixes: typed generated `DataModel`-aware `ctx.db` in the compat package (right fix), or planner tolerance for JS narrowing forms; EX9.1 owns reconciling this gate**. **TYPECHECK GATE CLOSED (2026-07-11, app-level fix, this commit)**: `tasks.ts:45`'s `toggle` handler redesigned to the idiomatic-Convex shape — the client computes and passes the next value (`toggle: { id: v.id("tasks"), completed: v.boolean() }`) and the handler does a direct `ctx.db.patch(id, { completed })` with no `ctx.db.get`/narrowing at all, eliminating the `{}`-typed-return/narrowing conflict at its source instead of working around the planner or the compat package. `smoke.ts` and `src/App.tsx` (`handleToggle`) updated to match (the UI already holds the full `task` client-side from `useQuery`, so `!task.completed` is computed there); `convex/_generated/api.ts` and `.../scheduled_functions.ts` regenerated. `npm run typecheck -w convex-tasks` now clean (zero errors) and `npm run build -w convex-tasks` succeeds. **Live smoke re-verified end-to-end against a freshly rebuilt binary (incorporating the runtime-bridge resolution above) and a clean `nimbus dev` boot: real 5/5 PASS** — `PASS tasks.create`, `PASS tasks.list`, `PASS tasks.toggle`, `PASS tasks.delete`, `PASS tasks.live-update` — confirming the runtime fix carries `toggle`/`delete`/`live-update` once the app-level typecheck blocker is also cleared. The underlying product gap (DataModel-typed `ctx.db` in the compat package, or planner tolerance for JS narrowing) is deliberately left unfixed and recorded as EX9.1's follow-up per team-lead decision — this closure fixes the app, not the product gap. Remaining open, unrelated-to-this-app follow-ups, still not fixed: (1) machine-config schema-version mismatch (compose-sideline workaround still required for every `nimbus dev` boot), (2) compose-auto-discovery-on-app-boot DX question, (3a) non-deterministic boot-time bundle-hash race (intermittent, root-cause notes recorded above, ~80 LoC sized fix), (3b) codegen/live-server bundle race (severe, untouched). **REVIEW FOLLOW-UP (2026-07-11, review of c8db8f895):** The focused review found one HIGH regression introduced by the wholesale-plan-replay change and one MEDIUM test gap; both closed by `1774488d1`. **HIGH — null-get replay dropped reactive invalidation (fixed).** The wholesale replay sends compiled Get plans through `op_nimbus_ctx_query` carrying the protocol-facing table-scoped id; the pre-execution recorder (`crates/nimbus-server/src/adapters/convex/host_bridge/read_tracking/recording/queries.rs`) recorded it verbatim while commits intersect on raw storage ids (`crates/nimbus-core/src/dependency.rs:321-323`). A successful get re-recorded the resolved raw id from the result `_id`, but a null result recorded nothing, so a subscription over a get of an absent document was never invalidated by the write creating it — stuck on null. The pre-replay path did not have this defect (`op_nimbus_ctx_db_get` resolves before the bridge capability records, including absent reads — `crates/nimbus-bridge/src/capabilities.rs`). Fix: resolve the Get id before recording. Nuance verified: an absent TABLE records a missing-table dependency and self-heals on the first write to the table; the stuck case was "table exists, document absent". Tests: read-set-form unit tests (`crates/nimbus-server/src/adapters/convex/tests/read_tracking.rs`; captured red pre-fix with the scoped id in the read set) and the absent-to-visible reactive regression `convex_plan_replay_get_delivers_document_created_after_null_result` (`crates/nimbus-server/tests/reactive_loop/runtime_queries/get_and_query/get.rs`; red pre-fix on a 5s no-push timeout, green post-fix). **MEDIUM — HTTP/WS id-shape parity untested (fixed, corrected location).** The review cited `tests/reactive_loop/transport/subscription_pushes.rs:30`, but that suite exercises the native `/ws` transport where the table-scoped id contract does not apply; the parity test lives on the Convex socket instead: `convex_direct_get_subscription_push_id_matches_http_query_id` asserts the Convex HTTP named-query `_id` and Convex WS push `_id` for the same document are byte-identical and table-scoped. Verification: fmt clean; `make test-rust-runtime` 432/0; full workspace nextest 4257 run / 4257 passed / 31 skipped (zero failures — the two machine-env tests red in the prior run passed here); focused `cargo clippy -p nimbus-server --lib --tests` clean.) |
| EX3.3 | `examples/firebase/tasks` (from `demos/firebase/html`): stock `firebase/app`+`firebase/firestore` imports against Nimbus | Same acceptance; exercises live `onSnapshot` per spec | ~200 | done(commit 1f0c65423. Stock `firebase/firestore` app (addDoc/getDocs/updateDoc/deleteDoc) plus a real `onSnapshot` live subscription. smoke.ts asserts all 5 anchors against a live `nimbus dev` server (Node 22 required — Firestore protobuf sources use TS enums, needing `--experimental-transform-types`), real PASS output for each. Root workspace registered, package-lock regenerated) |
| EX3.4 | `examples/mongodb/tasks` (from `demos/mongodb/node`): stock driver CRUD; spec table marks the no-live-query subset honestly | Same acceptance for the supported subset | ~150 | done(commit 1ff54e1f4. Stock `mongodb` driver CRUD against a `tasks` collection via `@nimbus/mongodb`'s `mongoUri()`; `tasks.live-update` satisfied by genuine polling (200ms interval, asserts pollCount >= 2), documented honestly as not a live subscription. smoke.ts: all 5 anchors PASS against a live server with the MongoDB wire-protocol listener enabled. Root workspace registered, package-lock regenerated) |
| EX3.5 | `examples/dynamodb/tasks` (new): stock AWS SDK client via `packages/dynamodb` against Nimbus; spec table marks the supported subset | Same acceptance for the supported subset | ~150 | done(commit 3fec8140e. New `examples/dynamodb/` adapter directory + README (first one for this surface). Stock `@aws-sdk/client-dynamodb` + `@nimbus/dynamodb`'s `clientConfig()`; CreateTableCommand is idempotent (catches ResourceInUseException only), tables go ACTIVE synchronously. `tasks.live-update` satisfied by polling a Scan, documented honestly. smoke.ts: all 5 anchors PASS against a live server with the DynamoDB listener, run twice to confirm idempotent table creation on both runs. Root workspace registered, package-lock regenerated) |
| EX3.6 | `examples/cloud-functions/tasks` (new): `firebase-functions/v2` handlers on the tasks data — an HTTP/callable endpoint plus an `onDocumentCreated` Firestore trigger with durable retry (per `docs/developers/cloud-functions/`) | Smoke asserts the trigger's observable side effect (derived write lands after a task insert) and the HTTP handler response | ~200 | done(commit 83b9ea9d3. New `examples/cloud-functions/` adapter directory + README. Functions bundle (`nimbus init cloud-functions` layout): `taskDetails` (`onRequest` HTTP handler, directly curl-able) plus `deriveTask` (`onDocumentCreated("tasks/{taskId}")`, `retry: true`) writing an idempotent per-task derived document (deterministic id, so at-least-once redelivery overwrites rather than double-counts). `nimbus codegen` produced `.nimbus/firebase/{artifact.json,targets.json,bundle.mjs,bundle.sha256}`. smoke.ts against a live server: PASS tasks.create-triggers-derived-write, PASS cloud-functions.http-handler-response. Also fixed a real bug found in scope: `crates/nimbus-server/src/adapters/cloud_functions/http/tenant.rs` was counting the internal `_nimbus` system tenant as an application tenant, making any normal single-app-tenant deployment look ambiguous; fixed with a unit test, `cargo test -p nimbus-server --lib adapters::cloud_functions::http::tenant` 1/0, clippy/fmt clean) |
| EX3.7 | Unblock EX3.2: the #41 Convex team-binding gate (`crates/nimbus-convex/src/tenancy.rs`) is all-fail-closed and refuses every anonymous application-Convex request, including loopback local-dev traffic — no operator config admits an anonymous principal, and no CI job boots any application-Convex demo live, so this went uncaught since the gate landed (2026-06-29). Chosen fix (option 3, team-lead decision): dev-mode tenancy defaults, not a production posture change | Anonymous local Convex traffic works under `nimbus dev` with zero env config, and under plain `start` via an explicit opt-in env; production default fail-closed posture is provably unchanged (existing #41 tests untouched, all green); full workspace test suite green per the blast-radius rule (a fail-closed gate touch is repo-wide blast radius, not crate-scoped) | ~450 + tests | done(commits b26f29e52, d1fe82f38, 7a14e1090. **EX3.7a** `ConvexTenancyConfig` gains `anonymous_team: Option<TeamId>` + `with_anonymous_team` builder, consulted by `authorize_silo_selection` only for anonymous principals when explicitly set; default `None` preserves prior behavior exactly (16/16 tenancy tests, incl. new anonymous-passes-only-for-bound-silo + verified-principals-unaffected cases). **EX3.7b** `crates/nimbus-cli/src/start/adapters/convex_tenancy.rs` adds opt-in `NIMBUS_CONVEX_ANONYMOUS_TEAM` for `start`; any of the three `NIMBUS_CONVEX_*` envs present ingests wholesale (no merging); all-absent plain `start` still returns `(None, None)` — refuse-everything, byte-for-byte the pre-EX3.7 behavior. **EX3.7c** `nimbus dev` boot only (gated on `StartCommand.auto_tenant.is_some()`): when all three envs are absent, auto-provisions a `SiloTeamRegistry` binding the auto-tenant to a fixed process-internal dev team plus `with_anonymous_team` on that same team, and prints a loud boot notice naming the auto-bound silo and all three override envs; `AdapterEnablement::status_lines()` surfaces it. 34/34 nimbus-cli `start::adapters` tests, 16/16 nimbus-convex `tenancy` tests; `cargo fmt --all --check` and `cargo clippy -p nimbus-cli -p nimbus-convex --all-targets` clean. **EX3.7d** root `package.json`'s `convex:server:{node,html,http}` scripts (which use `start`, not `dev`) now set `NIMBUS_CONVEX_SILO_TEAMS=demo:demo-team NIMBUS_CONVEX_ANONYMOUS_TEAM=demo-team` explicitly, plus a new `convex:server:tasks` script for EX3.2's closure; live-verifying `convex:server:node` + `convex:demo:node` surfaced a second, pre-existing, structurally independent bug — `examples/convex/node/script.ts`'s `ensureTenant()` never sent `NIMBUS_ADMIN_TOKEN`, so its `POST /api/tenants` 401'd against the local-admin-router regardless of the tenancy fix (every sibling demo script already had this `Authorization: Bearer` pattern; node's was the one missing it) — fixed to match. Real live PASS recorded: `Initial messages: []` then `Inserted message: messages:01KX81E4E0X0J0Q1VJEXP5ZF2K` against a locally-booted server with the new envs. **Full workspace suite** (blast-radius rule, [[feedback_gate_blast_radius_full_suite]]) GREEN: naive `make test` (bare `cargo test --workspace`) was blocked twice by a genuine shared-machine disk shortfall ("No space left on device" during linking; team-lead cleaned 124.7GiB of regenerable artifacts from the main checkout's `target/`, `df -h /` 125Gi free), then once the disk was clear it still hit a known, pre-existing, non-regression flake in `nimbus-runtime` (SIGABRT signal 6 — parallel V8 isolate construction poisoning shared V8 cage/RO-heap state under libtest's default parallelism; the Makefile's own `test-rust-runtime` comments document exactly this hazard, and CI itself never runs `nimbus-runtime` under plain `cargo test --workspace` for this reason). Ran the CI-faithful decomposition instead (the same three Rust lanes `ci-required` composes): `make test-rust-runtime` (serial nimbus-runtime lane) — 8/8 lib tests passed, all doctests ok/appropriately-ignored, 0 failed; `make test-rust-workspace` (`cargo nextest run --workspace --exclude nimbus-runtime`) — 4250 tests run, 4250 passed, 0 failed, 31 skipped; `make test-rust-docs` (workspace doctests, excludes nimbus-runtime) — 39 doc-test suites, all `ok`, 0 failed. Genuine full-workspace green, real counts recorded above. **Discovered, deliberately unfixed follow-up**: `examples/convex/http/src/main.ts` has the identical missing-`NIMBUS_ADMIN_TOKEN` gap as node's script.ts had; only one working demo was required to close this band, so http's fix is left for whoever next touches that app. **Flagged, explicitly out-of-scope, adapter-owner decision**: whether a *production* Convex deployment needs an anonymous-public-functions posture (Convex Cloud allows unauthenticated public functions by default) is unresolved — this work deliberately does not build that; it only restores local-dev parity. If pursued, it is a `ConvexTenancyConfig` policy decision for the adapter owner, not an extension of the dev-mode defaults added here. FIX ROUND 2026-07-11: EX3.7d's `nimbus/agent-chat` dev-tenancy env wiring re-verified live under the tsconfig self-containment pass (see EX2.1) — `agent-chat.schedule` smoke anchor exercises this row's `NIMBUS_CONVEX_ANONYMOUS_TEAM`/`NIMBUS_CONVEX_SILO_TEAMS` boot envs end-to-end and now passes cleanly post-fix) |

### Band EX4 — Agent examples (scope fixed by EX0.3)

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX4.1 | `examples/nimbus/agent-chat`: web app where a durable agent answers with tool calls, persists memory in the DB, and schedules a follow-up via the scheduler — using only surfaces EX0.3 confirmed | Smoke asserts an observable agent behavior (e.g., scheduled follow-up message lands); README explains the sovereignty angle (agent runs inside your trust boundary) | ~350 | done(commit c8fc47537. First app in the repo to use a native `nimbus/` source root end to end (`examples/nimbus/tasks` predates it but never exercised codegen's `@nimbus/nimbus/server` type-alias path or a scheduled internal mutation). Handlers designed to avoid the recorded `ctx.db`-read `unknown`-typing product gap (EX3.2) entirely rather than work around it: flat single-fact `agentMemory` schema, `facts.length` (array-level, safe) instead of element-level field access for the recall count, all per-field formatting of DB reads pushed client-side where generated `Doc<TableName>` types are sound. Spec: `examples/specs/agent-chat.md`, 4 named anchors (`agent-chat.converse`/`remember`/`recall`/`schedule`). **Two real product bugs found and fixed, both because this is the first app to combine a `nimbus/` source root with these particular codegen/runtime paths**: (1) `packages/codegen/src/emit/generated_files.mjs` unconditionally emits `export type { ActionCtx, MutationCtx, QueryCtx } from "${packageNamespace}/server"` into every generated `_generated/server.ts`; `packages/nimbus/src/server.ts` only had the `Generic*Ctx` names (unlike `packages/convex/src/server.ts`, which already had all three aliases) — added the three aliases plus the matching CB0 capability-surface-contract manifest entries (`npm run test -w @nimbus/nimbus` green, incl. the manifest lock). (2) `ctx.scheduler.runAfter` requires its target mutation to compile to a single, statically-analyzable plan (`crates/nimbus-convex/src/registry/resolution/functions/writes.rs::resolve_scheduled_mutation_for_visibility` deserializes `definition.plan` into `nimbus_core::Mutation`, an `Insert`/`Update`/`Delete`-only enum — a `null` plan, produced whenever a handler's compiled body isn't exactly one `ctx.db` op whose result is returned directly per `packages/codegen/src/parser/function_parsers.mjs`, fails deserialization with `invalid type: null, expected internally tagged enum Mutation`); confirmed live via direct HTTP repro before the fix. `examples/convex/http`'s `sendInternal` never tripped this because its scheduled target was already a pure single-insert pass-through; `deliverReminder` initially wasn't (multi-field literal insert with `return null`). Fixed at the app level, not the product: `deliverReminder` now does exactly one `ctx.db.insert` and returns its result directly, with the reminder-text formatting and delivery-time `createdAt` (`Date.now() + delayMs`) computed by the caller (`send`, an unrestricted runtime-handler mutation) and passed in as plain args — deliberately not baking `Date.now()` into the scheduled handler itself, since the plan-compiler literally executes handler bodies at codegen time and would have frozen a stale codegen-time timestamp into the static plan forever. **Live verification, real 4/4 PASS** against a freshly built `cargo run -p nimbus-bin -- start --port 8080 --app-dir ./examples/nimbus/agent-chat` (not `nimbus dev` — see note below) with `NIMBUS_CONVEX_SILO_TEAMS=demo:demo-team NIMBUS_CONVEX_ANONYMOUS_TEAM=demo-team`: `PASS agent-chat.converse`, `PASS agent-chat.remember`, `PASS agent-chat.recall`, `PASS agent-chat.schedule` (the last via a real `NimbusClient.onUpdate` WebSocket push proving the scheduled follow-up lands server-side with zero client polling). `npm run typecheck -w nimbus-agent-chat` and `npm run build -w nimbus-agent-chat` both clean. **`nimbus dev` vs `start` for smoke**: an initial `nimbus dev` boot hit a `runtime bundle integrity check failed` 500 on the very first mutation — running `npm run smoke` (which chains `npm run codegen &&`) against an already-running `nimbus dev` server races the server's own watcher-triggered codegen against a second, independent `nimbus-codegen --app .` invocation, producing two different bundle hashes for the same path; switched to the `start` subcommand (matching the existing `convex:server:tasks`/`http` precedent, which has no competing watcher) and the race disappeared. Not a fix, a mechanism note for future smoke runs against `nimbus/`- or `convex/`-rooted apps. Compose-sideline recipe applied and restored byte-identical; the `nimbus dev`/`start` boot-time `package.json`/`package-lock.json` `file:./.nimbus/packages/...` dependency rewiring was reverted after each boot (`git status` clean on both before commit). Root workspace registered (`examples/nimbus/agent-chat`), `nimbus:server:agent-chat`/`nimbus:demo:agent-chat` scripts added. **FIX ROUND (2026-07-11, tail-review finding 1, MEDIUM):** the README's tenant-provisioning caveat covered the unauthenticated `POST /api/tenants` convenience but said nothing about the app's functions themselves having no auth. Added an explicit no-auth paragraph to `examples/nimbus/agent-chat/README.md`: `list`/`listMemory`/`send` (`nimbus/agent.ts`) are public functions taking a plain `conversationId` string with no identity/ownership check, by design — the UI hardcodes one shared id (`CONVERSATION_ID` in `src/App.tsx`) — stating plainly that identity/ownership checks are required before deploying beyond your own machine, linking `../../../docs/developers/auth.md`. Mirrored into `docs/agents/agent-chat.md` (linking `/developers/auth/`, the docs-site route form). Evidence: `bash scripts/check-docs.sh` → `PASS — 99 pages link-clean, source map resolves, private fence intact, titles unique`) |
| EX4.2 | `examples/nimbus/agent-worker`: headless autonomous agent workload (run/sandbox surface) with scheduling; demonstrates egress policy only if EX0.3 confirms a public knob | Smoke asserts the worker's observable side effects; no fabricated APIs | ~300 | done(no UI: `runWorker` (unrestricted mutation) schedules a fixed-cadence batch of `processJob` hops via `ctx.scheduler.runAfter`, one per job id, staggered by `intervalMs`; every job goes `pending` → `done` entirely server-side, no polling. EX0.3 confirmed no public cron API and no sandbox/`run exec`/egress-policy surface, so the workload story is scheduler + DB only — the honest subset, not the full "run/sandbox workload" framing in this row's original scope text. `runWorker` never re-queries "pending" jobs and reads `._id` off an untyped `ctx.db` result (the recorded EX3.2 gap); it takes `jobIds: v.array(v.id("jobs"))` as an explicit validator-typed argument instead, with real ids threaded through from `enqueue`'s return value by the caller. **A second real product bug found and fixed** (distinct from EX4.1's two): every client-facing document id is table-scoped (`"table:rawId"`, `ResolvedDocumentId::resolve_table_scoped` in `crates/nimbus-core/src/types.rs`), while the engine stores/looks up documents by their bare id. Direct `ctx.db.get`/`patch`/`delete` calls always un-scope first via `resolve_convex_document_id` (`crates/nimbus-server/src/adapters/convex/host_bridge/db_ops/documents.rs`), but `resolve_scheduled_mutation_for_visibility` (`crates/nimbus-convex/src/registry/resolution/functions/writes.rs`) — the path a fired `ctx.scheduler.runAfter` timer takes — went straight from raw `resolve_template` JSON substitution to `serde_json::from_value::<Mutation>` with no un-scoping step at all, so any `v.id("table")`-typed arg threaded through the scheduler into an Update/Delete/Insert-with-id target built a `DocumentId` that still carried its "table:" prefix and could never match a stored document. Failed silently to the caller (`ctx.scheduler.runAfter` itself returned success immediately); only visible as a server-side `WARN nimbus_engine::scheduler: scheduled job failed ... error=document not found: jobs:<id>` log line, confirmed with both 0ms and 2000ms delays (ruling out a race) via direct HTTP repro before the fix. Fixed in product code, not worked around at the app level: added `resolve_scheduled_mutation_document_ids` in `writes.rs`, routing the deserialized `Mutation`'s `id` field(s) through `resolve_convex_document_id` for `Insert{id:Some}`/`Update`/`Delete`, mirroring the runtime host bridge's existing behavior exactly; `cargo check -p nimbus-convex` clean. **Also found (codegen, worked around at the app level, not a product bug)**: `packages/codegen/src/compile_time_interpreter.mjs`'s `evaluateStatement` only supports `Block`/`Return`/`ExpressionStatement`/`VariableStatement`/`IfStatement`; any other statement kind (e.g. a `for` loop) anywhere in an exported handler's traced body is a hard, non-fallback-eligible codegen failure that kills the whole app's codegen run (`packages/codegen/src/parser/function_parsers.mjs::canFallbackToRuntimeHandler` only matches 3 specific runtime-fallback message substrings, and the interpreter's unsupported-statement message isn't one of them) — this holds even for a handler, like `runWorker`, that was never intended to plan-compile. `runWorker`'s original `for` loop over `jobIds` was rewritten as `Promise.all(jobIds.map(...))`, which fails differently (a plain "not a function" TypeError on the args-proxy object, fallback-eligible) during interpretation, letting codegen fall back to a runtime handler for that function as intended. **Live verification, real 2/2 PASS** against a rebuilt `cargo run -p nimbus-bin -- start --port 8080 --app-dir ./examples/nimbus/agent-worker` (post-fix binary) with `NIMBUS_CONVEX_SILO_TEAMS=demo:demo-team NIMBUS_CONVEX_ANONYMOUS_TEAM=demo-team`: `PASS agent-worker.enqueue`, `PASS agent-worker.schedule` (the latter via a real `NimbusClient.onUpdate` WebSocket push proving every scheduled job reaches `done` with zero client polling beyond the initial `runWorker` call); server log showed zero `document not found`/scheduler-failure warnings on this run, versus reproducible failures on every job pre-fix. `run.ts` (the headless run-story driver) also verified live end to end (`exit=0`, all jobs done); its own completion-tracking bug — waiting for *all* rows in the shared `jobs` table rather than just the ids it enqueued that run, which hung against stale `pending` rows left over from pre-fix debugging — was fixed alongside it to track only its own `jobIds`, matching `smoke.ts`'s pattern. `npm run typecheck` (bare `tsc -p tsconfig.json --noEmit`) clean. Compose-sideline recipe applied and restored byte-identical; boot-time `package.json` `file:./.nimbus/packages/...` rewrite reverted to `"*"`, `.nimbus/` removed, `npm install` re-run clean (809 packages, no drift). Root workspace registered (`examples/nimbus/agent-worker`), `nimbus:server:agent-worker`/`nimbus:demo:agent-worker` scripts added) |

### Band EX5 — Smoke verification lane

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX5.1 | Headless smoke script per example (seed → exercise spec flows → assert observable behavior), deterministic, runnable by one command per example; at least one smoke runs the real `nimbus run` binary with stdout and stderr captured separately and asserts the stream contract (stdout alone parses as the result JSON; the resolved-target banner appears on stderr) — the process-level guard the 2026-07-11 review noted unit tests cannot provide | Each smoke red/green demonstrated locally at least once; the stdio-contract assertion present and green | ~80/example | done(new `scripts/examples-verify.sh`, one pipe-delimited manifest entry per app covering all 8 examples; each app's own existing smoke is the per-app assertion, boots on a fresh `--data-dir` and stops the server before the next app. `check_run_stdio_contract()` (convex/tasks only) spawns the real `nimbus run <TARGET-URL> functions tasks:list` binary with stdout/stderr captured to separate files: asserts exit 0, stdout alone parses via `node -e JSON.parse(...)`, stderr contains `"Running against"`, and the banner does NOT leak into stdout. Required routing through an explicit TARGET URL rather than omitted-TARGET local dispatch: `LocalServerHttpClient::request()` unconditionally attaches the local admin bearer, which the Convex-style auth verifier 401s for any app with no `convex/auth.config.ts` (every example) — passing a TARGET URL routes through `invoke_remote_run_function` instead, which sends no Authorization header and succeeds anonymously under the dev-mode `NIMBUS_CONVEX_ANONYMOUS_TEAM` bypass. No `crates/**` change; workaround only, confirmed live. Red/green demonstrated per app: convex/tasks stdio-contract genuinely failed (401) before the TARGET-URL fix, green after; firebase/tasks and cloud-functions/tasks both genuinely failed under `nimbus start` (Firestore's #24 admission gate, `crates/nimbus-firebase/src/project_tenant_registry.rs`, refuses any principal without a verified project claim — anonymous and `start`-issued bearers both fail it) and went green only after switching their manifest `boot_mode` to `dev`, which is the only local path that reaches the Firebase emulator-token-verification bypass (matches both apps' own README-documented `nimbus dev` run instructions); the remaining 5 apps (nimbus/tasks, nimbus/agent-chat, nimbus/agent-worker, mongodb/tasks, dynamodb/tasks) passed their existing smokes unchanged. All 8 confirmed PASS in one full sequential run. **FIX ROUND (2026-07-11, tail-review finding 2, MEDIUM):** see EX5.2 evidence for the port-hazard fix and full re-verification; every smoke's URL env var is now threaded from a per-run ephemeral port rather than assuming the old fixed 8080 default) |
| EX5.2 | `make examples-verify` (single-flight wrapped) that boots a fresh local server and runs all smokes; wire into `make ci` or a dedicated required lane per EX0 findings | Target green locally; wiring decision recorded with reason | ~120 | done(`Makefile` target `examples-verify: $(SINGLE_FLIGHT) --key examples-verify -- bash scripts/examples-verify.sh`, added to `.PHONY`; `make examples-verify` runs all 8 apps sequentially and is green locally (`==> all examples verified`). Wiring decision recorded inline in the Makefile comment: deliberately NOT folded into `ci-required`/`ci` — it boots eight Nimbus servers sequentially (each paying its own health-check wait plus, for four apps, a codegen preflight), so its wall-clock cost and port/data-dir contention risk don't belong on `ci`'s fast-feedback critical path; it runs as its own dedicated CI lane instead (EX5.3). **FIX ROUND (2026-07-11, tail-review findings 2-4, this commit):** three hardening fixes to `scripts/examples-verify.sh`, all re-verified together. **(2, MEDIUM) Port hazard:** `PORT` was hardcoded to 8080, so a pre-existing unrelated local server already answering `/health` on 8080 could silently green-light a binary the lane never actually booted. `PORT` now binds an OS-assigned ephemeral port per run (`python3 -c 'socket.socket().bind(("127.0.0.1", 0))'`, same pattern as `scripts/nimbus-kv-conformance.sh`), still overridable via `NIMBUS_EXAMPLES_VERIFY_PORT`. Threaded the resolved `NIMBUS_URL` through every affected smoke's URL env var (`NIMBUS_NATIVE_URL`/`NIMBUS_FIRESTORE_URL`/`NIMBUS_CLOUD_FUNCTIONS_URL`) in the manifest's `smoke_env` field — each smoke.ts's own `localhost:8080` fallback only matched by coincidence when PORT was fixed; mongodb/tasks and dynamodb/tasks are unaffected (separate wire-protocol listener ports, 27017/8000, independent of the main HTTP PORT). `wait_for_health()` hardened to check the launched pid's liveness both before each curl attempt (clear early-exit message) and immediately after a successful curl, failing loudly ("a different process answered /health on this port, not the binary under test") if the pid is already gone by then — the direct guard against the TOCTOU/pre-existing-server hazard. **(3, MEDIUM) Compose-sideline stranding:** a SIGKILLed run bypasses the EXIT trap and can leave `compose.yaml.smoke-bak` on disk with `compose.yaml` missing, silently changing every subsequent `nimbus dev`/`compose` invocation in the repo. Added `heal_stranded_compose_sideline()`, called at lane start before `ensure_nimbus_binary`: if `compose.yaml.smoke-bak` exists and `compose.yaml` doesn't, restores it and logs that it did. The EXIT trap's own `restore_compose()` call is unchanged. The real fix is the recorded product follow-up (an opt-out so `nimbus dev` skips Compose auto-discovery for a plain example boot, "compose-auto-discovery-on-app-boot DX question" above) — this is lane-local recovery, not that fix; linked via a script comment. **(4, LOW) ONLY-selector false-green:** an unmatched `NIMBUS_EXAMPLES_VERIFY_ONLY` value used to fall through the manifest loop zero times and exit 0 from the script's last statement, reporting "all examples verified" without booting anything. Now tracks whether `ONLY` matched any manifest entry and, if not, fails with the value and a list of valid names. Verification: (a) induced-red — `NIMBUS_EXAMPLES_VERIFY_ONLY=nimbus/typo-does-not-exist bash scripts/examples-verify.sh` → exit 1, error lists all 8 valid names, confirmed booting nothing; (b) same command against a manually-stranded `compose.yaml.smoke-bak` (compose.yaml removed first) → self-heal log line fires before the ONLY-selector error, `git status --short compose.yaml` clean after, confirming the heal is byte-identical restore; (c) full lane, all three fixes together: `make examples-verify` real 8/8 apps PASS (`nimbus/tasks`, `nimbus/agent-chat`, `nimbus/agent-worker`, `convex/tasks` incl. stdio-contract, `firebase/tasks`, `mongodb/tasks`, `dynamodb/tasks`, `cloud-functions/tasks`), `==> all examples verified`, exit 0, run against the CI-matching Node 24 toolchain (`lts/*`/explicit 22/24 per `.github/workflows/ci.yml`; this machine's Homebrew default `node` is 26.0.0, which removed `--experimental-transform-types` outright with no replacement — a real, unrelated Node-version regression confirmed via direct repro, not a defect in this fix round or a regression from these changes, worked around locally by putting `node@24`'s bin first on `PATH` for the verification run only; no repo file depends on the local default `node` version). All boot-time `package.json`/`package-lock.json` `file:./.nimbus/packages/...` dependency rewiring from these runs reverted (`git checkout --`) before commit; `.nimbus/` build artifacts are gitignored and untouched) |
| EX5.3 | CI lane for examples-verify; prove fail-closed once by inducing a breakage locally and observing red, then reverting | Induced-red evidence recorded; lane green on branch; full-suite run for the gate addition (blast-radius rule) | ~60 CI | done(new `examples-verify` job in `.github/workflows/ci.yml`, `needs: [nimbus-bin-build]`, downloads the prebuilt `nimbus-bin` artifact (`chmod +x`), sets up Node (`lts/*`, matching the `js` job), `npm ci`, then `NIMBUS_EXAMPLES_VERIFY_BIN=.../target/ci-archive/nimbus make examples-verify` — standalone lane, not folded into `rust-gate-summary` (that summary aggregates only Rust/crates-owned lanes; `js`/`proof-helpers`/`harness*` are likewise independent required checks outside it), matching the EX5.2 wiring decision. YAML validated via `python3 -c "yaml.safe_load(...)"`. Induced-red proof: temporarily changed `check_run_stdio_contract()`'s stderr grep pattern to a string that can never appear; ran `NIMBUS_EXAMPLES_VERIFY_ONLY=convex/tasks bash scripts/examples-verify.sh`, observed `FAIL stdio-contract: resolved-target banner missing from stderr` and exit 1 (the real banner was present in the captured stderr the whole time, proving the check itself was broken, not the underlying behavior); reverted the one-line change, reran the same command, observed `PASS stdio-contract` and exit 0. Blast-radius (gate-addition rule): full JS suite run clean on every workspace — `npm run typecheck` exit 0, `npm run build --workspaces --if-present` exit 0, `npm run test --workspaces --if-present` exit 0, `npm run lint:capability-boundary` exit 0 (49 files, no fixes); `make -n ci-required` dry-run clean (Makefile syntax intact, no existing target disturbed). No `crates/**` changes in this band, so no Rust-suite blast radius applies. Every boot-time `package.json`/`package-lock.json` codegen-preflight rewrite from these runs was reverted (`git checkout --` + stray `.nimbus-codegen-*` cleanup) before each commit) |

### Band EX6 — Public docs

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX6.1 | Example pages per EX0.4 list: Developers group per adapter + Agents group for agent examples; each explains the example and links to its GitHub source path on `nimbus/nimbus`; one Diátaxis mode per page | Pages pass docs skill rules; every behavior claim source-verified | ~100 doc/page | done(eight pages covering the full EX0.4 list, each authored against the app's committed README read this session. Five new how-to pages `docs/developers/{convex,firebase,cloud-functions,dynamodb,native}/examples.md`; the sixth adapter, mongodb, extended its existing `docs/developers/mongodb/examples.md` with a "Runnable example apps" section (EX0.4's "extend existing" call) rather than a second page. Two new Agents-group pages: `docs/agents/agent-chat.md` (tutorial — guided run of the durable chat agent through converse/remember/recall/schedule) and `docs/agents/agent-worker.md` (how-to). One Diátaxis mode each: the six adapter example pages and agent-worker are how-to, agent-chat is tutorial. Every page links its apps to their GitHub source path (`https://github.com/nimbus/nimbus/tree/main/examples/<adapter>/<app>`; the `tasks` spec to its `blob/main/examples/specs/tasks.md`). Uniform run story on every page: `nimbus dev` then `nimbus deploy [TARGET]`, no node/cluster vocabulary. Convex page carries the copy-out caveat with the accurate mechanism (`"convex": "*"` → official package, loud `convex codegen --app .` failure, no silent Convex Cloud traffic), consistent with `examples/convex/README.md`. Honest subset caveats mirrored from the READMEs: mongodb + dynamodb `tasks.live-update` polled (change streams / no live view), cloud-functions framed as trigger + HTTP handler not a CRUD client with at-least-once idempotency. Docs skill rules met: one mode/page, source-available (no "open source"), Markdown only, no `docs/private` links, no `make verify-*`/plan/ledger vocabulary in the public pages. **FIX ROUND (2026-07-11, tail-review finding 5, LOW):** `docs/developers/convex/examples.md`'s copy-out section blanketly listed Showcase among apps whose `"convex": "*"` resolves to the official npm package on copy-out; Showcase actually pins `"convex": "file:./.nimbus/packages/convex"` (a workspace-relative local-file dependency, confirmed via its committed `package.json`), so its real copy-out failure is a missing local-path dependency-resolution error, not a silent Convex Cloud swap. Scoped the `"convex": "*"` claim to React/browser/Node/Tasks and added a paragraph qualifying Showcase separately. Same defect found and fixed in `examples/convex/README.md`'s matching warning banner (in-scope spillover, same claim, same fix). Also added explicit no-auth framing to `docs/agents/agent-chat.md` (tail-review finding 1, MEDIUM) mirroring the existing tenant-provisioning caveat: `list`/`listMemory`/`send` are public with no identity/ownership check by design, linking `/developers/auth/`. Evidence: `bash scripts/check-docs.sh` → `PASS — 99 pages link-clean, source map resolves, private fence intact, titles unique`) |
| EX6.2 | Sidebar entries (`website/astro.config.mjs`), `docs/source-map.md` rows, natural cross-links (quickstart → tasks example) | Sidebar renders; source-map rows added for every new/changed page | ~50 | done(no `astro.config.mjs` edit required: every target directory (`developers/{convex,firebase,cloud-functions,mongodb,dynamodb,native}` and `agents`) is already an autogenerated sidebar group (`:82-91`), so a page dropped in appears automatically ordered by frontmatter `sidebar.order` — examples pages `order: 2`/`3` (right after Overview/Migrate), agent-chat `order: 6`, agent-worker `order: 7` after the sandbox/service/session resource pages. Verified by the astro build rendering all 7 new pages into the sidebar (100 pages total). `docs/source-map.md`: 8 rows added, one per new/extended page, each citing the app directory plus the SDK package/crate that backs the behavior claim (all cited paths confirmed on disk before writing; `check-docs.sh` re-validates them). Cross-links: `get-started/quickstart.md` Next-steps → Convex example apps; `developers/index.md` adapter list → each adapter's examples page; each adapter `index.md` Next-steps/Where-next → its examples page; `agents/index.md` new "Example agents" section → agent-chat + agent-worker; `developers/native/examples.md` → both agent pages. All internal links resolve (check-docs link check clean)) |
| EX6.3 | Docs gates green: `bash scripts/check-docs.sh` and `bash scripts/verify-nimbus-docs-site.sh` | Both gate outputs recorded | n/a | done(`bash scripts/check-docs.sh` → `PASS — 99 pages link-clean, source map resolves, private fence intact, titles unique` (was 92 before this band; +7 pages). `npm --prefix website run build` → `100 page(s) built`, all 7 new pages emitted to `website/dist`, `llms.txt`/`llms-full.txt`/`llms-small.txt` all emitted (agent-chat content present in llms-full). `bash scripts/verify-nimbus-docs-site.sh` → 15/17 after the build; the 2 remaining failures are pre-existing and environmental, unrelated to EX6: condition 5 and condition 16 both flag untracked local-only top-level dirs (`docs/architecture`, `docs/code-review`, `docs/plans`) that this worktree has not relocated under `docs/private` — none touched by this band, none in the EX6 diff. The third of the three pre-existing failures the team-lead noted (condition 6, "website build emits dist/llms artifacts") was purely the un-built `website/dist` on this laptop and flipped to PASS once the site was built. STALE-DOC DEBT (EX0.4): the `--url` references it flagged (`get-started/deploy.md`, `developers/cloud-functions/{index,migrate}.md`, `reference/cli.md`, `source-map.md`) were already rewritten to positional `TARGET` in EX1/EX1.5/the foundation review; re-swept `docs/` + `website/src` this session — zero remaining `nimbus deploy/run --url` forms (the only `--target` hits are the encryption/backup-restore commands, a different flag that legitimately keeps it)) |

### Band EX7 — Nice-to-haves (each row ends `done` or `decision-recorded(reason)`)

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX7.1 | `nimbus init --example <adapter>/<app>` scaffolder sourcing from the monorepo tree; its core job is rewriting workspace deps to published pins (this is what closes the XD1 copy-out hazard, including the `convex` name collision) | Scaffolded app `npm install`s cleanly outside the repo and runs its smoke; or decision recorded | ~250 | decision-recorded(deferred to a fast-follow PR — the scaffolder is the copy-out closure mechanism (rewrites workspace deps, including `convex`, to published pins) and is a large enough surface, with its own install/publish-pin correctness risk, to deserve a dedicated review rather than riding this plan's closeout. Until it ships, `examples/convex/*` stays monorepo-only per the loud copy-out warning already in `examples/convex/README.md` (XD1); the other adapter directories degrade safely without it (unpublished `@nimbus/*` workspace deps fail installation loudly, and Firebase's stock dependency just talks to a Nimbus server as documented in `examples/README.md`). Trigger: scaffolder PR ships, or `nimbus init` grows an `--example` subcommand shape worth building against) |
| EX7.2 | `examples/s3/filedrop` (blob plane + S3 surface, upload URLs) | Spec + smoke; or decision recorded | ~250 | decision-recorded(deferred — not required for EX3's tier-1 adapter-surface coverage or EX4's agent-surface coverage, and no existing spec/smoke infrastructure targets the S3 surface yet. Trigger: a public docs page for the S3/blob surface lands (mirrors EX7.7's KV/Cloudflare trigger), or a developer-facing request for an upload-URL walkthrough) |
| EX7.3 | `examples/nimbus/jobs` (cron/scheduled functions — the old "planned next demos") | Spec + smoke; or decision recorded | ~200 | decision-recorded(partially superseded — `examples/nimbus/agent-worker` (EX4.2) already demonstrates fixed-cadence, multi-step scheduled work via `ctx.scheduler.runAfter`, which is as much of "jobs" as the public surface supports today (EX0.3 confirmed there is no public cron API; see agent-worker's README "No self-chaining scheduled functions" section for the exact constraint). A dedicated `jobs` example would only add value once a public cron/recurring-schedule API exists to demonstrate — until then it would just be agent-worker's pattern under a different name. Trigger: a public cron/recurring-scheduling API ships) |
| EX7.4 | Docs pages generated from example READMEs (NATS-by-example style) instead of hand-written | Generation wired into docs gates; or decision recorded | ~200 | decision-recorded(deferred — EX6 ships hand-written docs pages first so each page's Diátaxis mode, source-map row, and behavior claims are authored and source-verified individually; generation is a follow-up once there is a stable corpus of README shapes worth mechanizing over. Trigger: EX6 ships and a second adapter-surface docs pass is scheduled, making the hand-written maintenance cost worth automating) |
| EX7.5 | `nimbus/examples` mirror repo as automated one-way publish with released pins | Default decision: defer until launch; record it (with trigger condition) unless implemented | n/a | decision-recorded(deferred per the plan's own default — a one-way mirror publish is a release-infrastructure concern that depends on the scaffolder (EX7.1) for correct published-pin rewriting, and there is no launch date yet to publish against. Trigger: launch) |
| EX7.6 | `chat` spec + one implementation (subscriptions, presence, pagination) | Spec + smoke; or decision recorded | ~300 | decision-recorded(deferred — every adapter's existing per-surface demo (convex html/http/node's `messages` collection, in particular) already exercises live inserts, subscriptions, and paginated queries; a dedicated `chat` spec would mostly restate that coverage under presence semantics with no SDK-level presence primitive to build against today. Trigger: a presence primitive (or a documented convention for building one) lands, making a dedicated spec more than a `tasks`-style CRUD-plus-subscription restatement) |
| EX7.7 | Not-yet-public surfaces (KV/RESP, Cloudflare adapters): record the decision for when each gets an examples directory (trigger = its public docs page landing) | Decision recorded per surface with trigger condition | n/a | decision-recorded(deferred for both — `nimbus-kv` (RESP surface) and the Cloudflare adapters (Workers/DO/R2) have no public docs page under `docs/developers/` yet (see EX0.4's page inventory), so an examples directory would document a surface with no canonical reference to link back to. Trigger, per surface: KV/RESP gets `examples/kv/` once its public docs page lands; Cloudflare adapters get `examples/cloudflare/` once their public docs page lands. Each is independent — one landing does not gate the other) |
| EX7.8 | Convex runtimes showcase (owner-requested 2026-07-11): a `"use node"` action example in the convex examples tree demonstrating the two-runtime story (default V8 vs node-compat action module), plus a runtime web-API parity audit (streams/crypto/performance/seeded-randomness determinism semantics vs the Convex runtimes doc) | Example + smoke, and a parity matrix recorded in the compatibility reference; or decision recorded | ~250 + audit | decision-recorded(defer to the scaffolder fast-follow window: docs claims corrected this branch — migrate.md no longer overclaims convex.json; "use node" support is real (codegen parser + node-compat runtime + node canaries) but has no example showcasing it, and the API-by-API parity matrix is unaudited; trigger = first post-merge examples iteration) |

### Band EX8 — Cleanup

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX8.1 | Move demo-era parity/status notes (the 4B compiled-subset changelog in `demos/README.md`) to an internal home (`docs/private/` note or per-dir DEVNOTES) or delete if stale | User-facing README free of internal status; internal home linked from the owning adapter code area | ~50 | done(already landed in EX2.2: the 4B compiled/runtime-subset changelog moved out of the (now-deleted) `demos/README.md` into `examples/convex/DEVNOTES.md` — confirmed present, correctly scoped ("Internal notes... not user-facing documentation"), and it is the only `DEVNOTES.md` in the tree (`find . -iname DEVNOTES.md` → one hit). No other adapter directory accumulated demo-era parity notes needing a similar move — confirmed via `grep -rl "4B"` across `docs/` and `examples/` returning only this file and the plan's own reference. Closing this row as a confirm-and-close per team-lead's framing; no further move needed) |
| EX8.2 | Delete the stale "planned next demos" list (superseded by `examples/specs/` + EX7 rows) | Gone; no dangling references | n/a | done(no stale list exists to delete: `grep -rln "planned next\|planned-next\|Planned Next"` across the whole tree returns zero hits, and the EX2.3 "Planned, not yet in this directory" per-adapter labels noted in that row's FOUNDATION REVIEW are also gone — `grep -rln "Planned, not yet\|coming soon"` across `examples/**/*.md` returns zero hits, because EX3 subsequently built real `tasks` apps for every adapter, superseding those placeholder labels naturally. Confirm-and-close, no deletion needed) |
| EX8.3 | Tenant-provisioning-from-frontend default: examples must not silently ship the insecure `POST /api/tenants` pattern — dev-only guard or explicit documented flag | Each example's provisioning path reviewed; behavior + caveat recorded in its README | ~80 | done(reviewed every app's provisioning path via `grep -rln "ensureTenant\|api/tenants"` over non-generated, non-`smoke.ts` app source. Four apps do unauthenticated-by-default frontend/driver-side `POST /api/tenants` and were missing the dev-only caveat sentence, now added: `examples/convex/html/README.md` (`src/App.tsx`), `examples/convex/http/README.md` (`src/main.ts`), `examples/nimbus/agent-chat/README.md` (`src/main.tsx`) — same wording as the existing `tasks` apps' caveat ("Tenant creation in browser code... is a local-development convenience. Provision tenants separately before deploying beyond your own environment."); `examples/convex/node/README.md` (`script.ts`) got a variant noting it sends `NIMBUS_ADMIN_TOKEN` when set but falls back to unauthenticated when not. `examples/convex/tasks/README.md` and `examples/nimbus/tasks/README.md` already carried the caveat from EX3. Reviewed and confirmed out of scope: `firebase/*`, `mongodb/*`, `dynamodb/*`, `cloud-functions/tasks` app source never calls `ensureTenant`/`POST /api/tenants` at all (only their `smoke.ts` dev-tooling does, which is not shipped/frontend code); `examples/nimbus/agent-worker` has no frontend — its `run.ts`/`smoke.ts` are dev-tooling drivers with the same admin-token-when-set pattern as convex/node, not app code a user would deploy, so no README caveat was added there) |
| EX8.4 | npm script naming `*:demo:*` → `*:example:*`; drop dead scripts | Scripts renamed and exercised; `npm run` list clean | ~30 | done(repo-wide `grep -rln ":demo:"` over `*.json`/`*.md`/`*.yml`/`*.mjs`/`*.sh`/`Makefile` found 10 files: root `package.json` (7 script definitions) plus 9 READMEs referencing them (`examples/convex/{html,http,node,README}.md`, `examples/firebase/{html,README}.md`, `examples/nimbus/{agent-worker,agent-chat,html,README}.md` — agent-chat's reference was caught and fixed inline during the EX8.3 edit). All 10 renamed `:demo:` → `:example:` (`convex:demo:node`→`convex:example:node`, `convex:demo:html`→`convex:example:html`, `convex:demo:http`→`convex:example:http`, `firebase:demo:html`→`firebase:example:html`, `nimbus:demo:html`→`nimbus:example:html`, `nimbus:demo:agent-chat`→`nimbus:example:agent-chat`, `nimbus:demo:agent-worker`→`nimbus:example:agent-worker`), comment-column alignment fixed in the 5 READMEs where the longer name knocked it out of place. Re-verified clean: zero `:demo:` hits remain anywhere in the tree except one unrelated, pre-existing match in `docs/operators/encryption.md` (`db:sqlite:tenant:demo:demo.sqlite3`, a tenant-id path pattern, not a script name — not in scope). No dead scripts found: every one of the 7 renamed root scripts is referenced by exactly one README's run instructions, so none were dropped. The workspace-local `demo` script inside `examples/convex/node/package.json` (invoked via `npm run demo --workspace convex-node`) does not match the `*:demo:*` colon-wrapped pattern this row targets and was left as-is) |
| EX8.5 | Final reference sweep: no stale `demos/` references in code, tests, docs, compose/container files, CI workflows | `git grep` output recorded clean | n/a | done(three sweeps run, all clean. (1) `demos/` residuals: `grep -rn "demos/"` over `*.rs`/`*.ts`/`*.tsx`/`*.mjs`/`*.sh`/`*.yml`/`*.md`/`Containerfile`/`compose.yaml`, excluding this plan file and the plans README (both of which reference the string only in their own rename-history/decision text) — zero hits; a last-resort unfiltered-extension full-tree grep for the literal `demos/` also returned zero hits outside those two files. (2) `NIMBUS_DEPLOY_URL` residuals: zero hits anywhere — EX1's positional-`TARGET` migration removed every reference. (3) node/cluster target vocabulary: zero real hits — the only matches are inside this plan file's own goal-prompt/rename-history text quoting EX1.4's constraint, and EX1.4's own row already recorded this exact sweep clean with two justified non-target incidental hits (compose placement note, architecture doc prose) that still hold since nothing in EX3–EX8 touched that surface. All three sweeps re-run fresh this session, not assumed from EX1/EX2's prior closures) |

### Band EX10 — Convex runtimes-spec parity (owner-directed 2026-07-11, in-PR)

Owner directive: existing Convex apps must run seamlessly on the adapter, per
the upstream runtimes spec (default V8 runtime semantics, `"use node"` Node
runtime for actions, `convex.json`, Node client story). Supersedes EX7.8's
defer decision for the example half.

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX10.1 | Audit the default-runtime semantics vs the Convex runtimes doc: seeded deterministic `Math.random()` (per-invocation seed, deploy-frozen at import time), frozen `Date.now()` per spec (import-time = deploy time; execution = invocation start, constant through queries/mutations), `performance.now()` rules (fixed in queries, incrementing in mutations, `timeOrigin` = deploy), and the web-standard globals matrix (network/encoding/streams/crypto/timing) | Gap matrix recorded (implemented / missing / divergent, with file:line for each); bounded gaps fixed in EX10.2, unbounded ones recorded with sized proposals | audit | todo |
| EX10.2 | Fix the bounded determinism/global gaps from EX10.1 in the runtime (V8 bootstrap + engine-provided seed/timestamp per invocation as needed) — queries/mutations must be deterministic per spec | Each fixed gap has a behavior test (e.g. two `Math.random()` calls differ within one run, global-scope value stable across runs; `Date.now()` constant within a handler); runtime lane + full workspace suite green | per audit | todo |
| EX10.3 | `convex.json` support at the app root: honor `generateCommonJSApi` (codegen emits `api_cjs.cjs` per the upstream Node.js doc), honor or cleanly reject `functions` (custom functions dir) with an explicit error. Owner amendment 2026-07-11 (premise-correction): the original row also directed "warn-loudly-and-ignore `node.nodeVersion`/`node.externalPackages`," premised on `docs/developers/convex/migrate.md` (edited the same day at `164ad3ca7`, "correct migrate.md convex.json overclaim") asserting convex.json is "not read by anything." That doc edit was itself a regression, not a correction — an over-narrow grep (filename search in `nimbus-cli`/the adapter crates) missed that consumption flows through `projectConfig` in `packages/codegen`. Both settings are in fact read and enforced, and have been since `e62d5e0b5` (2026-05-12, "checkpoint node runtime compatibility work"), well before the erroneous doc edit: `node.externalPackages` triggers a hard codegen error in `packages/codegen/src/node_external_packages.mjs` for any undeclared npm import in a `"use node"` module (Convex Cloud externalPackages zip-size-limit parity), with passing selftest coverage in `action_fixtures.mjs`; `node.nodeVersion` selects a real `RuntimeCompatibilityTarget` lane (Node20/22/24/26) backed by `crates/nimbus-runtime/src/limits/axes.rs`'s `RuntimeNodeLtsRegistry`, threaded through `crates/nimbus-convex/src/manifest.rs` as `node_runtime_target`. Caught pre-build, before any code was written against the wrong premise; owner independently re-verified `node_external_packages.mjs` and the `axes.rs` lanes and confirmed. No warn-and-ignore build item remains — verifying and doc-truthing `node.nodeVersion`/`node.externalPackages`'s actual behavior is folded into EX10.5, which replaces `migrate.md`'s erroneous "not read" lines (~106-108) and the matching `docs/source-map.md:60` row with the accurate statement, citing `node_external_packages.mjs` + `axes.rs` + `manifest.rs` | Fixture apps with each setting verify the behavior (CJS file emitted and importable from a `require()` script; `functions` override relocates the source dir and takes precedence over the default `nimbus/`/`convex/` heuristic, with an explicit error naming the setting and path when the declared directory is missing); codegen selftests + Rust dev-adapter-detection tests | ~300 | todo |
| EX10.4 | `"use node"` end-to-end verification + showcase: prove a `"use node"` action importing a Node builtin executes on the node-compat runtime via a live smoke, and add the two-runtime showcase to the convex examples (default-runtime action + node action side by side) | Live smoke PASS for both runtimes; example README explains the two-runtime story per the upstream doc | ~250 | todo |
| EX10.6 | Packages/libraries parity (owner amendment 2026-07-11, "full parity"): the default runtime's documented Node-API subset (`process.env`, `AsyncLocalStorage`, `AsyncResource`) present and behaving per spec (incl. the no-propagation-into-ctx.run* caveat); npm library bundling semantics (every convex-dir module bundles for default or node runtime; libraries that work in browser/Deno/Workers work here); `"use node"` full builtin surface via node-compat; WebAssembly API (`WebAssembly.instantiate`/`Module`/`Instance`, `.wasm` module imports through the bundler) | Audited matrix per item; bounded gaps fixed with behavior tests; anything genuinely large gets a sized proposal to the owner BEFORE building (in-PR by default per directive) | per audit | todo |
| EX10.5 | Docs truth-up from the audit: runtimes section under `docs/developers/convex/` (two runtimes, determinism semantics, what `convex.json` now does), compatibility reference updated with the audited matrix, migrate.md re-aligned | Both docs gates green; every claim carries a source-map row grounded in the audited code | ~150 doc | todo |

### Band EX9 — Closeout: PR, green, merge

| ID | Item | Acceptance | Size | Status |
| --- | --- | --- | --- | --- |
| EX9.1 | Full local gate on the branch: `cargo fmt --all --check`, `make clippy`, `make ci`, `npm run typecheck && npm run test && npm run build`, both docs gates | Command outputs (counts, not "passed") recorded here | n/a | done(2026-07-11, run by team-lead at tip 4cce4f713: `make ci` exit 0 end-to-end — covers fmt, clippy, deny, Rust suites, harness, JS build/test, docs checks, proof helpers. Full workspace nextest 4261 run / 4261 passed / 31 skipped (zero failures; the two laptop machine-env tests passed this run); runtime serial lane 432 passed / 0 failed / 122 ignored; install-helper 44/44; full log at scratchpad ex91-make-ci.log. Note: a duplicate concurrent `make ci` briefly overlapped at start (per-sub-step single-flight keys don't lock the whole gate); the duplicate was SIGTERMed by its owner and this run completed cleanly with zero failures, so no contention artifact survived. check-docs 99 pages PASS and site build 100 pages recorded under EX6.3; site-verify 15/17 with the 2 known laptop-local untracked-docs-dir conditions recorded there) |
| EX9.2 | Push branch (explicit refspec, never `-u`) and open the PR to `nimbus/nimbus` with a summary mapping bands → changes | PR URL recorded | n/a | done(2026-07-11: https://github.com/nimbus/nimbus/pull/181 — 41 commits, 254 files, +12001/−4130; body maps bands → changes, lists the five live-found product bugs, the three-review trail, and the recorded follow-ups) |
| EX9.3 | CI verdict CONFIRMED green by checking PR state (never assumed), then squash-merge (standing merge-on-green authorization) + post-merge routine | Merge SHA recorded; main pulled and verified | n/a | in_progress(CI hardening rounds on the new examples-verify lane, 2026-07-11: (r1) package-lock had platform-pruned optional native bindings from darwin lock regens (npm/cli#4828) — rebuilt from main baseline, all six original red lanes cleared; (r2) lane binary lacked the embedded-packages payload staged by every other nimbus-bin consumer (debug-assertions runtime read) — staged per convention; (r3) added server-log dump on smoke failure, which immediately identified (r4): the anchor-snapshot provenance re-check and runtime-build fallback read every deno extension source from the BUILDER-absolute $CARGO_HOME/git/checkouts path — fixed with cargo fetch --locked in the lane (deterministic checkout paths), plus staged the UI-assets gap (silent placeholder SPA) and swept the full debug-mode runtime-read asset inventory (no further gaps). One runtime-test flake observed and cleared on rerun. (r5, owner-spotted) 18 tracked neovex-era generated artifacts (demos-era `.neovex/convex/` snapshots: bundles, schemas, function manifests) rode the demos→examples rename invisibly — kept alive only by a `.gitattributes linguist-generated` rule, read by nothing (current tooling uses `.nimbus/`). Deleted along with the gitattributes rule; residual "neovex" mentions verified limited to the historical CHANGELOG entry, this plan's inventory record, and the capability-segregation script's absence-check. Sweep lesson recorded: EX8.5 grepped for demos/ paths and target vocabulary but never for the pre-rename project name — old-project-name greps belong in rename sweeps. **FLAGGED FOLLOW-UP [owner: nimbus-runtime, release-critical]**: the r4 mechanism is not CI-specific — any deployed nimbus-bin that ever takes the anchor-snapshot fallback path requires the builder machine's $CARGO_HOME/git/checkouts tree; a release/customer binary hitting that path fails to initialize JsRuntime. Needs a portability decision (fail-closed with a clear error, true self-contained snapshot, or embedded extension sources) in a nimbus-runtime follow-up) |
| EX9.4 | Post-merge: verify every docs GitHub source link resolves against merged main; docs site deploy green | Link-check output recorded | n/a | todo |
| EX9.5 | Archive this plan, remove its plans-README entry (fold provenance into the archive header), update agent memory | README shows no archived entry; archive committed direct to main | n/a | todo |

## Execution Order And Dependencies

EX0 first (it fixes EX1 flag decisions, EX2 rename surface, EX4 scope, EX6
page list). EX1 and EX2 are parallel-safe after EX0. EX3 needs EX2; EX4 needs
EX0.3 and EX2; EX5 needs at least one EX3 example and covers everything as
bands land; EX6 needs EX2–EX4 shapes settled (paths in links); EX7 anytime
after EX2; EX8 after EX3/EX4 settle what is user-facing; EX9 last.

## Verification Contract

- Per-item: the acceptance line in its ledger row, with evidence recorded in
  the Status cell (test names + counts, command output summary, or SHA).
- Band EX1: focused `nimbus-cli` tests plus any consumer crates' tests.
- Band EX2/EX5: the gate-blast-radius rule — route/gate changes run the full
  relevant suites, not filtered subsets.
- Band EX6: both docs gates.
- Closeout: EX9.1's full local gate before the PR; hosted CI is the merge
  source of truth; merge only on a CONFIRMED green verdict.

## Suggested Goal Prompt

Paste after `/goal`:

/goal Execute docs/private/plans/examples-and-target-resolution-plan.md bands EX0 through EX9 in the documented order. For each ledger item: do its exploration first, implement to the acceptance line, run the named verification, and update its Status cell to done with one evidence line (test names+counts, command output summary, or SHA); mark wrong or already-satisfied items no-action(reason), EX7 rows may close decision-recorded(reason), and truly blocked items blocked(reason) — then continue. All code and public-docs changes go on branch examples-and-target-resolution (never direct to main); this plan file and its ledger ride the same branch (they are tracked and merge with the PR); only the plans-README entry on main is updated direct-to-main, at closeout. Binding constraints: one optional positional TARGET (URL or configured name; omitted = local) with no node/cluster distinction anywhere in CLI help, examples, or docs; examples import workspace packages; example smokes assert observable behavior per examples/specs/; docs pages follow the docs skill rules and both docs gates must pass; gate-adding or fail-closed changes run the full workspace suite. Decide rather than ask. The goal is met when every EX0–EX8 ledger row is done, no-action(reason), decision-recorded(reason) (EX7 only), or blocked(reason) with evidence; make ci, the JS suite, and both docs gates are green locally; the PR to nimbus/nimbus is open with its CI verdict CONFIRMED green by checking PR state and it is squash-merged with the post-merge routine and EX9 rows done with evidence; and the plans README entry reflects completion — or stop after 70 turns and record the blocking state in this plan.

## Appendix — EX0 Inventory (filled during EX0)

### EX0.1 rename surface (`demos/` → `examples/`)

Load-bearing (filesystem / route / served-path literals the rename MUST change):

- `package.json:14-19` — six workspace entries (`demos/convex/html`, `demos/convex/http`,
  `demos/convex/node`, `demos/firebase/html`, `demos/mongodb/node`, `demos/nimbus/html`).
- `package.json:28,29,33,34,35` — scripts passing `--app`/`--app-dir` `demos/...` paths.
- `package-lock.json` — mirrors every workspace path (`:17-22` block + packages keys); regenerate via `npm install`.
- `crates/nimbus-server/src/router.rs:596,600,601,604,606,607` — `demos_dir()`, `/demos` redirect route,
  `.nest_service("/demos/", …)`, `join("../../demos")`.
- `crates/nimbus-server/src/http/metadata.rs:148-150` — `demos_redirect()` → `Redirect::permanent("/demos/")`.
- `crates/nimbus-server/src/http/mod.rs:56` — re-export of `demos_redirect`.
- `crates/nimbus-server/src/tests/registry_and_license/routes.rs:27` — asserts `GET /demos/nimbus/html/`.
- `crates/nimbus-cloud-functions/src/lib.rs:566,573` — reserved-prefix match on `/demos`, `/demos/`.
- `crates/nimbus-operator/src/policy.rs:31,91` — path classification `== "/demos"` / `starts_with("/demos/")`, `Self::Demos => "demos"`.
- `demos/convex/html/vanilla.html:88` — `<script src="/demos/convex/vendor/browser.bundle.js">`.
- `demos/convex/http/vite.config.ts:8` — `base: ".../demos/convex/http/dist/"`.
- `packages/convex/build.mjs:10` — `path.resolve(packageRoot, "../../demos/convex/vendor")`.
- `scripts/verify-repo-architecture-quality.sh:100,110` — scans `${REPO_ROOT}/demos` and excludes `demos/convex/vendor`.
- `.gitattributes:18,19` — `demos/convex/*/.neovex/**`, `demos/convex/*/convex/_generated/**` linguist globs.
- `.gitignore:66` — `demos/convex/vendor/browser.bundle.js`.
- `.claude/audit-full.mjs:154,155` — audit config path + description mentioning `demos`.
- `tests/demos.smoke.md` (filename + `:6` served-route URL) — smoke doc.
- `demos/README.md:73,124,152` — served-path/URL literals; whole file is rewritten by EX2.2.
- `demos/convex/showcase/README.md:19` — `nimbus deploy --url … --app-dir demos/convex/showcase` (also carries stale `--url`).

Naming-convention (coordinate; not path-load-bearing):

- `package.json:36-41` — `*:demo:*` scripts (rename to `*:example:*` — EX8.4 territory, but touched here).
- `Makefile:4,699-718` — `convex-demo*` targets operate on an EXTERNAL upstream `convex-demos` checkout, NOT the in-repo tree; renaming is optional and decoupled from the tree rename.
- `scripts/convex-demo-overlay.mjs`, `scripts/stop-demo-processes.sh` — external-overlay tooling (Makefile-coupled).
- CLI help examples `crates/nimbus-cli/src/cli_ux.rs:42,52,53,69,84,85,92,93` (`--app ./demos/…`) and their asserting tests `crates/nimbus-cli/src/start/tests/cli_surface.rs:193,293,294,312,315,938,941,962,963`.
- Docs refs (EX6 territory, not rename-blocking): `docs/concepts/architecture/server-transport.md:55`, `docs/source-map.md:39,309`, `AGENTS.md:4`.

NOT affected: `compose.yaml`, `Containerfile` (neither references `demos`); `.github/` workflows (zero hits).
Landing page `demos/index.html` relocates but has no `/demos/` path literals (its links use dev-server ports).

### EX0.2 target-seam chain

`TargetSelector` (`crates/nimbus-cli/src/target_context.rs:7-93`) TODAY = three flags `--local` / `--target NAME` /
`--url URL` + env fallbacks `NIMBUS_TARGET`→`NamedTarget`, `NIMBUS_DEPLOY_URL`→`RemoteUrl`, with an exactly-one-source
rule (`:83-92`). Resolves to `TargetContextKind::{LocalDiscovery | NamedTarget(String) | RemoteUrl(String)}` (`:28-33`).

Consumers:
- `run.rs` — `RunCommand` flattens `TargetSelector` (`:29`); `resolve_run_target_with_env` (`:128`) calls `.resolve("run", …)`
  then on empty-source error falls back to `LocalDiscovery`/`ImplicitLocalDefault` (`:135-146`) → **run already defaults to local**.
- `sandbox.rs` — `SandboxCreateCommand`/`SandboxListCommand` flatten it (`:30,40`); `resolve_sandbox_target` (`:50-59`)
  calls `.resolve(…)` with **no local fallback** → sandbox currently REQUIRES an explicit source. EX1.3 unifies this to local-default.
- `deploy.rs` — does NOT use `TargetSelector`. Own `--url` field (`:27`) + `resolve_deploy_url` (`:274-296`) requires
  `--url` or `NIMBUS_DEPLOY_URL`. The resolved `target_url: String` feeds token lookup (`:164` keyed by URL), loopback
  admin-token discovery (`:170-175,225-250`), and the POST endpoint (`:196-198`). EX1.2 migrates this onto `TargetSelector`.

How `NamedTarget` resolves to an endpoint+credentials TODAY: **it does not.** `run.rs:290-292` returns
`InvalidInput("named target … is not yet backed by a target registry; pass --local or --url")`; `sandbox` never executes
(returns "reserved for the service-sandbox-node workload-control path"). There is no target registry — `NamedTarget` is a
carried string with no resolver. LocalDiscovery IS wired: `LocalServerHttpClient::discover` (`local_server_client.rs:25-59`)
reads `read_live_server_discovery` and exposes `base_url()` (`:61-63`) = `http://127.0.0.1:PORT`, which EX1.2 feeds to deploy.

Exact flag/env surface EX1 changes: delete `--local`/`--target`/`--url` from `TargetSelector` and deploy's `--url`; replace
with one optional positional `TARGET`; keep `NIMBUS_TARGET`/`NIMBUS_DEPLOY_URL` env fallbacks; move the local default INTO
`resolve()` so every consumer defaults local when absent. Help/error strings to rewrite: `cli_ux.rs:40,41,65-79`,
`sandbox.rs:12`, `run.rs:282,291`, `target_context.rs:86,89`, deploy help test `deploy.rs:934-945`, parse tests
`deploy.rs:897-931`, `run.rs:356-376,383`, `sandbox.rs:68-95`, `target_context.rs:156-209`.

### EX0.3 agent-surface scope (for EX4 — no fabrication)

Verified usable today:
- **Scheduled functions** (SDK): `scheduleAfter`/`scheduleAt`/`cancel` (`packages/nimbus/src/http-client.ts:138,155,173`,
  public `browser.ts:223,231`, convex-compat `packages/convex/src/browser.ts:246,268`); function-authoring
  `ctx.scheduler` (`packages/nimbus/src/server.ts:323`, `packages/convex/src/server.ts:92`); server routes
  `crates/nimbus-server/src/router.rs:776-785`. Delay/timestamp only.
- **DB read/write / agent memory** (SDK + CLI): `query`/`mutation`/`action`/`paginatedQuery`
  (`packages/nimbus/src/http-client.ts:79,90,101,112`, convex-compat `browser.ts:184,206`); `ctx.db` insert/patch/delete
  (`server.ts:307,314`); `nimbus run functions query|mutation|action` (`run.rs:116`, endpoints `:90-95`).
- **Sandbox / Sessions / Services lifecycle** (SDK only): `control-plane/client.ts:110+,234,245,251,264,277,289,295,304`;
  server handlers `crates/nimbus-server/src/http/sandboxes.rs:26`, `.../sessions.rs`. Execution caveat: libkrun fails
  closed for process-exec; containers Linux-only; macOS/WSL2 need `nimbus machine`.

NOT publicly usable (must not appear in examples as working):
- **Cron jobs** — only orphaned `packages/nimbus/src/rest.ts:298-307` (not in `package.json` exports) + raw HTTP routes; no
  SDK method, no CLI. Simulate recurrence with `scheduler.runAfter` re-scheduling.
- **`nimbus run exec`** — stubbed (`run.rs:117-120`). **`nimbus sandbox` CLI** — stubbed (`sandbox.rs:45-47`).
- **Egress policy knob** — no SDK/CLI field; config/compose-only (`compose/file.rs:209-223`), deny-by-default server side.

Safe EX4 scope: `agent-chat` = `@nimbus/nimbus/browser|react` query/mutation/action + `ctx.scheduler.runAfter/runAt` for
durable follow-ups; NO sandbox/machine needed (functions run server-side in V8). `agent-worker` = same DB surface +
`ctx.scheduler` for cadence (no cron API) + optionally `nimbus run functions` from CLI. A real-sandbox example may use ONLY
the SDK lifecycle (`nimbus.sandboxes.*` + `nimbus.sessions.*`), never the stubbed CLI, and must note the machine/Linux
prerequisite. Do not show an egress SDK/CLI knob, a JS cron API, `run exec`, or the `sandbox` CLI as working.

### EX0.4 docs page list (for EX6)

Site = Astro + Starlight, content authored in `docs/` (config `website/astro.config.mjs`). Sidebar array `:74-129`:
`Developers` group `:76-90` (per-adapter autogenerated subgroups: Convex `:82`, Firestore=firebase `:83`,
Cloud Functions `:84`, MongoDB `:85`, DynamoDB `:86`, Native API=native `:87`, Node.js runtime `:88`), `Agents` group `:91`
(fully autogenerated from `docs/agents/`), parallel `Reference` per-adapter tree `:119-126`. Autogenerate means a new page
dropped in an already-listed directory appears automatically (order via frontmatter `sidebar.order`); a brand-new directory
must be added to the array (`:71-73`).

Existing adapter pages under `docs/developers/`: native `index.md`; convex `index.md`+`migrate.md`; firebase
`index.md`+`migrate.md`; cloud-functions `index.md`+`migrate.md`; mongodb `index.md`+`examples.md` (the examples-page
precedent); dynamodb `index.md`. Agents: `docs/agents/{index,sandbox-quickstart,sandboxes,services,sessions}.md`.

`docs/source-map.md` = 3-col table `| Doc page | Claim / surface | Source |` (header `:12-13`); every new example page with a
behavior claim needs a row or `scripts/check-docs.sh` (`:7`) flags it. Docs gates: `scripts/check-docs.sh`,
`scripts/verify-nimbus-docs-site.sh` (both present, executable).

Proposed EX6 page list (path + Diátaxis mode): per-adapter example pages go under `docs/developers/<adapter>/examples.md`
(how-to; mongodb precedent) — convex, firebase, cloud-functions, mongodb (extend existing), dynamodb, native. Agent example
pages under `docs/agents/` — `agent-chat.md` (tutorial), `agent-worker.md` (how-to). Each links to its GitHub source path on
`nimbus/nimbus` and gets a `source-map.md` row.

STALE-DOC DEBT for EX6 (public docs referencing the deleted `--url`/`--local|--target|--url` CLI surface, out of EX1-EX2
scope): `docs/get-started/deploy.md:38,56,71,90`, `docs/reference/cli.md:154,183,184`,
`docs/developers/cloud-functions/index.md:123`, `docs/developers/cloud-functions/migrate.md:196`, `docs/source-map.md:92`.
EX6 must rewrite these to the positional `TARGET` form.
