# NNC1.1 Low-Dependency Crate Proof

Date: 2026-07-23

Status: `passed`

Source commit before the item: `ccc0977ec42d0209e3bdcb4bacc4946ee5d09743`

Owner worktree:
`/Users/jack/src/github.com/nimbus/nimbus-network-architecture-audit`

## Result

`nimbus-network` is a workspace library crate with exactly one declared and
resolved workspace edge:

```text
nimbus-network -> nimbus-core
```

The crate root defines the transport-free ownership boundary without adding
portable identities, provider interfaces, effect execution, or upper-layer
dependencies ahead of their owning tasks. Socket binding, forwarding,
namespaces, bridges, firewalls, TLS termination, and protocol parsing remain in
their current effect-owning crates.

The package does not depend on `workspace-hack`. That edge would make the
NNC1.1 claim false even though it is mechanically common in upper crates.

## Dependency Evidence

The full generated graph is
`nnc1.1-dependency-graph.json` (SHA-256
`ec8bb2313cebf2f3e37a5c6aa4f7bdd473e02837ebf202fa4feb2af1f6a54281`).
It was generated by:

```text
node scripts/capture-nimbus-network-dependency-baseline.mjs
```

The artifact records the source commit before the item and captures the
NNC1.1 working-tree manifests. Compared with the NNC0.1 baseline, the declared
workspace edge count increases from 244 to 245 solely because of the new
`nimbus-network -> nimbus-core` edge.

| Profile | Target | Resolved edges | Network edges | Cycles |
| --- | --- | ---: | ---: | ---: |
| normal/default | `aarch64-apple-darwin` | 216 | 1 | 0 |
| normal + dev/test/build/default | `aarch64-apple-darwin` | 239 | 1 | 0 |
| all features/all kinds | `aarch64-apple-darwin` | 245 | 1 | 0 |
| all features/all kinds | `x86_64-unknown-linux-gnu` | 245 | 1 | 0 |
| all features/all kinds | `aarch64-unknown-linux-gnu` | 245 | 1 | 0 |
| all features/all kinds | `x86_64-pc-windows-msvc` | 245 | 1 | 0 |

Every network row is the same unconditional normal dependency on
`nimbus-core`. There is no dev, build, target-conditioned, optional, or reverse
network edge in any profile. The non-host profiles are Cargo resolution
evidence, not cross-target compilation claims.

An independent `cargo metadata --no-deps --format-version 1` assertion found
the crate in `workspace_members` and returned exactly:

```json
{
  "name": "nimbus-core",
  "kind": null,
  "target": null,
  "optional": false,
  "features": []
}
```

## Behavioral and Quality Gates

```text
cargo test -p nimbus-network
# 1 passed, 0 failed; 0 doctests

cargo check -p nimbus-network --all-targets
# exit 0

cargo clippy -p nimbus-network --all-targets -- -D warnings
# exit 0

cargo fmt --all --check
# exit 0

git diff --check
# exit 0

bash scripts/check-docs.sh
# 108 pages link-clean, source map resolves, private fence intact, titles unique

bash scripts/verify-nimbus-docs-site.sh
# 17/17 conditions green
```

The single boundary test constructs and renders a real `nimbus_core::Cidr`.
This proves the intended dependency is usable while leaving all new
control-plane vocabulary to its explicitly sequenced tasks.

The expected-red control-plane verifier now reports ten passes and only the two
deliberately later failures:

- `NNCV005 no-duplicate-port-allocation-authority` remains red until NNC3; and
- `NNCV011 single-portable-vocabulary-owner` remains red until the NNC1
  vocabulary extraction.

`NNCV003 nimbus-network-crate` and
`NNCV004 network-dependency-contract` both changed from expected red to green.
The verifier still exits `1`, as required while later plan obligations remain.

## Scope Guard

NNC1.1 does not:

- define IDs, generations, epochs, plans, or provider capabilities;
- move endpoint or segment vocabulary;
- execute or wrap any provider effect;
- introduce a compatibility alias or a second authority; or
- change a consumer dependency.

Those changes remain ordered under NNC1.2 through NNC1.6.

## Independent Review

Claude Opus 4.8 at maximum reasoning reported no accepted or actionable
findings and rated the patch correct (`0.8`). The review independently
reconciled the normal/dev/optional edge arithmetic in all six profiles, the
244-to-245 declared-edge delta, the exact one-edge network dependency, the
8/4-to-10/2 expected-red transition, the absence of premature vocabulary or
effects, and the single-`in_progress` recovery ledger.

The reviewer also checked the generated artifact's provenance: its
`source_head` intentionally names the commit before the item while the graph
captures the disclosed NNC1.1 working-tree manifests. That convention matches
the plan's pre-commit proof model and does not claim the new crate is already
present in the named commit.
