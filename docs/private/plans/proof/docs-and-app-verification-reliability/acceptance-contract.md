# Acceptance Contract

Date: 2026-08-17
Owner: `docs-and-app-verification-reliability-plan.md`

## Decisions

| ID | Date | Decision | Evidence and consequence | Re-open condition |
|---|---|---|---|---|
| AVRD1 | 2026-08-17 | Put the network page in the autogenerated architecture directory. | The docs skill and current renderer generate this sidebar. No manual sidebar owner is added. | The renderer stops generating its sidebar entry. |
| AVRD2 | 2026-08-17 | Run codegen and app boots in disposable workspaces. | The live lane changed tracked manifests and the lockfile. Source becomes read-only input. | Codegen gains a proven read-only mode. |
| AVRD3 | 2026-08-17 | One validated manifest owns case identity, surfaces, boot, smoke, and update semantics. | Runner and report data currently drift. One schema becomes their shared contract. | The runner and report validator stop sharing those fields. |
| AVRD4 | 2026-08-17 | Extract permanent verifier data before plan archival. Plan prose is not API. | Thirteen scripts read the completed plan path. Stable fixtures replace that dependency. | No executable needs the data. |
| AVRD5 | 2026-08-17 | Add parallel execution only after isolation passes. Keep serial diagnostic mode. | Shared ports and operator paths make current parallel execution unsafe. | Isolation or coverage cannot hold. |
| AVRD6 | 2026-08-17 | Share one run-global network root and isolate all other per-case state. | Network leases are host-global, while local discovery is a single platform path. This prevents duplicate lease authority and cross-case discovery. | Nimbus gains a multi-server discovery registry with explicit identities. |
| AVRD7 | 2026-08-17 | Use three implementation PRs and one cleanup PR. | The work has three reviewable value bands. Each merge becomes a recovery checkpoint. | A phase candidate exceeds reviewable size before implementation starts. |
| AVRD8 | 2026-08-17 | Require one meaningful fail-closed mutation per verifier condition. | Behavioral suites own additional adversarial cases. The aggregate self-test stays proportional. | A condition contains two independent predicates that need separate mutations. |

## Verifier conditions

`scripts/verify-docs-app-verification.sh` prints
`Summary: N passed, M failed`. `--through-phase N` evaluates only conditions
due through that phase.

| Conditions | Owner | Contract |
|---|---|---|
| AVRC01-AVRC04 | AVR1 | No executable reads the active network-plan path; stable fixtures preserve verifier behavior; private status is merged truth; archive routing resolves. |
| AVRC05-AVRC10 | AVR2 | Public network page, thirteen-page indexes, source-map rows, transport separation, desired/durable/observed separation, cross-links, and private fence are correct. |
| AVRC11-AVRC12 | AVR3 | Both direct and Make entry points have fresh prerequisites; Node and supplied-binary preflights fail or skip exactly. |
| AVRC13-AVRC15 | AVR4 | Nine unique cases validate; preparation uses disposable workspaces; pre/post source-byte manifests match on success and failure. |
| AVRC16-AVRC17 | AVR5 | Default Compose discovery stays unchanged; explicit opt-out performs no discovery or tracked rename. |
| AVRC18 | AVR6 | Explicit and bare-local targets have equal result, stdio, and fail-closed trust behavior. |
| AVRC19-AVRC20 | AVR7 | Product provider-assigned leases own binds; case-local operator state and complete cleanup survive concurrency and fault cuts. |
| AVRC21-AVRC22 | AVR8 | Versioned JSON validates and redacts; JUnit is a deterministic projection with truthful cleanup state. |
| AVRC23 | AVR9 | Parallel coverage equals serial coverage and meets both relative and absolute time budgets. |
| AVRC24 | AVR10 | Counts, commands, Node range, push-versus-polling language, and retained-artifact instructions match the manifest and tested behavior. |

| Completed phase | Required verifier result |
|---|---|
| 0 | Full red baseline recorded; mutation self-test `24/24`. |
| 1 | `--through-phase 1`: `10 passed, 0 failed`. |
| 2 | `--through-phase 2`: `20 passed, 0 failed`. |
| 3-5 | Full verifier: `24 passed, 0 failed`; mutation self-test `24/24`. |

## Exact commands

AVR0 creates `scripts/verify-docs-app-verification.sh` and
`scripts/examples-verify-contract-test.sh`. Later tasks extend these stable
entry points. Every task runs its row and the focused commands in its plan body.

| Task | Required commands |
|---|---|
| AVR0 | `bash -n scripts/verify-docs-app-verification.sh`; `shellcheck scripts/verify-docs-app-verification.sh`; `bash scripts/verify-docs-app-verification.sh --baseline`; `bash scripts/verify-docs-app-verification.sh --self-test` |
| AVR1 | `bash scripts/verify-nimbus-network-control-plane.sh`; `bash scripts/verify-nimbus-network-control-plane.sh --self-test`; `bash scripts/verify-docs-app-verification.sh --task AVR1`; `bash scripts/check-docs.sh`; `bash scripts/verify-nimbus-docs-site.sh` |
| AVR2 | `bash scripts/verify-docs-app-verification.sh --task AVR2`; `bash scripts/check-docs.sh`; `npm --prefix website run build`; `bash scripts/verify-nimbus-docs-site.sh` |
| AVR3-AVR8 | `bash scripts/examples-verify-contract-test.sh --task <TASK>`; `bash scripts/verify-docs-app-verification.sh --task <TASK>`; task-focused Rust or live commands from the owner plan |
| AVR9 | `bash scripts/examples-verify-contract-test.sh --task AVR9`; `bash scripts/examples-verify-benchmark.sh --serial-samples 3 --parallel-samples 5 --max-seconds 1200`; `bash scripts/verify-docs-app-verification.sh --task AVR9` |
| AVR10 | `bash scripts/examples-verify-contract-test.sh --task AVR10`; `bash scripts/check-docs.sh`; `npm --prefix website run build`; `bash scripts/verify-nimbus-docs-site.sh`; technical-writing lint over every changed Markdown file |
| AVR11 | `make ci`; `make examples-verify`; full AVR verifier and self-test; both docs gates; docs site build; `nimbus-autoreview --gate pre-pr --mode auto` once |
| AVR12 | Full AVR verifier; both docs gates; `git diff --check`; final routing and archive searches |

## Numbered task actions

| Actions | Contract |
|---|---|
| AVR0.1-AVR0.6 | Verify the baseline and durable checkpoint. Author six missing private routing and runbook documents from current source. Update bootstrap references. Inventory source, bind, operator-path, and command effects. Author 24 red conditions and mutations. Create the AVR0 proof and capture the baseline. |
| AVR1.1-AVR1.6 | Inventory every plan reader. Extract stable fixtures. Update readers without behavior drift. Correct stale active-status claims. Archive the network plan. Update routing. |
| AVR2.1-AVR2.4 | Add the source-verified network page and source-map rows. Change both page counts. Add lifecycle-plane cross-links. Inspect every generated `llms` file. |
| AVR3.1-AVR3.5 | Define direct-script and Make contracts. Add explicit prerequisites. Keep the supplied-binary fast path. Add the Node preflight. Test every host mode. |
| AVR4.1-AVR4.5 | Define nine cases. Copy declared inputs to owned workspaces. Run codegen there. Record pre/post byte manifests. Compare them on every exit without source restoration. |
| AVR5.1-AVR5.5 | Add explicit no-discovery mode. Preserve existing default and explicit-file behavior. Route two dev cases through it. Delete sideline recovery. Update CLI documentation. |
| AVR6.1-AVR6.4 | Reproduce omitted-target behavior in isolated operator state. Fix the credential seam if red. Remove the workaround if green. Test explicit, omitted, wrong-case, and invalid credentials. |
| AVR7.1-AVR7.6 | Inventory all listeners. Disable unused surfaces. Consume provider-assigned product leases. Define global network and local case roots. Propagate case context. Own process and cleanup state. |
| AVR8.1-AVR8.5 | Define the report schema. Write reports atomically. Redact credentials. Validate case and cleanup outcomes. Derive JUnit only from valid reports. |
| AVR9.1-AVR9.6 | Verify host activity. Capture three serial samples. Add bounded scheduling. Drain after failure. Capture five parallel samples. Separate raw samples from the verdict. |
| AVR10.1-AVR10.6 | Derive counts. State nine-app status. Separate push from polling. Document jobs and artifacts. Delete stale text. Record baseline-delta lint. |
| AVR11.1-AVR11.9 | Confirm PR 2 merge. Reconcile current main. Run local and minicloud matrices. Run repository gates. Commit and freeze. Run one Sol review. Open PR 3. Resolve hosted failures. Merge only with owner authority. |
| AVR12.1-AVR12.6 | Confirm PR 3 merge. Reconcile and close prior rows. Archive the plan and retain proof. Replace active routing. Merge the cleanup PR. Remove only the clean merged worktree. |
