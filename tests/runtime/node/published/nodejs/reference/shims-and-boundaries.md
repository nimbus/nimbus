# Node Shim And Boundary Inventory

<!-- generated by scripts/runtime/node/publish_docs.py; do not edit by hand -->

Source: `docs/architecture/runtime/node-isolate-shim-inventory.json`

This generated reference lists the Node-compatible isolate shims, emulations, test-harness-only helpers, diagnostic stubs, and unsupported surfaces tracked for the Nimbus V8 isolate runtime and the `nimbus/deno` fork.

## Capability Classes

| Class | Public label | Meaning |
| --- | --- | --- |
| `native` | Native isolate implementation | Implemented by native Rust or V8-backed isolate operations in the Nimbus runtime or nimbus/deno fork. |
| `shimmed` | Shimmed Node-compatible API | Implemented as JavaScript or TypeScript compatibility code that mirrors Node behavior while preserving the isolate permission boundary. |
| `emulated` | Emulated isolate-local behavior | Emulates process, stream, or host-adjacent state inside the isolate without mutating host-global process state. |
| `test-harness-only` | Test-harness-only support | Exists only to execute and measure upstream Node fixtures; it is not a production application capability. |
| `diagnostic` | Diagnostic denial or service route | The runtime intentionally denies or routes the surface and records an actionable boundary diagnostic instead of granting host authority. |
| `unsupported` | Unsupported platform boundary | Not supported in the in-process V8 isolate profile; requires upstream, native, host-capable service, or microVM work before it can become a support claim. |

## Inventory Entries

| Entry | Class | Owner | Lanes | Surfaces | Claimed capability | Limits | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `deno-node-native-builtins` | `native` | `nimbus/deno` | `node22`, `node24`, `node26` | `node:buffer`, `node:crypto`, `node:fs`, `node:http`, `node:http2`, 4 more | Native Rust and V8-backed Node extension operations provide the low-level isolate implementation for Node builtins that are promoted into the V8-isolate-required surface. | Native isolate support remains bounded by Nimbus runtime permission profiles and by the generated default-support posture; it is not a grant of ambient host process, filesystem, network, or native-addon authority. | `docs/architecture/runtime/node-default-support-posture.json`, `tests/runtime/node/compat/node-compat-evidence/latest/status-summary.json`, `nimbus/deno:ext/node/lib.rs`, 2 more |
| `deno-node-js-polyfills` | `shimmed` | `nimbus/deno` | `node22`, `node24`, `node26` | `process.loadEnvFile`, `fs.promises`, `fs streams`, `http2 compatibility state`, `internal stream base wrappers`, 1 more | JavaScript and TypeScript polyfills mirror Node-visible behavior for promoted APIs while delegating authority checks to Deno/Nimbus runtime permissions. | A shimmed API may match Node semantics for measured fixtures without implying full Node CLI parity or access outside declared runtime roots. | `nimbus/deno:ext/node/lib.rs`, `nimbus/deno:ext/node/polyfills/internal/fs/promises.ts`, `nimbus/deno:ext/node/polyfills/internal/fs/streams.js`, 2 more |
| `deno-node-isolate-local-emulation` | `emulated` | `nimbus/deno` | `node22`, `node24`, `node26` | `process.cwd metadata`, `process.env metadata`, `stream state`, `HTTP/2 state buffers`, `WebStreams and MessagePort transfer helpers` | Host-adjacent Node state is emulated inside each isolate so fixture-visible semantics can pass without mutating host-global process state. | Emulation is per-isolate and capability-scoped; it must not expose host cwd mutation, process exit, OS signals, ambient subprocesses, or unbounded filesystem access. | `docs/architecture/runtime/node-default-support-posture.json`, `nimbus/deno:ext/node/lib.rs`, `nimbus/deno:ext/node/polyfills/internal/http2/util.ts`, 2 more |
| `nimbus-node-official-fixture-harness` | `test-harness-only` | `nimbus/nimbus` | `node22`, `node24`, `node26` | `official Node fixture runner`, `subprocess-shaped fixture parser`, `fixture prelude rendering`, `diagnostic artifact writer`, `watchpoint batches` | Nimbus carries harness-only emulation so upstream Node fixtures can be executed, classified, and promoted with reproducible diagnostics. | Harness helpers are evidence infrastructure and do not become public production Node APIs or application capabilities. | `crates/nimbus-runtime/src/runtime/tests/node/mod.rs`, `crates/nimbus-runtime/src/runtime/bootstrap/ops/test_runtime`, `crates/nimbus-runtime/src/runtime/tests/node/cases`, 2 more |
| `nimbus-host-heavy-diagnostic-boundaries` | `diagnostic` | `nimbus/nimbus` | `node22`, `node24`, `node26` | `node:child_process`, `node:worker_threads`, `node:inspector`, `node:repl`, `node --test`, 4 more | Production in-process application profiles return denial or service/microVM route diagnostics for host-heavy behavior instead of granting host authority. | Diagnostic evidence is not positive in-process support; these surfaces require a service or microVM profile when the application genuinely needs host authority. | `tests/runtime/node/host-heavy-canaries/README.md`, `tests/runtime/node/canary-registry.json`, `tests/runtime/node/compat/node-compat-surface-matrix.md`, 1 more |
| `node-upstream-and-native-platform-boundaries` | `unsupported` | `node-compat/platform-boundary` | `node22`, `node24`, `node26` | `native addon ABI`, `FFI`, `exact host process control`, `startup snapshot CLI topology`, `V8 wire-format exactness`, 1 more | Unsupported entries remain visible as measured boundaries in the generated posture and lane classification catalogs. | These entries are excluded from V8-isolate-required support claims until an owner lands fixture-backed native, shimmed, emulated, or service-routed behavior. | `docs/architecture/runtime/node-default-support-posture.json`, `tests/runtime/node/classifications/node22.json`, `tests/runtime/node/classifications/node24.json`, 1 more |

## Boundary Rule

Diagnostic entries are not positive support claims. Test-harness-only entries measure official fixtures and do not become user-facing runtime support. Unsupported entries remain visible gaps until an owner lands fixture-backed native, shimmed, or emulated behavior.
