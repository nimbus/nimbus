# Release-train provenance receipts

Tracked digest receipts the NDS release-train gate validates in CI
(`scripts/runtime/node/release_train.py check`). One file per receipt:

- nfrc11-release-train-automation.md — release-train automation receipt
  (lane-registry and latest-suite-tag source digests).
