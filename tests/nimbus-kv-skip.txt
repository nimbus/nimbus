# Nimbus KV Valkey external-mode skip accounting.
#
# This file is intentionally valid as a Valkey --skipfile: each non-empty line
# is interpreted as an exact test-name pattern. Section headers are therefore
# comments for humans and non-matching patterns for the runner.

# --- encoding ---
# Encoding/introspection checks such as DEBUG, OBJECT ENCODING, and INFO are not
# part of the NKV0 smoke slice.

# --- behavioral ---
# Behavioral skip budget: 0 for the NKV0 smoke subset.
# Future behavioral skips must cite a tracking issue on the skip line.
