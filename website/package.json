{
  "name": "nimbus-docs-website",
  "private": true,
  "type": "module",
  "description": "Renderer for nimbusdocs.com — Astro Starlight over docs/ content. Standalone npm project, deliberately not a workspace member.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler deploy",
    "preview:deploy": "wrangler versions upload"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "@fontsource-variable/jetbrains-mono": "^5.2.8",
    "astro": "^7.2.2",
    "starlight-llms-txt": "^0.11.0"
  },
  "devDependencies": {
    "simple-icons": "^16.23.0",
    "wrangler": "^4.99.0"
  }
}
