macro_rules! runtime_bundle_source {
    ($definitions:literal) => {
        concat!(
            r#"
const definitions = new Map([
"#,
            $definitions,
            r#"
]);

function compileRuntimeHandler(definition) {
  return new Function(
    "ctx",
    "args",
    "request",
    "return (" + definition.runtime_handler + ")(ctx, args, request);",
  );
}

const handlers = new Map(
  [...definitions.values()].map((definition) => [
    definition.name,
    compileRuntimeHandler(definition),
  ]),
);

globalThis.__neovexInvoke = async function(request) {
  try {
    const handler = handlers.get(request.function_name);
    return {
      status: "ok",
      value: await handler(
        globalThis.__neovexCreateContext({
          request,
          sessionId: `${request.kind}:${request.function_name}`,
        }),
        request.args ?? {},
        request,
      ),
    };
  } catch (error) {
    if (error && typeof error === "object" && "neovexHostError" in error) {
      return { status: "error", error: error.neovexHostError };
    }
    throw error;
  }
};

export {};
"#
        )
    };
}

mod query;
mod subscription;

pub(crate) use query::{runtime_auth_bundle_source, runtime_verified_auth_bundle_source};
pub(crate) use subscription::runtime_auth_subscription_bundle_source;
