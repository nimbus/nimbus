{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "type": "module",
  "dependencies": {
    "convex": "^{{CONVEX_VERSION}}"
  },
  "devDependencies": {
    "@nimbus/codegen": "^{{CODEGEN_VERSION}}"
  }
}
