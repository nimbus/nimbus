schema_version: 1
tenant: tenant-a
workloads:
  - kind: runtime_function
    name: "messages:send"
    services:
      allow:
        - db
    network:
      endpoints:
        - service: db
          name: postgres
          protocol: tcp
          host: 127.0.0.1
          host_port: 0
