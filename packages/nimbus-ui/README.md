# Nimbus UI Control Plane Bundle

This package currently contains the backend-only Convex function bundle for
the reserved `_nimbus` system tenant. It intentionally does not contain React
UI code yet.

The embedded desktop/operator UI will consume these functions after
`docs/plans/archive/system-tenant-api-plan.md` ST1-ST4 are complete.
