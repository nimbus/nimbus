/* `cn` is the class-name merge that the shadcn registry imports directly
   (`import { cn } from "cn"`). Nimbus-owned code imports it from here so the
   alias in components.json (`@/lib/utils`) has one owner. */
export { cn } from "cn";
