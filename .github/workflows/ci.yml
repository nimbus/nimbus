name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
  schedule:
    # Weekly security audit — catches newly disclosed CVEs between PRs
    - cron: "0 8 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  CARGO_PROFILE_DEV_DEBUG: "0"
  CARGO_PROFILE_TEST_DEBUG: "0"

permissions:
  contents: read

jobs:
  rust-format:
    name: Rust Format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name != 'schedule'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all --check

  rust-clippy:
    name: Rust Clippy
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: github.event_name != 'schedule'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-clippy-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      # nimbus-server's loading.rs `include_str!`s files under
      # packages/nimbus-ui/.nimbus/convex/ which are gitignored build
      # artifacts produced by `convex codegen`. Without this step the
      # workspace fails to compile in CI.
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Generate nimbus-ui convex codegen artifacts
        run: npm run codegen -w packages/nimbus-ui

      - name: Run clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  deny:
    name: Rust Dependency Audit
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-deny-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      - name: Install cargo-deny
        uses: taiki-e/install-action@cargo-deny

      - name: Run cargo deny
        run: cargo deny check

  rust-runtime-tests:
    name: Rust Runtime Tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: github.event_name != 'schedule'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-runtime-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      - name: Run isolated runtime test lane
        # V8-sensitive runtime tests are subprocess-isolated in the harness, so
        # CI keeps a dedicated runtime lane instead of falling back to a global
        # single-thread workaround. The official Node compatibility corpus is a
        # conformance signal with its own dedicated workflow; keep required
        # runtime tests focused on product runtime behavior and lightweight
        # manifest/unit checks.
        run: "cargo test -p nimbus-runtime -- --skip runtime::tests::node_compat::"

  rust-workspace-tests:
    name: Rust Workspace Tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: github.event_name != 'schedule'
    env:
      NIMBUS_DISABLE_IMPLICIT_EXTERNAL_PROVIDER_FIXTURES: "1"

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-workspace-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: nextest
          fallback: none

      # See rust-clippy's codegen step — nimbus-server include_str!s the
      # convex codegen output, so generate it before any cargo invocation.
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Generate nimbus-ui convex codegen artifacts
        run: npm run codegen -w packages/nimbus-ui

      - name: Run workspace test lane
        # Verification-harness corpus tests are ignored by default and run in
        # the dedicated harness matrix below, so this lane stays focused on the
        # main workspace suite outside the runtime crate.
        run: cargo nextest run --workspace --exclude nimbus-runtime

      - name: Run workspace doctests
        # nextest intentionally does not run doctests, so keep this explicit
        # libtest pass to preserve the coverage of the previous cargo test lane.
        run: cargo test --workspace --exclude nimbus-runtime --doc

  external-provider-tests:
    name: External Provider Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: github.event_name != 'schedule'
    env:
      NIMBUS_REQUIRE_EXTERNAL_PROVIDER_FIXTURES: "1"
      NIMBUS_TEST_POSTGRES_URL: host=127.0.0.1 port=5432 user=postgres password=postgres dbname=postgres
      NIMBUS_MYSQL_URL: mysql://root@127.0.0.1:3306/test
      NIMBUS_LIBSQL_URL: http://127.0.0.1:18080
      NIMBUS_LIBSQL_ADMIN_URL: http://127.0.0.1:18081
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d postgres"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=12
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
          MYSQL_DATABASE: test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 --silent"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=12

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-external-providers-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      - name: Start libsql provider fixture
        run: |
          docker run --detach \
            --name nimbus-libsql-provider-tests \
            --publish 18080:8080 \
            --publish 18081:8081 \
            --env SQLD_ADMIN_LISTEN_ADDR=0.0.0.0:8081 \
            ghcr.io/tursodatabase/libsql-server:latest \
            /bin/sqld --enable-namespaces --no-welcome

      - name: Wait for libsql provider fixture
        run: |
          for _ in {1..60}; do
            if curl -fsS -X POST http://127.0.0.1:18081/v1/namespaces/nimbus_provider_probe/create \
              -H 'content-type: application/json' \
              --data '{}' >/dev/null; then
              curl -fsS -X DELETE http://127.0.0.1:18081/v1/namespaces/nimbus_provider_probe >/dev/null || true
              exit 0
            fi
            sleep 1
          done
          docker logs nimbus-libsql-provider-tests
          exit 1

      - name: Run external provider integration tests
        run: make test-external-providers

  rust-gate-summary:
    name: Rust Gate Summary
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always() && github.event_name != 'schedule'
    needs:
      - rust-format
      - rust-clippy
      - deny
      - rust-runtime-tests
      - rust-workspace-tests
      - external-provider-tests

    steps:
      - name: Check Rust gate results
        env:
          FORMAT_RESULT: ${{ needs['rust-format'].result }}
          CLIPPY_RESULT: ${{ needs['rust-clippy'].result }}
          DEPENDENCY_AUDIT_RESULT: ${{ needs['deny'].result }}
          RUNTIME_TEST_RESULT: ${{ needs['rust-runtime-tests'].result }}
          WORKSPACE_TEST_RESULT: ${{ needs['rust-workspace-tests'].result }}
          EXTERNAL_PROVIDER_TEST_RESULT: ${{ needs['external-provider-tests'].result }}
        run: |
          printf 'format=%s\n' "${FORMAT_RESULT}"
          printf 'clippy=%s\n' "${CLIPPY_RESULT}"
          printf 'dependency-audit=%s\n' "${DEPENDENCY_AUDIT_RESULT}"
          printf 'runtime-test=%s\n' "${RUNTIME_TEST_RESULT}"
          printf 'workspace-test=%s\n' "${WORKSPACE_TEST_RESULT}"
          printf 'external-provider-test=%s\n' "${EXTERNAL_PROVIDER_TEST_RESULT}"
          test "${FORMAT_RESULT}" = success
          test "${CLIPPY_RESULT}" = success
          test "${DEPENDENCY_AUDIT_RESULT}" = success
          test "${RUNTIME_TEST_RESULT}" = success
          test "${WORKSPACE_TEST_RESULT}" = success
          test "${EXTERNAL_PROVIDER_TEST_RESULT}" = success

  harness:
    name: ${{ matrix.surface-label }} Verification Harness
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event_name != 'schedule'
    strategy:
      fail-fast: false
      matrix:
        include:
          - surface: storage
            surface-label: Storage
          - surface: engine
            surface-label: Engine
          - surface: server
            surface-label: Server
          - surface: runtime
            surface-label: Runtime

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-harness-${{ matrix.surface }}-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      # The server harness surface compiles nimbus-server, which include_str!s
      # the convex codegen output from packages/nimbus-ui/.nimbus/convex/.
      # The other surfaces don't strictly need it but generating it
      # unconditionally is cheap and keeps the matrix uniform.
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Generate nimbus-ui convex codegen artifacts
        run: npm run codegen -w packages/nimbus-ui

      - name: Run required verification harness corpus
        run: bash scripts/verification-harness.sh required ${{ matrix.surface }}

  harness-nightly:
    name: Nightly ${{ matrix.surface-label }} Verification Harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: github.event_name == 'schedule'
    strategy:
      fail-fast: false
      matrix:
        include:
          - surface: storage
            surface-label: Storage
          - surface: engine
            surface-label: Engine
          - surface: server
            surface-label: Server
          - surface: runtime
            surface-label: Runtime

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-harness-nightly-${{ matrix.surface }}-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Generate nimbus-ui convex codegen artifacts
        run: npm run codegen -w packages/nimbus-ui

      - name: Run nightly verification harness corpus
        run: bash scripts/verification-harness.sh nightly ${{ matrix.surface }}

  js:
    name: JavaScript Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: github.event_name != 'schedule'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build --workspaces --if-present

      - name: Test
        run: npm run test --workspaces --if-present

  proof-helpers:
    name: Proof Helper Checks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'schedule'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Check helper script syntax
        run: |
          bash -n scripts/collect-sqlcipher-proof-bundles.sh
          bash -n scripts/collect-nimbus-machine-guest-proof.sh
          bash -n scripts/collect-nimbus-machine-service-proof.sh
          bash -n scripts/collect-nimbus-homebrew-cask-proof.sh
          bash -n scripts/verify-collect-sqlcipher-proof-bundles-helper.sh
          bash -n scripts/verify-nimbus-machine-guest-proof-helper.sh
          bash -n scripts/verify-nimbus-machine-service-proof-helper.sh
          bash -n scripts/verify-nimbus-homebrew-cask-proof-helper.sh
          bash -n scripts/verify-machine-os-release-default-gate.sh
          bash -n scripts/verify-machine-os-release-default-gate-helper.sh
          bash -n scripts/verify-bootc-default-promotion-gate.sh
          bash -n scripts/verify-bootc-default-promotion-gate-helper.sh
          bash -n scripts/verify-machine-os-release-ref-contract.sh
          bash -n scripts/verify-machine-os-release-ref-contract-helper.sh
          bash -n scripts/install.sh
          bash -n scripts/verify-install.sh
          bash -n scripts/verify-install-helper.sh

      - name: Verify SQLCipher proof bundle collector helper
        run: bash scripts/verify-collect-sqlcipher-proof-bundles-helper.sh

      - name: Verify guest proof helper
        run: bash scripts/verify-nimbus-machine-guest-proof-helper.sh

      - name: Verify service proof helper
        run: bash scripts/verify-nimbus-machine-service-proof-helper.sh

      - name: Verify Homebrew cask proof helper
        run: bash scripts/verify-nimbus-homebrew-cask-proof-helper.sh

      - name: Verify machine-os release default gate helper
        run: bash scripts/verify-machine-os-release-default-gate-helper.sh

      - name: Verify bootc default promotion gate helper
        run: bash scripts/verify-bootc-default-promotion-gate-helper.sh

      - name: Verify machine-os release ref contract helper
        run: bash scripts/verify-machine-os-release-ref-contract-helper.sh

      - name: Verify install script helper
        run: bash scripts/verify-install-helper.sh

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event_name != 'schedule'
    env:
      NIMBUS_REQUIRE_EXTERNAL_PROVIDER_FIXTURES: "1"
      NIMBUS_TEST_POSTGRES_URL: host=127.0.0.1 port=5432 user=postgres password=postgres dbname=postgres
      NIMBUS_MYSQL_URL: mysql://root@127.0.0.1:3306/test
      NIMBUS_LIBSQL_URL: http://127.0.0.1:18080
      NIMBUS_LIBSQL_ADMIN_URL: http://127.0.0.1:18081
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d postgres"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=12
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
          MYSQL_DATABASE: test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 --silent"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=12

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure googlesource credentials
        if: env.GOOGLESOURCE_COOKIE != ''
        env:
          GOOGLESOURCE_COOKIE: ${{ secrets.GOOGLESOURCE_COOKIE }}
        run: |
          touch ~/.gitcookies && chmod 0600 ~/.gitcookies
          git config --global http.cookiefile ~/.gitcookies
          echo ".googlesource.com	TRUE	/	TRUE	2147483647	o	${GOOGLESOURCE_COOKIE}" >> ~/.gitcookies
          git config --global url."https://chromium.googlesource.com/a/".insteadOf "https://chromium.googlesource.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-ubuntu-stable-coverage-no-bin-v1
          cache-directories: ~/.cargo/.rusty_v8
          cache-on-failure: "true"
          cache-bin: "false"

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      # Coverage instruments the full workspace including nimbus-server.
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Generate nimbus-ui convex codegen artifacts
        run: npm run codegen -w packages/nimbus-ui

      - name: Start libsql provider fixture
        run: |
          docker run --detach \
            --name nimbus-libsql-coverage \
            --publish 18080:8080 \
            --publish 18081:8081 \
            --env SQLD_ADMIN_LISTEN_ADDR=0.0.0.0:8081 \
            ghcr.io/tursodatabase/libsql-server:latest \
            /bin/sqld --enable-namespaces --no-welcome

      - name: Wait for libsql provider fixture
        run: |
          for _ in {1..60}; do
            if curl -fsS -X POST http://127.0.0.1:18081/v1/namespaces/nimbus_coverage_probe/create \
              -H 'content-type: application/json' \
              --data '{}' >/dev/null; then
              curl -fsS -X DELETE http://127.0.0.1:18081/v1/namespaces/nimbus_coverage_probe >/dev/null || true
              exit 0
            fi
            sleep 1
          done
          docker logs nimbus-libsql-coverage
          exit 1

      - name: Generate coverage report
        # Coverage now provisions explicit external provider fixtures for
        # Postgres, MySQL, and libsql so provider suites run against the same
        # explicit env-driven fixture contract the tests already support.
        #
        # Keep runtime correctness in the blocking runtime test lane, the
        # verification harness matrix, and the scheduled/manual Node compatibility
        # evidence workflow. Do not run the V8/Node compatibility corpus again
        # under coverage instrumentation; it is a conformance signal, not useful
        # line-coverage input, and instrumentation has made it exceed the PR
        # coverage budget.
        #
        # Keep the instrumented workspace build serialized. GitHub-hosted Linux
        # runners have shown rust-lld bus errors when multiple large coverage
        # test binaries link in parallel.
        run: cargo llvm-cov -j 1 --workspace --exclude nimbus-runtime --lcov --output-path lcov.info

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-lcov
          path: lcov.info

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
