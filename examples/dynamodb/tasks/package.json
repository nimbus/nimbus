{
  "name": "dynamodb-tasks",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "dev": "node --experimental-strip-types ./script.ts",
    "smoke": "node --experimental-strip-types ./smoke.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@nimbus/dynamodb": "0.1.46"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "6.0.3"
  }
}
