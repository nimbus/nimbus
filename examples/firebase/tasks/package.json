{
  "name": "firebase-tasks",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "node --experimental-strip-types --experimental-transform-types ./smoke.ts"
  },
  "dependencies": {
    "firebase": "*"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vite": "8.0.9"
  }
}
