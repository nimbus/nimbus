{
  "name": "convex-node",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "codegen": "convex codegen --app .",
    "demo": "npm run codegen && node --experimental-strip-types ./script.ts",
    "test": "npm run codegen && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "convex": "*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "6.0.3"
  }
}
